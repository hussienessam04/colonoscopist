---
slug: add-about-tab-and-update-email-and-revie
created: 2026-09-12
type: feature
source: ad-hoc user request
---

# Add About tab, fix developer email, fix translation gaps

## Task 1 — About tab + developer email references

**Files:**
- `src/renderer/src/pages/SettingsAbout.tsx` (new)
- `src/renderer/src/lib/router.ts` (add `settings-about` route variant)
- `src/renderer/src/App.tsx` (add `case 'settings-about'` + import)
- `src/renderer/src/components/SettingsSidebar.tsx` (add `about` to `SettingsTab` union, add Info button right before Storage)
- `src/renderer/src/pages/SettingsHub.tsx` (add About paragraph between Users and Storage)
- `src/renderer/src/i18n/en/translation.json` (add `about` namespace + update `license.modalExpiredBody`)
- `src/renderer/src/i18n/ar/translation.json` (add `about` namespace + update `license.modalExpiredBody`)
- `src/main/auth/index.ts:265` (replace vendor email in `mailto:` URL)

**Action:**

1. **Create `SettingsAbout.tsx`** — mirror the `License.tsx` pattern. Single `<Card>` inside `<SettingsLayout activeTab="about">` with `<CardHeader>` (CardTitle "About" + Info icon, CardDescription from `t('about.pageDescription')`) and `<CardContent>` showing three rows in the same `grid-cols-3` treatment used by License: app name (`t('about.appName')`), developer (`t('about.developerName')`), phone (rendered as `<a href="tel:+201026524116">+201026524116</a>`), email (rendered as `<a href="mailto:hussienessam04@gmail.com">hussienessam04@gmail.com</a>`). Add a short description paragraph from `t('about.pageDescription')`. Hardcode a `const APP_VERSION = '1.0';` near the top with a `ponytail:` comment noting it should be bumped on release; render it as a fourth grid row (`Version: 1.0`) so the page has a clear "About this app" identity. Use the same `border-[#E0D9C6] bg-[#FBF7EE] shadow-...` class strings used by every other Settings Card — no new design tokens. Set `backTestId="settings-about-back"`. Use `<Mail>` + `<Phone>` lucide-react icons in the email/phone rows so they read as affordances (mirrors how License uses KeyRound).

2. **Add route variant** in `lib/router.ts`: insert `| { name: 'settings-about' }` in the `Route` union right after `| { name: 'settings-storage' }` (keeps the `quick task` cluster contiguous). No params needed.

3. **Wire App.tsx**: add `import SettingsAbout from './pages/SettingsAbout';` next to the other Settings imports, and add `case 'settings-about': routeElement = <SettingsAbout />; break;` in the switch, right before the `patient-procedures` case (or after `license`, order doesn't matter — pick a stable spot).

4. **Update `SettingsSidebar.tsx`**:
   - Add `'about'` to the `SettingsTab` union, right before `'storage'` so Storage stays the last position (per user instruction — Storage is "infrastructure they set once then forget").
   - Import `Info` from `lucide-react`.
   - Add a new `<Button>` for About positioned right BEFORE the Storage `<Button>` (so order: Capture → Profile → Audit → License → Backup & restore → Users → **About** → Storage). Same `variant`/`className`/`data-active`/`data-testid="settings-hub-about"` treatment as siblings. `onClick={() => navigate({ name: 'settings-about' })}`. Use the `Info` icon. Label via `t('about.sidebarEntry')` for i18n parity. No admin gate — every doctor sees it.

5. **Update `SettingsHub.tsx`**: add an About `<p>` right before the Storage `<p>` (between Users and Storage) using the same `<span className="font-medium">{t('about.sidebarEntry')}</span> — {t('about.pageDescription')}` pattern as the other entries.

6. **Update `src/main/auth/index.ts:265`** — change the `mailto:` value from `mailto:licensing@colonoscopist.example?subject=Admin%20PIN%20recovery&body=fingerprint:` to `mailto:hussienessam04@gmail.com?subject=Admin%20PIN%20recovery&body=fingerprint:`. This is the developer contact for the offline recovery flow. No other `colonoscopist.example` references exist (grep confirms 1 match total).

7. **Update both translation.json files**:
   - **EN `license.modalExpiredBody`**: change from `"Your 14-day trial has ended. Please email your machine ID to Colonoscopist support for a license file."` to `"Your 14-day trial has ended. Please email your machine ID to hussienessam04@gmail.com to request a license file."`
   - **AR `license.modalExpiredBody`**: change from `"انتهت الفترة التجريبية البالغة 14 يوماً. يرجى إرسال معرّف الجهاز إلى دعم Colonoscopist للحصول على ملف ترخيص."` to `"انتهت الفترة التجريبية البالغة 14 يوماً. يرجى إرسال معرّف الجهاز إلى hussienessam04@gmail.com لطلب ملف ترخيص."`
   - Add a new top-level `"about"` namespace to BOTH bundles with keys: `sidebarEntry`, `pageTitle`, `pageDescription`, `appName`, `versionLabel`, `versionValue`, `developerLabel`, `developerName`, `phoneLabel`, `emailLabel`, `description` (the short tagline). English version: keep professional tone matching the rest of the bundle. Arabic version: hand-written (no machine translation) per D-20 parity convention used everywhere else in this file.

**Verify:**
- `npm run typecheck` (or the project's equivalent) passes with no new TS errors.
- `grep -r "licensing@colonoscopist.example" src/` returns zero matches.
- `grep -r "colonoscopist.example" src/` returns zero matches.
- The renderer builds (`npm run dev` or the project's equivalent) and the Settings sidebar shows the new "About" entry; clicking it renders SettingsAbout with app name, version, developer, phone (tel: link), and email (mailto: link).

**Done:**
- Settings → About page renders with the locked content (app name "Colonoscopist", version, developer name, phone, email) and the email/phone are clickable links.
- The "About" entry appears in the SettingsSidebar right before Storage (order: Capture → Profile → Audit → License → Backup & restore → Users → About → Storage).
- SettingsHub's "Workspace" card lists an About paragraph.
- The vendor email at the recovery mailto: link is `hussienessam04@gmail.com`.
- The `license.modalExpiredBody` strings in BOTH EN and AR reference the developer email.
- The new `about` namespace exists in both EN and AR bundles with matching keys.

---

## Task 2 — Translation review: i18n the obvious hardcoded English UI strings

**Files:**
- `src/renderer/src/i18n/en/translation.json` (add missing keys)
- `src/renderer/src/i18n/ar/translation.json` (add matching AR keys)
- `src/renderer/src/pages/Wizard.tsx`
- `src/renderer/src/pages/Login.tsx`
- `src/renderer/src/pages/SettingsUsers.tsx`
- `src/renderer/src/pages/RecoveryFilePicker.tsx`
- `src/renderer/src/pages/PatientsList.tsx`
- `src/renderer/src/pages/PatientForm.tsx`
- `src/renderer/src/pages/PatientProcedures.tsx`
- `src/renderer/src/pages/ProcedureRoom.tsx`
- `src/renderer/src/pages/ProcedureReview.tsx`
- `src/renderer/src/pages/ProcedurePreview.tsx`
- `src/renderer/src/pages/SettingsCapture.tsx`
- `src/renderer/src/pages/ReportEditor.tsx`
- `src/renderer/src/pages/ProfileEditor.tsx`

**Action:**

ponytail: this is a thin sweep — same pattern everywhere (`<English text>` → `{t('namespace.key')}` + add key to both bundles). One commit per file cluster is fine; no need to split per file. Skip files where every string already goes through `t()` (Audit, BackupRestore, License, SettingsStorage, SettingsHub — already done in prior quick tasks).

1. **Add new i18n keys** to BOTH `en/translation.json` and `ar/translation.json`. Group by file/namespace:

   - Under `wizard.*` — verify all existing keys exist (most do from prior work). Add: `languageOptionEnglish`, `languageOptionArabic`, `fullNamePlaceholder`, `clinicNamePlaceholder`, `pinPlaceholder`, `confirmPinPlaceholder`.
   - Under `auth.*` — add `signInTitle` ("Sign in" / "تسجيل الدخول"), `loadingUsers` already exists.
   - Under `settingsUsers.*` (new namespace, OR add to existing `settings.*` group) — add keys for: `dialogAddTitle`, `dialogAddDescription`, `dialogResetTitle`, `dialogResetDescription`, `lastLoginLabel`, `confirmPinLabel`, `tableName`, `tableLastLogin`, `tableActions`, `adminOnlyTitle`.
   - Under `recovery.*` — extend with: `dialogTitle`, `dialogDescription`, `emailButton`, `selectFileButton`, `sending`, `selecting`, `toastSuccessWithMailto`, `toastSuccessNoMailto`, `toastFileSuccess`, `toastFileSuccessDeferred`, `toastFileFailed`, `toastRequestFailed`.
   - Under `patient.*` — add `columnName`, `columnGender`, `columnPhone`, `columnActions` (table headers).
   - Under `patientForm.*` (new) — `loading`.
   - Under `patientProcedures.*` — add `genderLabel`.
   - Under `procedure.*` — add `roomStudioLabel`, `roomNoDeviceHint`, `roomShortcutSpace`, `roomShortcutEsc`, `roomShortcutsHint`, `roomNotesLabel`, `roomScreenshotsLabel`, `reviewTimelineLabel`, `reviewNotesLabel`, `reviewTrimLabel`, `reviewProcedureLabel`, `reviewPatientLabel`, `reviewStartedLabel`, `reviewDurationLabel`, `reviewEndedLabel`, `reviewScreenshotsLabel`, `previewLabel`, `previewCaptureDeviceLabel`, `previewNoDeviceBody`.
   - Under `settings.*` — add `captureDeviceLabel`, `captureDevicePlaceholder`, `qualityPresetLabel`, `resolutionLabel`, `framerateLabel`.
   - Under `report.*` — add `missingProcedureId`.
   - Under `profile.*` — `languageOptionEnglish` (the literal "English" radio label inside the language card).

   Match each EN key with an AR value (hand-written, no machine translation — matches the D-20 parity convention used everywhere in this project).

2. **Swap hardcoded strings for `t()` calls** in each priority file:
   - `Wizard.tsx`: CardTitle, all `<Label>` text (fullName / clinicName / pin / confirmPin / language en+ar), the language section heading. Re-use existing `wizard.title`, `wizard.fullNameLabel`, etc. and the new keys.
   - `Login.tsx`: CardTitle and the loading text. Use `auth.signInTitle` and `auth.loadingUsers`.
   - `SettingsUsers.tsx`: table headers (Name / Last login / Actions), both dialog titles + descriptions, both Pin labels, the "Admin only" CardTitle, and the dialog Cancel/Add/Reset button text. Reuse `common.cancel` for Cancel.
   - `RecoveryFilePicker.tsx`: DialogTitle, DialogDescription, the `Forgot admin PIN?` trigger (move to `auth.forgotPin` or `recovery.triggerForgot`), the Email vendor / Sending / Select recovery file / Selecting buttons, all the toast strings.
   - `PatientsList.tsx`: the four `<th>` cells (Name / Gender / Phone / Actions).
   - `PatientForm.tsx`: the "Loading…" `<p>`.
   - `PatientProcedures.tsx`: the "Gender:" label.
   - `ProcedureRoom.tsx`: "Studio" small-caps label, the "No device selected..." hint, the "Space"/"Esc" keyboard hints, and the "Notes"/"Screenshots" small-caps labels.
   - `ProcedureReview.tsx`: every small-caps label (Timeline / Notes / Trim / Procedure / Patient / Started / Ended / Duration / Screenshots), each row's `<span>` field label.
   - `ProcedurePreview.tsx`: "Preview", "Capture device", "Could not open the capture device.", and the "Pick a capture device" placeholder.
   - `SettingsCapture.tsx`: "Capture device", "Select a device" (placeholder), "Quality preset", "Resolution (W×H)", "Framerate".
   - `ReportEditor.tsx`: the "Missing procedure id." paragraph.
   - `ProfileEditor.tsx`: the "English" radio Label inside the language card.

   For each swap: use `useTranslation()` if not already imported, or add it. Wire `t()` directly in the JSX (e.g. `<CardTitle>{t('wizard.title')}</CardTitle>`). Don't introduce a constants object — ponytail: `t()` inline is the project pattern.

3. **Parity safety**: every EN key added MUST have a matching AR value with no `null`/`undefined`. The existing `tests/renderer/i18n/i18n-parity.test.ts` (or whichever parity test the project uses) will gate this — if it exists, run it.

**Verify:**
- `npm run typecheck` passes.
- `npm run test:unit` (or equivalent) passes; the i18n parity test (if present) passes — confirms every new EN key has an AR value.
- `grep -rn ">[A-Z][a-z]" src/renderer/src/pages/` returns no obvious orphan English UI strings (the leftover matches in ProcedureRoom.tsx, SettingsUsers.tsx, ProcedureReview.tsx, ProcedurePreview.tsx, SettingsCapture.tsx, PatientsList.tsx, PatientProcedures.tsx are now gone or wrapped in `t()`).
- `grep -rn 'placeholder="[A-Z]' src/renderer/src/pages/` returns no orphan English placeholders.
- Manual: switch the app to Arabic (Settings → Profile → Language) and walk through Wizard → Login → Settings Users → Procedure Room → Procedure Review → Settings Capture. Every page renders Arabic, no English leaks.

**Done:**
- Every priority file identified in the audit now routes its visible UI strings through `t()`.
- Every new EN key has a matching AR value.
- No new English leaks visible in the app when language is set to Arabic.
- No new dependencies, no new abstractions, no new components — just literal string swaps and i18n key additions.
