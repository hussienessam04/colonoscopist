---
task: settings-pages-clinical-palette-from-report-editor
task_count: 3
mode: quick
type: execute
files_modified:
  - src/renderer/src/components/SettingsLayout.tsx
  - src/renderer/src/components/SettingsSidebar.tsx
  - src/renderer/src/pages/SettingsHub.tsx
  - src/renderer/src/pages/SettingsCapture.tsx
  - src/renderer/src/pages/SettingsUsers.tsx
  - src/renderer/src/pages/ProfileEditor.tsx
  - src/renderer/src/pages/Audit.tsx
  - src/renderer/src/pages/BackupRestore.tsx
  - src/renderer/src/pages/License.tsx
autonomous: true
---

<objective>
Bring the clinical-workstation palette from the ReportEditor (warm ivory background, hairline teal accents, small-caps clinical labels) to every Settings page so the app reads as one product, not two. Currently the ReportEditor feels like a clinical chart on a desk while every Settings page feels like a generic SaaS dashboard — same data, two visual languages.

Output: every Settings page (Hub, Capture, Users, Profile, Audit, Backup & Restore, License) sits on the warm ivory `#F7F1E6` background, uses the ivory panel/card surface `#FBF7EE` with hairline `#E0D9C6` borders, the small-caps teal `#0E3A47` eyebrow treatment for section headings, and the deep teal `#0E3A47` for primary action buttons. A 4px signature teal stripe anchors the SettingsLayout's left edge (rhymes with the ReportEditor's document stripe).
</objective>

<context>
@.planning/PROJECT.md
@AGENTS.md
@src/renderer/src/pages/ReportEditor.tsx (the canonical palette — see FIELD_CLASS on line 75–76, signature stripe on line 800)
@src/renderer/src/components/SettingsLayout.tsx (currently `bg-slate-50`)
@src/renderer/src/components/SettingsSidebar.tsx
@src/renderer/src/pages/SettingsHub.tsx
@src/renderer/src/pages/SettingsCapture.tsx
@src/renderer/src/pages/SettingsUsers.tsx
@src/renderer/src/pages/ProfileEditor.tsx
@src/renderer/src/pages/Audit.tsx
@src/renderer/src/pages/BackupRestore.tsx
@src/renderer/src/pages/License.tsx
@src/renderer/src/components/ui/button.tsx (variant defaults — primary variant is the deep slate; do NOT change variants, only override classes via className on specific buttons)
@src/renderer/src/components/ui/card.tsx
@test files matching `tests/renderer/**/*.test.tsx` (all data-testid contracts must remain)
</context>

<design_tokens>
Palette (lifted directly from ReportEditor.tsx):
- bg-clinic:        #F7F1E6  (main <main> background)
- bg-panel:         #FBF7EE  (card / sidebar / input surface)
- bg-panel-strong:  #EFEAE0  (right-rail screenshots panel — used in ReportEditor; reuse here for sidebar)
- border-hairline:  #E0D9C6  (1px borders)
- border-hover:     #A8C5B5  (sage, on input hover)
- bg-hover-teal:    #E6EFF1  (light teal hover for ghost/outline buttons)
- ink-primary:      #13202E  (text)
- ink-secondary:    #5C6770  (secondary text)
- ink-muted:        #8C8478  (small-caps eyebrow / labels)
- ink-placeholder:  #A39A86  (input placeholder)
- accent-teal:      #0E3A47  (primary brand — Stripe, active toggle, eyebrow, primary button)
- accent-teal-dark: #0B2C36  (primary button hover)
- accent-coral:     #C66B4D  (destructive finalize / remove-confirm)
- accent-coral-dark:#B05C40  (coral hover)

Typography:
- Eyebrow:  text-[11px] font-medium uppercase tracking-[0.18em] text-[#0E3A47] (for card titles + section headers)
- Kicker (page-level above h1): text-[11px] font-semibold uppercase tracking-[0.22em] text-[#8C8478] (kept slate, matches ReportEditor page kicker)
- H1 (page title): text-2xl font-medium tracking-tight text-[#13202E] (no font-serif — Settings doesn't need the chart-feel serif)
- Body: default sans
- Mono (machine id, mrn, paths, byte sizes): font-mono text-xs text-[#5C6770]

Surface treatment:
- Page: <main class="min-h-screen bg-[#F7F1E6] p-6 font-sans text-[#13202E]">
- Card: keep shadcn <Card> but override className with `bg-[#FBF7EE] border-[#E0D9C6] shadow-[0_1px_2px_rgba(19,32,46,0.04),0_8px_24px_-12px_rgba(19,32,46,0.12)]`
- Input/Select: ivory surface `bg-[#FBF7EE] border-[#E0D9C6]` with sage hover and teal focus ring (matches ReportEditor FIELD_CLASS)
- Signature stripe: 4px `bg-[#0E3A47]` on the inner content card so each page has a visible "this is part of the same product" anchor

Anti-patterns to avoid:
- Do NOT change Button variants — keep `default`/`outline`/`ghost` semantic meaning. Override individual buttons via className only when needed (e.g. primary teal where slate default feels wrong).
- Do NOT change shadcn Card component source — override via className.
- Do NOT touch i18n keys.
- Do NOT remove or rename any data-testid — tests must pass unchanged.
- License dot colors (emerald/blue/red/slate) stay semantic (licensed/trial/expired/unactivated) — do not flatten to teal; the color encodes state, not brand.
</design_tokens>

<must_haves>
- Every Settings page (`/settings`, `/settings-capture`, `/settings-users`, `/profile-edit`, `/audit`, `/license`, `/backup-restore`) renders on the warm ivory `#F7F1E6` background instead of `bg-slate-50`.
- The SettingsLayout shows a small "Settings" kicker in `#8C8478` uppercased, then a slate h1, then the page subtitle, then the right-side actions (Back + Save / Add user / etc.). The kicker matches ReportEditor's kicker treatment.
- The SettingsLayout adds a thin teal signature accent (could be a left-edge stripe on the inner content area OR a 1px teal accent under the kicker) — the same teal `#0E3A47` the ReportEditor uses for its document stripe + active toggle.
- Card surfaces in Settings pages use `#FBF7EE` ivory with `#E0D9C6` hairline borders and the soft 2-layer shadow `shadow-[0_1px_2px_rgba(19,32,46,0.04),0_8px_24px_-12px_rgba(19,32,46,0.12)]`.
- All <Input> / <Select> / <Textarea> rendered in Settings pages use the unified ivory surface treatment matching the ReportEditor's FIELD_CLASS: `bg-[#FBF7EE] border-[#E0D9C6]` with `hover:border-[#A8C5B5]`, focus `focus:border-[#0E3A47] focus:bg-white focus:ring-1 focus:ring-[#0E3A47]/30`, placeholder `text-[#A39A86]`.
- Settings sidebar uses ivory panel background `#EFEAE0` (matches ReportEditor's screenshots rail tint), hairline `#E0D9C6` borders, and teal `#0E3A47` active state. Inactive items render as ghost buttons with a soft hover.
- CardTitle in Settings pages gets the small-caps teal treatment: `text-[11px] font-semibold uppercase tracking-[0.18em] text-[#0E3A47]` so the page reads as clinical, not SaaS.
- Primary action buttons (Save in Capture, Add user in Users, Apply in Audit, Load .lic file in License, Create backup in Backup & Restore) inherit the default shadcn primary variant OR are explicitly given the teal treatment `bg-[#0E3A47] text-white hover:bg-[#0B2C36]` to make the page-primary CTA visually anchored in teal.
- Every data-testid preserved. Every test contract preserved. No semantic UI changes (button copy, form fields, table columns, dialog bodies all unchanged).
- No new files. No package changes. No i18n key changes.
</must_haves>

<tasks>

<task type="auto">
  <name>Task 1: SettingsLayout + SettingsSidebar — bring warm ivory + teal anchor</name>

<files>
- src/renderer/src/components/SettingsLayout.tsx
- src/renderer/src/components/SettingsSidebar.tsx
</files>

<action>
**SettingsLayout.tsx**

Replace the `<main className="min-h-screen bg-slate-50 p-6">` with the warm-ivory clinical surface:

```tsx
<main className="min-h-screen bg-[#F7F1E6] p-6 font-sans text-[#13202E]">
```

The `<div className="mx-auto grid max-w-6xl gap-4 lg:grid-cols-[16rem_minmax(0,1fr)]">` becomes:

```tsx
<div className="mx-auto grid max-w-6xl gap-5 lg:grid-cols-[16rem_minmax(0,1fr)]">
```

The header kicker (currently `text-xs font-semibold uppercase tracking-[0.2em] text-muted-foreground`) becomes the clinical small-caps teal eyebrow matching ReportEditor's h2 labels:

```tsx
<p className="text-[11px] font-semibold uppercase tracking-[0.22em] text-[#8C8478]">
  Settings
</p>
```

The h1 keeps `text-2xl font-semibold tracking-tight text-[#13202E]` (drop the default `text-foreground` so it inherits the deep ink color).

The header gets a thin teal hairline below the title block as the page-level signature accent:

```tsx
<header className="flex flex-wrap items-end justify-between gap-3 border-b border-[#E0D9C6] pb-4">
```

The right column wraps the children in a relative container with the signature teal stripe:

```tsx
<div className="relative flex flex-col gap-5 overflow-hidden rounded-lg border border-[#E0D9C6] bg-[#FBF7EE] p-5 shadow-[0_1px_2px_rgba(19,32,46,0.04),0_8px_24px_-12px_rgba(19,32,46,0.12)]">
  {/* Signature teal stripe (left edge) */}
  <div aria-hidden="true" className="absolute inset-y-0 left-0 w-1 bg-[#0E3A47]" />
  <div className="pl-3 flex flex-col gap-5">
    {/* header goes here */}
    {children}
  </div>
</div>
```

Wait — that nests the header inside the same card. Better approach: keep the header outside the card so each Settings page gets its own anchor card body, but apply the stripe to the body card.

Refined structure:

```tsx
<main className="min-h-screen bg-[#F7F1E6] p-6 font-sans text-[#13202E]">
  <div className="mx-auto grid max-w-6xl gap-5 lg:grid-cols-[16rem_minmax(0,1fr)]">
    <SettingsSidebar activeTab={activeTab} />
    <div className="flex flex-col gap-5">
      <header className="flex flex-wrap items-end justify-between gap-3 border-b border-[#E0D9C6] pb-4">
        {/* kicker + h1 + subtitle on the left */}
        {/* headerAction + backButton on the right */}
      </header>
      <div className="relative overflow-hidden rounded-lg border border-[#E0D9C6] bg-[#FBF7EE] shadow-[0_1px_2px_rgba(19,32,46,0.04),0_8px_24px_-12px_rgba(19,32,46,0.12)]">
        <div aria-hidden="true" className="absolute inset-y-0 left-0 w-1 bg-[#0E3A47]" />
        <div className="relative flex flex-col gap-5 p-6 pl-7">
          {children}
        </div>
      </div>
    </div>
  </div>
</main>
```

The signature stripe lives on the inner content card (the "page body"). The header sits above it on the ivory background, anchored by a hairline rule — same hierarchy the ReportEditor uses for its page header.

The Back button stays as a default `<Button variant="outline">` but gets an explicit teal hover treatment matching the ReportEditor's outline buttons:

```tsx
className="border-[#E0D9C6] bg-white text-[#5C6770] hover:border-[#0E3A47] hover:bg-[#E6EFF1] hover:text-[#0E3A47]"
```

(This matches the ReportEditor's openPdf/back buttons.)

**SettingsSidebar.tsx**

Replace the `<aside className="flex flex-col gap-3 rounded-lg border bg-card p-4 shadow-sm">` with:

```tsx
<aside className="flex flex-col gap-2 rounded-lg border border-[#E0D9C6] bg-[#EFEAE0] p-4 shadow-[0_1px_2px_rgba(19,32,46,0.04),0_8px_24px_-12px_rgba(19,32,46,0.12)]">
```

The "Sections" label keeps its small-caps treatment but shifts to teal:

```tsx
<p className="text-[11px] font-semibold uppercase tracking-[0.18em] text-[#0E3A47]">
  Sections
</p>
```

Each `<Button>` (Capture / Profile / Audit / License / Backup & Restore / Users) — active or inactive — uses an explicit className that overrides the default variant colors with the clinical palette:

```tsx
className={
  activeTab === 'capture'
    ? 'justify-start bg-[#0E3A47] text-white hover:bg-[#0B2C36]'
    : 'justify-start border border-[#E0D9C6] bg-[#FBF7EE] text-[#5C6770] hover:bg-[#E6EFF1] hover:text-[#0E3A47] hover:border-[#0E3A47]'
}
```

(The active/inactive swap is per-button; keep the `variant="default"` for active and `variant="outline"` for inactive as the shadcn primitive — the override classes win.)

The disabled Users button (admin gate) keeps `disabled={!isAdmin}` and gets a soft disabled treatment — `disabled:opacity-50 disabled:cursor-not-allowed`.

License dot colors (`bg-emerald-500`, `bg-blue-500`, `bg-red-500`, `bg-slate-500`) STAY unchanged — semantic encoding must remain readable.

The "Sections" header keeps its current text and tests (no testid changes).

The license badge text inside the License button keeps its current `text-xs font-normal opacity-80` but the color shifts to `#A39A86` (matching the placeholder ink) so it doesn't fight the button's hover state.

Both files commit together as one atomic change. No new files.
</action>

<verify>
- `npm run lint -- --quiet src/renderer/src/components/SettingsLayout.tsx src/renderer/src/components/SettingsSidebar.tsx` exits 0.
- `grep -n "bg-slate-50" src/renderer/src/components/SettingsLayout.tsx` returns nothing.
- `grep -n "bg-card" src/renderer/src/components/SettingsSidebar.tsx` returns nothing (replaced by `bg-[#EFEAE0]`).
- All `data-testid` values in SettingsSidebar unchanged: `settings-hub-capture`, `settings-hub-profile`, `settings-hub-audit`, `settings-hub-license`, `settings-hub-license-dot`, `settings-hub-license-badge`, `settings-hub-backup-restore`, `settings-hub-users`.
- `data-testid` on the Back button unchanged (driven by `backTestId` prop).
</verify>

<done>
- SettingsLayout + SettingsSidebar render on warm ivory `#F7F1E6`.
- Page content wrapped in a rounded ivory card with a 4px teal signature stripe.
- Sidebar uses `#EFEAE0` panel with teal active state.
- All test contracts preserved.
</done>
</task>

<task type="auto">
  <name>Task 2: SettingsCards + SettingsInputs — ivory surface + teal accents across Hub / Capture / Users / Profile / Audit / Backup / License</name>

<files>
- src/renderer/src/pages/SettingsHub.tsx
- src/renderer/src/pages/SettingsCapture.tsx
- src/renderer/src/pages/SettingsUsers.tsx
- src/renderer/src/pages/ProfileEditor.tsx
- src/renderer/src/pages/Audit.tsx
- src/renderer/src/pages/BackupRestore.tsx
- src/renderer/src/pages/License.tsx
</files>

<action>
For every Settings page, apply the same three surface treatments (these are atomic visual swaps, not functional changes):

**1. Card surface** — every `<Card>` (or `<Card className="...">`) gets the ivory treatment:

```tsx
<Card className="border-[#E0D9C6] bg-[#FBF7EE] shadow-[0_1px_2px_rgba(19,32,46,0.04),0_8px_24px_-12px_rgba(19,32,46,0.12)]">
```

When `Card` already has a className (e.g. `bg-card`), merge the new classes with the existing ones. If a Card already has `bg-slate-*`, replace it. Never delete an existing class without reading the surrounding context first — some Cards have `bg-amber-50` (BackupRestore warning) which must STAY.

**2. CardTitle small-caps teal** — every `<CardTitle>` gets the clinical eyebrow treatment:

```tsx
<CardTitle className="text-[11px] font-semibold uppercase tracking-[0.18em] text-[#0E3A47]">
```

(Some CardTitle's already carry `text-base` — keep the existing size override where present; the new classes should override or supplement. If `text-base` is present, switch to `text-sm` so the small-caps eyebrow reads smaller; the Audit page's `CardTitle className="text-base"` can stay `text-sm` for consistency.)

**3. Field surface** — every `<Input>`, `<SelectTrigger>`, `<Textarea>` (when used in Settings pages) gets the FIELD_CLASS treatment matching ReportEditor:

```tsx
className="bg-[#FBF7EE] border-[#E0D9C6] text-[#13202E] placeholder:text-[#A39A86] hover:border-[#A8C5B5] focus:border-[#0E3A47] focus:bg-white focus:ring-1 focus:ring-[#0E3A47]/30"
```

When an Input/SelectTrigger already has a className, MERGE — never drop the existing class without checking the contract. For example, the custom-resolution input in SettingsCapture has `aria-invalid={!customResolutionValid}` plus a default className; keep both. The `class` attribute is additive, not destructive.

**4. Primary CTA teal** — for pages that have ONE clear primary action (SettingsCapture's Save, SettingsUsers' Add user, Audit page's Apply, BackupRestore's Create backup, License page's Load .lic file), override the className so it lands in teal:

```tsx
className="bg-[#0E3A47] text-white hover:bg-[#0B2C36] disabled:bg-[#E0D9C6] disabled:text-[#8C8478]"
```

(Apply this only to the primary action button on each page. Outlined/secondary buttons (e.g. Choose file, Preview, Stop Preview) keep the default `variant="outline"` with the explicit teal hover treatment `border-[#E0D9C6] bg-white text-[#5C6770] hover:border-[#0E3A47] hover:bg-[#E6EFF1] hover:text-[#0E3A47]`.)

**5. Page-specific overrides:**

- **SettingsHub.tsx**: the inner `<Card>` gets the ivory treatment. The CardContent paragraphs use the existing `text-sm text-muted-foreground` — replace `text-muted-foreground` with `text-[#5C6770]` for consistency. The "Open profile editor" inline button keeps its `variant="link"` style.

- **SettingsCapture.tsx**: the preview `<div className="overflow-hidden rounded-xl border border-slate-800 bg-black shadow-2xl">` STAYS black (it's a live video preview — black is correct). The right aside `<aside className="flex flex-col gap-5 rounded-xl border bg-card p-5 shadow-sm">` becomes ivory: `border-[#E0D9C6] bg-[#FBF7EE] shadow-[0_1px_2px_rgba(19,32,46,0.04),0_8px_24px_-12px_rgba(19,32,46,0.12)]`. The "Last used" text + labels shift from `text-muted-foreground` to `text-[#5C6770]`. The "No device saved yet" hint shifts to `text-[#8C8478]`. The radio buttons + labels get the teal accent when checked (replace the default browser radio styling — too generic — with the ReportEditor-style teal accent. Tailwind: `accent-[#0E3A47]` on the parent fieldset).

- **SettingsUsers.tsx**: the "Admin only" Card (when not admin) gets the ivory surface. The table header keeps `text-xs uppercase text-muted-foreground` — switch to `text-[11px] font-semibold uppercase tracking-[0.18em] text-[#0E3A47]` to match the clinical eyebrow. Table row hover `hover:bg-slate-50` becomes `hover:bg-[#E6EFF1]`. The `border-slate-200` rows become `border-[#E0D9C6]`. The "Admin" / "Staff" badges inside the Name cell get `text-[#8C8478]` instead of `text-muted-foreground`.

- **ProfileEditor.tsx**: AssetUploader quadrants currently use `bg-slate-50` and `border-slate-200` — switch to `bg-[#FBF7EE]` and `border-[#E0D9C6]` for consistency. The empty preview box uses `border-dashed border-slate-300 bg-white` — switch to `border-dashed border-[#E0D9C6] bg-white`. The used-device list rows use `border-slate-200 bg-slate-50` — switch to `border-[#E0D9C6] bg-[#FBF7EE]`. The "add new device" row uses `border-dashed border-slate-300` — switch to `border-dashed border-[#E0D9C6]`. The save indicator paragraph `text-xs text-muted-foreground` shifts to `text-xs text-[#8C8478]`.

- **Audit.tsx**: the filter Card + results Card get the ivory surface. The filter `<Label>` text keeps default — that's fine. The audit-row hover `hover:bg-slate-50` becomes `hover:bg-[#E6EFF1]`. The skeleton rows `bg-slate-200` become `bg-[#E0D9C6]/60`. The detail Dialog body — its background is the default DialogContent — replace `bg-slate-50` (the pre block in audit detail metadata) with `bg-[#FBF7EE]`.

- **BackupRestore.tsx**: the warning `<Alert className="border-amber-200 bg-amber-50 text-amber-900">` KEEPS its amber treatment (it's a warning, not brand surface). The backup Card + restore Card get the ivory surface. The "Last backup" indicator `text-xs text-muted-foreground` becomes `text-xs text-[#8C8478]`. The preview Dialog's `border-slate-200` row separators become `border-[#E0D9C6]`. The integrity-passed `text-emerald-600` and integrity-failed `text-destructive` STAY (semantic).

- **License.tsx**: the status Card gets the ivory surface. The state-dot colors stay. The `border-b border-border` row separators (used for status / trial / vendor / licensed-at / machine id rows) STAY (they already use the design-system border token, which the global CSS maps to ivory-friendly). The `bg-slate-50` text inside the machine-id pre block (in audit detail) — none here, License doesn't have a pre block. The license status label icons keep their semantic colors.

For all changes: MERGE className strings with existing classes — never replace a className outright without keeping the existing test-anchor classNames (data-testid, role, aria-*). Use the search-and-find pattern: locate `<Input`, `<SelectTrigger`, `<Card`, `<CardTitle`, then update the className in place.

Verify after every page-level edit that the surrounding JSX still parses (mismatched quotes / unclosed tags are the most common break).

Commit atomically per page if you want clean git history, OR commit all 7 page files in one commit if atomicity-of-meaning matters (one commit = "all settings pages now use the clinical palette"). Pick whichever produces the smaller diff for review — recommended: ONE commit for all 7 pages because the changes are parallel visual swaps across parallel pages.
</action>

<verify>
- `npm run lint -- --quiet src/renderer/src/pages/SettingsHub.tsx src/renderer/src/pages/SettingsCapture.tsx src/renderer/src/pages/SettingsUsers.tsx src/renderer/src/pages/ProfileEditor.tsx src/renderer/src/pages/Audit.tsx src/renderer/src/pages/BackupRestore.tsx src/renderer/src/pages/License.tsx` exits 0.
- `grep -rn "bg-card" src/renderer/src/pages/SettingsHub.tsx src/renderer/src/pages/SettingsCapture.tsx src/renderer/src/pages/SettingsUsers.tsx src/renderer/src/pages/ProfileEditor.tsx src/renderer/src/pages/Audit.tsx src/renderer/src/pages/BackupRestore.tsx src/renderer/src/pages/License.tsx` returns nothing inside the page body where the visual swap happened (some `bg-card` may legitimately remain inside the table head sticky bg in Audit — verify each match manually).
- `grep -rn "bg-slate-50" src/renderer/src/pages/ProfileEditor.tsx` returns nothing (the AssetUploader quadrants and used-device rows were the slate-50 sites — replaced).
- All `data-testid` values on all 7 pages unchanged.
- Run `npx tsc --noEmit -p tsconfig.json` (or the project's typecheck command) — must exit 0.
- Run the project's test suite (`npm test` or `vitest run`) — every test must still pass. The settings-page tests are in `tests/renderer/`. No test should fail because of a className change (tests query via data-testid, not class).
</verify>

<done>
- All 7 Settings pages render on the warm-ivory clinical surface.
- Cards, inputs, primary CTAs, hover states, focus rings, table rows, and skeleton rows all match the ReportEditor palette.
- No semantic / functional changes. No new files. No test contracts broken. Typecheck + lint + tests all pass.
</done>
</task>

<task type="auto">
  <name>Task 3: Visual screenshot review + verify against ReportEditor</name>

<files>
- (no file edits — verification + a short follow-up commit if anything regressed)
</files>

<action>
After Tasks 1 + 2 land, do a visual sanity pass:

1. Run `npm run build` (or the project's renderer build) to confirm the Tailwind JIT picks up the new literal class strings (`bg-[#F7F1E6]`, `text-[#0E3A47]`, `border-[#E0D9C6]`, etc.). Any unmatched literal strings show up as unstyled DOM in dev mode.

2. Run the test suite. All tests must pass — the className changes don't affect functional contracts.

3. If the project has a dev-mode launch script, spot-check by booting the app and:
   - Open Report Editor → confirm the palette reads as before (no regression).
   - Open Settings → Hub → confirm the warm-ivory background + ivory cards + teal stripe + small-caps eyebrow.
   - Open Settings → Capture → confirm the preview area is still BLACK (do not change to ivory — that breaks the live video surface).
   - Open Settings → Profile → confirm the asset quadrants are ivory with hairline borders.
   - Open Settings → Audit → confirm the table rows hover in light teal `#E6EFF1`.
   - Open Settings → Backup & Restore → confirm the amber warning Alert is unchanged.
   - Open Settings → License → confirm the state dot colors are unchanged.

4. If any visual regression is found, fix in a follow-up commit under the same plan.

5. Update `.planning/STATE.md`'s "Quick Tasks Completed" table (per workflow) and write `SUMMARY.md` for the quick task directory.
</action>

<verify>
- `npm test` exits 0.
- `npm run lint` exits 0.
- `npm run typecheck` exits 0.
- Visual review notes (if any) appended to SUMMARY.md.
</verify>

<done>
- All tests pass, lint clean, typecheck clean.
- Visual sanity pass complete.
- SUMMARY.md + STATE.md updated.
- Commit atomically (the docs commit can be one step after the code commit, per the workflow).
</done>
</task>

</tasks>
