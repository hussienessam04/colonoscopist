---
phase: 08-licensing-ed25519-signed-lic-14-day-trial-activation-flow
plan: 06
type: execute
wave: 4
depends_on:
  - 08-01
  - 08-02
  - 08-03
  - 08-04
  - 08-05
files_modified:
  - tests/main/license/audit.test.ts
  - tests/main/license/gate-coverage.test.ts
  - tests/renderer/pages/license.test.tsx
  - tests/renderer/rtl/license.test.ts
  - scripts/check-license-gate.cjs
  - package.json
  - .planning/phases/08-licensing-ed25519-signed-lic-14-day-trial-activation-flow/08-UAT.md
autonomous: true
requirements:
  - LIC-04
  - LIC-03
  - AUDIT-01
  - I18N-03
user_setup: []

must_haves:
  truths:
    - "Every `ipcMain.handle(` call in `src/main/ipc/*.ts` is wrapped with `licenseGated(IPC.X, ...)` OR appears in `EXEMPT_CHANNELS` (per PITFALLS §Pitfall 6 + RESEARCH Pitfall 1)"
    - "`scripts/check-license-gate.cjs` exits non-zero when ANY `ipcMain.handle(` line is NOT wrapped (drift detection)"
    - "Audit row `license.activated` is emitted by `loadAndVerifyLicense` on success with `metadata: { vendorId, fingerprintHash, expiresAt }` (per D-11)"
    - "Audit row `license.invalid` is emitted by `loadAndVerifyLicense` on failure with `metadata: { reason, fingerprintHash }` (per D-11)"
    - "Audit row `license.gate_rejected` is emitted by `licenseGated` on every rejection with `metadata: { channel, fingerprintHash, code }` (per D-11)"
    - "Audit row `license.trial_started` is emitted by `wizardBootstrap` on first launch (Plan 02)"
    - "Renderer License sub-page tests cover: status card renders all 4 states, sidebar badge color-codes state, Load .lic file button calls the IPC, error toast on bad file, machine-id copy button"
    - "Renderer LicenseGate modal tests cover: modal shows on unactivated, modal shows on expired, dismiss button persists sessionStorage flag, 'Activate now' navigates to license route"
    - "Playwright RTL smoke test: `dir='rtl'` after language switch; `scrollWidth <= clientWidth` on License sub-page; screenshot captured"
    - "`08-UAT.md` documents manual Windows hardware smoke (disk-serial spawn) + the phase acceptance checklist per requirement"
  artifacts:
    - tests/main/license/audit.test.ts
    - tests/main/license/gate-coverage.test.ts
    - tests/renderer/pages/license.test.tsx
    - tests/renderer/rtl/license.test.ts
    - scripts/check-license-gate.cjs
    - .planning/phases/08-licensing-ed25519-signed-lic-14-day-trial-activation-flow/08-UAT.md
  key_links:
    - "`scripts/check-license-gate.cjs` follows the same shape as `scripts/check-ipc-contract.cjs` (the existing IPC contract grep gate): reads source files, greps for `ipcMain.handle(` lines, asserts every match is followed by `licenseGated(IPC.X, ...)` OR the channel name appears in EXEMPT_CHANNELS"
    - "`tests/main/license/gate-coverage.test.ts` imports `EXEMPT_CHANNELS` from `src/main/license/gate.ts` + uses `fs.readFileSync` to scan `src/main/ipc/*.ts` files + asserts every `ipcMain.handle(` line is either wrapped or in EXEMPT_CHANNELS — runtime guard for the grep gate"
    - "`tests/main/license/audit.test.ts` exercises the 4 license audit actions: `license.trial_started` (Plan 02), `license.activated` (Plan 04), `license.invalid` (Plan 04), `license.gate_rejected` (Plan 03). For each, asserts `action`, `entityType: 'license'`, `outcome`, and `metadata` shape"
    - "`tests/renderer/pages/license.test.tsx` mocks `window.api.license.{status, activate}` + `useRoute` + `useLicenseStatus`; renders <License /> inside <SettingsLayout>; asserts status card, sidebar entry, machine-id copy, picker trigger, error toast on bad file"
    - "`tests/renderer/rtl/license.test.ts` is a Playwright test that boots `npm run dev` + navigates to `/license` + asserts `dir='rtl'` after language switch + asserts `scrollWidth <= clientWidth` + captures screenshot — mirrors the Phase 7 `tests/renderer/rtl/backup-restore.test.ts` pattern"
---

<objective>
Close the phase with the audit/gate/test/UAT layer: a static grep gate (`scripts/check-license-gate.cjs`) that fails CI on any unwrapped `ipcMain.handle(`, runtime unit tests that assert every channel is gated or exempt, audit row emission tests for all 4 license actions, renderer component tests for the License sub-page + LicenseGate modal, a Playwright RTL smoke test, and the phase UAT.md acceptance plan.

Purpose: This is the test-coverage + drift-detection + manual-verification plan. The verify path (Plan 01), trial clock (Plan 02), IPC gate (Plan 03), load path (Plan 04), and renderer UX (Plan 05) are all in place; this plan proves they hold up under adversarial inputs + adds the manual acceptance checklist for the GCC-market ship gate.

Output: `scripts/check-license-gate.cjs` (CI grep gate) + `tests/main/license/gate-coverage.test.ts` (runtime guard) + `tests/main/license/audit.test.ts` (4 audit actions covered) + `tests/renderer/pages/license.test.tsx` (8+ License sub-page cases) + `tests/renderer/pages/license-gate.test.tsx` (4+ modal cases) + `tests/renderer/rtl/license.test.ts` (Playwright RTL smoke) + `package.json` `test:integration:smoke:phase8` script + `08-UAT.md` phase acceptance plan.
</objective>

<execution_context>
@C:/Users/Hussien Essam/.config/opencode/gsd-core/workflows/execute-plan.md
@C:/Users/Hussien Essam/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-licensing-ed25519-signed-lic-14-day-trial-activation-flow/08-CONTEXT.md
@.planning/phases/08-licensing-ed25519-signed-lic-14-day-trial-activation-flow/08-RESEARCH.md
@.planning/phases/08-licensing-ed25519-signed-lic-14-day-trial-activation-flow/08-VALIDATION.md
@.planning/phases/08-licensing-ed25519-signed-lic-14-day-trial-activation-flow/08-01-PLAN.md
@.planning/phases/08-licensing-ed25519-signed-lic-14-day-trial-activation-flow/08-03-PLAN.md

# Source-of-truth files the executor MUST read before touching anything
@scripts/check-ipc-contract.cjs (the existing grep gate shape: `readOrFail(p)` + grep + exit 2 on drift + console.log on pass. The new gate mirrors this shape exactly.)
@src/main/license/gate.ts (Plan 03 exports `EXEMPT_CHANNELS: ReadonlySet<string>`; the gate-coverage test imports this set)
@src/main/db/audit.ts (the audit() helper; the new audit tests exercise the 4 license actions)
@src/main/ipc/license.ts (Plan 04 wired LICENSE_ACTIVATE → loadAndVerifyLicense; the audit tests exercise the activation path)
@tests/renderer/pages/BackupRestore.test.tsx (the existing pattern for a Settings sub-page test; license.test.tsx follows the same shape — mock api + render <SettingsLayout> + assert)
@tests/renderer/rtl/_helpers.ts (the shared `smokeRoute(page, path, name)` helper from Phase 7 — the new Playwright test reuses this)
@playwright.config.ts (the existing E2E config from Phase 7 — the new test runs against the dev server on port 5173)
</context>

<tasks>

<task type="auto">
  <name>Task 1: grep gate + gate-coverage + audit tests + renderer tests + Playwright RTL + UAT + smoke script</name>
  <files>
    - tests/main/license/audit.test.ts
    - tests/main/license/gate-coverage.test.ts
    - tests/renderer/pages/license.test.tsx
    - tests/renderer/rtl/license.test.ts
    - scripts/check-license-gate.cjs
    - package.json
    - .planning/phases/08-licensing-ed25519-signed-lic-14-day-trial-activation-flow/08-UAT.md
  </files>
  <read_first>
    - scripts/check-ipc-contract.cjs (the existing gate shape: read 3 files, grep for literals, exit 2 on missing; console.log success; the new gate follows the SAME shape but scans `src/main/ipc/*.ts` for `ipcMain.handle(` lines and asserts every match is wrapped)
    - src/main/license/gate.ts (Plan 03 exports `EXEMPT_CHANNELS` as `ReadonlySet<string>`; the gate-coverage test imports this set as the source of truth)
    - src/main/db/audit.ts (the audit() helper inserts into audit_log with `(user_id, action, entity_type, entity_id, metadata, outcome, created_at)`; the new audit tests verify each license action emits the correct row)
    - tests/renderer/pages/BackupRestore.test.tsx (the existing renderer page test pattern; the new license.test.tsx follows the SAME shape — mock api.license + render <License /> + assert)
    - playwright.config.ts (the E2E config from Phase 7; uses port 5173 for dev server; the new Playwright test reuses `smokeRoute(page, path, name)` from `tests/renderer/rtl/_helpers.ts`)
  </read_first>
  <behavior>
    - scripts/check-license-gate.cjs: exits 0 when every `ipcMain.handle(` in `src/main/ipc/*.ts` is wrapped with `licenseGated(IPC.X, ...)` OR the channel is in `EXEMPT_CHANNELS`; exits 2 with a stderr message identifying the unwrapped channel otherwise
    - gate-coverage.test.ts: runtime mirror of the grep gate — runs in vitest, asserts the same coverage guarantee
    - audit.test.ts (4 cases):
      - `license.trial_started` row: `metadata: { trialStartedAt: <ms> }`, `user_id` matches the wizard admin's userId (set by wizardBootstrap's audit emit)
      - `license.activated` row: `metadata: { vendorId: 'test-vendor', fingerprintHash, expiresAt: null }`, `user_id IS NULL`
      - `license.invalid` row: `metadata: { reason: 'SIGNATURE_MISMATCH', fingerprintHash }`, `user_id IS NULL`
      - `license.gate_rejected` row: `metadata: { channel: 'patients:create', fingerprintHash, code: 'IPC_LICENSE_INVALID' }`, `user_id IS NULL`
    - license.test.tsx (8+ cases):
      - state='unactivated' → "Not activated" label + gray dot + Load button enabled
      - state='trial' → "Trial" label + blue dot + "X days remaining" line + Load button enabled
      - state='expired' → "Expired" label + red dot + Load button enabled
      - state='licensed' → "Licensed" label + green dot + vendor id + licensed at + Load button enabled (allows re-loading)
      - "Load .lic file…" button click → `window.api.license.pickAndActivate` called once with NO arguments
      - bad file (IPC returns `{ok: false, code: 'IPC_LICENSE_INVALID', reason: '...'}`) → error toast surfaces
      - success (IPC returns `{ok: true, vendorId, licensedAt}`) → toast.success + `useLicenseStatus().refresh()` is called
      - machine-id copy button → `navigator.clipboard.writeText` called with the raw hex (NOT the formatted grouped blocks)
      - sidebar entry `settings-hub-license` navigates to `{name: 'license'}` route
    - license-gate.test.tsx (or fold into license.test.tsx) (4+ cases):
      - state='unactivated' → modal renders + Activate now button + Continue in trial button
      - state='expired' → modal renders + Activate now button (NO Continue in trial button — grayed out per D-05)
      - state='trial' → modal does NOT render (sidebar badge only)
      - state='licensed' → modal does NOT render
      - "Continue in trial" click → sessionStorage flag set + modal disappears
      - "Activate now" click → sessionStorage flag set + route navigates to `{name: 'license'}`
    - tests/renderer/rtl/license.test.ts (1 case):
      - `await smokeRoute(page, '/license', 'license')` — asserts `dir='rtl'` after the language switch in the shared helper, asserts `scrollWidth <= clientWidth`, captures screenshot
  </behavior>
  <action>
    **1. Create `scripts/check-license-gate.cjs`:**

    ```
    #!/usr/bin/env node
    // Grep gate: assert every ipcMain.handle() registration in src/main/ipc/*.ts
    // is wrapped with licenseGated(IPC.X, ...) OR appears in EXEMPT_CHANNELS.
    // Mirrors scripts/check-ipc-contract.cjs shape; exits non-zero on drift.

    const fs = require('node:fs');
    const path = require('node:path');
    const glob = require('node:fs'); // simple — use readdirSync instead

    const ROOT = path.join(__dirname, '..');
    const IPC_DIR = path.join(ROOT, 'src', 'main', 'ipc');
    const GATE_PATH = path.join(ROOT, 'src', 'main', 'license', 'gate.ts');

    function readOrFail(p) {
      if (!fs.existsSync(p)) {
        console.error(`LICENSE GATE DRIFT: missing file ${p}`);
        process.exit(2);
      }
      return fs.readFileSync(p, 'utf8');
    }

    // Extract EXEMPT_CHANNELS from gate.ts — parse the Set<string>([...]) literal.
    const gateSrc = readOrFail(GATE_PATH);
    const exemptMatch = gateSrc.match(/EXEMPT_CHANNELS[^=]*=\s*new Set<string>\(\[([\s\S]*?)\]\)/);
    if (!exemptMatch) {
      console.error(`LICENSE GATE DRIFT: could not parse EXEMPT_CHANNELS from ${GATE_PATH}`);
      process.exit(2);
    }
    const exemptChannels = new Set(
      exemptMatch[1]
        .split(',')
        .map((s) => s.trim())
        .filter((s) => s.startsWith('IPC.'))
        .map((s) => s.slice(4)) // strip "IPC."
    );

    // Scan every src/main/ipc/*.ts file for ipcMain.handle( occurrences.
    const ipcFiles = fs.readdirSync(IPC_DIR).filter((f) => f.endsWith('.ts'));
    let drift = false;
    for (const file of ipcFiles) {
      const src = readOrFail(path.join(IPC_DIR, file));
      const lines = src.split('\n');
      for (let i = 0; i < lines.length; i++) {
        const line = lines[i];
        const handleMatch = line.match(/ipcMain\.handle\(\s*(IPC\.[A-Z_]+|'([a-z:-]+)')/);
        if (!handleMatch) continue;
        const channel = handleMatch[1] ? handleMatch[1].slice(4) : handleMatch[2];
        const isExempt = exemptChannels.has(channel);
        const isWrapped = /licenseGated\(\s*IPC\.[A-Z_]+|licenseGated\(\s*'[a-z:-]+'/.test(line);
        if (isExempt && !isWrapped) continue; // exempt channels don't need wrapping (gate.ts handler is no-op)
        if (isWrapped) continue;
        console.error(`LICENSE GATE DRIFT: ${file}:${i + 1}  ipcMain.handle(${channel}) is not wrapped with licenseGated(IPC.${channel}, ...)`);
        drift = true;
      }
    }

    if (drift) {
      console.error('LICENSE GATE DRIFT: unwrapped ipcMain.handle() calls found. Wrap each call with licenseGated(IPC.X, handler) per src/main/license/gate.ts.');
      process.exit(2);
    }
    console.log('license gate OK (' + ipcFiles.length + ' IPC files scanned)');
    ```

    **2. Create `tests/main/license/gate-coverage.test.ts`:**

    Runtime mirror of the grep gate. Uses `fs.readdirSync` + `fs.readFileSync` to scan the same files. Imports `EXEMPT_CHANNELS` from `src/main/license/gate.ts` as the source of truth. The test fails if the source-of-truth drift exists; it's the in-process check that complements the shell script.

    ```
    import { describe, it, expect } from 'vitest';
    import fs from 'node:fs';
    import path from 'node:path';
    import { EXEMPT_CHANNELS } from '../../src/main/license/gate';

    describe('IPC gate coverage (LIC-04)', () => {
      const IPC_DIR = path.join(__dirname, '..', '..', 'src', 'main', 'ipc');
      const ipcFiles = fs.readdirSync(IPC_DIR).filter((f) => f.endsWith('.ts'));

      it('every ipcMain.handle() is wrapped or in EXEMPT_CHANNELS', () => {
        const drift: string[] = [];
        for (const file of ipcFiles) {
          const src = fs.readFileSync(path.join(IPC_DIR, file), 'utf8');
          const lines = src.split('\n');
          for (let i = 0; i < lines.length; i++) {
            const line = lines[i];
            const handleMatch = line.match(/ipcMain\.handle\(\s*(IPC\.[A-Z_]+|'([a-z:-]+)')/);
            if (!handleMatch) continue;
            const channel = handleMatch[1] ? handleMatch[1].slice(4) : handleMatch[2];
            const isExempt = [...EXEMPT_CHANNELS].some((c) => c.endsWith(':' + channel.split(':').pop()) || c === 'IPC.' + channel || c === channel);
            const isWrapped = /licenseGated\(/.test(line);
            if (isExempt) continue;
            if (isWrapped) continue;
            drift.push(`${file}:${i + 1}  ipcMain.handle(${channel})`);
          }
        }
        expect(drift).toEqual([]);
      });
    });
    ```

    **3. Create `tests/main/license/audit.test.ts`:**

    4 cases from the `<behavior>` block. Each test:
    - Sets up a fresh in-memory DB (mirror `tests/main/db/migrations/0002_procedures.test.ts` setup)
    - Calls the appropriate audit-emitting function (Plan 02's `wizardBootstrap`, Plan 04's `loadAndVerifyLicense`, Plan 03's `licenseGated` wrapper)
    - Reads the audit_log via `auditRepo.list({action: 'license.trial_started'})` (or similar)
    - Asserts the row's `metadata`, `user_id`, `outcome` shape

    The test uses a real DB instance to exercise the full audit-log path (the `audit()` helper writes via `better-sqlite3` prepared statements). The test mocks `getLicenseStatus` via `vi.mock('../license/status')` and stubs `computeMachineFingerprint` via `vi.mock('../license/fingerprint')` for deterministic assertions.

    **4. Create `tests/renderer/pages/license.test.tsx`:**

    8+ cases from the `<behavior>` block. Mock `window.api.license` with the `MockApi` pattern from `tests/renderer/setup.ts`. Mock `useRoute` + `useLicenseStatus` with `vi.mock`. Render `<License />` inside a test wrapper that includes the i18n provider (mirror BackupRestore.test.tsx setup). Use `@testing-library/user-event` for click interactions. Assert via `data-testid` attributes (the License.tsx Plan 05 already exposes them).

    **5. Create `tests/renderer/rtl/license.test.ts`:**

    Playwright test. Mirrors `tests/renderer/rtl/backup-restore.test.ts` from Phase 7:

    ```
    import { test, expect } from '@playwright/test';
    import { smokeRoute } from './_helpers';

    test('License page renders correctly in RTL', async ({ page }) => {
      await smokeRoute(page, '/license', 'license');
      const html = page.locator('html');
      await expect(html).toHaveAttribute('dir', 'rtl');
      await expect(html).toHaveAttribute('lang', 'ar');
    });
    ```

    The shared `smokeRoute` helper (Phase 7) handles the auth.status mock + route navigation + scrollWidth check + screenshot capture.

    **6. Add a `test:integration:smoke:phase8` script to `package.json`:**

    Append to the `scripts` section (after the existing `test:integration:smoke:phase7`):
    ```
    "test:integration:smoke:phase8": "node scripts/run-vitest.cjs --run tests/integration/license-verify-roundtrip.test.ts tests/integration/license-trial-survives-reboot.test.ts tests/integration/license-gate-blocks-procedure.test.ts",
    ```

    **7. Create `.planning/phases/08-licensing-ed25519-signed-lic-14-day-trial-activation-flow/08-UAT.md`:**

    Phase acceptance plan (mirror the Phase 7 `07-UAT.md` shape):
    ```
    # Phase 8 — User Acceptance Test Plan

    **Phase:** 8 — Licensing (Ed25519 signed `.lic` + 14-day trial + activation)
    **Ship gate:** Windows desktop, offline-only, GCC small clinics

    ## Prerequisites
    - Fresh Windows 10/11 workstation
    - Built installer from `electron-vite build` + `electron-builder`
    - No `.lic` file present
    - No internet connection (offline-only)

    ## Test cases per requirement

    ### LIC-01: 14-day trial with full features, no card required
    1. Install the app on a fresh workstation
    2. Complete the wizard (any PIN)
    3. Verify the boot-time modal appears with "Welcome to Colonoscopist" copy
    4. Click "Continue in trial"
    5. Verify the Patient List renders; sidebar shows "Trial · 14d remaining"
    6. Close the app; reopen
    7. Verify the modal does NOT re-appear (sessionStorage flag persists across launches? NO — sessionStorage is cleared on Electron restart; this is per the design)
    8. Verify the sidebar still shows "Trial · 13d remaining"
    9. (Manual-only) Set the system clock forward 14 days
    10. Reopen the app
    11. Verify the boot-time modal appears with EXPIRED copy
    12. Verify all gated IPC channels return `IPC_LICENSE_EXPIRED` when invoked (procedures.create, etc.)

    ### LIC-02: Ed25519-signed `.lic` bound to fingerprint
    1. Generate a test keypair: `node -e "import('@noble/ed25519').then(m => console.log(Array.from(m.keygen().secretKey).join(',')))"` — save to `./secrets/ed25519.private`
    2. Vendor-side: `node scripts/gen-license.cjs <machine-fingerprint> <vendor-id>` — produces `<machineId>.lic`
    3. Copy the `.lic` file to the clinic workstation
    4. On the workstation: Settings → License → "Load .lic file…"
    5. Verify the dialog opens; select the `.lic` file
    6. Verify the sidebar badge updates to green dot + "Licensed · Perpetual"
    7. Verify the status card shows the machine id + vendor id + activation timestamp
    8. Tampered test: edit one byte in the `.lic` JSON
    9. Re-load the tampered `.lic` → verify the toast error surfaces with "Invalid license" copy
    10. Verify the audit page shows a `license.invalid` row with the tampering attempt metadata

    ### LIC-03: Activation prompt + expired gate
    1. From LIC-02.6 above: load a valid `.lic` → modal disappears + sidebar updates
    2. Uninstall the app via Windows Settings
    3. Reinstall the app
    4. Verify the `<userData>` folder persists (Electron installer preserves it)
    5. Verify the License sub-page still shows "Licensed · Perpetual" (the sidecar files survived reinstall)
    6. Manually delete `<userData>/data/license/{license.json,license.sig}` to simulate corruption
    7. Reopen the app → verify the boot-time modal appears (no license)
    8. Verify the IPC gate returns `IPC_LICENSE_INVALID` on `procedures.create` until a new `.lic` is loaded

    ### LIC-04: All IPC handlers gated
    1. From a fresh install (no license, no trial):
    2. Attempt each IPC channel via the renderer:
       - `auth.status` → succeeds (EXEMPT)
       - `auth.bootstrap` → succeeds (EXEMPT)
       - `patients.list` → returns `{ok: false, code: 'IPC_LICENSE_INVALID'}`
       - `procedures.create` → returns `{ok: false, code: 'IPC_LICENSE_INVALID'}`
       - `audit.log` → succeeds (EXEMPT)
       - `license.status` → succeeds (EXEMPT, returns `{state: 'unactivated'}`)
    3. Verify the audit page shows `license.gate_rejected` rows for each gated channel that was invoked
    4. After activating a valid `.lic`, re-invoke the same gated channels → they succeed

    ## Pass/Fail
    - All automated tests pass: `npm run test:unit && RUN_SMOKE=1 npm run test:integration:smoke:phase8 && npm run test:e2e`
    - Manual Windows hardware smoke for the disk-serial `wmic` spawn passes
    - Vendor CLI smoke: vendor generates a `.lic` from a machine fingerprint, sends it to the clinic, clinic activates successfully

    ## Manual-Only Verifications
    - `wmic diskdrive get serialnumber` returns a real serial on the workstation
    - `vendor gen-license.cjs <fingerprint> <vendor>` produces a `.lic` the clinic can activate
    - First-launch modal blocks procedure capture until activation
    ```

    **8. Add the grep gate to the test:unit npm script (optional but recommended):**

    Per Phase 1's existing pattern (the `check-ipc-contract.cjs` runs as part of CI), the license gate could be wired into `npm run test:unit` — but the simpler approach is to document that both gates run in CI (the orchestrator wires CI later). For this plan, just ship the script + the runtime guard test.
  </action>
  <verify>
    <automated>node scripts/check-license-gate.cjs && node scripts/run-vitest.cjs --run tests/main/license/ tests/renderer/pages/license.test.tsx tests/renderer/pages/license-gate.test.tsx && npm run typecheck && RUN_SMOKE=1 npm run test:integration:smoke:phase8</automated>
  </verify>
  <done>
    - `node scripts/check-license-gate.cjs` exits 0 (every `ipcMain.handle(` is wrapped or in EXEMPT_CHANNELS)
    - All 4 audit.test.ts cases pass (license.trial_started, license.activated, license.invalid, license.gate_rejected — correct metadata + user_id + outcome shape)
    - gate-coverage.test.ts passes (runtime mirror of the grep gate)
    - All 8+ license.test.tsx cases pass + 4+ license-gate cases pass (the License sub-page + the LicenseGate modal)
    - Playwright RTL test (`tests/renderer/rtl/license.test.ts`) passes (requires `npm run dev` running — matches Phase 7's pattern)
    - `npm run typecheck` clean (both typecheck:node + typecheck:web)
    - RUN_SMOKE=1 npm run test:integration:smoke:phase8 passes (3 integration tests: roundtrip + reboot + gate-blocks)
    - `08-UAT.md` documents the manual Windows hardware smoke + the per-requirement acceptance checklist
    - `package.json` `test:integration:smoke:phase8` script is wired
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CI ↔ grep gate | The grep gate runs in CI; drift exits non-zero → CI fails |
| Renderer ↔ sandbox | Renderer tests run under happy-dom; the IPC bridge is mocked; the sandbox boundary is not exercised in unit tests |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-08-T01 | Tampering | Grep gate drift — new IPC channel added in a later phase without the wrapper | medium | mitigate | The grep gate runs in CI; drift exits non-zero. The runtime guard test (`gate-coverage.test.ts`) catches the same drift in unit tests. |
| T-08-T02 | Repudiation | Audit row missing on a license event | medium | mitigate (covered by audit.test.ts) | The 4 audit tests cover the 4 license actions; future license actions MUST add a matching test (or the test file fails to grow). |
| T-08-T03 | Information Disclosure | License metadata leaks via test failure logs | low | accept | The audit test asserts metadata shape including the fingerprint hash; the hash is NOT reversible to the raw MAC/serial. Test logs are not user-facing. |
| T-08-T04 | Spoofing | Playwright test runs against a dev server with mock auth (Phase 7 pattern) | low | accept | The Playwright harness is for RTL smoke + scrollWidth check, not for license logic. The license logic is covered by the unit + integration tests. |
| T-08-SC | Tampering | New test packages install (`@playwright/test` was installed Phase 7; no new packages) | low | mitigate | No new packages; the Playwright config from Phase 7 is reused. |

## Mitigations implemented by this plan

- **T-08-T01** (grep drift): `scripts/check-license-gate.cjs` + `tests/main/license/gate-coverage.test.ts` provide defense-in-depth — CI + unit test coverage.
- **T-08-T02** (audit coverage): 4 audit tests cover the 4 license actions; new actions require new tests.

## Mitigations deferred

- **T-08-UAT** (Windows hardware smoke for `wmic`): documented in `08-UAT.md` as a manual-only verification per RESEARCH §Validation Architecture.
- **T-08-VENDOR** (vendor CLI smoke on the vendor's machine): documented in `08-UAT.md` as a manual-only verification.
</threat_model>

<verification>
- Full suite green (`npm run test:unit` + `RUN_SMOKE=1 npm run test:integration:smoke:phase8`)
- Grep gate passes (`node scripts/check-license-gate.cjs` exits 0)
- Typecheck clean (`npm run typecheck`)
- Playwright RTL smoke runs (requires `npm run dev` running — matches Phase 7's documented behavior)
- `08-UAT.md` is readable + complete + matches the Phase 7 `07-UAT.md` shape
</verification>

<success_criteria>
- All 4 audit.test.ts cases pass
- All 8+ license.test.tsx cases pass (License sub-page)
- All 4+ license-gate cases pass (modal visibility + dismissal + navigation)
- gate-coverage.test.ts passes (runtime gate mirror)
- Playwright RTL test passes (when dev server is running)
- Grep gate passes (`scripts/check-license-gate.cjs` exits 0)
- 08-UAT.md is complete
- All 3 phase integration tests pass with RUN_SMOKE=1 (roundtrip + reboot + gate-blocks)
</success_criteria>

<output>
Create `.planning/phases/08-licensing-ed25519-signed-lic-14-day-trial-activation-flow/08-06-SUMMARY.md` when done
</output>
