---
phase: 08-licensing-ed25519-signed-lic-14-day-trial-activation-flow
plan: 05
type: execute
wave: 3
depends_on:
  - 08-01
  - 08-02
  - 08-04
files_modified:
  - src/renderer/src/components/SettingsSidebar.tsx
  - src/renderer/src/lib/router.ts
  - src/renderer/src/App.tsx
  - src/renderer/src/pages/License.tsx
  - src/renderer/src/components/LicenseGate.tsx
  - src/renderer/src/hooks/useLicenseStatus.ts
  - src/renderer/src/i18n/en/translation.json
  - src/renderer/src/i18n/ar/translation.json
autonomous: true
requirements:
  - LIC-03
  - I18N-01
  - I18N-03
user_setup: []

must_haves:
  truths:
    - "License sub-page renders a status card with machine id (4-char grouped blocks), trial countdown, license summary, AND a 'Load .lic file…' button (per D-04)"
    - "SettingsSidebar has a `License` entry between `Audit` and `Backup & Restore`; the entry routes to `{ name: 'license' }` (per D-04)"
    - "Route union extends with `| { name: 'license' }`; `App.tsx` renders `<License />` for the new case (per D-04)"
    - "`<LicenseGate>` mounted above the route render in `App.tsx` shows the activation modal on `state: 'unactivated'` or `state: 'expired'` (per D-05)"
    - "Activation modal has 'Activate now' button (navigates to `{name: 'license'}`) AND 'Continue in trial' button (only shown for unactivated; grayed out for expired per D-05); `sessionStorage` flag dismisses the modal for the rest of the session (per CONTEXT agent discretion)"
    - "Sidebar badge color-codes license state: green dot = licensed, blue dot = trial, red dot = expired, gray dot = unactivated (per CONTEXT.md specific ideas)"
    - "Every new license string lands in BOTH `en/translation.json` AND `ar/translation.json`; the existing D-24 parity check (`tests/main/license/i18n-parity.test.ts` or similar) covers the new keys (per D-14)"
    - "Renderer NEVER composes absolute paths; 'Load .lic file…' button calls `dialog.showOpenDialog` via the `licensePickAndActivate` IPC handler (Plan 04 wraps the dialog + `loadAndVerifyLicense` in one main-side call — Phase 7 D-13 verbatim pattern)"
  artifacts:
    - src/renderer/src/pages/License.tsx
    - src/renderer/src/components/LicenseGate.tsx
    - src/renderer/src/hooks/useLicenseStatus.ts
  key_links:
    - "`useLicenseStatus()` is an SWR-style hook (calls `license.status()` on mount, exposes `{status, refresh}`); the LicenseGate AND the License sub-page both consume this hook so the activation success refreshes the gate visibility"
    - "`SettingsSidebar.tsx` extends `SettingsTab` union with `'license'`; the new entry mirrors the `Audit` button styling (icon = `KeyRound` from lucide-react; `data-testid=\"settings-hub-license\"`; `data-active=\"true\"` when `activeTab === 'license'`) — visual order: Capture → Profile → Audit → License → Backup & Restore → Users"
    - "`App.tsx` switch adds `case 'license': return <License />`; the `<LicenseGate>` wrapper renders ABOVE the switch so the modal appears regardless of which authenticated route is active"
    - "`License.tsx` reuses `SettingsLayout` (from Plan 03-06 unified shell) with `title={t('license.pageTitle')}`, `subtitle={t('license.pageKicker')}`, `activeTab=\"license\"`, `backTestId=\"license-back\"` — matches the BackupRestore sub-page shape from Plan 07-05"
    - "The 'Load .lic file…' button in License.tsx calls `window.api.license.pickAndActivate()` with NO arguments; Plan 04 ships `IPC.LICENSE_PICK_AND_ACTIVATE` which wraps `dialog.showOpenDialog` + `loadAndVerifyLicense` in one main-side call (Phase 7 D-13 verbatim pattern); the renderer never composes the path"
    - "`i18n/en/translation.json` + `i18n/ar/translation.json` add a top-level `license.*` namespace with these keys: `pageTitle`, `pageKicker`, `pageDescription`, `statusLicensed`, `statusTrial`, `statusExpired`, `statusUnactivated`, `machineIdLabel`, `machineIdCopy`, `vendorIdLabel`, `licensedAtLabel`, `trialDaysRemainingLabel`, `loadLicButton`, `loadLicButtonInFlight`, `loadLicSuccess`, `loadLicFailed`, `loadLicInvalidReason`, `modalTitle`, `modalUnactivatedBody`, `modalExpiredBody`, `continueTrial`, `activateNow`, `sidebarEntry`, `sidebarBadgeLicensed`, `sidebarBadgeTrial`, `sidebarBadgeExpired`, `sidebarBadgeUnactivated` — EN + AR pair per CONTEXT D-14"
---

<objective>
Ship the renderer-side license UX: the `License` sub-page (status card + machine-id display + Load .lic button) mounted in the Settings sidebar, the boot-time `<LicenseGate>` modal that fires on `unactivated` / `expired` states, the SWR-style `useLicenseStatus` hook, the i18n keys for both EN + AR, and the router union extension. Everything reuses the Phase 7 `SettingsLayout` + `SettingsSidebar` pattern from `BackupRestore`.

Purpose: Closes LIC-03 + I18N-01 + I18N-03 UI halves. Plan 04 ships the activation IPC + vendor CLI; this plan ships the user-facing surface that calls them. Without this plan, the clinic sees no UI to activate a license — the verify path (Plan 01) + load path (Plan 04) work but the renderer can't reach them.

Output: Production-quality `License.tsx` sub-page + `LicenseGate.tsx` boot-time modal + `useLicenseStatus.ts` hook + SettingsSidebar entry + Router extension + App.tsx wrapper + bilingual i18n keys.
</objective>

<execution_context>
@C:/Users/Hussien Essam/.config/opencode/gsd-core/workflows/execute-plan.md
@C:/Users/Hussien Essam/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-licensing-ed25519-signed-lic-14-day-trial-activation-flow/08-CONTEXT.md
@.planning/phases/08-licensing-ed25519-signed-lic-14-day-trial-activation-flow/08-RESEARCH.md
@.planning/phases/08-licensing-ed25519-signed-lic-14-day-trial-activation-flow/08-VALIDATION.md
@.planning/phases/08-licensing-ed25519-signed-lic-14-day-trial-activation-flow/08-01-PLAN.md
@.planning/phases/08-licensing-ed25519-signed-lic-14-day-trial-activation-flow/08-04-PLAN.md

# Source-of-truth files the executor MUST read before touching anything
@src/renderer/src/components/SettingsSidebar.tsx (the existing sidebar pattern: Video/Capture, UserCircle/Profile, FileSearch/Audit, HardDrive/Backup & Restore, Shield/Users; add KeyRound/License between Audit and Backup & Restore)
@src/renderer/src/lib/router.ts (the Route union ends with `{name: 'patient-procedures'; patientId: string}`; append `| {name: 'license'}`)
@src/renderer/src/App.tsx (the switch structure: case 'wizard' | 'login' | ... | 'patient-procedures'; add case 'license' BEFORE 'patient-procedures'; wrap the switch with <LicenseGate>)
@src/renderer/src/pages/BackupRestore.tsx (the sub-page shape: SettingsLayout + Card grid + status indicators + toast.success/error + integration with `dialog.showOpenDialog` via IPC; License.tsx follows the same shape)
@src/renderer/src/components/SettingsLayout.tsx (the unified shell: title + subtitle + activeTab + backTestId; License.tsx uses activeTab='license' + backTestId='license-back')
@src/renderer/src/i18n/en/translation.json (the existing top-level keys: audit, backup, recovery, etc.; append a `license` namespace at the end before the closing brace; keys match CONTEXT D-14)
@src/renderer/src/i18n/ar/translation.json (mirror the EN structure for the same keys; RTL-aware copy)
@tests/renderer/pages/BackupRestore.test.tsx (the existing test pattern: render <SettingsLayout> with mock api; the new license.test.tsx follows the same shape — Plan 06 ships the test)
</context>

<tasks>

<task type="auto">
  <name>Task 1: License sub-page + SettingsSidebar entry + LicenseGate modal + useLicenseStatus hook + Router + App + bilingual i18n</name>
  <files>
    - src/renderer/src/components/SettingsSidebar.tsx
    - src/renderer/src/lib/router.ts
    - src/renderer/src/App.tsx
    - src/renderer/src/pages/License.tsx
    - src/renderer/src/components/LicenseGate.tsx
    - src/renderer/src/hooks/useLicenseStatus.ts
    - src/renderer/src/i18n/en/translation.json
    - src/renderer/src/i18n/ar/translation.json
  </files>
  <read_first>
    - src/renderer/src/components/SettingsSidebar.tsx (the visual order is Capture → Profile → Audit → Backup & Restore → Users per the existing comment; License goes BETWEEN Audit and Backup & Restore per CONTEXT D-04. The icon is `KeyRound` from lucide-react (same library as the other icons — `Video`, `UserCircle`, `FileSearch`, `HardDrive`, `Shield`). The `data-testid` is `settings-hub-license`. The `SettingsTab` union extends with `'license'`.)
    - src/renderer/src/pages/BackupRestore.tsx (the sub-page pattern: `useState` for async state, `useEffect` for mount-time data fetch, `t()` for i18n, `toast.success`/`toast.error` for results, `data-testid` attributes for test seams. License.tsx follows the same shape but simpler — only ONE Card instead of two side-by-side.)
    - src/renderer/src/App.tsx (the switch ends with `case 'patient-procedures'`; add `case 'license': return <License />` BEFORE that case. The `<LicenseGate>` wrapper renders ABOVE the switch — place it as a sibling of the switch's return value, inside the same component.)
    - src/renderer/src/i18n/en/translation.json (the file ends with a closing brace; append the `license.*` keys at the end before the closing brace. The D-24 parity check in `tests/renderer/i18n-parity.test.ts` or similar will assert AR has matching keys.)
    - src/shared/ipc-contract.ts (the LicenseStatus shape from Plan 01: `{state: 'licensed' | 'trial' | 'expired' | 'unactivated', vendorId, licensedAt, expiresAt, trialStartedAt, trialDaysRemaining, machineId}` — the renderer consumes this exact shape via `useLicenseStatus`)
  </read_first>
  <behavior>
    - Renderer behavior verified end-to-end in Plan 06's `tests/renderer/pages/license.test.tsx` (8+ cases). This plan ships the production code; the test plan is Plan 06.
    - Manual visual check: open the app on a fresh DB → boot-time modal appears (state='unactivated'); navigate to Settings → License → see machine id grouped in 4-char blocks; click 'Load .lic file…' → dialog opens; load a valid `.lic` → toast.success + sidebar badge updates to green dot + modal disappears on next navigation
    - Manual RTL check: switch language to AR via ProfileEditor → `<html dir='rtl'>` flips → License sub-page renders correctly in RTL (the existing Phase 7 Playwright RTL smoke harness covers this in Plan 06)
  </behavior>
  <action>
    **1. Update `src/renderer/src/lib/router.ts`:**

    Append to the `Route` union (after the `{name: 'patient-procedures'; patientId: string}` line):
    ```
    // Phase 8 / Plan 05 — License sub-page (LIC-03). Reached via
    // SettingsSidebar; the boot-time <LicenseGate> modal navigates here
    // on the 'Activate now' button click. The route carries no params.
    | { name: 'license' }
    ```

    **2. Update `src/renderer/src/components/SettingsSidebar.tsx`:**

    - Add `KeyRound` to the lucide-react import line (alongside `FileSearch`, `HardDrive`, `Shield`, `UserCircle`, `Video`).
    - Extend the `SettingsTab` union with `'license'`.
    - Add the new `<Button>` entry BETWEEN the `Audit` and `Backup & Restore` buttons (per CONTEXT D-04 visual order: Capture → Profile → Audit → License → Backup & Restore → Users):
    ```
    <Button
      variant={activeTab === 'license' ? 'default' : 'outline'}
      className="justify-start"
      onClick={() => navigate({ name: 'license' })}
      data-testid="settings-hub-license"
      data-active={activeTab === 'license' ? 'true' : 'false'}
    >
      <KeyRound className="size-4 mr-2" aria-hidden="true" />
      {t('license.sidebarEntry')}
    </Button>
    ```
    - Import `useTranslation` from `react-i18next` at the top of the file: `import { useTranslation } from 'react-i18next';`. Wrap the existing function body so the `t()` call works.

    **3. Update `src/renderer/src/App.tsx`:**

    - Add `import License from './pages/License';` at the top.
    - Add `import LicenseGate from './components/LicenseGate';` at the top.
    - Add `case 'license':` BEFORE `case 'patient-procedures':` returning `<License />`.
    - Wrap the `switch (route.name) { ... }` with `<LicenseGate>{switch result}</LicenseGate>` — the wrapper reads `useLicenseStatus()` and renders the modal ABOVE the route when state is `unactivated` or `expired`. The switch returns ONE element; the wrapper takes `children: ReactNode`.

    **4. Create `src/renderer/src/hooks/useLicenseStatus.ts`:**

    SWR-style hook:
    ```
    import { useEffect, useState, useCallback } from 'react';
    import type { LicenseStatus } from '@shared/ipc-contract';

    export function useLicenseStatus(): {
      status: LicenseStatus | null;
      loading: boolean;
      refresh: () => Promise<void>;
    } {
      const [status, setStatus] = useState<LicenseStatus | null>(null);
      const [loading, setLoading] = useState(true);

      const refresh = useCallback(async () => {
        setLoading(true);
        try {
          const next = await window.api.license.status();
          setStatus(next);
        } catch {
          setStatus(null);
        } finally {
          setLoading(false);
        }
      }, []);

      useEffect(() => {
        void refresh();
      }, [refresh]);

      return { status, loading, refresh };
    }
    ```

    The hook is consumed by `LicenseGate` (modal visibility) AND `License.tsx` (status card).

    **5. Create `src/renderer/src/components/LicenseGate.tsx`:**

    Per RESEARCH §Pattern 5 + CONTEXT D-05:
    ```
    import { useState, type ReactNode } from 'react';
    import { Dialog, DialogContent, DialogHeader, DialogTitle, DialogDescription, DialogFooter } from '@/components/ui/dialog';
    import { Button } from '@/components/ui/button';
    import { useTranslation } from 'react-i18next';
    import { useRoute } from '@/lib/router';
    import { useLicenseStatus } from '@/hooks/useLicenseStatus';

    export function LicenseGate({ children }: { children: ReactNode }): JSX.Element {
      const { status } = useLicenseStatus();
      const { navigate } = useRoute();
      const { t } = useTranslation();
      const [dismissedThisSession, setDismissedThisSession] = useState(
        sessionStorage.getItem('license.modal.dismissed') === '1',
      );

      const showModal =
        !dismissedThisSession &&
        status !== null &&
        (status.state === 'unactivated' || status.state === 'expired');

      function dismiss(): void {
        sessionStorage.setItem('license.modal.dismissed', '1');
        setDismissedThisSession(true);
      }

      function activate(): void {
        dismiss();
        navigate({ name: 'license' });
      }

      return (
        <>
          {children}
          <Dialog open={showModal}>
            <DialogContent data-testid="license-gate-modal">
              <DialogHeader>
                <DialogTitle>{t('license.modalTitle')}</DialogTitle>
                <DialogDescription>
                  {status?.state === 'expired'
                    ? t('license.modalExpiredBody')
                    : t('license.modalUnactivatedBody')}
                </DialogDescription>
              </DialogHeader>
              <DialogFooter>
                {status?.state === 'unactivated' ? (
                  <Button
                    variant="outline"
                    onClick={dismiss}
                    data-testid="license-gate-continue-trial"
                  >
                    {t('license.continueTrial')}
                  </Button>
                ) : null}
                <Button
                  onClick={activate}
                  data-testid="license-gate-activate-now"
                >
                  {t('license.activateNow')}
                </Button>
              </DialogFooter>
            </DialogContent>
          </Dialog>
        </>
      );
    }
    ```

    **6. Create `src/renderer/src/pages/License.tsx`:**

    Per CONTEXT D-04 + Phase 7 D-13 verbatim (file picker pattern):
    ```
    import { useState } from 'react';
    import { useTranslation } from 'react-i18next';
    import { KeyRound, Copy, Loader2 } from 'lucide-react';
    import { toast } from 'sonner';
    import { Button } from '@/components/ui/button';
    import { Card, CardContent, CardDescription, CardHeader, CardTitle } from '@/components/ui/card';
    import { SettingsLayout } from '@/components/SettingsLayout';
    import { useLicenseStatus } from '@/hooks/useLicenseStatus';
    import type { LicenseStatus, LicenseState } from '@shared/ipc-contract';

    function formatMachineId(id: string): string {
      // Group the 64-char hex into 4-char blocks: a3f1-b9c2-7e4d-... (per CONTEXT agent discretion)
      return id.match(/.{1,4}/g)?.join('-') ?? id;
    }

    function statusBadgeColor(state: LicenseState): string {
      switch (state) {
        case 'licensed': return 'bg-emerald-500';
        case 'trial': return 'bg-blue-500';
        case 'expired': return 'bg-red-500';
        case 'unactivated': return 'bg-slate-400';
      }
    }

    export default function License(): JSX.Element {
      const { t } = useTranslation();
      const { status, loading, refresh } = useLicenseStatus();
      const [activating, setActivating] = useState(false);

      async function handleLoadLic(): Promise<void> {
        setActivating(true);
        try {
          // Plan 04 ships IPC.LICENSE_PICK_AND_ACTIVATE which wraps
          // dialog.showOpenDialog + loadAndVerifyLicense in one main-side
          // call (Phase 7 D-13 verbatim pattern). The renderer never
          // composes the path; the IPC handler owns the picker.
          const result = await window.api.license.pickAndActivate();
          if (result.ok) {
            toast.success(t('license.loadLicSuccess'));
            await refresh();
          } else {
            toast.error(
              t('license.loadLicInvalidReason', {
                reason: result.reason ?? t('license.loadLicFailed'),
              }),
            );
          }
        } catch (err) {
          toast.error(err instanceof Error ? err.message : t('license.loadLicFailed'));
        } finally {
          setActivating(false);
        }
      }

      async function handleCopyMachineId(): Promise<void> {
        if (!status) return;
        await navigator.clipboard.writeText(status.machineId);
        toast.success(t('license.machineIdCopied'));
      }

      return (
        <SettingsLayout
          title={t('license.pageTitle')}
          subtitle={t('license.pageKicker')}
          activeTab="license"
          backTestId="license-back"
        >
          <Card data-testid="license-status-card">
            <CardHeader>
              <CardTitle className="flex items-center gap-2">
                <KeyRound className="size-4" aria-hidden="true" />
                {t('license.statusTitle')}
                {status !== null ? (
                  <span
                    className={`inline-block size-2 rounded-full ${statusBadgeColor(status.state)}`}
                    data-testid="license-status-dot"
                    data-state={status.state}
                  />
                ) : null}
              </CardTitle>
              <CardDescription>{t('license.pageDescription')}</CardDescription>
            </CardHeader>
            <CardContent className="flex flex-col gap-4">
              {status !== null ? (
                <>
                  <div className="grid grid-cols-3 gap-2 pb-2 border-b border-slate-200">
                    <span className="text-muted-foreground">{t('license.statusLabel')}</span>
                    <span className="col-span-2" data-testid="license-status-label">
                      {t(`license.status${status.state.charAt(0).toUpperCase()}${status.state.slice(1)}`)}
                    </span>
                  </div>
                  {status.state === 'trial' ? (
                    <div className="grid grid-cols-3 gap-2 pb-2 border-b border-slate-200">
                      <span className="text-muted-foreground">{t('license.trialDaysRemainingLabel')}</span>
                      <span className="col-span-2" data-testid="license-trial-days">
                        {t('license.trialDaysRemainingValue', { days: status.trialDaysRemaining ?? 0 })}
                      </span>
                    </div>
                  ) : null}
                  {status.state === 'licensed' ? (
                    <>
                      <div className="grid grid-cols-3 gap-2 pb-2 border-b border-slate-200">
                        <span className="text-muted-foreground">{t('license.vendorIdLabel')}</span>
                        <span className="col-span-2 font-mono" data-testid="license-vendor-id">{status.vendorId ?? '—'}</span>
                      </div>
                      <div className="grid grid-cols-3 gap-2 pb-2 border-b border-slate-200">
                        <span className="text-muted-foreground">{t('license.licensedAtLabel')}</span>
                        <span className="col-span-2 font-mono" data-testid="license-licensed-at">
                          {status.licensedAt !== null ? new Date(status.licensedAt).toLocaleString() : '—'}
                        </span>
                      </div>
                    </>
                  ) : null}
                  <div className="grid grid-cols-3 gap-2">
                    <span className="text-muted-foreground">{t('license.machineIdLabel')}</span>
                    <div className="col-span-2 flex items-center gap-2">
                      <span className="font-mono break-all" data-testid="license-machine-id">
                        {formatMachineId(status.machineId)}
                      </span>
                      <Button
                        variant="ghost"
                        size="sm"
                        onClick={() => { void handleCopyMachineId(); }}
                        data-testid="license-copy-machine-id"
                      >
                        <Copy className="size-3" aria-hidden="true" />
                      </Button>
                    </div>
                  </div>
                </>
              ) : (
                <p className="text-sm text-muted-foreground italic">{t('license.loading')}</p>
              )}

              <Button
                size="lg"
                onClick={() => { void handleLoadLic(); }}
                disabled={activating}
                data-testid="license-load-lic"
                className="w-full sm:w-auto"
              >
                {activating ? (
                  <>
                    <Loader2 className="size-4 mr-2 animate-spin" aria-hidden="true" />
                    <span>{t('license.loadLicButtonInFlight')}</span>
                  </>
                ) : (
                  t('license.loadLicButton')
                )}
              </Button>
            </CardContent>
          </Card>
        </SettingsLayout>
      );
    }
    ```

    **Note on the dialog picker:** The renderer CANNOT call `dialog.showOpenDialog` directly (sandbox). Plan 04 ships `IPC.LICENSE_PICK_AND_ACTIVATE` which wraps `dialog.showOpenDialog` + `loadAndVerifyLicense` in one main-side call (Phase 7 D-13 verbatim pattern). The IPC channel + preload bridge + gate exemption are all owned by Plan 04; the renderer simply calls `window.api.license.pickAndActivate()` with NO arguments and consumes the `LicenseActivateResult` discriminated union. The handler IS exempt from the IPC gate (the user is actively activating).

    **7. i18n keys:**

    Append to BOTH `src/renderer/src/i18n/en/translation.json` AND `src/renderer/src/i18n/ar/translation.json` (BEFORE the closing brace). The EN keys (machine-translation as a placeholder for the AR side; the AR values are hand-written per D-20):

    EN (`src/renderer/src/i18n/en/translation.json`):
    ```
    "license": {
      "sidebarEntry": "License",
      "statusTitle": "License status",
      "pageTitle": "License",
      "pageKicker": "Manage your Colonoscopist license",
      "pageDescription": "Activate a license file from Colonoscopist support or track your 14-day trial.",
      "statusLabel": "Status",
      "statusLicensed": "Licensed",
      "statusTrial": "Trial",
      "statusExpired": "Expired",
      "statusUnactivated": "Not activated",
      "trialDaysRemainingLabel": "Days remaining",
      "trialDaysRemainingValue": "{{days}} days",
      "vendorIdLabel": "Vendor",
      "licensedAtLabel": "Activated at",
      "machineIdLabel": "Machine ID",
      "machineIdCopy": "Copy",
      "machineIdCopied": "Machine ID copied",
      "loadLicButton": "Load .lic file…",
      "loadLicButtonInFlight": "Activating…",
      "loadLicFailed": "Failed to activate license",
      "loading": "Loading license status…",
      "modalTitle": "Activate Colonoscopist",
      "modalUnactivatedBody": "Welcome to Colonoscopist. To continue using the app, please activate your license or start the 14-day trial.",
      "modalExpiredBody": "Your 14-day trial has ended. Please email your machine ID to Colonoscopist support for a license file.",
      "continueTrial": "Continue in trial",
      "activateNow": "Activate now",
      "sidebarBadgeLicensed": "Licensed · Perpetual",
      "sidebarBadgeTrial": "Trial · {{days}}d remaining",
      "sidebarBadgeExpired": "Expired",
      "sidebarBadgeUnactivated": "Not activated"
    }
    ```

    AR (`src/renderer/src/i18n/ar/translation.json`): mirror the keys with hand-written Arabic values (e.g. `sidebarEntry: "الترخيص"`, `pageTitle: "الترخيص"`, `loadLicButton: "تحميل ملف .lic…"` — the full set is hand-written per D-20; a partial example for `modalExpiredBody` is `"انتهت الفترة التجريبية البالغة 14 يومًا. يرجى إرسال معرف الجهاز إلى دعم Colonoscopist للحصول على ملف ترخيص."`).

    The D-24 parity check (existing test in `tests/renderer/i18n-parity.test.ts` or similar — verify by globbing) covers the new keys; if it doesn't exist, Plan 06 ships a new one.
  </action>
  <verify>
    <automated>npm run typecheck:web && npm run test:unit && node -e "const fs=require('node:fs'); const en=JSON.parse(fs.readFileSync('src/renderer/src/i18n/en/translation.json','utf8')); const ar=JSON.parse(fs.readFileSync('src/renderer/src/i18n/ar/translation.json','utf8')); const enKeys=Object.keys(en.license||{}).sort(); const arKeys=Object.keys(ar.license||{}).sort(); if (JSON.stringify(enKeys)!==JSON.stringify(arKeys)) { console.error('PARITY DRIFT: en=' + enKeys.join(',') + ' ar=' + arKeys.join(',')); process.exit(2); } else { console.log('i18n parity OK (' + enKeys.length + ' license.* keys)'); }"</automated>
  </verify>
  <done>
    - License sub-page renders correctly with all 4 license states (verified by the i18n key switch on `state`)
    - SettingsSidebar has the License entry between Audit and Backup & Restore; `data-testid=\"settings-hub-license\"` is present
    - Route union includes `{name: 'license'}`; App.tsx switch handles the case
    - `<LicenseGate>` renders above the switch; modal appears on `state: 'unactivated'` or `state: 'expired'`; dismisses on sessionStorage flag
    - `npm run typecheck:web` clean
    - `npm run test:unit` green (existing tests still pass; new renderer code doesn't break the contract tests)
    - i18n parity check passes (EN keys === AR keys for the `license` namespace)
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Renderer ↔ LICENSE_STATUS IPC | Renderer reads license state via the contract; status is cached server-side |
| Renderer ↔ LICENSE_PICK_AND_ACTIVATE IPC | Renderer triggers a dialog + activation in one round-trip; main owns the path |
| `<LicenseGate>` ↔ sessionStorage | Modal dismissal persists for the session; cleared on Electron app restart (per `sessionStorage` semantics) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-08-U01 | Spoofing | Renderer bypasses license gate via direct DOM manipulation | low | accept | The `<LicenseGate>` modal is a visual layer; the IPC gate (Plan 03) is the actual enforcement. A renderer that hides the modal still can't reach the gated IPC channels. Defense-in-depth: the gate AND the IPC wrapper both check `getLicenseStatus()`. |
| T-08-U02 | Tampering | sessionStorage flag manipulation to re-trigger the modal after dismissal | low | accept | Re-triggering the modal is cosmetic — the IPC gate remains enforced. The flag is per-session (cleared on app restart); a manipulated flag only hides the modal, doesn't grant license validity. |
| T-08-U03 | Information Disclosure | Machine id exposed in the renderer (PII concern) | low | accept | The machine id is a SHA-256 hash of CPU + disk serial + MAC — NOT a raw MAC or disk serial. The hash is bound to the workstation hardware; clinics sharing the id with the vendor is the documented workflow (per LIC-02). |
| T-08-U04 | Spoofing | Renderer sends a forged `LicenseStatus` from `useLicenseStatus()` | low | mitigate | The hook reads via IPC (`window.api.license.status()`); the renderer cannot forge the IPC response (sandbox baseline). The cached status on the main side is the source of truth (Plan 01's `status.ts`). |
| T-08-U05 | Denial of Service | User dismisses + re-triggers the modal thousands of times | low | accept | Cosmetic only; no resource exhaustion. |
| T-08-U06 | Spoofing | Picker returns a `.lic` file from a different machine (clinic email forwards it) | medium | mitigate (Plan 04) | The verify path checks the machine fingerprint; a `.lic` signed for a different machine fails `FINGERPRINT_MISMATCH` → `IPC_LICENSE_INVALID`. The picker UX surfaces this via the `loadLicFailed` toast. |

## Mitigations implemented by this plan

- **T-08-U01** (renderer bypass): the IPC gate (Plan 03) is the actual enforcement; the modal is cosmetic.
- **T-08-U04** (forged status): the hook reads via IPC; renderer is sandboxed.
- **T-08-U06** (license theft): Plan 04's verify path checks the fingerprint.

## Mitigations deferred to later plans

- **T-08-IPARITY** (i18n parity drift): Plan 06 ships the parity grep test (if it doesn't exist).
- **T-08-GREP** (drift in IPC gate coverage of `LICENSE_PICK_AND_ACTIVATE`): Plan 06 grep gate verifies the new channel is in EXEMPT_CHANNELS + is wrapped with `licenseGated`.
</threat_model>

<verification>
- Typecheck clean (typecheck:web)
- Full unit suite green (existing tests still pass)
- Manual visual check: boot a fresh app → modal appears → click Activate now → lands on License sub-page → click Load .lic file… → dialog opens → load a valid .lic → toast.success + sidebar badge updates
- Manual RTL check: switch to AR → `<html dir='rtl'>` flips → License sub-page renders correctly in RTL (visual inspection; Plan 06 ships the Playwright test)
</verification>

<success_criteria>
- License sub-page renders all 4 states with correct i18n keys
- SettingsSidebar has the License entry; router/App switch handles the case
- LicenseGate modal appears on unactivated/expired; dismisses via sessionStorage flag
- useLicenseStatus hook is the single source of license state in the renderer
- i18n parity: EN keys === AR keys for the license namespace
- typecheck:web passes; full unit suite passes
</success_criteria>

<output>
Create `.planning/phases/08-licensing-ed25519-signed-lic-14-day-trial-activation-flow/08-05-SUMMARY.md` when done
</output>
