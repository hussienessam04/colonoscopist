---
phase: 08-licensing-ed25519-signed-lic-14-day-trial-activation-flow
plan: 11
type: execute
wave: 1
depends_on: []
files_modified:
  - src/renderer/src/pages/SettingsCapture.tsx
  - src/renderer/src/pages/ProcedureRoom.tsx
  - tests/renderer/pages/settings-capture.test.tsx
  - tests/renderer/pages/procedure-room.test.tsx
autonomous: true
gap_closure: true
gap_ids: [G-08-5]
requirements: [LIC-04]

must_haves:
  truths:
    - "Settings → Capture renders <EmptyStateCard> instead of crashing when the license gate rejects capture.getDefaultDevice / capture.getPreset"
    - "Procedure Room renders without crashing and shows its existing no-device overlay when the license gate rejects capture.getDefaultDevice / capture.getPreset"
    - "Settings → Capture Save surfaces a clear toast (not a crash) when setDefaultDevice / setPreset are gate-rejected"
    - "The existing 08-10 fixes (8 other gated-IPC consumers) remain green and the full renderer suite still passes"
  artifacts:
    - "src/renderer/src/pages/SettingsCapture.tsx — wraps 4 capture.* IPC sites with safeInvoke + new `gated` state + renders EmptyStateCard in the body"
    - "src/renderer/src/pages/ProcedureRoom.tsx — wraps 3 capture.* IPC sites with safeInvoke (no EmptyStateCard; existing no-device overlay handles the null case)"
    - "tests/renderer/pages/settings-capture.test.tsx — 2 new regression tests (gated hydration + gated save toast)"
    - "tests/renderer/pages/procedure-room.test.tsx — 1 new regression test (gated hydration renders the no-device overlay, no crash)"
  key_links:
    - "src/renderer/src/lib/ipc-result.ts — safeInvoke<T>(p) helper from Plan 08-10 (already exists, no changes)"
    - "src/renderer/src/components/EmptyStateCard.tsx — empty-state component from Plan 08-10 (already exists, no changes)"
    - "src/renderer/src/pages/SettingsCapture.tsx:74-108 hydration useEffect — first failure point"
    - "src/renderer/src/pages/ProcedureRoom.tsx:52-68 hydration useEffect + 80-97 preset effect + 199-236 record toggle — same shape-assumption bug class"

---

<objective>
Mirror the Plan 08-10 (G-08-4) gated-IPC graceful-degrade fix on the two pages that plan missed: SettingsCapture and ProcedureRoom. Both pages call `window.api.capture.{getDefaultDevice,getPreset,setDefaultDevice,setPreset}` and currently treat the main-process gate's `{ok:false, code: 'IPC_LICENSE_*'}` rejection as a successful payload (string / object / preset). On the `expired` or `unactivated` license state this crashes the React tree to a white screen.

Purpose: ship the smallest fix that satisfies G-08-5 (settings-capture crash) and prevents the same crash on ProcedureRoom (which has the identical shape assumption), keeping the Plan 03 main-process gate contract untouched.

Output: 4 source/test files modified; 3 new regression tests; the Plan 08-10 baseline remains green.
</objective>

<execution_context>
@C:/Users/Hussien Essam/.config/opencode/gsd-core/workflows/execute-plan.md
@C:/Users/Hussien Essam/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/08-licensing-ed25519-signed-lic-14-day-trial-activation-flow/08-UAT-session.md

# Pattern precedents (read first)
@src/renderer/src/lib/ipc-result.ts
@src/renderer/src/components/EmptyStateCard.tsx
@src/renderer/src/pages/ProcedurePreview.tsx (mirrors the gated pattern at lines 101-149 + 281)

# Files to edit (read first)
@src/renderer/src/pages/SettingsCapture.tsx
@src/renderer/src/pages/ProcedureRoom.tsx
@tests/renderer/pages/settings-capture.test.tsx
@tests/renderer/pages/procedure-room.test.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wrap SettingsCapture gated IPC sites with safeInvoke + render EmptyStateCard</name>
  <files>src/renderer/src/pages/SettingsCapture.tsx, tests/renderer/pages/settings-capture.test.tsx</files>
  <action>
Per G-08-5, SettingsCapture is the crash surface. Mirror the Plan 08-10 (G-08-4) pattern from `src/renderer/src/pages/ProcedurePreview.tsx` (line 104 `gated` state + line 281 `{gated ? <EmptyStateCard /> : null}`) and `src/renderer/src/pages/PatientsList.tsx` (line 73 + line 95-102).

In `src/renderer/src/pages/SettingsCapture.tsx`:

1. Add two imports at the top of the file (next to the existing `@/components/*` imports):
   - `import { safeInvoke } from '@/lib/ipc-result';`
   - `import EmptyStateCard from '@/components/EmptyStateCard';`

2. Add a `gated` boolean state alongside the existing `useState` declarations: `const [gated, setGated] = useState<boolean>(false);`.

3. Rewrite the hydration useEffect (lines 74-108) to be async and use safeInvoke:
   - Call `const device = await safeInvoke(window.api.capture.getDefaultDevice())` inside an async IIFE so the existing `cancelled` ref pattern still works.
   - If `device === null`: `setGated(true); setHydrated(true);` and return. Do NOT call `setSavedDeviceId` with the gate object.
   - Else proceed with the existing `setSavedDeviceId(device)` + `pickBrowserId(device)` flow.
   - Inside the inner `getPreset({ deviceId: device })` call (line 90), also wrap with safeInvoke. Pass the result through `fromPreset(preset)` — `fromPreset` already handles `null`/`undefined` (returns the default form at line 37).
   - Keep the existing `cancelled` cleanup ref.

4. In `handleDeviceChange` (lines 114-128), wrap `window.api.capture.getPreset({ deviceId: name })` (line 120) with safeInvoke. On null, fall through to the existing `setForm(defaultForm()); setHydrated(true)` branch (treat null as "no preset for this device"). No other change.

5. In `handleSave` (lines 154-169), wrap BOTH `window.api.capture.setDefaultDevice` (line 160) and `window.api.capture.setPreset` (line 161) with safeInvoke. If either returns null, `toast.error('License required to save capture settings. Activate your license first.')` (or similar — message must NOT contain the literal substring of any acceptance criterion negative-gate) and `return` after `setSaving(false)`. Keep the local `qualityPresetSchema.parse(preset)` validation BEFORE the IPC calls (the form is already validated before hitting main).

6. In the JSX, render `<EmptyStateCard />` next to the existing no-device hint. The simplest placement: inside the `<aside>` (after the existing `{noSavedDevice ? ... : null}` block) as `{gated ? <EmptyStateCard /> : null}`. The Save button in the header stays disabled because `canSave` requires `selectedBrowserId !== null` which stays null when gated — no other UI changes.

In `tests/renderer/pages/settings-capture.test.tsx`, add 2 new `it(...)` blocks inside the existing `describe('Settings → Capture', ...)`:

- **`G-08-5: hydration gate rejection renders EmptyStateCard (no crash)`** — `setRoute({ name: 'settings-capture' })`; `makeEmptyMediaMock()`; override `api.capture.getDefaultDevice.mockResolvedValue({ ok: false, code: 'IPC_LICENSE_EXPIRED' as const })` AFTER the per-test `beforeEach` (so the override wins). Render `<SettingsCapture />`. Assert `screen.findByTestId('gated-empty-state')` resolves and the page text contains the message from `t('license.emptyStateMessage')` (no white screen). This is the exact failure the user reported.

- **`G-08-5: Save toast on gate-rejected persistence`** — same setup; restore `api.capture.getDefaultDevice.mockResolvedValue('EasyCap USB Video')` (so hydration succeeds and Save becomes clickable) and `api.capture.setDefaultDevice.mockResolvedValue({ ok: false, code: 'IPC_LICENSE_EXPIRED' as const })`. Render; `findByTestId('save-capture')`; click Save; assert a toast-like assertion (either `expect(api.capture.setDefaultDevice).toHaveBeenCalledTimes(1)` AND a regex search for "license" / "activate" appears in document OR use a `vi.spyOn(toast, 'error')` if sonner is mockable in tests). If sonner mocking is unreliable in happy-dom, fall back to: assert the call happened AND no crash (the render must still resolve).

Both new tests must run AFTER the existing 8 tests; verify the test file now has 10 cases total.

Avoid these anti-patterns:
- Do NOT add a `gated ? <EmptyStateCard /> : null` in the header (Save stays in the header per the existing test contract — `data-testid="save-capture"` placement is asserted by the existing tests).
- Do NOT change `fromPreset`, `toQualityPreset`, or `buildPreviewPreset`.
- Do NOT remove the `noSavedDevice` state — it's still used for the legitimate "no device saved" branch.

  </action>
  <verify>
    <automated>pnpm typecheck:web && pnpm vitest run tests/renderer/pages/settings-capture.test.tsx</automated>
  </verify>
  <done>SettingsCapture shows EmptyStateCard on gate-rejected hydration; Save surfaces a clear toast on gate-rejected persistence; all 8 existing settings-capture tests still pass; 2 new tests pass (10 total).</done>
</task>

<task type="auto">
  <name>Task 2: Wrap ProcedureRoom gated IPC sites with safeInvoke (no EmptyStateCard needed)</name>
  <files>src/renderer/src/pages/ProcedureRoom.tsx, tests/renderer/pages/procedure-room.test.tsx</files>
  <action>
ProcedureRoom has the identical shape assumption as SettingsCapture (per the gap's hint to audit it). Confirmed failure points:

- **Lines 52-68 hydration useEffect:** `setSavedDevice(device)` is set to the `{ok:false, code: ...}` object. Not a string. Downstream `pickBrowserId(savedDevice)` on line 72 may crash; `canRecord` on line 287 evaluates truthy because `preset` becomes a polluted object — Record button stays enabled.
- **Lines 80-97 preset useEffect:** `setPreset(nextPreset ?? undefined)` — `??` only catches nullish. The `{ok:false}` object is truthy and non-nullish, so `preset` stays polluted. `canRecord = !!(procedureId && selectedCanonical && preset)` becomes truthy (object is truthy). User clicks Record → garbage payload sent to main.
- **Lines 199-236 handleRecordToggle:** `const device = await window.api.capture.getDefaultDevice()` (line 211) returns the `{ok:false}` object. `if (!device)` (line 212) is false. `getPreset({ deviceId: device })` (line 216) sends a malformed deviceId to main (gate rejects OR Zod fails). `recording.start({ deviceId: device, ... })` (line 221) sends garbage.

In `src/renderer/src/pages/ProcedureRoom.tsx`:

1. Add one import at the top of the file: `import { safeInvoke } from '@/lib/ipc-result';`. No EmptyStateCard import — the page already renders a centered "No device selected — set a default in Settings → Capture" overlay when `selectedCanonical` is undefined (lines 365-378). That overlay handles the gated path naturally: with safeInvoke, `savedDevice` stays `null`, `selectedBrowserId` stays `null`, `selectedCanonical` is `undefined`, the existing overlay renders. Do NOT add EmptyStateCard to this page.

2. **Lines 52-68 (hydration useEffect):** rewrite to async/await with safeInvoke:
   - Call `const device = await safeInvoke(window.api.capture.getDefaultDevice())`.
   - If `device === null`: `setSavedDevice(null)`. Else: `setSavedDevice(device)`.
   - `setDefaultLoaded(true)` in `.finally()` (move from `.then`/`.catch`).
   - Keep the `cancelled` guard.

3. **Lines 80-97 (preset useEffect):** rewrite the inner promise to use safeInvoke:
   - Call `const nextPreset = await safeInvoke(window.api.capture.getPreset({ deviceId: selectedCanonical }))`.
   - `setPreset(nextPreset ?? undefined)` (safeInvoke returns T | null; null is handled by `??`).
   - Keep the `cancelled` guard.

4. **Lines 211-220 (handleRecordToggle inner async IIFE):**
   - Replace `const device = await window.api.capture.getDefaultDevice()` with `const device = await safeInvoke(window.api.capture.getDefaultDevice())`.
   - After the existing `if (!device)` check (now correctly catches the gated null), if device is null, `setStartError('License required to record. Activate your license in Settings → License.')` and `return` (do NOT keep the existing "No default capture device set" message — that message is for the legitimate no-device case; a separate message for the gated case is clearer).
   - Replace `const resolvedPreset = await window.api.capture.getPreset({ deviceId: device })` with `const resolvedPreset = await safeInvoke(window.api.capture.getPreset({ deviceId: device }))`. The existing `if (!resolvedPreset)` branch ("No preset saved...") now correctly handles both the legitimate no-preset case AND the gated case for the preset channel — keep the message as-is.

In `tests/renderer/pages/procedure-room.test.tsx`, add 1 new `it(...)` block:

- **`G-08-5: ProcedureRoom renders without crashing on gate-rejected hydration`** — `setRoute({ name: 'procedure-room', patientId: 'pat-1', procedureId: '00000000-0000-4000-8000-000000000050' })`; override `api.capture.getDefaultDevice.mockResolvedValue({ ok: false, code: 'IPC_LICENSE_EXPIRED' as const })`. Render ProcedureRoom (or wrap the render through whatever entry point procedure-room.test.tsx already uses — likely `setRoute` + `<ProcedureRoom />`). Assert the existing no-device overlay text is present: `screen.findByText(/No device selected/i)` resolves and the page renders without throwing. The key assertion is "no white screen" — i.e. the render call returns and a findable text node exists.

Avoid these anti-patterns:
- Do NOT add `EmptyStateCard` to this page (the no-device overlay is the correct UX).
- Do NOT add a `gated` state — the overlay already handles all three null cases (no device saved / no license / browser list empty) uniformly.
- Do NOT change `canRecord` semantics beyond what `setPreset(undefined)` already achieves (when preset is undefined, `canRecord` becomes false automatically).

  </action>
  <verify>
    <automated>pnpm typecheck:web && pnpm vitest run tests/renderer/pages/procedure-room.test.tsx</automated>
  </verify>
  <done>ProcedureRoom renders the existing no-device overlay on gate-rejected hydration; canRecord stays false because preset is undefined; Record click surfaces a clear toast; the 1 new regression test passes alongside the existing tests.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Renderer → main IPC | Renderer invokes `window.api.capture.*`; main-side license gate may return `{ok:false, code:'IPC_LICENSE_*'}` instead of the expected payload. |
| User → renderer UI | User navigates to Settings → Capture or Procedure Room; renderer must not crash on a gated state. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-08-11-01 | Tampering | n/a | n/a | n/a | This plan is a renderer-side graceful-degrade fix; no new attack surface. |
| T-08-11-02 | Denial of Service | Renderer (React tree) | high | mitigate | Plan 08-10 (G-08-4) fixed 8 consumers but missed SettingsCapture and ProcedureRoom. On `expired`/`unactivated` state, both pages crashed the entire renderer to a white screen via the `pickBrowserId({ok:false})` / `setPreset({ok:false})` shape assumption. Fix: wrap every gated capture.* call with safeInvoke, render EmptyStateCard (SettingsCapture) or rely on the existing no-device overlay (ProcedureRoom). |
| T-08-11-03 | Information Disclosure | n/a | n/a | n/a | The fix reveals no new info; the EmptyStateCard already existed and uses i18n strings. |
| T-08-11-04 | Repudiation | n/a | n/a | n/a | n/a |
| T-08-11-05 | Spoofing | n/a | n/a | n/a | The main-side gate is the source of truth for `{ok:false}`; renderer no longer trusts the shape blindly. |
| T-08-11-06 | Elevation of Privilege | n/a | n/a | n/a | n/a |
| T-08-11-SC | Tampering | npm install | high | mitigate | No new package installs in this plan. Existing dependencies only — no Package Legitimacy Gate trigger. |
</threat_model>

<verification>
After both tasks complete:

1. `pnpm typecheck:web` — exit 0 (no TS regressions across the 2 source files + 2 test files).
2. `pnpm vitest run tests/renderer/pages/settings-capture.test.tsx` — 10/10 pass (8 existing + 2 new).
3. `pnpm vitest run tests/renderer/pages/procedure-room.test.tsx` — passes (existing + 1 new).
4. `pnpm vitest run` — full renderer suite green (was 76/76 after Plan 08-10; expect 79/79 after this plan).
5. Manual smoke (out of scope for this plan — Plan 08-06 already has the smoke harness): set `trial_started_at` to 15 days ago, restart, navigate Settings → Capture → expect EmptyStateCard, NOT a white screen. Same for Procedure Room → expect the existing no-device overlay.

Main-process gate contract UNCHANGED per Plan 03 documented intent (the renderer simply stops misinterpreting the gate's `{ok:false}` shape).
</verification>

<success_criteria>
- [ ] G-08-5 closed: SettingsCapture and ProcedureRoom no longer crash on `expired` / `unactivated` license state.
- [ ] SettingsCapture renders `<EmptyStateCard>` when the gate rejects the hydration `getDefaultDevice` call.
- [ ] SettingsCapture Save surfaces a clear toast when the gate rejects `setDefaultDevice` / `setPreset`.
- [ ] ProcedureRoom's existing no-device overlay renders when the gate rejects the hydration `getDefaultDevice` call.
- [ ] ProcedureRoom Record button is disabled (`canRecord === false`) when the gate has rejected (preset stays `undefined`).
- [ ] 3 new regression tests pass; all 8 existing settings-capture tests + all existing procedure-room tests still green.
- [ ] Full renderer suite (was 76/76 after Plan 08-10) now at 79/79 green.
- [ ] No changes to main-process gate contract, no new dependencies, no changes to SettingsCapture's UI structure beyond the gated-IPC handling.
</success_criteria>

<output>
Create `.planning/phases/08-licensing-ed25519-signed-lic-14-day-trial-activation-flow/08-11-SUMMARY.md` when done.
</output>