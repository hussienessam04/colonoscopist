---
phase: 08-licensing-ed25519-signed-lic-14-day-trial-activation-flow
plan: 07
type: execute
wave: 1
gap_closure: true
gap_ids: [G-08-1]
depends_on: []
files_modified:
  - src/main/license/verify.ts
  - tests/main/license/verify.test.ts
autonomous: true
requirements:
  - LIC-02
  - LIC-03

must_haves:
  truths:
    - "App boots cleanly after fresh install — no `ERR_REQUIRE_ESM` on `@noble/ed25519` or `@noble/hashes/sha2.js`"
    - "Bundled `out/main/index.js` contains no `require('@noble/ed25519')` or `require('@noble/hashes/sha2.js')` calls (proves the CJS require is gone)"
    - "`verifyLicense` still exported as a single frozen function reference; `Object.isFrozen(verifyLicense) === true`"
    - "All 7 verify.test.ts cases (happy / tampered JSON / tampered sig / wrong pubkey / fingerprint mismatch / VENDOR_PUBLIC_KEY_HEX / malformed JSON + Object.isFrozen) pass"
    - "`loadAndVerifyLicense` + `computeLicenseStatus` callers compile and run (both already async — await propagation only)"
  artifacts:
    - path: "src/main/license/verify.ts"
      issue: "Static imports of `@noble/ed25519` + `@noble/hashes/sha2.js` (lines 19-20) and module-level `ed.hashes.sha512 = sha512` (line 26) compile to CJS require() in the bundled main process and crash Node 24 at boot"
    - path: "tests/main/license/verify.test.ts"
      issue: "Calls `verifyLicense(...)` synchronously; once async, all 7 cases need `await` + the malformed-JSON test's `it` callback needs `async`"
  key_links:
    - "src/main/license/verify.ts → src/main/license/load-license.ts (caller already awaits; no caller-side edit needed)"
    - "src/main/license/verify.ts → src/main/license/status.ts (caller already awaits; no caller-side edit needed)"
    - "src/main/backup/index.ts:52 — existing precedent for `await import('archiver')` pattern in main process (already used + tested)"
---

<objective>
Convert `src/main/license/verify.ts`'s static ESM imports of `@noble/ed25519` + `@noble/hashes/sha2.js` to dynamic `await import()` so the bundled CJS main process no longer emits `require("@noble/ed25519")` and crashes at boot with `ERR_REQUIRE_ESM`. The exported `verifyLicense` becomes `async`; the existing `Object.freeze` invariant is preserved. Update `tests/main/license/verify.test.ts` to await the now-async `verifyLicense` calls.

Purpose: Closes gap G-08-1 (Phase 8 blocker — app crashes on boot, blocks every downstream test in the UAT). The fix mirrors the existing `src/main/backup/index.ts:52` pattern that Plan 07-01 already used for `archiver v8` (same root cause: ESM-only package + electron-vite externalizeDepsPlugin = CJS require in bundle). Phase 8 research explicitly rejected downgrading `@noble/ed25519` to v2 — v3's `sha512` hookup is required by design.

Output: A one-file source edit + a one-file test edit. No new files, no dependency changes, no build-config changes.
</objective>

<execution_context>
@C:/Users/Hussien Essam/.config/opencode/gsd-core/workflows/execute-plan.md
@C:/Users/Hussien Essam/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-licensing-ed25519-signed-lic-14-day-trial-activation-flow/08-UAT-session.md
@.planning/phases/08-licensing-ed25519-signed-lic-14-day-trial-activation-flow/08-01-SUMMARY.md
@.planning/phases/08-licensing-ed25519-signed-lic-14-day-trial-activation-flow/08-04-SUMMARY.md

# The two files this plan edits
@src/main/license/verify.ts
@tests/main/license/verify.test.ts

# Existing pattern (dynamic ESM import in main process)
@src/main/backup/index.ts
</context>

<tasks>

<task type="auto">
  <name>Convert verify.ts static ESM imports to dynamic; propagate await to verify.test.ts</name>
  <files>src/main/license/verify.ts, tests/main/license/verify.test.ts</files>
  <action>
Two coupled edits — one source, one test. Keep the public surface stable (`verifyLicense` stays the only verifier export, stays frozen, signature change is just `Promise<VerifyResult>`).

**Edit 1 — `src/main/license/verify.ts`:**

1. Delete the two static ESM imports at lines 19-20:
   ```
   import * as ed from '@noble/ed25519';
   import { sha512 } from '@noble/hashes/sha2.js';
   ```
   `yauzl` and `node:crypto` imports stay — they're CJS-friendly and don't crash.

2. Delete the module-level `ed.hashes.sha512 = sha512;` line at 26 (and its preceding ponytail comment at 23-25). The sha512 hookup moves inside `_verifyLicense` so it runs before the first `ed.verify()` call.

3. Inside `_verifyLicense` (currently a sync function declared at line 125), make these three changes:

   a. Mark the function `async` — return type becomes `Promise<VerifyResult>`. Keep the discriminated-union return shape byte-identical.
   b. As the first statement, do:
      ```
      // ponytail: dynamic ESM import — @noble/ed25519 v3 + @noble/hashes are pure ESM,
      // and the compiled CJS main module cannot statically `require()` them. The
      // dynamic `await import()` is the only interop shape that works under
      // electron-vite's externalizeDepsPlugin. Mirrors src/main/backup/index.ts:52.
      // sha512 hookup is REQUIRED for v3 — without it, ed.verify() throws
      // synchronously (A1 in RESEARCH Assumptions Log).
      const ed = await import('@noble/ed25519');
      const { sha512 } = await import('@noble/hashes/sha2.js');
      ed.hashes.sha512 = sha512;
      ```
   c. The rest of the function body is unchanged — `ed.verify(...)` at line 173 still reads from the local `ed` binding.

4. The frozen export at line 207 needs the cast adjusted for the new async return type. `Object.freeze` on an `async function` reference still produces a frozen object (the test asserts `Object.isFrozen(verifyLicense)`). The TypeScript cast `as typeof _verifyLicense` already infers the new async type — no edit to the cast itself:
   ```
   export const verifyLicense = Object.freeze(_verifyLicense) as typeof _verifyLicense;
   ```

5. Update the file's top-of-file comment block (lines 1-16) so the "Helper functions stay editable for tests" line and the "Object.freeze at module export" line remain accurate. The new behaviour — verifyLicense returns `Promise<VerifyResult>` — is already documented by the TypeScript return type; no prose rewrite needed beyond removing the now-misleading implicit-sync assumption if any.

**Edit 2 — `tests/main/license/verify.test.ts`:**

The 7 test cases call `verifyLicense({...})` synchronously. Add `await` to every call, and add the `async` keyword to the one `it` callback that doesn't already have it.

1. The `Object.isFrozen` test (line 37) — no edit (no `verifyLicense(...)` invocation).
2. Lines 47, 75, 93, 113, 130 (the 5 happy/tampered/wrong-key/mismatch cases) — add `await` before each `verifyLicense({...})` call. The outer `it` callbacks are already `async`.
3. Line 147 (the malformed-JSON test) — change `it('malformed JSON returns MALFORMED_PAYLOAD', () => {` to `it('malformed JSON returns MALFORMED_PAYLOAD', async () => {` AND add `await` before the `verifyLicense({...})` call.

That's the entire test edit. No new test cases, no removed assertions.

**What you do NOT touch:**

- `src/main/license/load-license.ts:89-94` — already `await`s `parseLicenseSidecar` inside an `async function`; the `verifyLicense({...})` call there is already in an `await` context, just needs the `await` keyword added before it. Add `await` before `verifyLicense(...)` on line 89 to match the new async signature. (TypeScript will complain at build if you miss this — but add it explicitly.)
- `src/main/license/status.ts:43-48` — same: add `await` before `verifyLicense({...})` on line 43. (The function `computeLicenseStatus` is already async; the call site just needs `await`.)
- `electron.vite.config.ts` — do NOT touch. `externalizeDepsPlugin` is correct; the bug is in the import shape, not the externalization.
- `@noble/ed25519` / `@noble/hashes` versions — do NOT downgrade. v3 sha512 hookup is required by the library's design.
- `tests/main/license/load-license.test.ts` and `tests/main/license/pick-and-activate.test.ts` — no edits needed. `load-license.test.ts` awaits `loadAndVerifyLicense`; the inner `verifyLicense` await is handled by the source edit propagating the change. `pick-and-activate.test.ts` mocks `loadAndVerifyLicense` directly (line 85-87), so it doesn't reach `verifyLicense` at all.
  </action>
  <verify>
    <automated>npm run typecheck && npm run test:unit -- tests/main/license/verify.test.ts tests/main/license/load-license.test.ts tests/main/license/pick-and-activate.test.ts && npm run build && (grep -c "require(\"@noble/ed25519\")\|require(\"@noble/hashes/sha2.js\")" out/main/index.js | findstr "^0$" >nul && echo "PASS: no CJS require of noble deps in bundle")</automated>
  </verify>
  <done>
    - `npm run typecheck` exits 0 (proves await propagation to load-license.ts + status.ts callers is correct)
    - `npm run test:unit -- tests/main/license/verify.test.ts` — all 7 cases pass, including `Object.isFrozen(verifyLicense) === true`
    - `npm run test:unit -- tests/main/license/load-license.test.ts tests/main/license/pick-and-activate.test.ts` — both pass unchanged
    - `npm run build` — electron-vite build succeeds
    - `grep` on `out/main/index.js` finds ZERO matches for `require("@noble/ed25519")` or `require("@noble/hashes/sha2.js")` — proves the CJS require is gone and the boot crash is fixed
    - The diff is contained to 3 files: `src/main/license/verify.ts` (imports + async + sha512 hookup move), `tests/main/license/verify.test.ts` (await propagation), and two single-line `await` adds in `src/main/license/load-license.ts` + `src/main/license/status.ts`
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Renderer → preload → main IPC | Untrusted renderer cannot reach `verifyLicense` directly; only via the typed IPC contract (Phase 8 Plan 03 gate) |
| Disk → verify path | `license.json` + `license.sig` are user-supplied bytes; verifyLicense validates signature before trusting the payload |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-08-7-01 | Tampering | verifyLicense async surface | low | accept | The dynamic-import change preserves `Object.freeze(verifyLicense)` — runtime reassignment still throws TypeError in strict mode (Vitest default). The function reference cannot be swapped at runtime by an attacker reaching the module object. |
| T-08-7-02 | Denial of Service | Dynamic import on every verifyLicense call | low | accept | Node caches the module specifier after first import — second-and-later calls hit the cache, not the filesystem. The first call on cold-boot adds one microtask; no measurable latency on the IPC handler. Memoization would add code without solving a real cost; defer per YAGNI. |
| T-08-7-03 | Information Disclosure | sha512 hookup visible in bundled main | low | accept | The hookup is a public library contract documented by `@noble/ed25519` v3. No secret material is leaked. |

No new threat categories introduced by this gap-closure plan — the fix is shape-only (CJS↔ESM interop), not a security boundary change.
</threat_model>

<verification>
- `npm run typecheck` — exit 0. The async return type of `verifyLicense` propagates to `load-license.ts` and `status.ts`; typecheck fails if any await is missed.
- `npm run test:unit -- tests/main/license/verify.test.ts` — 7 cases pass, including `Object.isFrozen(verifyLicense)`.
- `npm run test:unit -- tests/main/license/load-license.test.ts tests/main/license/pick-and-activate.test.ts` — both pass (no test edits in those files; the propagation flows through the await added to their `verifyLicense` call sites).
- `npm run build` — `out/main/index.js` produced; `grep -c` for the noble require patterns returns 0.
- The original UAT cold-start smoke (which produced the gap report) — after the fix, the user runs the smoke test again and the boot completes without `ERR_REQUIRE_ESM`.
</verification>

<success_criteria>
- G-08-1 resolved: app boots, no `ERR_REQUIRE_ESM`.
- `verifyLicense` is still the only exported verifier; still `Object.frozen`; signature change is `Promise<VerifyResult>` (only the await shape, not the data shape).
- All 4 license test files (`verify.test.ts`, `load-license.test.ts`, `pick-and-activate.test.ts`, `fingerprint.test.ts` if it exists) pass under `npm run test:unit`.
- `out/main/index.js` contains no static `require()` of `@noble/ed25519` or `@noble/hashes/sha2.js`.
- Three files modified, zero new files, zero dependency changes.
</success_criteria>

<output>
Create `.planning/phases/08-licensing-ed25519-signed-lic-14-day-trial-activation-flow/08-07-SUMMARY.md` when done, citing G-08-1 closure + the test/build/grep evidence.
</output>
