---
phase: 04-recording-timer-device-lost
plan: 02
type: execute
wave: 2
depends_on:
  - 01
files_modified:
  - src/renderer/src/components/ui/textarea.tsx
  - src/renderer/src/components/ui/scroll-area.tsx
  - src/renderer/src/components/ui/badge.tsx
  - src/renderer/src/components/ui/alert.tsx
  - src/main/ipc/procedures.ts
  - src/renderer/src/components/procedure-notes-panel.tsx
  - src/renderer/src/pages/ProcedureRoom.tsx
  - src/renderer/src/pages/ProcedureReview.tsx
  - tests/main/ipc/procedure-notes.test.ts
  - tests/renderer/components/procedure-notes-panel.test.tsx
autonomous: true
requirements:
  - CAPT-08
must_haves:
  truths:
    - "`src/main/db/procedures-repo.ts:proceduresRepo.insertNote({ procedureId, body })` writes one row to `procedure_notes`; the SQL `CHECK(length(body) > 0 AND length(body) <= 1000)` enforces the empty/over-length body rejection at the DB layer (per D-06 + Don't Hand-Roll)."
    - "`procedureNotes.list({ procedureId })` returns rows ordered by `created_at ASC` (chronological, oldest first) so the renderer renders the same SQL order it sees in the IPC response (per D-09)."
    - "Renderer `src/renderer/src/components/procedure-notes-panel.tsx` renders a `<Textarea>` + explicit Save button; clicking Save with an empty body does nothing (filtered at the form layer); clicking Save with a body calls `window.api.procedureNotes.create({ procedureId, body })` and appends the returned row to the chronological list (no optimistic insert — per D-07)."
    - "After a successful save, the panel emits one `audit_log` row `action='procedure.note_added'` (audited server-side in the IPC handler; no separate renderer audit) and the new note is appended to the bottom of the visible list (per D-07 + D-09)."
    - "The notes panel is always visible in Procedure Room regardless of recording state (per D-08); the textarea is `disabled` only when there is no current procedureId (transition state before the user has clicked Record); when recording is active the input remains enabled so the doctor can take notes mid-procedure."
    - "When no notes exist, the panel renders an empty-state message inside the ScrollArea; the ScrollArea has a fixed height (e.g. `h-[280px]`) so the layout does not push the device controls below the visible side-rail (per PITFALLS §5)."
  artifacts:
    - "src/main/db/procedures-repo.ts: `proceduresRepo.insertNote({ procedureId, body })` + `proceduresRepo.listNotes(procedureId)` (declared in Plan 01 but body filled in Plan 02)"
    - "src/main/ipc/procedures.ts: `registerProcedureNotesIpc()` (or extend `registerProceduresIpc`) with `procedureNotes.create` + `procedureNotes.list` handlers (doctorId from `requireSession()`)"
    - "src/renderer/src/components/ui/textarea.tsx: shadcn Textarea component via `npx shadcn@latest add textarea`"
    - "src/renderer/src/components/ui/scroll-area.tsx: shadcn ScrollArea component via `npx shadcn@latest add scroll-area`"
    - "src/renderer/src/components/ui/badge.tsx: shadcn Badge component via `npx shadcn@latest add badge`"
    - "src/renderer/src/components/ui/alert.tsx: shadcn Alert component via `npx shadcn@latest add alert` (used by Plan 04 for the partial banner)"
    - "src/renderer/src/components/procedure-notes-panel.tsx: panel component with Textarea + Save button + ScrollArea + chronological list"
    - "src/renderer/src/pages/ProcedureRoom.tsx: extended with the notes panel mounted in the existing right-hand side-rail (D-08)"
    - "src/renderer/src/pages/ProcedureReview.tsx: status text wrapped in shadcn Badge (Plan 02 fills the addition so Plan 04 can use Alert)"
    - "tests/main/ipc/procedure-notes.test.ts: create/list round-trip + empty-body rejection + over-length rejection + requireSession gate + audit per row"
    - "tests/renderer/components/procedure-notes-panel.test.tsx: empty body + click does nothing; non-empty body + click calls IPC and appends to list; ScrollArea renders 0/1/N notes correctly"
  key_links:
    - "`window.api.procedureNotes.create` -> IPC `procedure-notes:create` -> `requireSession()` -> `proceduresRepo.insertNote` -> `audit({ action: 'procedure.note_added', entityType: 'procedure', entityId: procedureId, metadata: { bodyLength } })` (per D-07 + AUDIT-01)"
    - "`procedure_notes` DB CHECK constraint -> rejected by better-sqlite3 -> translated to `IPC_VALIDATION` error so the renderer can switch on it (DB enforcement per Don't Hand-Roll)"
    - "Chronological list order: `(procedure_id, created_at ASC)` -> renderer reads `listNotes` result directly, no client-side sort -> consistent across reload + browser refresh"
    - "Side-rail DOM structure: existing device/preset/record controls remain above the notes panel; notes panel takes the bottom half of the side-rail (D-08)"
---

<objective>
Ship the mid-procedure notes feature: the `procedure-notes:create` + `procedure-notes:list` IPC handlers (per D-07), the chronological notes panel in the Procedure Room side-rail (per D-08 + D-09), the four shadcn component additions (`textarea` + `scroll-area` + `badge` + `alert`) the rest of Phase 4 depends on, and the empty-state copy + ScrollArea sizing constraints from PITFALLS §5.

Purpose: D-06 designs `procedure_notes` as an append-only log so doctors benefit from a chronological record when reviewing later — the singular CAPT-08 "quick note" wording is enriched into the chat-style log so the most recent note sits next to the input. Shipping the surface in Plan 02 (between the recording tracer in Plan 01 and the Pause/Resume in Plan 03) lets doctors validate the chat-style log UX before the Pause boundary arrives — Plan 03 then layers Pause/Resume over the same panel without schema changes.

Output: working `procedureNotes.create` / `procedureNotes.list` IPC handlers with audit per insert, the renderer `<ProcedureNotesPanel>` component, four shadcn additions, two Wave 0 test scaffolds, and the existing side-rail DOM in `ProcedureRoom.tsx` extended to mount the new panel.

</objective>

<execution_context>
@C:/Users/Hussien Essam/.config/opencode/gsd-core/workflows/execute-plan.md
@C:/Users/Hussien Essam/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/04-recording-timer-device-lost/04-CONTEXT.md
@.planning/phases/04-recording-timer-device-lost/04-RESEARCH.md
@.planning/phases/04-recording-timer-device-lost/04-01-PLAN.md
@.planning/research/PITFALLS.md
@src/shared/ipc-contract.ts
@src/shared/validators.ts
@src/main/ipc/procedures.ts
@src/main/db/procedures-repo.ts
@src/main/db/audit.ts
@src/main/auth/session.ts
@src/renderer/src/pages/ProcedureRoom.tsx
@src/renderer/src/components/ui/button.tsx
@src/renderer/src/components/ui/input.tsx
@src/renderer/src/components/ui/card.tsx
</context>

<tasks>

<task type="auto">
  <name>Main: shadcn UI additions (textarea + scroll-area + badge + alert) + procedure-notes IPC handlers + audit + tests</name>
  <files>
    src/renderer/src/components/ui/textarea.tsx,
    src/renderer/src/components/ui/scroll-area.tsx,
    src/renderer/src/components/ui/badge.tsx,
    src/renderer/src/components/ui/alert.tsx,
    src/main/ipc/procedures.ts,
    src/main/db/procedures-repo.ts,
    tests/main/ipc/procedure-notes.test.ts
  </files>
  <read_first>
    - .planning/phases/04-recording-timer-device-lost/04-CONTEXT.md
    - src/shared/ipc-contract.ts
    - src/shared/validators.ts
    - src/main/ipc/procedures.ts (existing procedures.create/get/list/finalize from Plan 01)
    - src/main/db/procedures-repo.ts (insertNote + listNotes stubs from Plan 01)
    - src/main/db/audit.ts
    - src/main/auth/session.ts
    - src/renderer/src/components/ui/button.tsx
    - src/renderer/src/components/ui/input.tsx
    - src/renderer/src/components/ui/card.tsx
  </read_first>
  <action>
    Add four shadcn components so the rest of Phase 4 can use them:
    `src/renderer/src/components/ui/textarea.tsx` — run `npx shadcn@latest add textarea` (uses `@radix-ui/react-slot` already present); match the existing `input.tsx` style (same `cn` helper from `src/renderer/src/lib/utils.ts` if present, or inline the class-merge; check first). The textarea exports `<Textarea>` with `React.forwardRef<HTMLTextAreaElement, React.TextareaHTMLAttributes<HTMLTextAreaElement>>(...)` and Tailwind classes for `flex min-h-[60px] w-full rounded-md border border-input bg-transparent px-3 py-2 text-sm shadow-sm ... disabled:cursor-not-allowed disabled:opacity-50`.
    `src/renderer/src/components/ui/scroll-area.tsx` — run `npx shadcn@latest add scroll-area` (pulls `@radix-ui/react-scroll-area` as a new dep; this is acceptable — `package.json` adds `@radix-ui/react-scroll-area@^1.x` and any required peer deps; run `npm install`). Match the existing shadcn component style (the existing `select.tsx` is a good analog since it also uses a Radix primitive). Exports `<ScrollArea>` + `<ScrollBar>` (shadcn convention).
    `src/renderer/src/components/ui/badge.tsx` — run `npx shadcn@latest add badge`. Uses `class-variance-authority` (already present). Exports `<Badge variant='default' | 'secondary' | 'destructive' | 'outline'>`.
    `src/renderer/src/components/ui/alert.tsx` — run `npx shadcn@latest add alert`. Exports `<Alert>` + `<AlertTitle>` + `<AlertDescription>` + `<AlertAction>` per shadcn convention. The shadcn Alert component does NOT require a new Radix dep (it is a styled `<div>` only).

    Fill `proceduresRepo.insertNote` and `proceduresRepo.listNotes` (declared in Plan 01 as stubs).
    `insertNote({ procedureId, body })`: prepare `INSERT INTO procedure_notes (procedure_id, body, created_at) VALUES (?, ?, ?)`. Pass `procedureId`, `body` (unchanged — DB CHECK enforces empty/overlength), and `created_at = Date.now()`. `better-sqlite3` throws `SqliteError` on CHECK violation with `code='SQLITE_CONSTRAINT_CHECK'`; the IPC layer translates this to `IPC_VALIDATION`. No return value (renderer appends the row from the IPC response — but wait, `procedure_notes.id` is INTEGER AUTOINCREMENT so the IPC needs to return the new id; mirror Phase 2 `audit_log` insert pattern and use `stmt.run(...)` then `stmt.lastInsertRowid` to return `{ id, procedureId, body, createdAt }` shaped to the `ProcedureNote` type).
    `listNotes(procedureId)`: prepare `SELECT id, procedure_id AS procedureId, body, created_at AS createdAt FROM procedure_notes WHERE procedure_id = ? ORDER BY created_at ASC`. The renderer reads this directly so D-09's chronological order is enforced at the SQL boundary, not the client.

    In `src/main/ipc/procedures.ts` (or a new `registerProcedureNotesIpc` — better to add to the same file to keep the procedures namespace coherent), add two handlers. Use the same `requireSession` / `safeParse` / `asIpcError` / `fromZodError` helpers already in the file (factored out so all handlers share them). Plan 01 already declared the IPC constants `PROCEDURE_NOTES_CREATE = 'procedure-notes:create'` and `PROCEDURE_NOTES_LIST = 'procedure-notes:list'` plus the `procedureNotes.*` methods in `IpcContract` and the `procedureNoteCreateInput` zod schema.

    `procedure-notes:create` handler:
    - `safeParse(procedureNoteCreateInput, raw)` (allow `{procedureId: string, body: string}`).
    - `requireSession()` — every note write is attributable to a doctor.
    - Wrap the insertion + audit in `db.transaction(() => { ... })` so a CHECK violation rolls back any partial work.
    - Call `proceduresRepo.insertNote({ procedureId, body })`.
    - Write one `audit_log` row: `audit({ action: 'procedure.note_added', entityType: 'procedure', entityId: procedureId, metadata: { bodyLength: body.length } })`. NOTE (per Fix 6): `metadata.bodyLength` carries the LENGTH only, never the body content — note content is PII-class and per Fix 6 the audit log never carries field values.
    - Return the inserted `ProcedureNote` (`{ id, procedureId, body, createdAt }`) so the renderer appends it without a second IPC round-trip.
    - On `SqliteError` with `code === 'SQLITE_CONSTRAINT_CHECK'` (empty body or length > 1000), translate to `ipcError('IPC_VALIDATION', 'Note body must be 1..1000 characters', { field: 'body' })` and re-throw. Per Don't Hand-Roll "Notes length validation" — keep this DB-layer enforcement; the form-layer pre-check in Plan 02 Task 2 only filters empty submit attempts.

    `procedure-notes:list` handler:
    - Schema: `procedureNoteListInput = z.object({ procedureId: z.string().uuid() })` (add to `src/shared/validators.ts`).
    - `requireSession()`.
    - `proceduresRepo.listNotes(procedureId)`.
    - Audit `procedure.note_list` with `metadata: { procedureId }` (just the id; not the bodies).
    - Return the array of `ProcedureNote`.

    Add `registerProcedureNotesIpc()` exported alongside `registerProceduresIpc()` (or extend `registerProceduresIpc` to call both — pick whichever keeps the file under 300 lines). Call it from `src/main/index.ts` after `registerProceduresIpc()` and before `registerRecordingIpc()` so the procedures namespace is fully wired when `recording.start` IPC runs.

    Tests: `tests/main/ipc/procedure-notes.test.ts` — mirror `tests/main/patients/create.test.ts` pattern. Use `vi.mock('electron')` like the existing `tests/main/db/migrations.test.ts`. Cover:
    1. `procedure-notes:create` happy path: returns `{ id, procedureId, body, createdAt }` where `createdAt` is within ±50 ms of `Date.now()` and the row is persisted.
    2. `procedure-notes:create` empty body: throws `IPC_VALIDATION` with `field: 'body'` (DB CHECK catches it).
    3. `procedure-notes:create` body length 1001: throws `IPC_VALIDATION` with `field: 'body'`.
    4. `procedure-notes:create` unauthenticated session: throws `IPC_VALIDATION` ('Not authenticated') WITHOUT inserting a row (no audit pollution per T-04-07).
    5. `procedure-notes:create` writes one `audit_log` row with `action='procedure.note_added'`, `entityType='procedure'`, `entityId=<procedureId>`, `metadata.bodyLength === <body.length>`. Assert by querying `_audit_log` table after the IPC call.
    6. `procedure-notes:list`: returns rows in `created_at ASC` order; empties an empty array; unauthenticated throws.
    7. `procedure-notes:list` writes one `audit_log` row with `action='procedure.note_list'` (per AUDIT-01 — reads are auditable).
    Use the same DB setup pattern as `tests/main/db/migrations.test.ts` (mkdtempSync + vi.mock('electron')); the seed requires a `patients` row and a `users` row to satisfy FK constraints (insert minimal rows via the `proceduresRepo.insert` + an INSERT for patients + users directly, OR pull the existing `patientRepo.create` + `userRepo.create` test helpers if they're wired). Simplest: SQL `INSERT INTO patients (id, full_name, dob, created_at, updated_at) VALUES ('p1', 'Test', '1970-01-01', 0, 0)` + same for `users`. Check existing `tests/main/patients/create.test.ts` for the seed pattern.

  </action>
  <acceptance_criteria>
    - `src/renderer/src/components/ui/textarea.tsx`, `scroll-area.tsx`, `badge.tsx`, `alert.tsx` are present and export the expected components matching the existing shadcn style (`button.tsx` is a good visual reference for the file structure).
    - `package.json` adds `@radix-ui/react-scroll-area` and any peer deps installed by `npx shadcn@latest add scroll-area`. No other package additions beyond this.
    - `proceduresRepo.insertNote` writes to `procedure_notes` with `created_at = Date.now()` and returns `{ id, procedureId, body, createdAt }`.
    - Empty body insert fails with `SQLITE_CONSTRAINT_CHECK`; the IPC handler re-throws as `IpcErrorException` with `code='IPC_VALIDATION'` and `field='body'`.
    - `proceduresRepo.listNotes(procedureId)` returns rows ordered by `created_at ASC` (renderer reads directly; no client-side sort).
    - The `procedure-notes:create` handler wraps the insert + audit in one `db.transaction()` (audit row rolls back on a DB-level failure — verified by attempting a CHECK violation and asserting the audit row is also absent).
    - The `procedure-notes:create` handler calls `requireSession()` BEFORE inserting; an unauthenticated call does NOT leave an empty/partial row or audit row in the DB.
    - The `procedure-notes:create` audit row has `metadata.bodyLength === <body.length>` and NEVER contains the body content.
    - The tests/main/ipc/procedure-notes.test.ts file lists 7 tests (happy path, empty body, over-length, unauthenticated, audit-row content, list ordering, list audit-row) and all pass under `npm run test:unit -- --run tests/main/ipc/procedure-notes.test.ts`.
  </acceptance_criteria>
  <verify>
    <automated>npm run typecheck:node && npm run typecheck:web && npm run test:unit -- --run tests/main/ipc/procedure-notes.test.ts tests/main/ipc/procedures.test.ts tests/main/db/migrations/0002_procedures.test.ts</automated>
  </verify>
  <done>The `procedure_notes` create/list IPC handlers round-trip with audit per row, the SQL CHECK constraint enforces the 1..1000 character body limit (DB layer per Don't Hand-Roll), and the four shadcn components (`textarea`, `scroll-area`, `badge`, `alert`) are added so Plan 03/04 can compose them.</done>
</task>

<task type="auto">
  <name>Renderer: ProcedureNotesPanel component + mount in ProcedureRoom side-rail + update ProcedureReview status with Badge</name>
  <files>
    src/renderer/src/components/procedure-notes-panel.tsx,
    src/renderer/src/pages/ProcedureRoom.tsx,
    src/renderer/src/pages/ProcedureReview.tsx,
    tests/renderer/components/procedure-notes-panel.test.tsx
  </files>
  <read_first>
    - .planning/phases/04-recording-timer-device-lost/04-CONTEXT.md
    - src/renderer/src/pages/ProcedureRoom.tsx
    - src/renderer/src/pages/ProcedureReview.tsx
    - src/renderer/src/components/ui/textarea.tsx (now available from Task 1)
    - src/renderer/src/components/ui/scroll-area.tsx
    - src/renderer/src/components/ui/button.tsx
    - src/renderer/src/components/ui/badge.tsx
    - src/renderer/src/components/ui/alert.tsx
    - tests/renderer/pages/procedure-room.test.tsx
    - tests/renderer/setup.ts
  </read_first>
  <action>
    Create `src/renderer/src/components/procedure-notes-panel.tsx` exporting a default `<ProcedureNotesPanel procedureId: string | null>` component. Behavior:
    - `useEffect` on mount and on `procedureId` change: if `procedureId` is non-null, call `window.api.procedureNotes.list({ procedureId })` and store the result in `useState<ProcedureNote[]>([])`. Cancelled flag prevents setState-after-unmount (mirror the `useVideoPreview` cleanup pattern). Errors are swallowed: a renderer-side read failure just leaves the list empty (the next save will retry the read on remount).
    - Render an `aside` with the existing side-rail visual style (`flex flex-col gap-2`) containing:
      1. A `<Textarea>` (from `components/ui/textarea.tsx`) bound to a `useState<string>('')` body. `placeholder` reads from i18n — for now use the plain English literal `'Add a note (e.g. polyp at 12 o'clock)'` (Phase 7 i18ns every visible string; Ponytail: do NOT add i18n hooks in Phase 4).
      2. A `<Button onClick={...} disabled={!body.trim() || !procedureId}>Save note</Button>`. On click: if `body.trim() === ''` OR `procedureId === null`, do nothing (form-layer pre-check). Else call `await window.api.procedureNotes.create({ procedureId, body: body.trim() })`. On success: append the returned `ProcedureNote` to the local list state (`setNotes(prev => [...prev, returnedNote])`), clear the body. On failure: surface an inline error string under the input (do not throw).
      3. A `<ScrollArea className='h-[280px] rounded-md border p-3'>` containing an unordered list of notes ordered chronologically (oldest first, newest at bottom per D-09). Each row: small `<time>` element with `Intl.DateTimeFormat(undefined, { hour: 'numeric', minute: 'numeric' })` formatting for `createdAt`, followed by `<p className='whitespace-pre-wrap text-sm'>{body}</p>`. Empty state when the list is empty: a single muted `<p>No notes yet.</p>`.
    - The textarea is `disabled={!procedureId}` only when there is no procedureId (D-08 says "disabled only when no procedure row exists yet"). When procedureId exists the textarea is enabled regardless of recording state — doctors can take notes before pressing Record or after Stop (per D-08 explicit).
    - DO NOT add optimistic UI: the new note is appended only after the IPC resolves. Per D-07 "no auto-save, no de-bup, no de-dup — predictable audit trail".
    - The component re-fetches the list on `procedureId` change via the `useEffect` (when the user starts a new procedure, the previous notes vanish and the new procedure's notes load).

    Mount the panel in `src/renderer/src/pages/ProcedureRoom.tsx` in the existing right-hand `<aside>` side-rail, BELOW the existing device/preset/record controls (per D-08). Implementation: after the existing `<div className='mt-auto flex flex-col gap-2'>` containing the Record/Stop button, add `<ProcedureNotesPanel procedureId={currentProcedureId} />`. The `currentProcedureId` derives from the row created by `procedures.create` on mount in Plan 01's task 2 — refactor: lift the procedureId into the ProcedureRoom component state and pass it down. If `procedures.create` is called BEFORE the device/preset load, the panel renders disabled until the procedureId lands; the existing `initialized` ref pattern needs an additional `procedureId` ref. Implementation pattern: a single `useEffect` that calls `procedures.create({ patientId })` once and stores the returned `procedure.id` in state; the existing `initialized.current` style applies.

    Update `src/renderer/src/pages/ProcedureReview.tsx` to wrap the status text in a `<Badge variant={status === 'completed' ? 'default' : status === 'recording' ? 'secondary' : 'destructive'}>` (the destructive variant lights up for both `'partial'` and `'crashed'`). Keep the destructive-style red `<p>` for `status === 'partial'` that Plan 01 shipped — Plan 04 will replace it with a full `<Alert>`.

    Tests: `tests/renderer/components/procedure-notes-panel.test.tsx`. Use the existing renderer setup pattern in `tests/renderer/setup.ts` and look at `tests/renderer/components/settings-sidebar.test.tsx` as a setup reference. Cover:
    1. Component renders with `'No notes yet.'` empty state when `procedureId` is `null` and textarea is `disabled`.
    2. After `procedureId` lands and `procedureNotes.list` returns 2 notes (oldest first, newest last), the list renders 2 rows with the bodies visible and the textarea is enabled.
    3. Typing into the textarea enables the Save button; clicking Save with empty/whitespace-only body does NOT call `procedureNotes.create` (form-layer pre-check).
    4. Typing `'Polyp at 12 o'clock'`, clicking Save, with `procedureNotes.create` stubbed to resolve `{ id: 1, procedureId, body: 'Polyp at 12 o'clock', createdAt: now }` — asserts the new note is appended at the BOTTOM of the list and the body input is cleared.
    5. `procedureNotes.create` failure surfaces the inline error string under the input WITHOUT crashing the component (failed `create` does not append the note to the list).
    6. When `procedureId` changes from `'p1'` to `'p2'`, the list re-fetches via the `useEffect` cleanup (the spy on `procedureNotes.list` is called twice with each procedureId).
    7. ScrollArea is in the DOM with `h-[280px]` Tailwind class (verifiable via `getByRole('region')` or the rendered class string).

  </action>
  <acceptance_criteria>
    - `src/renderer/src/components/procedure-notes-panel.tsx` renders a `<Textarea>` (always visible), a Save `<Button>` (disabled when body is empty), and a `<ScrollArea className='h-[280px]'>` containing the chronological notes list with an empty-state message.
    - Save click with empty/whitespace body does NOT call `procedureNotes.create` (form-layer pre-check; visible by spying on the stubbed IPC method).
    - Save click with non-empty body calls `procedureNotes.create({ procedureId, body: body.trim() })` and appends the returned note to the list (no optimistic UI — appended only on the awaited resolution).
    - The textarea is `disabled={!procedureId}` (only when `procedureId` is null). When `procedureId` is non-null, the textarea is enabled regardless of recording state.
    - The list renders newest at BOTTOM (chat-style) per D-09.
    - The Save button never calls `procedureNotes.create` with an unauthenticated session — when `procedureId` is null, the button is disabled so the IPC handler's `requireSession()` gate is unreachable from the renderer.
    - `ProcedureRoom.tsx` mounts `<ProcedureNotesPanel procedureId={currentProcedureId} />` BELOW the existing device/preset/record controls in the right-hand side-rail.
    - `ProcedureReview.tsx` wraps the procedure status text in a `<Badge variant='default' | 'secondary' | 'destructive'>` (default for completed, secondary for recording, destructive for partial/crashed).
    - All 7 tests in `tests/renderer/components/procedure-notes-panel.test.tsx` pass.
  </acceptance_criteria>
  <verify>
    <automated>npm run typecheck:web && npm run test:unit -- --run tests/renderer/components/procedure-notes-panel.test.tsx tests/renderer/pages/procedure-room-timer.test.tsx tests/renderer/pages/procedure-room.test.tsx</automated>
  </verify>
  <done>The notes panel is mounted in Procedure Room's side-rail, always visible regardless of recording state, accepts notes via an explicit Save button (form-layer pre-check on empty bodies), persists them through `procedureNotes.create` IPC (DB-layer CHECK on length 1..1000), renders chronologically (newest at bottom) inside a fixed-height `ScrollArea`, and re-fetches on `procedureId` change. ProcedureReview uses the new shadcn Badge for status.</done>
</task>

</tasks>

<artifacts>
This plan produces:

**Shared/contracts (no changes):**
- `src/shared/ipc-contract.ts` — `procedureNotes.create` / `procedureNotes.list` declared in Plan 01; Plan 02 fills the main-side handlers.
- `src/shared/validators.ts` — adds `procedureNoteListInput = z.object({ procedureId: z.string().uuid() })`.

**Main:**
- `src/main/db/procedures-repo.ts` — fills `insertNote` (returns `{ id, procedureId, body, createdAt }`) and `listNotes(procedureId)` (ordered `created_at ASC`).
- `src/main/ipc/procedures.ts` — extends `registerProceduresIpc()` (or adds `registerProcedureNotesIpc()`) with the two `procedure-notes:*` handlers; `procedure-notes:create` wraps insertion + audit in one `db.transaction()`; both handlers gate on `requireSession()`; empty/over-length bodies translate `SQLITE_CONSTRAINT_CHECK` to `IPC_VALIDATION { field: 'body' }`; audit metadata carries `bodyLength` only, never the body content (per Fix 6).

**Renderer:**
- `src/renderer/src/components/ui/textarea.tsx` — shadcn Textarea (Radix `Slot` re-export of native `<textarea>`).
- `src/renderer/src/components/ui/scroll-area.tsx` — shadcn ScrollArea (+ `@radix-ui/react-scroll-area` package added).
- `src/renderer/src/components/ui/badge.tsx` — shadcn Badge (class-variance-authority variants).
- `src/renderer/src/components/ui/alert.tsx` — shadcn Alert (compound components `Alert` + `AlertTitle` + `AlertDescription`).
- `src/renderer/src/components/procedure-notes-panel.tsx` — `<ProcedureNotesPanel procedureId>` with Textarea + Save Button + ScrollArea + chronological notes list + empty-state copy; re-fetches on `procedureId` change; no optimistic UI per D-07.
- `src/renderer/src/pages/ProcedureRoom.tsx` — mounts `<ProcedureNotesPanel procedureId>` BELOW the existing device/preset/record controls in the right-hand side-rail (per D-08).
- `src/renderer/src/pages/ProcedureReview.tsx` — wraps the procedure status text in a shadcn `<Badge>` variant matching the status; keeps the destructive red `<p>` placeholder for `status === 'partial'` until Plan 04 wraps it in `<Alert>`.

**Wave 0 tests:**
- `tests/main/ipc/procedure-notes.test.ts` — 7 tests covering create/list round-trip, empty-body rejection, over-length rejection, unauthenticated gate, audit-row content (`action`, `entityType`, `entityId`, `metadata.bodyLength`), list ordering, list audit-row.
- `tests/renderer/components/procedure-notes-panel.test.tsx` — 7 tests covering empty state, list render, Save with empty body no-op, Save with body IPC + append, IPC failure inline error, procedureId change re-fetch, ScrollArea sizing.

</artifacts>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Renderer -> procedure-notes IPC | Untrusted renderer payloads cross contextBridge. `doctorId` is NEVER in the payload; derived from `requireSession()` per Phase 2 BLOCKER 4 inheritance. |
| Renderer -> DB CHECK constraint | `procedure_notes.body` CHECK enforces 1..1000 character limit at the DB layer; renderer cannot smuggle a 0-char or 5000-char row past the form-layer pre-check. |
| Audit log metadata | `metadata.bodyLength` carries the LENGTH only; never the body content (PII per Fix 6 inheritance). |
| Side-rail UI state | The notes panel re-fetches on `procedureId` change; React strict-mode double-invocation is safe via the `cancelled` ref pattern. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-04-08 | Tampering | `procedure-notes:create` IPC | high | mitigate | doctorId from `requireSession()`; SQL `CHECK(length(body) > 0 AND length(body) <= 1000)` at the DB layer (PITFALLS §1 + Don't Hand-Roll); IPC validation re-checks the schema with zod. |
| T-04-09 | Information Disclosure | `audit_log` metadata | high | mitigate | `metadata.bodyLength` only; body content is NEVER written to audit_log (PII per Fix 6 inheritance). |
| T-04-10 | Denial of Service | notes list rendering | low | mitigate | ScrollArea `h-[280px]` bounds the rendered list height; no pagination in Phase 4 (acceptable per D-09 for typical 5..30 notes per procedure); Phase 5 review adds pagination if a procedure genuinely needs it. |
| T-04-11 | Repudiation | unauthenticated note writes | medium | mitigate | `requireSession()` runs BEFORE the DB insert; unauthenticated calls do not insert a row AND do not leave an audit row (verified by the test that asserts the audit_log row count is unchanged). |

</threat_model>

<verification>
- `npm run typecheck:node && npm run typecheck:web`
- `npm run test:unit -- --run tests/main/ipc/procedure-notes.test.ts tests/renderer/components/procedure-notes-panel.test.tsx`
- The four shadcn additions render without console warnings in the renderer; the new `@radix-ui/react-scroll-area` dependency is in `package.json` after `npm install`.
- `package.json` adds ONLY `@radix-ui/react-scroll-area` (and any peer dep that `npx shadcn@latest add` pulls); no other Phase 4 packages.

</verification>

<success_criteria>
Plan 02 ships the notes feature end-to-end: the `<ProcedureNotesPanel>` is mounted in Procedure Room's side-rail always visible; saving a non-empty body calls `procedureNotes.create`, persists to `procedure_notes` with the DB CHECK enforcing 1..1000 chars, writes one `audit_log` row with `metadata.bodyLength` only (no body content); saving an empty body is a form-layer no-op; the chronological list re-fetches on `procedureId` change; the four shadcn components added (textarea, scroll-area, badge, alert) keep Procedure Review's status display consistent. Plan 03 layers Pause/Resume over this surface; Plan 04 wraps the destructive styles in `<Alert>` for the device-lost banner.

</success_criteria>

<output>
Create `.planning/phases/04-recording-timer-device-lost/04-02-SUMMARY.md` when done.
</output>
