---
phase: 01-scaffold
plan: 03
type: execute
wave: 3
depends_on:
  - "01-02"
files_modified:
  - tests/renderer/Login.test.tsx
  - scripts/run-full-smoke.cjs
  - .planning/phases/01-scaffold/01-01-SUMMARY.md
  - .planning/phases/01-scaffold/01-02-SUMMARY.md
  - .planning/phases/01-scaffold/01-03-SUMMARY.md
autonomous: true
requirements:
  - AUTH-01
  - AUTH-04
  - SET-03
  - SET-04
user_setup: []

must_haves:
  truths:
    - `tests/renderer/Login.test.tsx` renders `<Login />` and asserts: (a) a disabled numeric 4-char masked PIN input, (b) a disabled `Enter` button, (c) the literal banner copy `Auth ships in Phase 2 — PIN input is disabled`, (d) the clinic-logo placeholder text `Clinic Logo`, (e) `window.api.auth.status` is called exactly once on mount (D-05, D-07, D-08 visual compliance)
    - The full `node scripts/verify-phase-1.cjs` (Plan 02 aggregate) exits 0 after this plan: tsc-node + tsc-web + 5 grep-gates + 3 vitest files (paths + auth-status + Login render)
    - Manual smoke confirmed: window opens at 1280×800, login placeholder visible, banner copy verbatim, dims min/cap honored, ABI log line emitted, no security-baseline drift
    - All Phase 1 success criteria (1, 2, 3, 4, 5) hold after Plan 03 — verified by the aggregate runner
    - Phase 1 SUMMARYs exist and are committed
  prohibitions:
    - statement: "Login render test must NOT use `dangerouslySetInnerHTML` to inject banner text"
      status: "blocked"
      verification: "`tests/renderer/Login.test.tsx` queries the banner with `screen.getByText(/Auth ships in Phase 2/i)` — confirms the text node exists in the React tree without HTML injection"
    - statement: "Login test must NOT enable the PIN input to make tests easier"
      status: "blocked"
      verification: "Test asserts `input.disabled === true` literally; flipping the source to `disabled={false}` makes the test fail"
    - statement: "Login render test must NOT touch any user-storage"
      status: "blocked"
      verification: "Test imports `Login`, renders it under `<MemoryRouter>` (or no router — Plan 03 uses no-router per D-04); no `localStorage.setItem` calls anywhere; Plan 02's `check-no-persistence.cjs` is the safety net"
    - statement: "Phase 1 SUMMARYs MUST NOT claim a feature that lacks a verification step"
      status: "blocked"
      verification: "Each SUMMARY lists a `Verification Evidence` section mapping every must-have truth to a command/file/output that proves it"

---

<objective>
Add the renderer render test (D-05/D-07/D-08 compliance) and extend the smoke runner with `electron-vite build` so success criterion #4 (Tailwind + shadcn render to a built bundle) is verifiable headlessly. Then run the full aggregate verifier end-to-end, capture the manual smoke transcript, and write the three Phase-1 SUMMARYs.

Purpose: Plan 02 wired CI scripts and unit tests for the lower layers; this plan closes the UI surface test gap and confirms the production build emits a Tailwind-styled bundle. The render test also prevents D-05/D-07/D-08 from regressing (e.g., a future PR "fixing" the disabled PIN input) by asserting the exact DOM contract.

Output: a green render test, a green `electron-vite build`, one combined run that proves all of Phase 1's success criteria pass, and three Phase SUMMARYs that downstream agents can grep.
</objective>

<execution_context>
@C:/Users/Hussien Essam/.config/opencode/gsd-core/workflows/execute-plan.md
@C:/Users/Hussien Essam/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-scaffold/01-CONTEXT.md (D-05, D-06, D-07, D-08)
@.planning/phases/01-scaffold/01-VALIDATION.md §Manual-Only Verifications (window + drag tests, ABI log line)
@.planning/research/ARCHITECTURE.md (renderer flow)
</context>

<tasks>

<task type="auto">
  <name>Add Login render test + install @testing-library/react; extend smoke runner with electron-vite build</name>
  <files>tests/renderer/Login.test.tsx, scripts/run-full-smoke.cjs</files>
  <action>
**Install test libraries:** `npm install -D @testing-library/react @testing-library/jest-dom jsdom`. Choose jsdom for the DOM environment in `vitest.config.ts` — make `vitest.config.ts`'s DOM project match `tests/renderer/**`.

**Update `vitest.config.ts` (this plan):** add a third project with `environment: 'jsdom'` for `tests/renderer/**`. Or simpler: switch the existing renderer project to `jsdom`. The two Node projects (paths + shell) stay as-is.

**`tests/renderer/Login.test.tsx`:**

1. Import: `render`, `screen` from `@testing-library/react`; `@testing-library/jest-dom` for matchers (via side-effect import `import '@testing-library/jest-dom/vitest'`).
2. Mock `window.api` BEFORE the import of `<Login />`:
   - Set `globalThis.window.api = { auth: { status: vi.fn().mockResolvedValue({ authenticated: false, reason: 'scaffold' }) } }` in a `beforeEach`.
   - Reset in `afterEach`.
3. Test 1 — D-05 (clinic logo placeholder):
   - `const { container } = render(<Login />)`.
   - Assert the placeholder text `Clinic Logo` is in the document.
4. Test 2 — D-07 (masked 4-digit numeric PIN input):
   - `const input = screen.getByLabelText('PIN') as HTMLInputElement` (note: the `<Label>` is `sr-only`, but `htmlFor` binding still works under RTL — use `screen.getByRole('textbox', { name: /pin/i })` or query by `getByPlaceholderText('Enter PIN')`).
   - Assert `input.type === 'password'`, `input.maxLength === 4`, `input.inputMode === 'numeric'`, `input.pattern === '[0-9]*'`, `input.disabled === true`.
5. Test 3 — D-08 (disabled Enter button + literal banner copy):
   - `const btn = screen.getByRole('button', { name: /enter/i })`.
   - Assert `btn.disabled === true` (HTMLButtonElement.disabled).
   - Assert `screen.getByText(/Auth ships in Phase 2 — PIN input is disabled/i)`.
6. Test 4 — IPC call observable (success #3 visible):
   - Use `vi.useFakeTimers()` and `vi.advanceTimersByTime(0)` OR `waitFor(() => expect(window.api.auth.status).toHaveBeenCalledTimes(1))` to assert the status fetch fires on mount.
7. Cleanup: `unmount()` and reset mocks.

**Update `scripts/verify-phase-1.cjs` (extend → wrap into `scripts/run-full-smoke.cjs`):**
- `run-full-smoke.cjs` calls `verify-phase-1.cjs` first (the Plan 02 aggregator). If it passes, then `run-full-smoke.cjs` runs `npm run build` (i.e., `electron-vite build`) synchronously.
- After `electron-vite build`:
  - Assert `fs.existsSync('out/renderer/index.html')` (electron-vite's renderer output) OR `fs.existsSync('dist/renderer/index.html')` (depending on template) — pick whichever the template produced and pin that path.
  - Read the bundle CSS file (`fs.readdirSync('out/renderer/assets').filter(f => f.endsWith('.css'))[0]`) and assert the CSS contains at least one shadcn/Tailwind utility class (e.g., `.min-h-screen` or `.bg-slate-50` from Login.tsx).
- Print `PHASE 1 SMOKE PASSED` and exit 0. Otherwise print the failing step and exit non-zero.

After writing the test, run `npx vitest run tests/renderer/Login.test.tsx` to confirm green. Then run `node scripts/run-full-smoke.cjs` end-to-end.

Ponytail discipline: one test file, six assertions, no snapshot tests, no fixture files. The render test is the lock against D-08 regression (a common temptation is to "fix" the placeholder to actually work).

  </action>
  <read_first>
    - src/renderer/src/pages/Login.tsx (the component under test)
    - src/renderer/src/components/ui/{button,input,label}.tsx (shadcn primitives used)
    - src/shared/ipc-contract.ts (AuthStatus shape)
    - .planning/phases/01-scaffold/01-CONTEXT.md (D-05, D-07, D-08 verbatim copy)
    - .planning/phases/01-scaffold/01-VALIDATION.md §Per-Task Verification Map (01-09 row)
  </read_first>
  <acceptance_criteria>
    - `npx vitest run tests/renderer/Login.test.tsx` reports `1 file, 4 tests passed` (or equivalent) and exits 0.
    - The four test cases match D-05/D-07/D-08 AND the IPC-call assertion.
    - `node scripts/run-full-smoke.cjs` exits 0 and prints both `PHASE 1 VERIFY PASSED` and `PHASE 1 SMOKE PASSED`.
    - After `electron-vite build`, `out/renderer/assets/*.css` (or whichever path electron-vite emits) contains `bg-slate-50` (Tailwind utility class used in `<Login />`).
    - Negative test: temporarily remove the `disabled` attribute from the Enter button in `Login.tsx`; the Login test fails with a clear message about D-08.
    - Negative test: temporarily change the banner copy in `Login.tsx` to `Coming soon`; the Login test fails with a clear message about D-08.
  </acceptance_criteria>
  <verify>
    <automated>cd "$(pwd)" && (npm install -D @testing-library/react @testing-library/jest-dom jsdom 2>&1 | tail -3) && (npx vitest run tests/renderer/Login.test.tsx 2>&1 | grep -E "(Tests|Test Files|passed|failed)" && echo RENDER-TEST:OK) && (npm run build 2>&1 | tail -5 && echo BUILD:OK) && (ls out/renderer/index.html >/dev/null 2>&1 && echo BUNDLE:PRESENT) && (grep -q "bg-slate-50" out/renderer/assets/*.css && echo TAILWIND:EMITTED) && (node scripts/run-full-smoke.cjs && echo SMOKE:OK)</automated>
  </verify>
  <done>Login render test gates D-05/D-07/D-08. The `electron-vite build` emits a Tailwind-styled bundle. `run-full-smoke.cjs` returns 0 on all-success. Phase 1 success criterion #4 (Tailwind + shadcn render) is observable at the file system level via the built CSS.</done>
  <precondition>Plan 01-02 has shipped `vitest.config.ts` with Node projects. Plan 01-01 has shipped `src/renderer/src/pages/Login.tsx` matching D-05/D-07/D-08 verbatim. If the source does not match exactly, the test fails — fix Plan 01-01 first (do NOT loosen the test).</precondition>
</task>

<task type="auto">
  <name>Final manual smoke + write three Phase-1 SUMMARYs + commit</name>
  <files>.planning/phases/01-scaffold/01-01-SUMMARY.md, .planning/phases/01-scaffold/01-02-SUMMARY.md, .planning/phases/01-scaffold/01-03-SUMMARY.md</files>
  <action>
1. **Run the manual smoke** described in VALIDATION.md §Manual-Only Verifications:
   - Start `npm run dev` in a separate terminal; leave it running for 30 seconds.
   - Confirm visually (or via the captured stderr) that: (a) window title is `Colonoscopist`; (b) window dims are 1280×800; (c) all the same visual elements from D-05/D-07/D-08 are visible; (d) the terminal prints `[boot] better-sqlite3 binding version: 11.10.0` AND `[boot] safeStorage encryption available: true`.
   - Drag the window past 1920×1080 — confirm it caps. Drag below 1280×800 — confirm it snaps. (You may need to script this with a tool, or document the observation. If scripting is too heavy, accept the static-grep evidence as sufficient for `dims` from Plan 02.)
   - Stop the dev server. Capture the smoke evidence into Plan 01-03 SUMMARY §Verification Evidence.
2. **Run final verifier:** `node scripts/run-full-smoke.cjs`. Confirm exit 0. Capture the output for SUMMARY §Verification Evidence.
3. **Write three SUMMARY files** following `.planning/.../templates/summary.md` shape:
   - Each SUMMARY has sections: Goal, Decisions Locked, Files Created, Artifacts, Verification Evidence (with command + output snippet), Deviations (or `None`).
   - 01-01 SUMMARY: focused on D-01 confirmation + scaffold + first ABI log line.
   - 01-02 SUMMARY: focused on the gate scripts and vitest setup.
   - 01-03 SUMMARY: focused on render test + build + smoke + final verifier pass.
4. **Commit Phase 1** as one atomic commit if possible (or one per plan if multiple): `git add -A && git commit -m "docs(phase-1): scaffold + security baseline + verification gates + tests"`.

Ponytail discipline: do not create additional files (no NOTES.md, no COMPLIANCE.md). Each SUMMARY's content is the verification evidence nothing else.

  </action>
  <read_first>
    - .planning/phases/01-scaffold/01-VALIDATION.md §Validation Sign-Off (set `nyquist_compliant: true` after this task)
    - .planning/STATE.md (update phase status to `completed`)
    - .opencode/skills/gsd-core/templates/summary.md (or whatever the orchestrator expects — used `@` reference if available)
  </read_first>
  <acceptance_criteria>
    - Manual smoke captured in 01-03 SUMMARY (or noted as `accepted by static-grep evidence` for the drag tests, with reason).
    - `node scripts/run-full-smoke.cjs` exits 0 and both verifier and smoke lines print `PASSED`.
    - Three SUMMARY files exist; each references its plan file, list decisions, files created, and Verification Evidence block.
    - `STATE.md` reflects `Phase 1 — Scaffold: completed`.
    - One or more git commits exist for Phase 1 (check `git log --oneline -5`).
  </acceptance_criteria>
  <verify>
    <automated>cd "$(pwd)" && (ls .planning/phases/01-scaffold/01-0?-SUMMARY.md 2>/dev/null | wc -l | grep -q "^3$" && echo SUMMARIES:PRESENT) && (node scripts/run-full-smoke.cjs 2>&1 | tail -3 && echo FINAL:OK) && (git log --oneline -5 -- .planning/phases/01-scaffold/ src/ scripts/ tests/ 2>&1 | head -5)</automated>
    <human-check>Open one of the rendered SUMMARYs. Confirm each section is non-empty and the Verification Evidence block references concrete commands + observed outputs (not "looks good" or "tested").</human-check>
  </verify>
  <done>Phase 1 closure conditions are met: all 5 success criteria verified, three SUMMARYs exist, manual smoke captured (or accepted-by-proxy), git committed. The orchestrator's next step (`/gsd-verify-work 1`) can proceed.</done>
  <precondition>Plans 01-01 and 01-02 have shipped cleanly. The render test from this plan's first task is green. If `run-full-smoke.cjs` fails, that is a regression — fix the failing gate or test, not the SUMMARY.</precondition>
</task>

</tasks>

<threat_model>
## Trust Boundaries (Plan 03)

| Boundary | Description |
|----------|-------------|
| Test JSX (jsdom) vs production renderer | jsdom emulates the DOM but has no Electron context; `window.api` is stubbed via `globalThis.window.api = ...`. |
| Build output (`out/renderer/**`) vs source (`src/renderer/**`) | `electron-vite build` writes to `out/renderer/`; CI checks the bundle existence + a Tailwind utility class substring inside `out/renderer/assets/*.css`. |
| Manual smoke (operator) vs headless CI | The drag/cap test is observed manually because no headless Electron harness ships with Phase 1. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation |
|-----------|----------|-----------|----------|-------------|------------|
| T-03-01 | TAMPERING (D-08 regression) | Future PR flips `disabled={false}` on the PIN input | medium | mitigate | `tests/renderer/Login.test.tsx` asserts `input.disabled === true` literally; flipping it makes the test fail. |
| T-03-02 | TAMPERING (D-08 regression) | Future PR replaces banner text with shorter copy | low | mitigate | Render test asserts `/Auth ships in Phase 2 — PIN input is disabled/i` substring. |
| T-03-03 | TAMPERING (Pitfall 7) | Future PR wires the Enter button to call a fake auth handler | medium | mitigate | Render test asserts `btn.disabled === true`; even if a handler is added, the button cannot fire. |
| T-03-04 | INFORMATION DISCLOSURE | `run-full-smoke.cjs` reads built CSS as UTF-8 string | low | accept | CSS contains only class names — no secrets. The script only checks substring presence, never extracts or forwards. |
| T-03-05 | ELEVATION (V8 IPC mock) | Login test sets `globalThis.window.api = ...` that leaks into other test files | low | mitigate | `afterEach(() => { delete globalThis.window.api })` to clean up; each test's `beforeEach` resets. |
| T-03-06 | REPUDIATION | SUMMARY content is faked/hallucinated | medium | mitigate | Each SUMMARY's Verification Evidence block quotes a literal command output. The orchestrator's reviewer (`/gsd-verify-work`) re-runs the verifier and confirms reproducibility. |

## Threat Verification Map

| Threat | Verification |
|--------|------------|
| T-03-01 | Negative test: flip `disabled` in Login.tsx, run Login test — see failure. |
| T-03-02 | Negative test: change banner text in Login.tsx, run Login test — see failure. |
| T-03-03 | Already covered by T-03-01. |
| T-03-05 | `grep "delete globalThis.window.api" tests/renderer/Login.test.tsx` returns ≥ 1. |
| T-03-06 | Re-run `node scripts/run-full-smoke.cjs` against a fresh checkout; output is identical. |
</threat_model>

<verification>
- [ ] `npx vitest run tests/renderer/Login.test.tsx` reports 4+ tests passed (D-05/D-07/D-08 + IPC call)
- [ ] `npm run build` (electron-vite build) exits 0
- [ ] `out/renderer/index.html` exists after build
- [ ] `out/renderer/assets/*.css` contains a Tailwind utility class from Login.tsx (e.g., `bg-slate-50`)
- [ ] `node scripts/run-full-smoke.cjs` exits 0
- [ ] Three SUMMARY files exist; each has `Goal`, `Decisions`, `Files Created`, `Verification Evidence`, `Deviations` sections
- [ ] `STATE.md` shows `Phase 1 — Scaffold: completed`
- [ ] git log shows ≥ 1 commit for Phase 1
- [ ] Phase 1 success criteria 1, 2, 3, 4, 5 are all GREEN after this plan (each verified by a CI command)
</verification>

## Artifacts this phase produces

Plan 03 closes the UI surface test gap and produces the final closure artifacts. It is the smallest plan in files but the largest in summarization.

### UI / test layer (created)

| File | Purpose | Validates |
|------|---------|-----------|
| `tests/renderer/Login.test.tsx` | RTL render of `<Login />` asserting D-05/D-07/D-08 + IPC call | success #4 visual (01-09), T-03-01/02/03 |
| `scripts/run-full-smoke.cjs` | Aggregate runner — delegates to `verify-phase-1.cjs` then runs `electron-vite build` + asserts Tailwind utility class in emitted CSS | success #4 (01-07), Phase 1 closure |
| `.planning/phases/01-scaffold/01-01-SUMMARY.md` | Phase 01-01 retrospective (with Verification Evidence) | gsd workflow |
| `.planning/phases/01-scaffold/01-02-SUMMARY.md` | Phase 01-02 retrospective (with Verification Evidence) | gsd workflow |
| `.planning/phases/01-scaffold/01-03-SUMMARY.md` | Phase 01-03 retrospective (with Verification Evidence) | gsd workflow |

### Configuration delta (added to existing file)

| File | Change |
|------|--------|
| `vitest.config.ts` | Add a third project (jsdom environment) for `tests/renderer/**` |

### Build artifact (one-shot output)

| Artifact | Where | Source |
|----------|-------|--------|
| `out/renderer/index.html` | `out/renderer/` | `electron-vite build` (one-time, not committed) |
| `out/renderer/assets/*.{js,css}` | `out/renderer/assets/` | `electron-vite build` (one-time, not committed) |

### Decision-controlled artifacts (D-05, D-07, D-08 verification)

The render test enforces the entire D-05/D-07/D-08 surface as text + DOM assertions:

| Decision | Source assertion in `tests/renderer/Login.test.tsx` |
|----------|-----------------------------------------------------|
| D-05 | `screen.getByText(/Clinic Logo/)` |
| D-07 | `input.type === 'password'`, `input.maxLength === 4`, `input.inputMode === 'numeric'`, `input.pattern === '[0-9]*'`, `input.disabled === true` |
| D-08 | `btn.disabled === true`; `screen.getByText(/Auth ships in Phase 2 — PIN input is disabled/i)` |
| D-06 (no i18n) | No `i18next` import — implicitly enforced by `check-no-any.cjs` not matching login text strings (and no `i18n` `useTranslation` in JSX) |

### Final phase status updates (committed)

| File | Update |
|------|--------|
| `.planning/STATE.md` | `Phase 1 — Scaffold: completed` |
| Git log | One (or more) Phase-1 commit(s) — `docs(phase-1): scaffold + security baseline + verification gates + tests` |

### Artifacts this phase DOES NOT produce

- No source code under `src/` (Plans 01-01 + 01-02 already covered)
- No CI gate scripts (Plan 01-02)
- No dep changes (`@testing-library/react`, `@testing-library/jest-dom`, `jsdom` install — but it's a devDep delta, not a new artifact)

<success_criteria>
Phase 1 closure conditions are met:
1. All 5 success criteria verified by automated command + captured in SUMMARYs.
2. Tailwind + shadcn render to a built bundle; CSS contains a Login.tsx utility class.
3. Login render test asserts D-05/D-07/D-08 — those decisions cannot regress without breaking tests.
4. The CI gate suite (`node scripts/run-full-smoke.cjs`) is the single command `/gsd-verify-work 1` will run.
5. Three SUMMARYs exist; STATE.md is updated; Phase 1 is committed.

Manual-Only checks deferred / accepted:
- Window drag cap at 1920×1080 (D-03): accepted via static-grep evidence from Plan 01-01 — the literal dims are verified, the cap behavior is a well-known Electron property and not re-tested headlessly for Phase 1.
- Window drag below 1280×800 snap (D-02): same rationale.
</success_criteria>

<output>
Create `.planning/phases/01-scaffold/01-03-SUMMARY.md` when the plan completes. Also ensure 01-01-SUMMARY.md and 01-02-SUMMARY.md are present (may be created retroactively by this plan's last task if prior plans did not produce them).
</output>
