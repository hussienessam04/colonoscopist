---
phase: 01-scaffold
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - .nvmrc
  - tsconfig.json
  - tsconfig.node.json
  - tsconfig.web.json
  - electron.vite.config.ts
  - tailwind.config.ts
  - postcss.config.js
  - components.json
  - .gitignore
  - src/main/index.ts
  - src/main/window.ts
  - src/main/paths.ts
  - src/main/startup-log.ts
  - src/main/ipc/auth.ts
  - src/preload/index.ts
  - src/preload/api.d.ts
  - src/shared/ipc-contract.ts
  - src/shared/errors.ts
  - src/renderer/index.html
  - src/renderer/src/main.tsx
  - src/renderer/src/App.tsx
  - src/renderer/src/env.d.ts
  - src/renderer/src/pages/Login.tsx
  - src/renderer/src/components/ui/button.tsx
  - src/renderer/src/components/ui/input.tsx
  - src/renderer/src/components/ui/label.tsx
  - src/renderer/src/lib/utils.ts
  - src/renderer/src/styles/globals.css
autonomous: false
requirements:
  - AUTH-01
  - AUTH-04
  - SET-03
  - SET-04
user_setup: []

must_haves:
  truths:
    - App boots with `contextIsolation: true, nodeIntegration: false, sandbox: true` literally as object properties in `src/main/window.ts` (Phase 1 success #1)
    - `app.whenReady()` runs, the `BrowserWindow` is created, and the renderer mounts the placeholder login visible to the operator
    - `[boot] better-sqlite3 binding version: X.Y.Z` is logged to stderr AND to `<userData>/logs/startup.log` (success #2)
    - `app.getPath('userData')` resolves to a per-user directory under `%APPDATA%` (verified via `dataDir()` test in Plan 02) and `src/main/paths.ts` exports `dataDir()` returning `<userData>/data` (SET-03 foundation)
    - Preload exposes `window.api.auth.status` via `contextBridge.exposeInMainWorld`; calling it from the renderer invokes `ipcMain.handle('auth:status')` and resolves to `{ authenticated: false, reason: 'scaffold' }` (success #3)
    - Single main `BrowserWindow` only — no second window is created anywhere in Phase 1 source (D-04)
    - Window dimensions are `width: 1280, height: 800, minWidth: 1280, minHeight: 800, maxWidth: 1920, maxHeight: 1080` literal in `src/main/window.ts` (D-02, D-03)
    - Window title is `'Colonoscopist'` literal and `package.json` `name` is `colonoscopist`, `productName` is `Colonoscopist` (D-01)
    - `postinstall` hook is present in `package.json` scripts and was triggered by `npm install` (Pitfall 4 prevention)
    - `better-sqlite3` is required inside `src/main/startup-log.ts` only to read its version — no `new Database()` call exists in Phase 1 source
    - NO `localStorage`, `sessionStorage`, `electron-store`, `document.cookie`, `cookieStore`, or `chrome.cookies` references appear in `src/` (AUTH-04)
    - NO `users:*` IPC channel exists in `src/shared/ipc-contract.ts` (SET-04 stub-only fidelity)
    - Login page renders the clinic-logo placeholder, a disabled numeric 4-character PIN input, a disabled `Enter` button, and the literal banner copy `Auth ships in Phase 2 — PIN input is disabled` (D-05, D-07, D-08)
    - `<html dir="ltr">` is present in `src/renderer/index.html` (D-06 — English-only this phase)
    - `src/shared/ipc-contract.ts` exports a `const IPC = { AUTH_STATUS: 'auth:status' } as const` literal — the channel name string appears exactly once across `src/main` + `src/preload` + `src/renderer`
  prohibitions:
    - statement: "No `new Database(...)` call exists in Phase 1 source"
      status: "blocked"
      verification: "grep `src/main/**/*.ts` for `new Database(` returns no matches; better-sqlite3 is only imported to `require` the package version"
    - statement: "No `users:*` IPC channel in Phase 1"
      status: "blocked"
      verification: "grep `src/shared/ipc-contract.ts` for `users:` returns no matches; SET-04 stub is rendered as a static React `<div>` placeholder (or omitted)"
    - statement: "Security baseline is hard-coded, not constructed by spread/merge"
      status: "blocked"
      verification: "`src/main/window.ts` declares `webPreferences` as an inline object literal with `contextIsolation: true, nodeIntegration: false, sandbox: true, webSecurity: true` — no spreading of constants"
    - statement: "Postinstall hook is `electron-rebuild`, added BEFORE the second `npm install`"
      status: "blocked"
      verification: "Plan 01-01 task edits `package.json` to add `postinstall` BEFORE running `npm install better-sqlite3...`; grep `package.json` for `\"postinstall\": \"electron-rebuild\"` exits 0"
    - statement: "Renderer cannot reach Node directly; only the preload bridge exposes IPC methods"
      status: "blocked"
      verification: "`src/renderer/` has no `import 'node:*'`, no `process.versions`, no `require('electron')` — TypeScript build will fail if any such reference exists"
    - statement: "Only the AUTH channel is wired in Phase 1"
      status: "blocked"
      verification: "`IPC` in `src/shared/ipc-contract.ts` has exactly one key (`AUTH_STATUS`); later phases append to this object"
    - statement: "Tailwind 3.4 is locked, not 4.x"
      status: "blocked"
      verification: "`package.json` `tailwindcss` pin reads `3.4.19`; no `@tailwindcss/postcss` package is installed"
    - statement: "No state management library in Phase 1 (Zustand/Redux/React Context for auth)"
      status: "blocked"
      verification: "`package.json` has no `zustand`, `redux`, `@reduxjs/toolkit`, `jotai`, `mobx`"
    - statement: "No router in Phase 1"
      status: "blocked"
      verification: "`package.json` has no `react-router`, `@tanstack/router`, `wouter`; `App.tsx` renders `<Login />` directly with no `<Routes>`"
    - statement: "No i18n in Phase 1"
      status: "blocked"
      verification: "`package.json` has no `i18next`, `react-i18next`, `i18next-browser-languagedetector`; Login page text is hard-coded English per D-06"
    - statement: "No MediaRecorder, no `<video>` recording in Phase 1"
      status: "blocked"
      verification: "`src/renderer/**/*.tsx` has no `MediaRecorder`, no `captureStream`, no `getUserMedia`"
    - statement: "Pin Exact-version pinning for Electron, better-sqlite3, Tailwind"
      status: "blocked"
      verification: "`package.json` uses `\"electron\": \"32.3.3\"` and `\"better-sqlite3\": \"11.10.0\"` and `\"tailwindcss\": \"3.4.19\"` (no caret ranges on the four locked libraries)"
    - statement: "`sandbox: true` is never relaxed"
      status: "blocked"
      verification: "`grep -n \"sandbox: false\" src/ -r` returns no matches; if a future phase needs Node in preload, it must document the reason and isolate to a second window"
    - statement: "Native module fingerprint verification"
      status: "blocked"
      verification: "`npm install` log shows `@electron/rebuild` (or `electron-rebuild`) was invoked during postinstall; `better-sqlite3/build/Release/*.node` exists with mtime after the install"
    - statement: "Out-of-phase artifacts deferred — no ffmpeg-static, @noble/ed25519, archiver, yauzl, node-hid, serialport, or MediaRecorder install per RESEARCH.md Standard Stack §Deferred; no `src/main/recorder/`, no `src/main/license/`, no `src/main/backup/` directories; no renderer-side auth flow, no router, no i18n scaffolding"
      status: "blocked"
      verification: "`grep -lE \"ffmpeg-static|@noble/ed25519|archiver|yauzl|node-hid|serialport|MediaRecorder\" package.json` returns no matches; `ls src/main/recorder src/main/license src/main/backup` returns `No such file or directory`; `scripts/check-set04-stub.cjs` (Plan 02) enforces absence of users:* channels; `scripts/check-no-persistence.cjs` (Plan 02) enforces absence of session storage"
    - statement: "postinstall script name is `postinstall` exactly (not `postInstall`, `after_install`, `post_install`)"
      status: "blocked"
      verification: "`cat package.json | jq -r '.scripts | keys[]' | grep -i postinstall` returns exactly `postinstall`; npm only honors the exact key"
    - statement: "Electron major is locked at 32 — no upgrade to 33+ in Phase 1"
      status: "blocked"
      verification: "`grep '\"electron\" package.json'` shows `\"32.3.3\"` exact pin (no caret); better-sqlite3 ABI is matched to Node 20 ABI per Electron 32"
    - statement: "electron-store is not installed (no window state persistence)"
      status: "blocked"
      verification: "`grep electron-store package.json` returns no matches; Plan 02 `scripts/check-no-persistence.cjs` enforces"
    - statement: "Tailwind PostCSS pipeline uses legacy `tailwindcss` package, not `@tailwindcss/postcss`"
      status: "blocked"
      verification: "`grep '@tailwindcss/postcss' package.json` returns no matches; `postcss.config.js` requires `tailwindcss` and `autoprefixer` only"
    - statement: "Renderer never imports node:* modules"
      status: "blocked"
      verification: "`grep -rE \"import ['\\\"]node:\" src/renderer/` returns no matches; TypeScript build will fail if any such reference exists"
    - statement: "Exactly one BrowserWindow is created across the entire Phase 1 source"
      status: "blocked"
      verification: "`grep -rE \"new BrowserWindow\\(\" src/main/` returns exactly one match (in `src/main/window.ts`); D-04 single-window rule"
---

<objective>
Scaffold a hardened Electron app shell with React + TypeScript + Tailwind + shadcn/ui, and prove the end-to-end boot path works: a single BrowserWindow with the locked security baseline, the `auth:status` IPC channel round-tripping from renderer through preload to main, and the placeholder login screen visible to the operator. This plan delivers the foundation for every later phase and addresses Phase 1's #1-#3 success criteria plus the Auth + Storage + SET-04-stub foundations.

Purpose: every later phase extends `src/main`, `src/preload`, `src/shared`, and `src/renderer` — they need this skeleton to exist. Avoid blowing scope: no DB connection, no router, no state mgmt, no i18n scaffolding; this is composition of maintained pieces (electron-vite scaffolder + shadcn CLI + @electron/rebuild), not invention.

Output: a runnable Electron app where `npm run dev` shows the placeholder login; the `better-sqlite3` binding is built against Electron 32's Node ABI; `webPreferences` is a locked literal; and a single startup log line proves the ABI.
</objective>

<execution_context>
@C:/Users/Hussien Essam/.config/opencode/gsd-core/workflows/execute-plan.md
@C:/Users/Hussien Essam/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-scaffold/01-CONTEXT.md
@.planning/phases/01-scaffold/01-RESEARCH.md
@.planning/research/STACK.md
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.opencode/skills/electron-vite/SKILL.md
@.opencode/skills/electron-sqlite/SKILL.md

# Locked decisions referenced by this plan
- D-01 productName=Colonoscopist, appId=com.colonoscopist.app (ONE-WAY)
- D-02 dims 1280×800 default + min
- D-03 resizable, cap 1920×1080
- D-04 single main BrowserWindow
- D-05 clinic logo placeholder + PIN input on the login page
- D-06 no language toggle this phase; <html dir="ltr">
- D-07 numeric 4-digit masked PIN input
- D-08 disabled Enter button + hint banner
</context>

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <name>Decision: Confirm Phase 1 productName + appId before committing the manifest</name>
  <files>package.json</files>
  <decision>Lock the productName `Colonoscopist` and appId `com.colonoscopist.app` in `package.json` (D-01). This decision is ONE-WAY: Windows registry keys, install directory, Start Menu label, and file associations will all derive from these.</decision>
  <context>Phase 1 sets the package identity that every downstream phase (electron-builder signing, native rebuild ABI tags, VSCode workspace recognition) inherits. A late rename forces a full uninstall + reinstall and a re-signed license. The CONTEXT.md lock is `Colonoscopist`/`com.colonoscopist.app`, but the user must explicitly confirm before this irreversible write.</context>
  <options>
    <option id="colonoscopist">
      <name>Colonoscopist / com.colonoscopist.app (locked decision D-01)</name>
      <pros>Matches brief + marketplace expectations; conservative camelCase appId; matches the repo folder name</pros>
      <cons>None — already locked by discussion</cons>
    </option>
    <option id="alt-name">
      <name>Alt name (override D-01)</name>
      <pros>Would mean clinic brand-shifted</pros>
      <cons>Forces a full uninstall + rebrand; no upside in this phase</cons>
    </option>
  </options>
  <resume-signal>Select: colonoscopist, or describe the override (alt-name + chosen identifiers).</resume-signal>
  <read_first>
    - .planning/phases/01-scaffold/01-CONTEXT.md §D-01 (the ONE-WAY decision rationale)
    - .planning/REQUIREMENTS.md §AUTH-01, §AUTH-04, §SET-03, §SET-04 (the four Phase 1 requirements this name ships against)
  </read_first>
  <acceptance_criteria>
    - User has explicitly selected one of: `colonoscopist`, `alt-name + <reason>`.
    - The chosen productName + appId are recorded in `.planning/phases/01-scaffold/01-DISCUSSION-LOG.md` as the final D-01 override (if any), or as "CONFIRMED — Colonoscopist / com.colonoscopist.app" if no override.
  </acceptance_criteria>
</task>

<task type="tracer">
  <name>Tracer — scaffold the app end-to-end so it boots, the IPC round-trips, and the placeholder login renders</name>
  <files>package.json, tsconfig.json, tsconfig.node.json, tsconfig.web.json, electron.vite.config.ts, tailwind.config.ts, postcss.config.js, components.json, .gitignore, src/main/index.ts, src/main/window.ts, src/main/paths.ts, src/main/startup-log.ts, src/main/ipc/auth.ts, src/preload/index.ts, src/preload/api.d.ts, src/shared/ipc-contract.ts, src/shared/errors.ts, src/renderer/index.html, src/renderer/src/main.tsx, src/renderer/src/App.tsx, src/renderer/src/env.d.ts, src/renderer/src/pages/Login.tsx, src/renderer/src/components/ui/button.tsx, src/renderer/src/components/ui/input.tsx, src/renderer/src/components/ui/label.tsx, src/renderer/src/lib/utils.ts, src/renderer/src/styles/globals.css</files>
  <action>
A production-quality end-to-end foundation slice — NOT a prototype. The artifacts below become the skeleton for every later phase.

**Scaffold procedure (per .opencode/skills/electron-vite/SKILL.md §1 + RESEARCH Key Pattern A):**

1. From the repo root run `npm create @quick-start/electron@latest @electron-vite-tmp -- --template react-ts --skip`, then move the contents up into the repo root (`src/`, `electron.vite.config.ts`, base `tsconfig*.json` set). Resolve conflicts with existing files (e.g., `.gitignore` may need a merge). Preserve `.planning/`, `AGENTS.md`, `.opencode/`, `.git/`.
2. `npm uninstall` template-injected packages we don't need for Phase 1 (e.g., `vite`, `electron-builder` if present) — defer `electron-builder` to Phase 8. Keep every runtime dep the template installed (React, electron, electron-vite, etc.).
3. Write `.nvmrc` to the repo root with literal content `20` (single line, no trailing newline issues) — pins dev-time tooling Node version to match Electron 32's Node ABI (RESEARCH §Open Questions Q3 RESOLVED).

**`package.json` edits (D-01 + RESEARCH Standard Stack):**
- Set `"name": "colonoscopist"` and `"productName": "Colonoscopist"`.
- Pin `"electron": "32.3.3"`, `"electron-vite": "2.3.0"`, `"better-sqlite3": "11.10.0"`, `"tailwindcss": "3.4.19"`, `"@electron/rebuild": "^4"`, `"@types/better-sqlite3": "^7"`, `"@types/node": "^20"`, `"@types/react": "^18"`, `"@types/react-dom": "^18"`. NO caret on the four locked libs.
- Add `"postinstall": "electron-rebuild"` to scripts — BEFORE the next `npm install` (Pitfall 1).
- Remove the template's electron-builder section if present.

**Native + Tailwind + shadcn install (one batch, only after `postinstall` is in `package.json`):**
- `npm install better-sqlite3@11.10.0`
- `npm install -D @electron/rebuild @types/better-sqlite3 tailwindcss@3.4.19 postcss autoprefixer`
- `npx shadcn@latest init --defaults --yes --base-color slate` then `npx shadcn@latest add button input label` (RESEARCH Key Pattern G). Verify `components.json` has `"rsc": false` and `"style": "default"` (Pitfall 5).
- Re-run `npm install` so the postinstall hook fires against the now-installed `better-sqlite3`.

**`src/main/window.ts` (Pitfall 6 + Pattern 2):**
- Export `createMainWindow(): BrowserWindow`.
- Use a literal `webPreferences` object — never spread/merge — containing exactly: `contextIsolation: true, nodeIntegration: false, sandbox: true, webSecurity: true, preload: path.join(__dirname, '../preload/index.js')`.
- Dims: `width: 1280, height: 800, minWidth: 1280, minHeight: 800, maxWidth: 1920, maxHeight: 1080`, `title: 'Colonoscopist'`.
- Call `logStartup('window-created')`.

**`src/main/index.ts`:**
- In `app.whenReady()`: call `app.setName('Colonoscopist')` (research note A7), then `createMainWindow()`, `registerAuthIpc()`, `logStartup('app-ready')`.
- macOS: keep the existing empty `activate` handler. Windows is the only target — `will-quit` is fine as the template shipped.
- `if (process.platform === 'win32') { app.setAppUserModelId('com.colonoscopist.app') }` for Windows toast identity (D-01).

**`src/main/paths.ts` (SET-03 foundation, RESEARCH Key Pattern D):**
- Import `app` from `'electron'`, `path` from `'node:path'`, `mkdirSync` from `'node:fs'`.
- Export `dataDir(): string` resolving to `path.join(app.getPath('userData'), 'data')` and `mkdirSync(dir, { recursive: true })`. (Returned string is sufficient; side effect of mkdir is intentional for callers that immediately write.)

**`src/main/startup-log.ts` (Pitfall 4 mitigation):**
- Import `app` + `safeStorage` from `'electron'`, `path` + `fs` from node.
- Cache `betterSqliteVersion` from `require('better-sqlite3/package.json').version` (NOT `import Database from 'better-sqlite3'`).
- `logStartup(event)` appends one line to `<userData>/logs/startup.log` (mkdirSync recursive) including ISO timestamp, event name, and the cached better-sqlite3 version.
- When `event === 'app-ready'`, ALSO emit to stderr: `[boot] better-sqlite3 binding version: <ver>` and `[boot] safeStorage encryption available: <bool>`. Use `console.log` (Electron pipes stderr to the terminal in dev).

**`src/main/ipc/auth.ts` (Pattern 1):**
- `registerAuthIpc()` calls `ipcMain.handle(IPC.AUTH_STATUS, () => ({ authenticated: false, reason: 'scaffold' as const }))`.

**`src/preload/index.ts` (Pattern 1):**
- Build an object of shape `{ auth: { status: () => ipcRenderer.invoke(IPC.AUTH_STATUS) } }` typed as `IpcContract`.
- `contextBridge.exposeInMainWorld('api', api)`. No other APIs this phase.

**`src/preload/api.d.ts`:**
- Declare global `interface Window { api: IpcContract }`. Use `export {}`. No `any`.

**`src/shared/ipc-contract.ts`:**
- Export `IPC = { AUTH_STATUS: 'auth:status' } as const`.
- Export `AuthStatus` union type with one variant: `{ authenticated: false; reason: 'scaffold' }`.
- Export `IpcContract` interface containing `auth: { status: () => Promise<AuthStatus> }`.

**`src/shared/errors.ts`:**
- Stub `IpcError` tagged union (e.g., `'IPC_AUTH_FAILED'`) with a `code` discriminator; one variant is sufficient for Phase 1.

**TS configs (Pitfall 3):**
- `tsconfig.json`: `compilerOptions.strict = true`, `compilerOptions.noImplicitAny = true`, references `tsconfig.node.json` and `tsconfig.web.json`.
- `tsconfig.node.json`: ESM target, `module: "ESNext"`, `moduleResolution: "Bundler"`, no DOM lib. `strict: true, noImplicitAny: true`. Include `src/main/**` and `src/preload/**` and `src/shared/**`.
- `tsconfig.web.json`: `jsx: "react-jsx"`, DOM lib, `strict: true, noImplicitAny: true`. Include `src/renderer/src/**` and `src/shared/**`.

**`src/renderer/index.html`:**
- `<html dir="ltr" lang="en">` (D-06 — English-only).
- `<meta charset="UTF-8">`, viewport meta, and a single `<script type="module" src="/src/main.tsx"></script>` (template default; verify path).

**`src/renderer/src/main.tsx`:**
- `ReactDOM.createRoot(document.getElementById('root')!).render(<App />)`.

**`src/renderer/src/App.tsx`:**
- `export default function App() { return <Login /> }` — no router, no providers.

**`src/renderer/src/pages/Login.tsx` (D-05, D-07, D-08):**
- JSX centered form (Tailwind: `min-h-screen grid place-items-center bg-slate-50`).
- Clinic logo placeholder: a 128×128 rounded `<div>` with `bg-slate-200` containing the text `Clinic Logo` (or an inline SVG placeholder per agent discretion).
- `Label htmlFor="pin" className="sr-only"` then `Input id="pin" type="password" inputMode="numeric" pattern="[0-9]*" maxLength={4} disabled placeholder="Enter PIN"`.
- `Button disabled>Enter</Button>`.
- Hint banner `<p className="text-sm text-slate-500">Auth ships in Phase 2 — PIN input is disabled</p>`.
- Optional: `useEffect(() => { void window.api.auth.status().then(setStatus) }, [])` and render `<pre>status: {JSON.stringify(status)}</pre>` to make the IPC round-trip observable in the renderer console for the manual smoke.

**Tailwind + shadcn plumbing:**
- Confirm `tailwind.config.ts` `content` includes `./src/renderer/**/*.{ts,tsx}` and `./src/renderer/index.html`. `darkMode: 'class'`.
- `src/renderer/src/styles/globals.css`: shadcn CLI writes `@tailwind base; @tailwind components; @tailwind utilities;` plus CSS variables — verify they are present, do not edit by hand.
- `src/renderer/src/lib/utils.ts`: `cn()` helper from shadcn — verify it is present.

**`.gitignore`:**
- Extend template to add `out/`, `dist/`, `node_modules/`. Do not untrack `.planning/`.

**Do NOT do (Prohibitions):**
- Open `new Database(...)` anywhere. Only `require('better-sqlite3/package.json').version` in `startup-log.ts`.
- Add `users:*` channels, `electron-store`, Zustand/Redux, `react-router`, `i18next`, `MediaRecorder`, `getUserMedia`.
- Spread/merge `webPreferences`. Modify the locked dims.
- Bump Tailwind past 3.4. Use @tailwindcss/postcss.
- Pre-install `electron-builder` (defer to Phase 8).
  </action>
  <read_first>
    - .planning/phases/01-scaffold/01-CONTEXT.md (D-01 through D-08)
    - .planning/phases/01-scaffold/01-RESEARCH.md (Key Patterns A-G, Edge Coverage predicates, Prohibitions)
    - .planning/research/STACK.md (locked versions)
    - .opencode/skills/electron-vite/SKILL.md (scaffold procedure + IPC pattern + security baseline §8)
    - .opencode/skills/electron-sqlite/SKILL.md (paths.ts pattern §2)
  </read_first>
  <acceptance_criteria>
    - `npm run dev` opens an Electron window with title `Colonoscopist` (D-01) and dims 1280×800 (D-02).
    - The window displays the placeholder login: clinic-logo placeholder text, a numeric 4-character masked PIN input, a disabled `Enter` button, and the literal banner copy `Auth ships in Phase 2 — PIN input is disabled` (D-05, D-07, D-08).
    - On the renderer's DevTools console, `JSON.stringify(await window.api.auth.status())` produces the literal `{"authenticated":false,"reason":"scaffold"}` (success #3 round-trip observable).
    - The terminal running `npm run dev` shows `[boot] better-sqlite3 binding version: 11.10.0` and `[boot] safeStorage encryption available: true` (success #2).
    - The startup log file `<userData>/logs/startup.log` exists with one entry per `logStartup` call (verifiable after `npm run dev` once and Ctrl+C).
    - `node -e "const w = require('./src/main/window.ts')"` is N/A — instead: `npx tsc --noEmit -p tsconfig.node.json` exits 0 AND `npx tsc --noEmit -p tsconfig.web.json` exits 0 (success #5 partial).
    - `grep -c "contextIsolation: true" src/main/window.ts` returns ≥ 1; same for `nodeIntegration: false`, `sandbox: true` (success #1 — locked literal).
    - `grep -c "new Database" src/main/**/*.ts` returns 0 (Prohibition — no DB open).
    - `grep -c "users:" src/shared/ipc-contract.ts` returns 0 (Prohibition — no SET-04 channels).
    - `grep -c "postinstall" package.json` returns ≥ 1 with value `electron-rebuild` (Pitfall 4 wiring).
    - `grep -c "app.setName('Colonoscopist')" src/main/index.ts` returns 1 (research A7).
    - `find node_modules/better-sqlite3/build/Release -name "*.node"` returns one file (proves the binding was rebuilt by postinstall).
    - `cat package.json | grep -E '"(electron|better-sqlite3|tailwindcss)"' shows exact pins (no caret) for those four.
  </acceptance_criteria>
  <verify>
    <automated>cd "$(pwd)" && (npm install 2>&1 | tail -5) && (npx tsc --noEmit -p tsconfig.node.json && echo "TYPECHECK-NODE:OK") && (npx tsc --noEmit -p tsconfig.web.json && echo "TYPECHECK-WEB:OK") && (grep -q "contextIsolation: true" src/main/window.ts && grep -q "nodeIntegration: false" src/main/window.ts && grep -q "sandbox: true" src/main/window.ts && echo "SECBASE:LITERALS:OK") && (grep -q "electron-rebuild" package.json && echo "POSTINSTALL:OK") && (grep -q "11.10.0" node_modules/better-sqlite3/package.json && echo "BS3-VERSION:OK") && (ls node_modules/better-sqlite3/build/Release/*.node >/dev/null 2>&1 && echo "BINDING:PRESENT")</automated>
    <human-check>Run `npm run dev` once. Confirm: (a) the Colonoscopist window opens at 1280x800, (b) the Login placeholder is visible with "Clinic Logo" text + masked 4-digit input + disabled Enter button + the banner copy, (c) the terminal prints the two `[boot]` lines with the correct versions, (d) the title bar reads "Colonoscopist". On success, stop the dev server.</human-check>
  </verify>
  <done>Phase 1 success criteria #1 (security baseline literals), #2 (ABI log line on stderr), #3 (IPC round-trip — visible via DevTools console returning the literal scaffold payload), and #4 (Tailwind + shadcn render) are observably met on the manual smoke. SET-03 (paths.ts foundation) and SET-04 (no users:* channels) are in place. AUTH-04 (no persistence) is structurally enforced by the absence of any storage path being used at Phase 1. The `postinstall: electron-rebuild` hook fired during install so the better-sqlite3 binding against Electron 32 ABI exists.</done>
  <precondition>User has confirmed D-01 productName/appId in the prior task. Node 20+ is installed; Windows 10/11 dev environment; VS Build Tools assumed present (Pitfall/research assumption) — if `electron-rebuild` fails, fall back to `npm rebuild better-sqlite3 --build-from-source --runtime=electron --target=32.3.3` (per .opencode/skills/electron-sqlite/SKILL.md §1 fallback).</precondition>
  <reversibility>rating="one-way"</reversibility>
</task>

</tasks>

<threat_model>
## Trust Boundaries (Phase 1)

| Boundary | Description |
|----------|-------------|
| `process.dlopen` (loader) | Loads `better-sqlite3/build/Release/*.node` at runtime. Boundary between JS and rebuilt C++ addon. |
| renderer → preload (contextBridge) | Only path the renderer can reach main. Renderer is untrusted Chromium; preload runs with sandboxed Node subset. |
| preload → main (IPC) | `ipcRenderer.invoke` → `ipcMain.handle`. Channel name must match the literal in `IPC.AUTH_STATUS`. |
| main → filesystem (`userData`) | Main writes the startup log under `userData/logs`. No DB writes yet. |
| install → native binding | `postinstall` rewrites the better-sqlite3 `.node` addon against Electron's Node ABI. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-01 | Tampering / ELEVATION | `src/main/window.ts` webPreferences | high | mitigate | Hard-coded literal `contextIsolation: true, nodeIntegration: false, sandbox: true, webSecurity: true` — NO spread/merge. The grep gate in Plan 02 fails CI if any literal flips. (maps to D-04 + success #1) |
| T-01-02 | Tampering / INFORMATION | `src/renderer/` reading arbitrary filesystem | medium | mitigate | Renderer has NO direct Node access. Only `window.api.auth.status()` exists. Preload uses `contextBridge.exposeInMainWorld` with a literal object — no `ipcRenderer.on` listeners, no `webFrame.executeJavaScript`. (`src/renderer/` is built with tsconfig.web.json which has DOM lib but no `node:fs` types.) (maps to security baseline) |
| T-01-03 | Spoofing / TAMPERING | `auth:status` channel mismatch between main and preload | medium | mitigate | Both `src/main/ipc/auth.ts` and `src/preload/index.ts` import the same `IPC.AUTH_STATUS` constant from `src/shared/ipc-contract.ts`. Single source of truth; Plan 02's `check-ipc-contract.cjs` greps for the literal. (maps to Pattern 1) |
| T-01-04 | TAMPERING (PITFALL 4) | `better-sqlite3` resolved against wrong ABI | high | mitigate | `postinstall: electron-rebuild` rewrites `.node` against Electron 32's Node 20 ABI. `startup-log.ts` reads the installed package version AND prints it to stderr/log so an ABI mismatch is observable at first launch — the user sees a wrong version and can rebuild manually. (maps to Pitfall 4 + success #2) |
| T-01-05 | REPUDIATION / ELEVATION | Renderer writes to `localStorage`/`electron-store` to spoof session | medium | mitigate | AUTH-04 enforced by ABSENCE — no localStorage/sessionStorage/cookie/electron-store reference exists in Phase 1 source. Plan 02's `check-no-persistence.cjs` greps for these tokens and fails if any appear. (maps to AUTH-04) |
| T-01-06 | TAMPERING / ELEVATION (Pitfall 7) | Future PR "improves" the placeholder by wiring onChange + a fake auth handler | medium | mitigate | D-08 locks the Enter button to `disabled`. The PIN input is `disabled` and has no `onChange`. A literal banner copy `Auth ships in Phase 2` is rendered (verbatim). Plan 03's `tests/renderer/Login.test.tsx` asserts disabled state + banner copy literally. |
| T-01-07 | INFORMATION DISCLOSURE | `dataDir()` writes outside `userData` | low | mitigate | `paths.ts` resolves via `app.getPath('userData')` and the literal suffix `'data'`. No path concatenation accepts user input this phase. (maps to SET-03) |
| T-01-08 | ELEVATION (Pitfall 5) | `shadcn init` mis-configures components.json (`rsc: true`) | low | mitigate | After `shadcn init` Plan 01-01 task runs, manually verify `components.json` has `"rsc": false` and `"style": "default"`. (maps to Pitfall 5) |
| T-01-09 | DoS / ELEVATION (Pitfall 6) | A Vite plugin silently spreads `nodeIntegration: true` into webPreferences | medium | mitigate | The `webPreferences` object is an inline literal — no `<Object.assign>` or spread in window.ts. Grep gate (Plan 02) verifies the literal token. |
| T-01-10 | TAMPERING (Pitfall 1) | `postinstall` added AFTER the first `npm install` lands | high | mitigate | Plan 01-01 task edits `package.json` to add `postinstall` BEFORE running `npm install better-sqlite3...`. Task order is enforced in the action. |

## Out-of-phase Threat Categories

- **V2 Authentication** — N/A (auth lives in Phase 2).
- **V4 Access Control** — N/A (no entities yet).
- **V6 Cryptography** — N/A (no crypto yet; safeStorage advertisement via `logStartup` is informational).
- **V8 Data Protection** — N/A (no data yet).
- **V9 Communications** — N/A (no network).

## Trust Boundary Crossings

| Boundary | Channels | Inputs |
|----------|---------|--------|
| renderer → preload → main | IPC `auth:status` only | `()` — no parameters |
| main → `userData/logs/startup.log` | file write | ISO timestamp + event name + better-sqlite3 version (no user data) |
| main → `userData/data/` | mkdirSync side-effect only | no Phase 1 writes |

## Threat Verification Map

| Threat | Verification Step |
|--------|---------------------|
| T-01-01 | `grep -c 'contextIsolation: true' src/main/window.ts` ≥ 1 (Plan 02 grep gate) |
| T-01-02 | `grep -r 'require(.electron.)' src/renderer/` returns no matches (`window.api` only) |
| T-01-03 | `grep -c "'auth:status'" src/shared/ipc-contract.ts` = 1; Plan 02 `check-ipc-contract.cjs` enforces |
| T-01-04 | Manual smoke: `[boot] better-sqlite3 binding version: 11.10.0` appears in stderr |
| T-01-05 | Plan 02 `check-no-persistence.cjs` greps for `localStorage\|sessionStorage\|electron-store\|cookie` and fails on hit |
| T-01-06 | Plan 03 `tests/renderer/Login.test.tsx` asserts `Enter` button `disabled` and banner copy verbatim |
| T-01-07 | Plan 02 `tests/main/paths.test.ts` asserts `dataDir()` returns `path.join(<userData>, 'data')` |
| T-01-08 | Manual inspection of `components.json` after `shadcn init` — must show `"rsc": false, "style": "default"` |
| T-01-09 | Grep gate (Plan 02 `check-security-baseline.cjs`) + literal-object contract |
| T-01-10 | Task ordering in this plan: `postinstall` is edited BEFORE `npm install better-sqlite3` |
</threat_model>

<verification>

- [ ] Phase 1 success criterion #1: `src/main/window.ts` contains literal `contextIsolation: true`, `nodeIntegration: false`, `sandbox: true`
- [ ] Phase 1 success criterion #2: `[boot] better-sqlite3 binding version: 11.10.0` printed to stderr once on launch
- [ ] Phase 1 success criterion #3 (partial): window.api.auth.status() returns the literal stub payload (renderer-side observable via DevTools console)
- [ ] Phase 1 success criterion #4: Tailwind + shadcn render the placeholder login
- [ ] D-01: package.json `name=colonoscopist`, `productName=Colonoscopist`, app user model id `com.colonoscopist.app`
- [ ] D-02: dims 1280×800 default + min
- [ ] D-03: max 1920×1080
- [ ] D-04: only ONE `BrowserWindow` constructor call exists in `src/main/`
- [ ] D-05: Login page shows clinic-logo placeholder text
- [ ] D-06: `<html dir="ltr">` in `src/renderer/index.html`
- [ ] D-07: PIN input masked (type=password), 4 chars max, numeric
- [ ] D-08: Enter button disabled + literal banner copy
- [ ] SET-03 foundation: `src/main/paths.ts` exports `dataDir()` resolving to `<userData>/data`
- [ ] SET-04 stub: NO `users:*` IPC channel
- [ ] AUTH-04: NO persistence references
- [ ] Pitfall 4: `postinstall: electron-rebuild` ran during install
- [ ] Pitfall 3: Both `tsconfig.node.json` and `tsconfig.web.json` have `strict: true, noImplicitAny: true`

## Wave 0 Gaps Shipped (per VALIDATION.md)

- [x] `tsconfig.json` + `tsconfig.node.json` + `tsconfig.web.json` — strict everywhere
- [x] `src/shared/ipc-contract.ts` — minimal (one constant + one union)
- [x] `src/preload/index.ts` + `src/preload/api.d.ts`
- [x] `src/main/index.ts` + `src/main/window.ts` + `src/main/paths.ts` + `src/main/ipc/auth.ts` + `src/main/startup-log.ts`
- [x] `src/renderer/src/pages/Login.tsx`
- [x] `components.json` (rsc:false, style:default)
- [x] `src/renderer/src/components/ui/{button,input,label}.tsx`
- [x] `src/renderer/src/styles/globals.css`
</verification>

## Artifacts this phase produces

This plan introduces the foundation skeleton. Every later phase extends these — this is the file set downstream agents will grep for.

### Source modules (one per layer)

| Layer | File | Purpose |
|-------|------|---------|
| Shared | `src/shared/ipc-contract.ts` | `IPC.AUTH_STATUS` constant, `AuthStatus` union, `IpcContract` interface |
| Shared | `src/shared/errors.ts` | Stub `IpcError` union (for Phase 2+) |
| Preload | `src/preload/index.ts` | `contextBridge.exposeInMainWorld('api', { auth: { status } })` |
| Preload | `src/preload/api.d.ts` | Global `Window['api']` typed via `IpcContract` (no `any`) |
| Main | `src/main/index.ts` | `app.whenReady()` → setName, createMainWindow, registerAuthIpc, logStartup |
| Main | `src/main/window.ts` | `createMainWindow()` with locked `webPreferences` literal + D-02/D-03 dims |
| Main | `src/main/paths.ts` | `dataDir()` resolving to `<userData>/data` (SET-03 foundation) |
| Main | `src/main/startup-log.ts` | ABI version log line (Pitfall 4 observability) |
| Main | `src/main/ipc/auth.ts` | `registerAuthIpc()` for `auth:status` channel |
| Renderer | `src/renderer/index.html` | `<html dir="ltr">` shell |
| Renderer | `src/renderer/src/main.tsx` | `ReactDOM.createRoot` mount |
| Renderer | `src/renderer/src/App.tsx` | Renders `<Login />` directly (no router) |
| Renderer | `src/renderer/src/env.d.ts` | Vite `import.meta.env` typings |
| Renderer | `src/renderer/src/pages/Login.tsx` | D-05/D-07/D-08 placeholder login |
| Renderer | `src/renderer/src/components/ui/{button,input,label}.tsx` | shadcn primitives |
| Renderer | `src/renderer/src/lib/utils.ts` | `cn()` helper from shadcn |
| Renderer | `src/renderer/src/styles/globals.css` | Tailwind directives + shadcn CSS vars |

### Configuration files

| File | Purpose |
|------|---------|
| `package.json` | name=colonoscopist, productName=Colonoscopist (D-01); pinned deps; `postinstall: electron-rebuild` |
| `tsconfig.json` | Base TS config with project references |
| `tsconfig.node.json` | main + preload target — `strict: true, noImplicitAny: true` |
| `tsconfig.web.json` | renderer target — `strict: true, noImplicitAny: true` (DOM lib + JSX) |
| `electron.vite.config.ts` | main + preload + renderer Vite pipelines |
| `tailwind.config.ts` | `content` globs for `src/renderer/**/*.{ts,tsx}` and `src/renderer/index.html`; `darkMode: 'class'` |
| `postcss.config.js` | tailwindcss + autoprefixer pipeline |
| `components.json` | shadcn registry config (`rsc: false`, `style: 'default'`) |
| `.gitignore` | `node_modules/`, `out/`, `dist/` |

### Symbols exported (for downstream plans to grep)

| Symbol | Source | Consumed by |
|--------|--------|-------------|
| `IPC.AUTH_STATUS` (`'auth:status'`) | `src/shared/ipc-contract.ts` | main `ipcMain.handle`, preload `ipcRenderer.invoke` |
| `AuthStatus` union type | `src/shared/ipc-contract.ts` | main handler return, preload typing, renderer consumer |
| `IpcContract` interface | `src/shared/ipc-contract.ts` | preload builds to this shape; `api.d.ts` declares `Window['api']: IpcContract` |
| `dataDir(): string` | `src/main/paths.ts` | Phase 2's `dbPath()` and `mediaDir()` build on this |
| `registerAuthIpc()` | `src/main/ipc/auth.ts` | `src/main/index.ts` calls it in `app.whenReady()` |
| `createMainWindow()` | `src/main/window.ts` | `src/main/index.ts` calls it in `app.whenReady()` |
| `logStartup(event: string)` | `src/main/startup-log.ts` | main lifecycle events |
| `window.api.auth.status()` | (renderer-side global) | `<Login />` in `src/renderer/src/pages/Login.tsx` |

### Native artifacts

| Artifact | Where | Source |
|----------|-------|--------|
| `better-sqlite3/build/Release/*.node` | `node_modules/better-sqlite3/` | `@electron/rebuild` postinstall hook in `package.json` |

### Decision-controlled identifiers (D-01)

| Field | Value | Source |
|-------|-------|--------|
| `package.json` `name` | `colonoscopist` | D-01 |
| `package.json` `productName` | `Colonoscopist` | D-01 |
| `app.setName()` | `Colonoscopist` | D-01, research A7 |
| `app.setAppUserModelId()` (Windows) | `com.colonoscopist.app` | D-01 |
| `BrowserWindow` dims | `1280×800` default + min | D-02 |
| `BrowserWindow` max dims | `1920×1080` | D-03 |

### Artifacts this phase DOES NOT produce (deferred per CONTEXT + RESEARCH)

- No `src/main/db/*` (Phase 2)
- No `src/main/recorder/*` (Phase 4)
- No `src/main/license/*` (Phase 8)
- No migration runner / `_migrations` table (Phase 2)
- No `users:*` IPC channel (SET-04 deferred to Phase 2)
- No electron-builder config (Phase 8)
- No ffmpeg-static install (Phases 3+)
- No i18next scaffolding (Phase 7)

<success_criteria>
A runnable Electron app where, on `npm run dev`:
1. A window titled `Colonoscopist` opens at 1280×800.
2. The placeholder login is visible (D-05/D-07/D-08 verbatim).
3. The DevTools console (`Ctrl+Shift+I`) shows `await window.api.auth.status()` resolving to `{ authenticated: false, reason: 'scaffold' }`.
4. The dev terminal shows `[boot] better-sqlite3 binding version: 11.10.0` AND `[boot] safeStorage encryption available: true`.
5. `<userData>/logs/startup.log` exists with at least one entry.
6. `npx tsc --noEmit -p tsconfig.node.json` and `npx tsc --noEmit -p tsconfig.web.json` both exit 0.
7. `npm run build` exits 0 and `dist/renderer/index.html` (or `out/renderer/index.html` per electron-vite) emits a built bundle.

Manual-Only verifications (deferred to Plan 03 manual smoke):
- Window drag past 1920×1080 caps (D-03)
- Window drag below 1280×800 snaps (D-02)
</success_criteria>

<output>
Create `.planning/phases/01-scaffold/01-01-SUMMARY.md` when the plan completes.
</output>
