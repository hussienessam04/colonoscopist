---
plan: 02-03
phase: 2
title: Renderer — Wizard + two-step Login + Recovery file picker + Patient List + Patient Form + Settings Users
wave: 3
depends_on:
  - 02-01
  - 02-02
files_modified:
  - src/renderer/src/components/ui/card.tsx
  - src/renderer/src/components/ui/dialog.tsx
  - src/renderer/src/components/ui/avatar.tsx
  - src/renderer/src/components/ui/dropdown-menu.tsx
  - src/renderer/src/components/ui/select.tsx
  - src/renderer/src/components/ui/checkbox.tsx
  - src/renderer/src/components/ui/sonner.tsx
  - src/renderer/src/styles/globals.css
  - src/preload/index.ts
  - src/renderer/src/App.tsx
  - src/renderer/src/main.tsx
  - src/renderer/src/lib/router.ts
  - src/renderer/src/lib/format.ts
  - src/renderer/src/lib/avatar.ts
  - src/renderer/src/store/session.ts
  - src/renderer/src/pages/Wizard.tsx
  - src/renderer/src/pages/Login.tsx
  - src/renderer/src/pages/RecoveryFilePicker.tsx
  - src/renderer/src/pages/PatientsList.tsx
  - src/renderer/src/pages/PatientForm.tsx
  - src/renderer/src/pages/SettingsUsers.tsx
  - src/renderer/src/components/UserPicker.tsx
  - src/renderer/src/components/PinEntry.tsx
  - src/renderer/src/components/PatientRow.tsx
  - src/renderer/src/components/PageSizeSelector.tsx
  - src/renderer/src/components/ConfirmDialog.tsx
  - package.json
  - tests/renderer/setup.ts
  - tests/renderer/pages/wizard.test.tsx
  - tests/renderer/pages/login.test.tsx
  - tests/renderer/pages/recovery-picker.test.tsx
  - tests/renderer/pages/patients-list.test.tsx
  - tests/renderer/pages/patient-form.test.tsx
  - tests/renderer/pages/settings-users.test.tsx
autonomous: true
requirements_addressed:
  - AUTH-01
  - PAT-01
  - PAT-02
  - PAT-03
  - PAT-04
  - AUDIT-01
  - D-01
  - D-02
  - D-03
  - D-04
  - D-05
  - D-06
  - D-07
  - D-08
---

# Phase 2 Plan 03 — Renderer (Wizard + Login + Recovery + Patient List + Settings Users)

<objective>
Ship the renderer surface for Phase 2: a state-based router that drives the wizard on first launch (per D-01) → two-step login on subsequent launches (per D-05/D-06/D-07/D-08) → patient list with search + soft-delete toggle (per PAT-02/PAT-04) → patient create/edit form (per PAT-01/PAT-03) → Settings → Users (per D-03 + SET-04) → Recovery file picker (per D-04 + Fix 7). Install the shadcn primitives deterministically via `npx shadcn@latest add card dialog avatar dropdown-menu select checkbox sonner --yes` and capture the exact file paths (per Warning fix). Tests cover every page.

**As a** doctor at the workstation
**I want to** open the app and either set it up (first time) or sign in (subsequent launches) and immediately find the patient I need to scope the next procedure to
**so that** the recording flow never has to wait on me.

</objective>

<execution>

<task type="tracer">
  <name>shadcn primitives + state-based router + session store + skeleton Wizard → patients route</name>
  <read_first>
    - src/renderer/src/App.tsx (Phase 1 — single-route shell, replaced)
    - src/renderer/src/main.tsx (Phase 1 — entry, add the `<Toaster />` mount)
    - src/renderer/src/lib/utils.ts (Phase 1 — `cn(...)` helper for the new components)
    - src/renderer/src/styles/globals.css (Phase 1 — extend with sonner + new keyframes)
    - src/preload/index.ts (Phase 1 — extend the `api` bridge with the Phase 2 surface)
    - src/shared/ipc-contract.ts (Plan 02-01 + 02-02 — `IpcContract` is the contract; preload must implement it exactly)
    - src/shared/errors.ts (Plan 02-01 — `IpcError` union incl. `IPC_ENCRYPTION_UNAVAILABLE`)
    - src/shared/validators.ts (Plan 02-01 + 02-02 — `wizardInput`, `pinInput`, `patientInput`, `patientPatchInput`)
    - src/main/ipc/auth.ts (Plan 02-01 — `auth:status` returns `{ hasUsers, authenticated, userId, clinicName, isFirstAdmin }`)
    - src/main/ipc/patients.ts (Plan 02-02 — `patients:*` channels registered with audit-on-every-mutation-and-read)
    - package.json (Phase 1 — extend with shadcn deps; shadcn CLI handles the runtime versions)
    - .opencode/skills/shadcn/SKILL.md (component registry + RTL handling caveats)
  </read_first>
  <action>
    Run `npx shadcn@latest add card dialog avatar dropdown-menu select checkbox sonner --yes` once during plan execution and capture the output paths (per Warning fix — exact files shadcn writes):
    - `src/renderer/src/components/ui/card.tsx`
    - `src/renderer/src/components/ui/dialog.tsx`
    - `src/renderer/src/components/ui/avatar.tsx`
    - `src/renderer/src/components/ui/dropdown-menu.tsx`
    - `src/renderer/src/components/ui/select.tsx`
    - `src/renderer/src/components/ui/checkbox.tsx`
    - `src/renderer/src/components/ui/sonner.tsx`
    List those exact paths in `<artifacts>` and `files_modified` (per Warning fix — captured deterministically, not "assumed to land somewhere").
    Add new dependencies to `package.json` via `npm install react-hook-form zod sonner date-fns @radix-ui/react-avatar @radix-ui/react-dialog @radix-ui/react-dropdown-menu @radix-ui/react-select @radix-ui/react-checkbox @hookform/resolvers` (per RESEARCH.md §Standard Stack).
    Add `sonner` styles to `src/renderer/src/styles/globals.css` (the shadcn recipe adds the necessary `@layer` block).
    Extend `src/preload/index.ts` so the `window.api` object satisfies the full `IpcContract` from Plans 02-01 + 02-02. **Implement, do not invent** — every method is `ipcRenderer.invoke(IPC.X, ...args)` against the matching `IPC.X` constant. Top-level bridge namespaces: `auth.*` (status, bootstrap, login, logout, listUsers, recoveryRequest, acceptRecoveryFile), `users.*` (create, remove, resetPin), `patients.*` (list, get, create, update, softDelete, restore), `audit.*` (list). Pin `auth.status` + `auth.login` channels consistently (per Fix 4). No extra methods.
    Create `src/renderer/src/lib/router.ts` exporting a tiny state-based router hook: `useRoute()` returns `{ route, navigate }` where `route` is a tagged union: `'wizard' | 'login' | 'patients' | 'patient-new' | 'patient-edit/:id' | 'patient-detail/:id' | 'settings-users'`. Persist across the wizard→login transition via `auth:status` polling at mount. No deep-link URLs in v1; no `react-router` dependency.
    Create `src/renderer/src/store/session.ts` exporting a minimal store (no `zustand` dep — 30-line `useSyncExternalStore` for `session.currentUserId` + `clinicName` + `isFirstAdmin` + `isAuthenticated`). The hook re-fetches `auth:status` on mount and exposes `refresh()` + `signOut()`.
    Create `src/renderer/src/lib/format.ts` exporting `relativeTime(epochMs): string` using `date-fns/formatDistanceToNowStrict` with `addSuffix: true`. Locale is `en` for v1; Phase 7 RTL pass swaps the locale.
    Create `src/renderer/src/lib/avatar.ts` exporting `palette: string[]` (12 deterministic colors) + `pickColor(userId: string): string` (hash of id modulo palette length) + `initials(fullName: string): string` (first letters of first + last words; single-word names get single letter).
    Replace `src/renderer/src/App.tsx` with the state-based router skeleton. On mount: call `window.api.auth.status()` (returns the authoritative `AuthStatus` shape from Plan 02-01 per Fix 4); if `hasUsers === false`, navigate to `'wizard'`; else navigate to `'login'`. Mount `<Toaster />` from `sonner` at the root.
    Create `src/renderer/src/pages/Wizard.tsx` (per D-01). Multi-field form: full name + clinic name + PIN (4 digits) + confirm PIN. React Hook Form + zod resolver against `wizardInput` from `src/shared/validators.ts`. On submit: `window.api.auth.bootstrap({ fullName, clinicName, pin })`; on success, `useSession().set(...)` + `navigate('patients')`. The wizard is the only path that creates the admin (per D-01 + D-02). Copy is English-only in v1 (Deferred Ideas: i18n ships in Phase 7).
    Create `tests/renderer/setup.ts` — happy-dom env, `@testing-library/react` cleanup, `window.api` mock router.
    Create `tests/renderer/pages/wizard.test.tsx` (Wave 0): renders the four fields; submit with empty `fullName` shows the inline validation error; submit with valid fields calls `window.api.auth.bootstrap` once and navigates to the patients route. The skeleton Patients route renders a placeholder heading so the navigation assertion is non-trivial.
  </action>
  <acceptance_criteria>
    - `package.json` adds `react-hook-form`, `zod`, `sonner`, `date-fns`, the five `@radix-ui/react-*` primitives, `@hookform/resolvers`.
    - The 7 shadcn primitive files exist at exactly the captured paths (per Warning fix):
      - `src/renderer/src/components/ui/card.tsx`
      - `src/renderer/src/components/ui/dialog.tsx`
      - `src/renderer/src/components/ui/avatar.tsx`
      - `src/renderer/src/components/ui/dropdown-menu.tsx`
      - `src/renderer/src/components/ui/select.tsx`
      - `src/renderer/src/components/ui/checkbox.tsx`
      - `src/renderer/src/components/ui/sonner.tsx`
    - `src/preload/index.ts` exposes the full Phase 2 `IpcContract` (auth.* incl. `acceptRecoveryFile`, users.*, patients.*, audit.*) — `auth.status` + `auth.login` channels pinned consistently with Plan 02-01 (per Fix 4).
    - `src/renderer/src/App.tsx` calls `auth:status` on mount and routes to `'wizard'` or `'login'` based on `hasUsers`.
    - `src/renderer/src/pages/Wizard.tsx` renders the four fields; submit calls `auth:bootstrap` and navigates on success (per D-01).
    - `tests/renderer/pages/wizard.test.tsx` passes.
    - npm run typecheck:web exits 0.
    - All actions tagged with `per D-01`, `per D-02`, `per D-05`, `per D-06`, `per D-07`, `per D-08` markers (per Fix 8).
  </acceptance_criteria>
  <verify>
    <automated>npm run typecheck:web && npm run test:unit -- --run tests/renderer/pages/wizard.test.tsx</automated>
  </verify>
  <done>A client-renders-the-Wizard test submits valid fields, sees `auth:bootstrap` called once, and observes navigation to the placeholder patients route. The 7 shadcn primitives are installed at deterministic paths captured by the `npx shadcn@latest add` command output (per Warning fix).</done>
</task>

<task type="auto">
  <name>Two-step Login + avatar + back arrow + rate-limit UX + Recovery file picker (Fix 7) + tests</name>
  <read_first>
    - src/renderer/src/pages/Login.tsx (Phase 1 placeholder — replaced entirely)
    - src/renderer/src/lib/format.ts (Task 1 — `relativeTime`)
    - src/renderer/src/lib/avatar.ts (Task 1 — `initials` + `pickColor`)
    - src/renderer/src/store/session.ts (Task 1 — session hook)
    - src/renderer/src/components/ui/avatar.tsx (Task 1 — shadcn primitive)
    - src/renderer/src/components/ui/dialog.tsx (Task 1 — shadcn primitive for Recovery file picker modal)
    - src/main/ipc/auth.ts (Plan 02-01 — `auth:login`, `auth:listUsers`, `auth:recoveryRequest`, `auth:acceptRecoveryFile` channels)
    - src/shared/errors.ts (Plan 02-01 — `IPC_AUTH_FAILED`, `IPC_RATE_LIMITED`, `IPC_LOCKED`, `IPC_ENCRYPTION_UNAVAILABLE`)
  </read_first>
  <action>
    Replace `src/renderer/src/pages/Login.tsx` with the two-step flow (per D-05/D-06/D-07/D-08):
    - **Step 1 (user picker):** calls `window.api.auth.listUsers()` on mount (returns `{ users: UserPublic[] }` per Fix 4); renders one row per user with `<Avatar>` (color = `pickColor(user.id)`, fallback text = `initials(user.fullName)`), `<span>{fullName}</span>`, `<span>last seen {relativeTime(user.lastLoginAt) ?? 'never'}</span>` (per D-06). Tapping a row sets `selectedUser` and transitions to step 2 (per D-05).
    - **Step 2 (PIN entry):** `<PinEntry user={selectedUser} onBack={() => setSelectedUser(null)} onSuccess={(user) => { session.set; navigate('patients') }} />`. A top-left `<ArrowLeft />` returns to step 1 and clears the PIN field (per D-08). The PIN input is `type="password"`, `maxLength=4`, `inputMode="numeric"`, `pattern="[0-9]*"`, `autoComplete="off"`.
    - On `IPC_AUTH_FAILED`: render inline red error "Incorrect PIN" below the input, clear the PIN field, refocus the input, and briefly disable the Enter button until the next keystroke (per D-07).
    - On `IPC_RATE_LIMITED` / `IPC_LOCKED`: render inline error "Try again in N seconds" or "Account locked — contact admin" (per D-07); the back arrow still works (per D-08).
    - On `IPC_ENCRYPTION_UNAVAILABLE`: render inline error "Encryption unavailable — see logs" (per Fix 3 fail-closed).
    - On success: `session.set(user); navigate('patients')`.
    - 10s idle timer clears the PIN field after inactivity; the back arrow is always available regardless of the timer.
    - **"Forgot admin PIN?" affordance** below the user list (per D-04 + Fix 7): renders `<RecoveryFilePicker />` — a button that opens a `<Dialog>` containing the affordance. On click "Email vendor", calls `window.api.auth.recoveryRequest()` and shows sonner toast "Recovery email queued — vendor will reply with a `.recover` file." On the user receiving the file, the same dialog shows "Select recovery file" button → `window.api.auth.acceptRecoveryFile({ filePath })` → on response `{ accepted: true, verificationDeferred: true }`, renders sonner toast "Recovery file accepted. License verification will complete in Phase 8." (per Fix 7 typed deferred response + Phase 8-compatible surface).
    Create `src/renderer/src/components/UserPicker.tsx` (the row + avatar + name + last-login composition).
    Create `src/renderer/src/components/PinEntry.tsx` (the 4-digit input + Enter button + back arrow + error display + idle timer).
    Create `src/renderer/src/pages/RecoveryFilePicker.tsx` (the D-04 affordance + the Fix 7 file-picker modal — exposes the two-step flow "Email vendor" + "Select recovery file" + sonner toasts).
    Create `tests/renderer/pages/login.test.tsx` (Wave 0):
    - Renders the user list with one row per user; each row shows the avatar, full name, and last-login (mock `auth.listUsers`).
    - Tapping a row transitions to the PIN entry; the back arrow returns to the list (per D-05 + D-08).
    - Submitting a wrong PIN calls `auth.login` once and renders the inline error "Incorrect PIN" (per D-07).
    - Submitting the correct PIN calls `auth.login` once and navigates to patients.
    - The "Forgot admin PIN?" affordance is visible; clicking "Email vendor" calls `auth.recoveryRequest` once and shows the sonner toast.
    - On `IPC_AUTH_FAILED` from `auth.login`, the PIN field clears and the Enter button briefly disables (per D-07).
    - On `IPC_RATE_LIMITED` / `IPC_LOCKED` from `auth.login`, the lockout message renders (per D-07).
    Create `tests/renderer/pages/recovery-picker.test.tsx` (Wave 0 — fills VALIDATION.md gap for Fix 7):
    - Clicking "Email vendor" button calls `auth.recoveryRequest` once.
    - The same dialog shows "Select recovery file" button.
    - Clicking "Select recovery file" calls `auth.acceptRecoveryFile({ filePath: '/mock/path/admin.recover' })` once.
    - On response `{ accepted: true, verificationDeferred: true }`, the sonner toast renders "Recovery file accepted. License verification will complete in Phase 8." (per Fix 7 typed deferred response).
  </action>
  <acceptance_criteria>
    - `src/renderer/src/pages/Login.tsx` is the two-step flow (no static placeholder).
    - `UserPicker` rows show avatar + name + last-login (per D-05, D-06).
    - `PinEntry` has the top-left `ArrowLeft` (per D-08), inline error "Incorrect PIN" on failure (per D-07), and the 10s idle clear.
    - `RecoveryFilePicker` (new file) is the surface for D-04 + Fix 7; the file picker uses `window.api.auth.acceptRecoveryFile` and shows the Phase 8 deferred-verify toast.
    - `tests/renderer/pages/login.test.tsx` passes (7+ test cases).
    - `tests/renderer/pages/recovery-picker.test.tsx` passes (3 cases).
    - npm run typecheck:web exits 0.
    - All actions tagged with `per D-04`, `per D-05`, `per D-06`, `per D-07`, `per D-08`, `per Fix 7` markers (per Fix 8).
  </acceptance_criteria>
  <verify>
    <automated>npm run typecheck:web && npm run test:unit -- --run tests/renderer/pages/login.test.tsx tests/renderer/pages/recovery-picker.test.tsx</automated>
  </verify>
  <done>A client-renders-the-Login test mocks `listUsers` + `login`, walks the picker → PIN → success path, and proves the back arrow + "Forgot admin PIN?" affordance are wired. A separate test for `RecoveryFilePicker` proves the Fix 7 surface: email-vendor + select-file + deferred-verify toast.</done>
</task>

<task type="auto">
  <name>Patients List + Patient Form + Settings → Users + tests</name>
  <read_first>
    - src/renderer/src/pages/Wizard.tsx (Task 1 — same form conventions: react-hook-form + zod)
    - src/renderer/src/pages/Login.tsx (Task 2 — same route + nav shape)
    - src/renderer/src/store/session.ts (Task 1 — `isFirstAdmin` + `signOut`)
    - src/renderer/src/components/ui/dropdown-menu.tsx (Task 1 — shadcn primitive)
    - src/renderer/src/components/ui/dialog.tsx (Task 1 — shadcn primitive)
    - src/renderer/src/components/ui/select.tsx (Task 1 — shadcn primitive)
    - src/renderer/src/components/ui/checkbox.tsx (Task 1 — shadcn primitive)
    - src/main/ipc/patients.ts (Plan 02-02 — `patients:*` channels with audit-on-every-mutation-and-read per Fix 6)
    - src/main/ipc/users.ts (Plan 02-01 — `users:create`, `users:remove`, `users:resetPin` admin-gated)
    - src/shared/validators.ts (Plan 02-01 + 02-02 — `patientInput`, `patientPatchInput`, `userInput`, `idInput`)
  </read_first>
  <action>
    Create `src/renderer/src/pages/PatientsList.tsx` (the default landing after login):
    - Top bar: search input (calls `patients.list({ search })` on debounced input — the IPC writes a `patient_list` audit row per Fix 6), MRN exact input (optional second input), "Show deleted" `<Checkbox>` toggle (calls `patients.list({ includeDeleted })`), page-size `<Select>` (defaults to 25), "+ New patient" button (navigates to `'patient-new'`).
    - Table body: rows from `patients.list`; each row shows name, DOB, gender, MRN, phone, and a row menu (DropdownMenu) with "Edit" + "Delete" (soft) actions. Soft-deleted rows show a "Deleted" badge and offer "Restore" instead of "Delete" (admin only — guards on `session.isFirstAdmin`).
    - Pagination: prev/next + page indicator + total count.
    - Empty state: "No patients yet — click + New patient to add the first one."
    Create `src/renderer/src/components/PatientRow.tsx` (the table row + actions menu).
    Create `src/renderer/src/components/PageSizeSelector.tsx` (the shadcn `<Select>` wrapper).
    Create `src/renderer/src/components/ConfirmDialog.tsx` (the shadcn `<Dialog>` for "Delete patient?" / "Remove user?" soft confirmations).
    Create `src/renderer/src/pages/PatientForm.tsx` (create + edit modes via `mode: 'create' | 'edit'` prop). React Hook Form + zod resolver against `patientInput` (for create) or `patientPatchInput` (for edit). Fields: full name (text), DOB (`<input type="date">`), gender (`<Select>`: male / female / other / —), MRN (text, optional), phone (text, optional), notes (textarea, optional). On submit: `patients.create` or `patients.update`; on success sonner toast + navigate back to `'patients'`. On `IPC_VALIDATION { field: 'mrn' }` show inline error "MRN already in use" (per Fix 6 audit-row metadata).
    Create `src/renderer/src/pages/SettingsUsers.tsx` (admin only — guarded by `session.isFirstAdmin` per D-03):
    - Lists all users via `auth.listUsers()`. Each row shows name + last-login + an actions menu (DropdownMenu) with "Reset PIN" + "Remove".
    - "Add user" button opens a `<Dialog>` with the `userInput` form (full name + PIN + confirm PIN). On submit: `users.create`; on success close + refresh list.
    - "Reset PIN" opens a `<Dialog>` with a new PIN + confirm. On submit: `users.resetPin`; on success sonner toast.
    - "Remove" opens the `ConfirmDialog`; on confirm: `users.remove`; on success sonner toast + refresh list.
    - The current admin cannot remove themselves (button is disabled with a tooltip — per D-02 admin cannot self-exclude).
    Create `tests/renderer/pages/patients-list.test.tsx` (Wave 0):
    - Renders a list of mocked patients; the table shows the rows.
    - Typing into the search input calls `patients.list` with `{ search: '...' }` after debounce (each call writes a `patient_list` audit row in the backend per Fix 6).
    - Toggling "Show deleted" calls `patients.list` with `{ includeDeleted: true }`.
    - "+ New patient" navigates to `'patient-new'`.
    - A row's "Delete" action opens the confirm dialog; confirming calls `patients.softDelete` and refreshes the list.
    Create `tests/renderer/pages/patient-form.test.tsx` (Wave 0):
    - Render the form in `create` mode; submit with valid fields calls `patients.create` once and navigates back.
    - Render the form in `edit` mode with a mocked `get`; submit a patch calls `patients.update` once.
    - Invalid DOB (non-ISO) shows the inline validation error.
    - On `IPC_VALIDATION { field: 'mrn' }` response, the inline error "MRN already in use" renders.
    Create `tests/renderer/pages/settings-users.test.tsx` (Wave 0):
    - Renders the user list when `session.isFirstAdmin === true`.
    - "Add user" opens the dialog; submit calls `users.create` once.
    - "Reset PIN" opens the dialog; submit calls `users.resetPin` once.
    - "Remove" opens the confirm; confirm calls `users.remove` once.
    - When `session.isFirstAdmin === false`, the page renders a "Admin only" gate.
    - The current admin's own "Remove" button is disabled with a tooltip (per D-02).
  </action>
  <acceptance_criteria>
    - `src/renderer/src/pages/PatientsList.tsx` is the default landing route; calls `patients.list` with the right filtering + pagination; toggles `includeDeleted` per the soft-delete UX.
    - `src/renderer/src/pages/PatientForm.tsx` handles both `create` and `edit` modes; uses the shared zod schemas; renders "MRN already in use" inline error on `IPC_VALIDATION { field: 'mrn' }`.
    - `src/renderer/src/pages/SettingsUsers.tsx` is admin-gated; `users.create`, `users.resetPin`, `users.remove` IPC channels are wired; admin cannot remove themselves (per D-02).
    - All 4 test files pass: patients-list (5 cases), patient-form (4 cases), settings-users (6 cases), login (7 cases), recovery-picker (3 cases), wizard (smoke).
    - npm run typecheck:web exits 0.
    - npm run build exits 0 (renderer builds with the 7 shadcn primitives).
    - All actions tagged with `per D-01`, `per D-02`, `per D-03`, `per D-04`, `per D-05`, `per D-06`, `per D-07`, `per D-08`, `per PAT-01`, `per PAT-02`, `per PAT-03`, `per PAT-04`, `per AUDIT-01`, `per Fix 6`, `per Fix 7` markers (per Fix 8).
  </acceptance_criteria>
  <verify>
    <automated>npm run typecheck:web && npm run test:unit -- --run tests/renderer/pages && npm run build</automated>
  </verify>
  <done>A signing-in test walks `login` → `patients` → `patient-new` → submits → lands back on `patients` with the new row (the `patient_create` audit row was written by the backend per Fix 6). A separate test walks `settings-users` → "Add user" → submits → list refreshes. A separate `recovery-picker.test.tsx` proves the Fix 7 surface. `npm run build` exits 0 with the 7 shadcn primitives bundled.</done>
</task>

</execution>

<must_haves>
truths:
  - "On first launch, the wizard collects full name + clinic name + PIN + confirm PIN and creates the admin user atomically (per D-01)"
  - "On subsequent launches, the two-step login shows the user list (avatar + name + last-login) → tap row → PIN entry (per D-05 + D-06); back arrow returns to the list (per D-08)"
  - "A wrong PIN shows the inline red error 'Incorrect PIN' and clears the field; the Enter button briefly disables (per D-07)"
  - "On `IPC_ENCRYPTION_UNAVAILABLE`, the inline error 'Encryption unavailable — see logs' renders (per Fix 3)"
  - "A logged-in user lands on the Patient List; search by name substring or MRN exact filters the rows; '+ New patient' opens the form"
  - "The Patient List 'Show deleted' toggle reveals soft-deleted rows with a 'Deleted' badge; restore is admin-only"
  - "Settings → Users is reachable only by the admin (per D-03); add / remove / reset PIN for non-admin users; admin cannot remove themselves (per D-02)"
  - "The 'Forgot admin PIN?' affordance on the login screen (per D-04) opens the Recovery file picker modal (per Fix 7) — emails vendor + accepts `.recover` file with typed deferred response `{ accepted: true, verificationDeferred: true }`"
  - "Every `patients:list` and `patients:get` call writes a `patient_list` / `patient_view` audit row in the backend (per Fix 6 + ROADMAP Phase 2 success criterion 5)"
prohibitions:
  - statement: "react-router dependency"
    status: enforced
    verification: state-based router only; git grep returns no matches
  - statement: "nodeIntegration in renderer"
    status: enforced
    verification: Phase 1 baseline unchanged (contextIsolation: true, nodeIntegration: false, sandbox: true)
  - statement: "Optimistic mutations on clinical data"
    status: enforced
    verification: every IPC round-trip awaited; sonner toast on success/error
  - statement: "Field values in audit_log.metadata"
    status: enforced
    verification: renderer never bypasses the IPC contract; backend enforces per Fix 6
  - statement: "Remove on the admin's own row"
    status: enforced
    verification: SettingsUsers button disabled with tooltip (per D-02)
  - statement: "Automatic sign-out on lockout"
    status: enforced
    verification: locked user's row stays in the user list; tapping shows lockout message; back arrow still works (per D-08)
  - statement: "Real recovery file verification in Phase 2"
    status: enforced
    verification: typed deferred response `{ accepted: true, verificationDeferred: true }` surfaces Phase 8 hand-off honestly (per Fix 7)
  - statement: "dangerouslySetInnerHTML in renderer"
    status: enforced
    verification: React's default escaping + no raw HTML injection; verified by git grep
key_links:
  - "src/renderer/src/App.tsx → window.api.auth.status() → src/main/auth/session.ts — renderer's route decision is driven by main-process session state (per Fix 4)"
  - "src/renderer/src/pages/Login.tsx → window.api.auth.login → src/main/auth/index.ts (login) — renderer's PIN entry funnels into rate-limit + audit-enforcing service (per AUTH-03 + D-07)"
  - "src/renderer/src/pages/PatientsList.tsx → window.api.patients.list → src/main/db/patients.ts — read path; backend writes `patient_list` audit row per Fix 6"
  - "src/renderer/src/pages/SettingsUsers.tsx → window.api.users.{create,remove,resetPin} → src/main/auth/index.ts (admin-gated per D-02 + D-03)"
  - "src/renderer/src/pages/RecoveryFilePicker.tsx → window.api.auth.{recoveryRequest,acceptRecoveryFile} → src/main/auth/index.ts (typed deferred response per Fix 7 + D-04)"
</must_haves>

<artifacts>
- src/renderer/src/components/ui/card.tsx (new — captured from `npx shadcn@latest add card`)
- src/renderer/src/components/ui/dialog.tsx (new — captured from `npx shadcn@latest add dialog`)
- src/renderer/src/components/ui/avatar.tsx (new — captured from `npx shadcn@latest add avatar`)
- src/renderer/src/components/ui/dropdown-menu.tsx (new — captured from `npx shadcn@latest add dropdown-menu`)
- src/renderer/src/components/ui/select.tsx (new — captured from `npx shadcn@latest add select`)
- src/renderer/src/components/ui/checkbox.tsx (new — captured from `npx shadcn@latest add checkbox`)
- src/renderer/src/components/ui/sonner.tsx (new — captured from `npx shadcn@latest add sonner`)
- src/renderer/src/lib/router.ts (new)
- src/renderer/src/lib/format.ts (new)
- src/renderer/src/lib/avatar.ts (new)
- src/renderer/src/store/session.ts (new)
- src/renderer/src/pages/Wizard.tsx (new)
- src/renderer/src/pages/Login.tsx (new)
- src/renderer/src/pages/RecoveryFilePicker.tsx (new)
- src/renderer/src/pages/PatientsList.tsx (new)
- src/renderer/src/pages/PatientForm.tsx (new)
- src/renderer/src/pages/SettingsUsers.tsx (new)
- src/renderer/src/components/UserPicker.tsx (new)
- src/renderer/src/components/PinEntry.tsx (new)
- src/renderer/src/components/PatientRow.tsx (new)
- src/renderer/src/components/PageSizeSelector.tsx (new)
- src/renderer/src/components/ConfirmDialog.tsx (new)
- tests/renderer/setup.ts (new)
- tests/renderer/pages/wizard.test.tsx (new)
- tests/renderer/pages/login.test.tsx (new)
- tests/renderer/pages/recovery-picker.test.tsx (new)
- tests/renderer/pages/patients-list.test.tsx (new)
- tests/renderer/pages/patient-form.test.tsx (new)
- tests/renderer/pages/settings-users.test.tsx (new)
</artifacts>

<threat_model>

| Trust Boundary | Description |
|----------------|-------------|
| Renderer ↔ IPC | Untrusted renderer + trusted main via contextBridge. Every IPC channel validated by main; no Node access in renderer. |
| Renderer ↔ DOM | React default-escaping; no dangerouslySetInnerHTML; CSP deferred to Phase 7. |
| Renderer ↔ Window | `setWindowOpenHandler` blocks arbitrary opens; mailto: allowed; recovery file picker uses IPC channel. |

| Threat ID | Category | Component | Severity | Disposition | Mitigation |
|-----------|----------|-----------|----------|-------------|------------|
| T-02-REN-01 | Tampering | src/renderer/src/pages/Wizard.tsx + PatientForm.tsx | medium | mitigate | React default text escaping; no `dangerouslySetInnerHTML`; zod schemas re-validate every input (per Fix 8). |
| T-02-REN-02 | Elevation of Privilege | src/preload/index.ts (api bridge) | high | mitigate | `nodeIntegration: false`, `contextIsolation: true`, `sandbox: true`, `webSecurity: true` (Phase 1 baseline); all DB access stays in main via IPC. |
| T-02-REN-03 | Spoofing | src/renderer/src/pages/Wizard.tsx (replay) | medium | mitigate | `wizardBootstrap` throws `IPC_VALIDATION` if `users` already has a row (per D-01 first-launch only); `App.tsx` routes via `auth:status` on mount. |
| T-02-REN-04 | Tampering | src/renderer/src/lib/router.ts (URL injection) | low | mitigate | No URL-based routing in v1; route is in renderer state only; deep-link URLs are v2 upgrade path (no `react-router` dep). |
| T-02-REN-05 | Information Disclosure | src/renderer/src/pages/Login.tsx (PIN entry) | medium | mitigate | 10s idle timer clears the PIN; back arrow always clears it; field is `type="password"` not `type="text"` (per D-08). |
| T-02-REN-06 | Denial of Service | src/renderer/src/pages/SettingsUsers.tsx (admin self-removal) | high | mitigate | "Remove" disabled on the admin's own row (button + tooltip) per D-02; `users.remove` IPC also throws `IPC_VALIDATION` if `userId === session.currentUserId`. |
| T-02-REN-07 | Tampering | src/renderer/src/pages/PatientForm.tsx + PatientsList.tsx | low | mitigate | Forms disable submit button while `react-hook-form` is in `isSubmitting` state; IPC errors render inline. |
| T-02-REN-08 | Repudiation | src/renderer/src/pages/RecoveryFilePicker.tsx (Fix 7) | medium | mitigate | `acceptRecoveryFile` returns `{ accepted: true, verificationDeferred: true }` with explicit deferred flag; renderer surfaces Phase 8 hand-off honestly (per Fix 7 + D-04). |
| T-02-REN-09 | Repudiation | src/renderer/src/pages/PatientsList.tsx + PatientForm.tsx | medium | mitigate | Every backend `patients:list`/`patients:get` call writes `patient_list`/`patient_view` audit row (per Fix 6 — backend-enforced, not renderer-bypassable). |
| T-02-REN-SC | Tampering | npm installs | high | mitigate | slopcheck + blocking human checkpoint for any [ASSUMED]/[SUS] packages; shadcn primitives pinned to the 7 captured paths (per Warning fix). |

| ASVS Level | Coverage |
|------------|----------|
| V1 Architecture | Three-process model; renderer = untrusted; main = trust boundary. |
| V2 Authentication | Login two-step flow + recovery file picker (Fix 7); idle timer; back arrow (D-05/D-06/D-07/D-08). |
| V4 Access Control | SettingsUsers admin-gated via `session.isFirstAdmin` (per D-03 + SET-04). |
| V5 Input Validation | zod schemas re-validated in main for every write. |
| V10 Malicious Code | `nodeIntegration: false`; `dangerouslySetInnerHTML` grep-banned; shadcn primitives sanitized. |
| V11 Business Logic | Admin cannot remove themselves; locked user stays in user list (per D-02 + D-08). |

</threat_model>

<verification>
- `npm run typecheck:web` exits 0.
- `npm run test:unit -- --run tests/renderer` exits 0 (6 renderer test files: wizard, login, recovery-picker, patients-list, patient-form, settings-users).
- `npm run build` exits 0; built renderer has bundled the 7 shadcn primitives at the exact captured paths (per Warning fix).
- `git grep -nE 'dangerouslySetInnerHTML' src/renderer` returns no matches.
- `git grep -nE 'react-router' src/renderer package.json` returns no matches.
- `git grep -nE 'nodeIntegration:\s*true' src/main` returns no matches.
- All 7 shadcn primitive files exist at the exact paths in `<artifacts>` (per Warning fix).
- All actions contain literal `per D-01..D-08` + `per Fix 6` + `per Fix 7` markers.
</verification>

<success_criteria>
- A brand-new install lands on the wizard; the admin completes the four fields and sees the Patient List.
- A restart shows the user list with the admin; tapping the row → PIN → success lands on the Patient List.
- A wrong PIN shows the inline error; a 10th bad PIN locks the account; the user list row is still present but tapping shows the lockout message (per D-07).
- The Patient List supports search, pagination, page-size, and the "Show deleted" toggle (per PAT-02 + PAT-04).
- Settings → Users is admin-only; add / remove / reset PIN work via the dropdown menus; admin cannot remove themselves (per D-02 + D-03).
- "Forgot admin PIN?" opens the Recovery file picker (per D-04 + Fix 7) — emails vendor + accepts `.recover` file with typed deferred response.
- Every `patients:list` / `patients:get` call writes an audit row in the backend (per Fix 6 + ROADMAP Phase 2 success criterion 5).
</success_criteria>

<output>
Create `.planning/phases/02-database-patient-audit-auth/02-03-SUMMARY.md` when done.
</output>