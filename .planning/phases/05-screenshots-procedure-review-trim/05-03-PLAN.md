---
phase: 05-screenshots-procedure-review-trim
plan: 03
type: execute
wave: 3
depends_on:
  - 01
  - 02
files_modified:
  - src/main/recorder/ffmpeg-args.ts
  - src/main/recorder/trim.ts
  - src/main/recorder/preview-server.ts
  - src/main/db/procedures-repo.ts
  - src/main/ipc/procedures.ts
  - src/main/ipc/screenshots.ts
  - src/main/paths.ts
  - src/main/index.ts
  - src/preload/index.ts
  - src/shared/ipc-contract.ts
  - src/shared/validators.ts
  - src/renderer/src/components/Scrubber.tsx
  - src/renderer/src/components/TrimControls.tsx
  - src/renderer/src/pages/ProcedureReview.tsx
  - src/renderer/src/store/trim.ts
  - src/renderer/src/hooks/useTrim.ts
  - src/renderer/src/lib/trim-clamp.ts
  - tests/main/recorder/ffmpeg-args.test.ts
  - tests/main/recorder/trim.test.ts
  - tests/main/recorder/preview-server.test.ts
  - tests/main/db/procedures-repo.test.ts
  - tests/main/ipc/procedures.test.ts
  - tests/renderer/components/Scrubber.test.tsx
  - tests/renderer/components/TrimControls.test.tsx
  - tests/renderer/lib/trim-clamp.test.ts
autonomous: false
requirements:
  - REV-04
must_haves:
  truths:
    - "`buildTrimArgs({ inputPath, inMs, outMs, outputPath })` emits exactly `['-ss', '<inSec>', '-i', '<inputPath>', '-t', '<durationSec>', '-c', 'copy', '-movflags', '+faststart', '-y', '<outputPath>']` per D-08. The `-ss` is BEFORE `-i` (fast, ±500ms), `-c copy` is mandatory (PITFALLS §2), and `-movflags +faststart` keeps the moov atom at the front of the file. The function throws `EmptyFfmpegArgsError('outMs must be greater than inMs')` when `outMs <= inMs` (PITFALLS §6)."
    - "`applyTrim({ procedureId, inMs, outMs })` orchestrates the one-shot ffmpeg spawn: validates `procedure.status === 'completed'` (D-13 gate; rejects `partial` + `crashed` + `recording`), derives input path from `procedures.videoPath` (resolved via `paths.procedureMediaDir(procedure.patientId, procedureId) + '/' + videoPath`), derives output path as `<sibling>-trimmed.mp4` (never overwrites original — D-07 + PITFALLS §3), spawns ffmpeg via `defaultFfmpegPath()` with `windowsVerbatimArguments: true`, awaits exit code 0, fsyncs the trimmed file, calls `proceduresRepo.updateVideoPath(id, trimmedRel, originalRel)` (sets `video_path_original` on first trim only — locked per D-07), and audits `procedure.trimmed` with `metadata: { inMs, outMs, originalVideoPath, trimmedVideoPath }` (Fix 6 — paths are userData-relative)."
    - "`proceduresRepo.updateVideoPath(id, newRel, originalRel)` updates `video_path = newRel` AND `video_path_original = originalRel` ONLY when `video_path_original IS NULL` (the first-trim case). Subsequent trims leave `video_path_original` UNCHANGED (D-07). The repo uses `UPDATE procedures SET video_path = ?, video_path_original = COALESCE(video_path_original, ?) WHERE id = ?` — the `COALESCE` is the canonical first-trim-only guard."
    - "`proceduresRepo.restoreFromOriginal(id)` updates `video_path = video_path_original` (no-op when `video_path_original IS NULL`). The operation is idempotent (re-restoring is a no-op because the row already points at the original). The repo also validates `video_path_original` is not null and the file exists via `fs.existsSync` (rejects `IPC_NOT_FOUND` if the original file is missing — defensive against backup-failure recovery paths)."
    - "The `procedures.trim` IPC handler (replacing the Plan 01 stub) validates input via `safeParse(proceduresTrimInput, raw)`, calls `requireSession()`, looks up the procedure via `proceduresRepo.get(id)`, rejects `IPC_VALIDATION` if `status === 'partial'` (D-13), calls `applyTrim({ procedureId, inMs, outMs })`, and audits `procedure.trimmed`. Returns the updated `Procedure`."
    - "The `procedures.restore` IPC handler (replacing the Plan 01 stub) validates input via `safeParse(proceduresRestoreInput, raw)`, calls `requireSession()`, looks up the procedure, rejects `IPC_NOT_FOUND` if `video_path_original` is null, calls `proceduresRepo.restoreFromOriginal(id)`, audits `procedure.restored`. Returns the updated `Procedure`."
    - "The `PreviewServer` extends its existing `/preview` MJPEG route with a NEW `/media/<patientId>/<procedureId>/<file>` route (RESEARCH.md §Pattern 4). The route: (1) parses the URL via `url.match(/^\\/media\\/([a-zA-Z0-9-]+)\\/([a-zA-Z0-9-]+)\\/([\\w.-]+)$/)` — the regex constrains the path components to alphanumeric + `-` + `.` (defense in depth against path-class attacks), (2) resolves the absolute path as `path.join(app.getPath('userData'), 'data', 'media', 'patients', patientId, procedureId, file)`, (3) validates `path.relative(userDataRoot, resolvedPath).startsWith('data/media/patients')` to prevent escape attacks (T-05-08), (4) returns `404` on regex mismatch, `fs.existsSync` false, OR path-escape; (5) returns the mp4 with `Accept-Ranges: bytes` header (T-05-22 + RESEARCH.md §Pattern 4). The current bare range support (single 200 response + Accept-Ranges header) per A7 is enough for v1."
    - "The renderer's `Scrubber` component grows a `trimMode` prop + `inMs` + `outMs` + `onTrim` props. When `trimMode === true`, the scrubber renders two draggable handles (left = in, right = out) at `inMs / durationMs * 100%` and `outMs / durationMs * 100%` left positions, with a red-shaded region between them (`bg-red-300/50`). The handles use `setPointerCapture` for the drag (PITFALLS §8). Clamping: when the in-handle is dragged, `inMs = clamp(ms, 0, outMs - 1000)`; when the out-handle is dragged, `outMs = clamp(ms, inMs + 1000, durationMs)` (PITFALLS §6)."
    - "`TrimControls` is a right-rail panel (rendered ABOVE the procedure metadata card, BELOW the Notes accordion) that the doctor toggles via a `<Button variant={trimMode ? 'default' : 'outline'}>Trim</Button>`. When toggled on, the controls show: (1) `In: <HH:MM:SS>` + `Out: <HH:MM:SS>` labels, (2) the `<Button>Apply</Button>` (disabled when `inMs >= outMs` OR `procedure.status === 'partial'`), (3) the `<Button variant='outline'>Restore original</Button>` (disabled when `videoPathOriginal === null`). Apply invokes `useTrim.apply()` which calls `window.api.procedures.trim({ id, inMs, outMs })` and waits for the IPC round-trip; the doctor sees a body-of-text spinner during the cut. Restore invokes `useTrim.restore()` similarly."
    - "`useProcedures` returns the new `videoPathOriginal` field (the `videoPathOriginal` column added in Plan 01's migration 0003). The trim controls' `Restore` button is enabled when `videoPathOriginal !== null`."
    - "Inline status (NOT a modal) per UX-Pitfalls: the trim Apply button shows a `Loader2` spinner icon during the IPC round-trip. On success, the `<video>` reloads with the new trimmed src. On failure, an inline `toast.error('Trim failed: <error message>')` surfaces."
    - "Trim handles drag clamping matches `tests/renderer/lib/trim-clamp.test.ts`: clamp handles drag attempts past the opposite handle (the in-handle cannot pass outMs; the out-handle cannot pass inMs). The `1000ms` minimum gap is a soft floor — the renderer can pass a smaller gap, but the main-side `proceduresTrimInput` zod refine enforces `outMs > inMs` strictly."
  artifacts:
    - "src/main/recorder/ffmpeg-args.ts — adds `buildTrimArgs({ inputPath, inMs, outMs, outputPath: string })` per D-08. Pure function. Throws `EmptyFfmpegArgsError('outMs must be greater than inMs')` when `outMs <= inMs`. Returns the argv array above."
    - "src/main/recorder/trim.ts (new) — `applyTrim({ procedureId, inMs, outMs }): Promise<{ trimmedVideoPath: string }>`. Spawns one-shot ffmpeg via `defaultFfmpegPath()`, awaits exit code 0, fsyncs + closes the output file, returns the trimmed path. The `proceduresRepo.updateVideoPath` call is delegated to the IPC handler (the trim function is the pure ffmpeg-orchestration layer)."
    - "src/main/recorder/preview-server.ts — extends the `onHttpRequest` handler with the `/media/<patientId>/<procedureId>/<file>` route. The route is gated by the same `started` flag as `/preview` (the server is active only during a recording OR while the review screen is open — see lifecycle below). The route response: `Accept-Ranges: bytes`, `Content-Type: video/mp4`, `Content-Length: stat.size`, then `createReadStream(filePath).pipe(res)` (single-200 response per A7)."
    - "src/main/recorder/preview-server.ts — new lifecycle: `startMediaServer()` opens a SEPARATE HTTP server on a different port (the existing `/preview` server keeps its port for the Recording live preview). The media server is long-lived (NOT torn down with the recording) because the review screen accesses it across many sessions. The `<video>` element uses an absolute URL with the media port; the renderer fetches the URL via `window.api.recording.mediaUrl` (a new IPC surface, registered as `IPC.RECORDING_GET_MEDIA_URL` that returns `http://127.0.0.1:<port>`)."
    - "src/main/db/procedures-repo.ts — adds `updateVideoPath(id, newRel, originalRel)` + `restoreFromOriginal(id)`. The COALESCE pattern in the SQL is the canonical first-trim-only guard (D-07). Existing methods unchanged."
    - "src/main/ipc/procedures.ts — replaces the Plan 01 stubs for `procedures.trim` + `procedures.restore` with the real handlers. The `trim` handler invokes `applyTrim` + `proceduresRepo.updateVideoPath` + audits `procedure.trimmed`. The `restore` handler invokes `proceduresRepo.restoreFromOriginal` + audits `procedure.restored`. Both handlers validate `status === 'partial'` (D-13) and reject with `IPC_VALIDATION` + descriptive messages."
    - "src/main/ipc/screenshots.ts — no new methods; the existing `screenshots.list` query is unchanged (the renderer fetches fresh after trim / restore via `useProcedures.refresh()`)."
    - "src/main/paths.ts — adds `videoFilePath(patientId, procedureId, videoRel): string` returning the absolute path for the `video_path` column. Used by both `applyTrim` and the PreviewServer `/media/` route. The helper is the canonical resolver; the IPC handler never re-computes the path inline."
    - "src/main/index.ts — extends the `initRecorder()` flow to call `startMediaServer()` on boot (long-lived). The server is killed on `before-quit` (existing shutdown hook)."
    - "src/preload/index.ts — exposes `api.recording.getMediaUrl(): Promise<string>` (returns `http://127.0.0.1:<port>`) for the renderer's `<video>` element to compose the `/media/<patientId>/<procedureId>/<videoFile>` URL."
    - "src/shared/ipc-contract.ts — adds `IPC.RECORDING_GET_MEDIA_URL` + `api.recording.getMediaUrl()`. The `Procedure` type grows `videoPathOriginal: string | null`."
    - "src/shared/validators.ts — extends `proceduresTrimInput` with `.max(outMs, '...')` (the existing `.refine` covers this) + adds `proceduresRestoreInput` (already added in Plan 01 as a stub)."
    - "src/renderer/src/lib/trim-clamp.ts — `clampHandle(otherMs, attemptedMs, isInHandle: boolean, durationMs: number): { inMs, outMs }` — pure function returning the clamped handle pair. The 1000ms minimum gap is enforced here. The function is exported for unit tests."
    - "src/renderer/src/hooks/useTrim.ts — `useTrim(procedureId, currentDurationMs): { trimMode: boolean; setTrimMode: (on: boolean) => void; inMs: number; outMs: number; setInMs: (n: number) => void; setOutMs: (n: number) => void; apply: () => Promise<void>; restore: () => Promise<void>; applying: boolean; restoring: boolean }`. The hook manages the in/out local state + the API calls. On mount, `inMs = 0` and `outMs = currentDurationMs`. Apply awaits the IPC; restore awaits the IPC; both reset `trimMode` to `false` on success."
    - "src/renderer/src/store/trim.ts — small Zustand store for `trimMode` (the boolean toggle). The hook reads from this store so the toggle state survives navigation. Default: `trimMode = false`."
    - "src/renderer/src/components/Scrubber.tsx — extends with `trimMode?: boolean; inMs?: number; outMs?: number; onTrim?: (range: { inMs: number; outMs: number }) => void`. When `trimMode === true`, render two draggable `<TrimHandle>` children (left = in, right = out) + a red-shaded `<TrimRegion>` between them. The handles use `setPointerCapture` (PITFALLS §8). Clamping: the handles call `clampHandle(otherMs, draggedMs, isInHandle, durationMs)` from `src/renderer/src/lib/trim-clamp.ts` and pass the result to `onTrim`."
    - "src/renderer/src/components/TrimControls.tsx (new) — `<TrimControls procedureId: string; status: ProcedureStatus; videoPathOriginal: string | null; durationMs: number; onTrimmed: () => void />`. The right-rail panel: `<ToggleTrimButton>` + `<ApplyButton>` + `<RestoreButton>` + inline spinner/labels. Apply calls `useTrim.apply()` → IPC → `useProcedures.refresh()` on success. Restore calls `useTrim.restore()` → IPC → refresh on success. The panel is DISABLED when `status === 'partial'` (D-13)."
    - "src/renderer/src/pages/ProcedureReview.tsx — extends the right rail with `<TrimControls>` BELOW the Notes accordion. The `<video>` element's `src` is now wired to the PreviewServer `/media/` URL via `useMediaUrl()` (a new hook that calls `api.recording.getMediaUrl()` once on mount + caches the URL in `useState`; refreshes on `procedureId` change). The `<video>` element reloads (sets `videoRef.current.load()`) when `procedure.videoPath` changes (the post-trim refresh)."
    - "Tests: `tests/main/recorder/ffmpeg-args.test.ts` (extend) — 4 new tests covering `buildTrimArgs` with `outMs <= inMs` rejection, exact expected argv shape, 1000ms gap, and 60-second gap (typical procedure). `tests/main/recorder/trim.test.ts` (new) — 5 tests covering the spawn mock + exit-code-0 path + repository update + audit + partial-status rejection. `tests/main/recorder/preview-server.test.ts` (extend) — 4 new tests for `/media/` route: valid path serves, invalid path returns 404, path-escape returns 403, partial-bypass returns 404. `tests/main/db/procedures-repo.test.ts` (extend) — 4 new tests for `updateVideoPath` first-trim + second-trim semantics + `restoreFromOriginal` idempotency + missing-original rejection. `tests/main/ipc/procedures.test.ts` (extend) — 3 new tests for `trim` rejects partial + `restore` rejects missing `video_path_original` + audit metadata shape. `tests/renderer/lib/trim-clamp.test.ts` (new) — 6 tests covering the canonical clamp matrix (in past out, in past out-1000, out past in, out past in+1000, edge=0, edge=durationMs). `tests/renderer/components/Scrubber.test.tsx` (extend) — 3 new tests for trim handle drag (in handle clamps at outMs-1000, out handle clamps at inMs+1000, drag past track end is ignored). `tests/renderer/components/TrimControls.test.tsx` (new) — 4 tests for Apply disabled state + Restore disabled state + status='partial' trim disabled + Apply calls IPC + onTrimmed."
  key_links:
    - "Renderer Apply click -> `useTrim.apply()` -> `window.api.procedures.trim({ id, inMs, outMs })` -> main `applyTrim` (one-shot ffmpeg spawn) -> exit code 0 -> `proceduresRepo.updateVideoPath(id, trimmedRel, originalRel)` (COALESCE'd `video_path_original`) -> audit `procedure.trimmed` -> return updated Procedure -> renderer `useProcedures.refresh()` -> `<video>` reloads to the new trimmed URL."
    - "Renderer Restore click -> `useTrim.restore()` -> `window.api.procedures.restore({ id })` -> main `proceduresRepo.restoreFromOriginal(id)` -> `video_path = video_path_original` -> audit `procedure.restored` -> return updated Procedure -> renderer reloads `<video>` to original URL."
    - "Trim handles on the scrubber -> `onTrim({ inMs, outMs })` -> `useTrim.setInMs/OutMs` -> `useProcedures` state doesn't change (the trim is local UI only until Apply) -> Apply calls IPC."
    - "`<video>.src` -> `useMediaUrl()` returns `http://127.0.0.1:<mediaPort>` + the `videoPath` is appended as `/media/<patientId>/<procedureId>/<videoFile>` -> PreviewServer's `/media/` route serves the file with `Accept-Ranges: bytes`."
    - "Status partial gate -> `procedures.trim` IPC rejects `status === 'partial'` with `IPC_VALIDATION` + message `'Cannot trim a partial recording'`. The renderer's Trim button is disabled (D-13 + UX consistency)."
    - "`video_path_original` is the FIRST-TRIM-ONLY column (D-07). The COALESCE in `proceduresRepo.updateVideoPath` is the canonical SQL guard. Subsequent trims leave it untouched."
---

<objective>
Ship the trim feature end-to-end: drag handles on the scrubber (D-06), `applyTrim` ffmpeg subprocess (D-07/D-08), `proceduresRepo.updateVideoPath` + `restoreFromOriginal` (D-09), `procedures.trim` + `procedures.restore` IPC handlers (replacing the Plan 01 stubs), the PreviewServer `/media/` route + the long-lived media HTTP server, and the `TrimControls` right-rail panel. The partial-status gate (D-13) is enforced at both the renderer (Apply disabled) and the IPC (rejects partial) layers.

Purpose: Trimming is the last high-leverage capability the review screen needs. After Plan 03, the doctor can trim the procedure to the salient portion, restore the original recording, and the review screen plays the trimmed mp4 via the new `/media/` localhost HTTP route. Plan 04 hardens the integration with the range-request security + the smoke test.

Output: `buildTrimArgs` + `applyTrim` + the IPC handlers + the `/media/` route + the trim UI. The Plan 01 stubs for `procedures.trim` / `procedures.restore` are replaced with the real implementations.
</objective>

<execution_context>
@C:/Users/Hussien Essam/.config/opencode/gsd-core/workflows/execute-plan.md
@C:/Users/Hussien Essam/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/05-screenshots-procedure-review-trim/05-CONTEXT.md (D-06, D-07, D-08, D-09, D-13)
@.planning/phases/05-screenshots-procedure-review-trim/05-RESEARCH.md §Pattern 1 (trim subprocess), §Pattern 4 (PreviewServer /media/ route), §Anti-Pattern 2 (userData-relative paths), §Pitfall 1 (corrupt mp4), §Pitfall 3 (never overwrite original), §Pitfall 5 (partial trim disabled), §Pitfall 6 (negative range)
@.planning/REQUIREMENTS.md §REV-04
@.planning/research/PITFALLS.md §1 (corrupt mp4), §Pitfall 2 (trim `-c copy` requires `-ss` BEFORE `-i`)
@.planning/research/ARCHITECTURE.md §Pattern 3 (ffmpeg supervision), §Anti-Pattern 5 (no ffmpeg pool)
@.opencode/skills/electron-ffmpeg/SKILL.md §1 (two-path), §2 (ffmpeg-static asarUnpack), §4 (start a recording), §6 (preview-server pattern)
@.planning/phases/04-recording-timer-device-lost/04-CONTEXT.md (D-11 procedure_segments + status enum)
@.planning/phases/05-screenshots-procedure-review-trim/05-01-PLAN.md (Scrubber base + IPC stubs)
@.planning/phases/05-screenshots-procedure-review-trim/05-02-PLAN.md (right rail Polish)
@src/main/db/procedures-repo.ts (Plan 01 — extend with trim methods)
@src/main/recorder/ffmpeg-args.ts (Plan 01 — extend with buildTrimArgs)
@src/main/recorder/preview-server.ts (Phase 4 — extend with /media/ route + long-lived media server)
@src/main/recorder/recorder.ts (Phase 4 — uses PreviewServer for MJPEG tee)
@src/main/recorder/init.ts (Phase 4 — current PreviewServer lifecycle)
@src/main/recorder/ffmpeg-path.ts (Phase 4 — `defaultFfmpegPath()`)
@src/main/recorder/concat.ts (Phase 4 — 1-segment concat pattern reference)
@src/main/ipc/procedures.ts (Plan 01 — replace stubs with real handlers)
@src/main/ipc/screenshots.ts (Plan 01 — no change)
@src/main/index.ts (extend with media server init)
@src/main/paths.ts (Plan 01 — add videoFilePath helper)
@src/shared/ipc-contract.ts (Plan 01 — extend with getMediaUrl)
@src/shared/validators.ts (Plan 01 — schemas already declared)
@src/preload/index.ts (Plan 01 — extends api.recording.getMediaUrl)
@src/renderer/src/components/Scrubber.tsx (Plans 01 + 02 — extend with trim handles)
@src/renderer/src/components/TrimControls.tsx (new)
@src/renderer/src/pages/ProcedureReview.tsx (Plans 01 + 02 — extend with TrimControls + mediaUrl)
@src/renderer/src/hooks/useProcedures.ts (Plan 02 — extend with applyTrim / restore methods)
@src/renderer/src/hooks/useMediaUrl.ts (new)
@src/renderer/src/lib/trim-clamp.ts (new — pure function)
@tests/main/recorder/ffmpeg-args.test.ts (Phase 4 — extend)
@tests/main/recorder/preview-server.test.ts (Phase 4 — extend)
@tests/main/recorder/concat.test.ts (Phase 4 — REF for the trim spawn pattern)
@tests/main/db/procedures-repo.test.ts (Plan 01 — extend)
@tests/main/ipc/procedures.test.ts (Plan 01 — extend)
@tests/renderer/components/Scrubber.test.tsx (Plan 01 — extend)
</context>

<tasks>

<task type="tracer">
  <name>Tracer: buildTrimArgs + applyTrim + IPC handlers + proceduresRepo.trim/restore + PreviewServer /media/ route + Scrubber trim handles + TrimControls</name>
  <files>
    src/main/recorder/ffmpeg-args.ts,
    src/main/recorder/trim.ts,
    src/main/recorder/preview-server.ts,
    src/main/recorder/init.ts,
    src/main/db/procedures-repo.ts,
    src/main/ipc/procedures.ts,
    src/main/paths.ts,
    src/main/index.ts,
    src/preload/index.ts,
    src/shared/ipc-contract.ts,
    src/shared/validators.ts,
    src/renderer/src/components/Scrubber.tsx,
    src/renderer/src/components/TrimControls.tsx,
    src/renderer/src/pages/ProcedureReview.tsx,
    src/renderer/src/hooks/useTrim.ts,
    src/renderer/src/hooks/useMediaUrl.ts,
    src/renderer/src/lib/trim-clamp.ts,
    src/renderer/src/store/trim.ts
  </files>
  <read_first>
    - .planning/phases/05-screenshots-procedure-review-trim/05-CONTEXT.md (D-06, D-07, D-08, D-09, D-13)
    - .planning/phases/05-screenshots-procedure-review-trim/05-RESEARCH.md §Pattern 1 (trim), §Pattern 4 (PreviewServer extension), §Pitfall 1, §Pitfall 3, §Pitfall 5, §Pitfall 6
    - .planning/research/PITFALLS.md §1 (corrupt mp4)
    - .planning/REQUIREMENTS.md §REV-04
    - src/main/recorder/ffmpeg-args.ts (Plan 01 — extend with buildTrimArgs)
    - src/main/recorder/recorder.ts (Phase 4 — REF for the spawn + exit pattern)
    - src/main/recorder/preview-server.ts (Phase 4 — extend with /media/ route + long-lived media server)
    - src/main/recorder/init.ts (Phase 4 — current PreviewServer lifecycle)
    - src/main/recorder/ffmpeg-path.ts (Phase 4 — `defaultFfmpegPath()`)
    - src/main/recorder/concat.ts (Phase 4 — REF for the trim spawn pattern)
    - src/main/db/procedures-repo.ts (Plan 01 — extend with updateVideoPath + restoreFromOriginal)
    - src/main/ipc/procedures.ts (Plan 01 — replace the trim/restore stubs)
    - src/main/paths.ts (Plan 01 — add videoFilePath)
    - src/main/index.ts (Plan 01 — wire the media server)
    - src/preload/index.ts (Plan 01 — exposes api.recording.getMediaUrl)
    - src/shared/ipc-contract.ts (Plan 01 — add RECORDING_GET_MEDIA_URL + Procedure.videoPathOriginal)
    - src/shared/validators.ts (Plan 01 — schemas already declared)
    - src/renderer/src/components/Scrubber.tsx (Plan 02 file — extend with trim handles)
    - src/renderer/src/pages/ProcedureReview.tsx (Plan 02 file — extend with TrimControls + mediaUrl)
    - src/renderer/src/hooks/useProcedures.ts (Plan 02 — extend with applyTrim / restore)
  </read_first>
  <!-- NOTE (planner, revision pass): This tracer task touches 19 files — intentionally above the
       15-file soft threshold. The plan slices end-to-end across main + preload + shared + renderer
       (ffmpeg args, trim subprocess, repo, IPC, preview-server, Scrubber, TrimControls, hook, store,
       media URL hook) and the architecture is already documented in RESEARCH.md + CONTEXT.md D-06..D-09.
       Splitting would lose the end-to-end vertical proof the tracer exists to provide. -->
  <action>
    Update `src/main/recorder/ffmpeg-args.ts`:
    - Add `buildTrimArgs({ inputPath, inMs, outMs, outputPath }): string[]`:
      - Validate `outMs > inMs` (throw `EmptyFfmpegArgsError('outMs must be greater than inMs')` if not — PITFALLS §6).
      - Validate `inputPath` and `outputPath` are non-empty.
      - Compute `inSec = inMs / 1000`, `durationSec = (outMs - inMs) / 1000`.
      - Return `['-ss', inSec.toString(), '-i', inputPath, '-t', durationSec.toString(), '-c', 'copy', '-movflags', '+faststart', '-y', outputPath]`. The comment cites D-08 + PITFALLS §2.
    - Keep the existing `buildFfmpegArgs` unchanged.

    Create `src/main/recorder/trim.ts`:
    - `applyTrim({ procedureId, inMs, outMs }): Promise<{ trimmedVideoPath: string }>`:
      1. `const proc = proceduresRepo.get(procedureId)` + throw `IPC_NOT_FOUND` if missing.
      2. D-13 gate: throw `IPC_VALIDATION('Cannot trim a partial recording')` if `proc.status === 'partial'`. Also reject `status === 'recording'` + `'crashed'` (defensive).
      3. Validate `outMs > inMs` (defensive — IPC zod already enforced).
      4. Resolve paths: `const inputAbs = videoFilePath(proc.patientId, procedureId, proc.videoPath)`. Derive output: `const outputAbs = inputAbs.replace(/\.mp4$/, '-trimmed.mp4')`. Compute `outputRel = path.relative(userData, outputAbs)`.
      5. `const args = buildTrimArgs({ inputPath: inputAbs, inMs, outMs, outputPath: outputAbs })`. Comment: PITFALLS §1 — the trimmed file is a sibling, NEVER overwrites the original.
      6. Spawn ffmpeg: `spawn(defaultFfmpegPath(), args, { windowsVerbatimArguments: true, stdio: ['pipe', 'ignore', 'pipe'] })`. Await exit code 0; reject with `IPC_VALIDATION` on non-zero exit.
      7. On exit code 0: `fsyncSync(openSync(outputAbs, 'r'))` (PITFALLS §1).
      8. Return `outputRel` to the IPC handler. The handler updates the DB row + audit.

    Update `src/main/db/procedures-repo.ts`:
    - Add `updateVideoPath(id: string, newRel: string, originalRel: string): Procedure`:
      - SQL: `UPDATE procedures SET video_path = ?, video_path_original = COALESCE(video_path_original, ?) WHERE id = ?`. The `COALESCE(video_path_original, ?)` is the canonical D-07 first-trim-only guard.
      - Cache the prepared statement.
      - Returns the updated `Procedure` row.
    - Add `restoreFromOriginal(id: string): Procedure`:
      - First SELECT the row. Throw `IPC_NOT_FOUND` if missing.
      - Throw `IPC_VALIDATION('No original recording to restore from')` if `videoPathOriginal` is null.
      - Throw `IPC_NOT_FOUND` if `fs.existsSync(videoFilePath(patientId, id, videoPathOriginal))` is false.
      - `UPDATE procedures SET video_path = video_path_original WHERE id = ?`.
      - Return the updated row.
    - `__resetProceduresRepoCache` clears the new cached statements.

    Update `src/main/paths.ts`:
    - Add `videoFilePath(patientId: string, procedureId: string, videoRel: string): string` returning `path.join(procedureMediaDir(patientId, procedureId), videoRel)`. The IPC handler + the PreviewServer both use this helper.

    Update `src/main/recorder/preview-server.ts`:
    - Add a new `startMediaServer()` method that opens a SEPARATE HTTP server on a different port (the existing `/preview` server keeps its port for the Recording live preview). The media server is long-lived (NOT torn down with the recording) because the review screen accesses it across many sessions.
    - The server's `onHttpRequest` handler routes:
      - `/preview` (existing) — MJPEG stream.
      - `/media/<patientId>/<procedureId>/<file>` (NEW) — serves the mp4 with `Accept-Ranges: bytes` + `Content-Type: video/mp4` + `Content-Length: stat.size`.
      - Else: 404.
    - The `/media/` route validates the file path with `path.relative(userData, resolvedPath)` per T-05-08; returns 403 if the resolved path escapes the userData root. The path components are also validated by a strict regex `/^/media/([a-zA-Z0-9-]+)/([a-zA-Z0-9-]+)/([\w.-]+)$/` per SECURITY §1.
    - The route returns a single 200 response (PITFALLS §1 + A7 single-200 strategy). Range request partial content is a v1.1 enhancement.
    - The `/media/` server runs on SEPARATE port from `/preview` (per A2 single-shared-server with route-by-path; revised to two-port due to lifecycle differences — the media server is boot-time, the preview server is recording-time).
    - Add `getMediaUrl(): string` returning `http://127.0.0.1:<mediaPort>` for the renderer's `<video>` to compose the URL.

    Update `src/main/recorder/init.ts`:
    - The `initRecorder()` flow is extended to call `initMediaServer()` on boot. The `scanForOrphans` call (Phase 4) remains unchanged.
    - The media server is killed on `before-quit` (existing shutdown hook).

    Update `src/main/index.ts`:
    - After `initRecorder()` + `registerProceduresIpc()` + `registerScreenshotsIpc()`, call `initMediaServer()` (which `start()`s the long-lived media server). The renderer accesses `window.api.recording.getMediaUrl()` after the server is up.

    Update `src/main/ipc/procedures.ts`:
    - Replace the Plan 01 stubs for `procedures.trim` + `procedures.restore` with the real handlers:
      - `procedures.trim`:
        1. `safeParse(proceduresTrimInput, raw)`.
        2. `requireSession()`.
        3. `proceduresRepo.get(id)` — reject `IPC_NOT_FOUND` if missing.
        4. Validates `status === 'completed'` (D-13 — the trim handler does NOT call `applyTrim` for partial/crashed/recording).
        5. Calls `applyTrim({ procedureId: id, inMs, outMs })` — catch errors and re-throw as `IPC_VALIDATION` with `applyTrim`'s message.
        6. Inside `db.transaction()`: `proceduresRepo.updateVideoPath(id, result.trimmedVideoPath, proc.videoPathOriginal ?? proc.videoPath)` + audit `procedure.trimmed` with `metadata: { inMs, outMs, originalVideoPath: <rel>, trimmedVideoPath: result.trimmedVideoPath }`. The `originalVideoPath` is the userData-relative path (Fix 6).
        7. Return the updated `Procedure` (re-fetched via `proceduresRepo.get(id)`).
      - `procedures.restore`:
        1. `safeParse(proceduresRestoreInput, raw)`.
        2. `requireSession()`.
        3. `proceduresRepo.get(id)` — reject `IPC_NOT_FOUND` if missing.
        4. `proceduresRepo.restoreFromOriginal(id)` — throws `IPC_VALIDATION` if `videoPathOriginal` is null (the IPC handler surfaces this).
        5. Inside `db.transaction()`: audit `procedure.restored` with `metadata: { restoredFrom: <rel> }`.
        6. Return the updated `Procedure`.

    Update `src/shared/ipc-contract.ts`:
    - Add `IPC.RECORDING_GET_MEDIA_URL = 'recording:get-media-url'`.
    - Extend `Procedure` type with `videoPathOriginal: string | null`.
    - Extend `IpcContract.recording` with `getMediaUrl(): Promise<string>`.

    Update `src/preload/index.ts`:
    - Expose `api.recording.getMediaUrl() = () => ipcRenderer.invoke(IPC.RECORDING_GET_MEDIA_URL)`.

    Update `src/main/ipc/recording.ts` (existing file):
    - Add `ipcMain.handle(IPC.RECORDING_GET_MEDIA_URL, () => getMediaUrl())` (returns the media server URL).

    Create `src/renderer/src/lib/trim-clamp.ts`:
    - `clampHandle(otherMs: number, attemptedMs: number, isInHandle: boolean, durationMs: number): { inMs: number; outMs: number }`:
      - If `isInHandle`: `const inMs = Math.max(0, Math.min(attemptedMs, otherMs - 1000))`; return `{ inMs, outMs: otherMs }`.
      - Else: `const outMs = Math.min(durationMs, Math.max(attemptedMs, otherMs + 1000))`; return `{ inMs: otherMs, outMs }`.
    - The 1000ms minimum gap is a soft floor (the renderer can pass smaller via direct `setInMs/OutMs`, but the drag handler clamps).

    Create `src/renderer/src/store/trim.ts`:
    - `useTrimMode()` selector returning `{ trimMode: boolean; setTrimMode: (on: boolean) => void }` (Zustand store, line ~25).
    - Default: `trimMode = false`.

    Create `src/renderer/src/hooks/useTrim.ts`:
    - `useTrim(procedureId, currentDurationMs, onApplied): { trimMode, setTrimMode, inMs, outMs, setInMs, setOutMs, apply, restore, applying, restoring }`:
      - `trimMode` reads from `useTrimMode()`.
      - `inMs`/`outMs` are local `useState` (default 0 / currentDurationMs).
      - `apply()`:
        1. `setApplying(true)`.
        2. `await window.api.procedures.trim({ id: procedureId, inMs, outMs })`.
        3. `setApplying(false)` + `toast.success('Trim applied')` + `setTrimMode(false)` + `onApplied()`.
        4. On error: `setApplying(false)` + `toast.error('Trim failed: <message>')`.
      - `restore()`:
        1. `setRestoring(true)`.
        2. `await window.api.procedures.restore({ id: procedureId })`.
        3. `setRestoring(false)` + `toast.success('Original restored')` + `setTrimMode(false)` + `onApplied()`.
        4. On error: `setRestoring(false)` + `toast.error('Restore failed: <message>')`.

    Create `src/renderer/src/hooks/useMediaUrl.ts`:
    - `useMediaUrl(): { url: string | null; loading: boolean }`:
      - `useEffect` on mount calls `window.api.recording.getMediaUrl()` and stores the result in `useState`.
      - Returns the URL (or `null` while loading).

    Update `src/renderer/src/components/Scrubber.tsx`:
    - Add optional props `trimMode?: boolean; inMs?: number; outMs?: number; onTrim?: (range: { inMs: number; outMs: number }) => void`.
    - When `trimMode === true` AND `inMs` AND `outMs` are provided:
      - Render two `<TrimHandle>` children + a `<TrimRegion>` rectangle:
        - `<TrimHandle side='in' left={inMs / durationMs * 100} onPointerDown={handleInDrag} />` — the handle is `<div className='absolute top-0 bottom-0 w-1 bg-red-600 cursor-ew-resize z-10' style={{ left: \`${left}%\` }} onPointerDown={onPointerDown} onPointerMove={...} onPointerUp={...} />`.
        - Same for `side='out'`.
        - `<TrimRegion>`: `<div className='absolute top-0 bottom-0 bg-red-300/50 pointer-events-none z-0' style={{ left: \`${inPct}%\`, width: \`${outPct - inPct}%\` }} />`.
      - The drag handler uses `setPointerCapture` (PITFALLS §8) and calls `clampHandle(otherMs, attemptedMs, isInHandle, durationMs)` + `onTrim(clamped)`.
    - DOM order: progress fill (z=0), trim region (z=0), pause markers (z=1), trim handles (z=10). The handles are on top so they capture pointer events first.

    Create `src/renderer/src/components/TrimControls.tsx`:
    - `<TrimControls procedureId: string; status: ProcedureStatus; videoPathOriginal: string | null; durationMs: number; onApplied: () => void; trimMode: boolean; setTrimMode: (on: boolean) => void; inMs: number; outMs: number; setInMs: (n: number) => void; setOutMs: (n: number) => void; applying: boolean; restoring: boolean; apply: () => Promise<void>; restore: () => Promise<void>; }>`.
    - Composition: `<div className='flex flex-col gap-2 p-3 border border-slate-200 rounded'>`. The header row: `<Button variant={trimMode ? 'default' : 'outline'} size='sm' onClick={() => setTrimMode(!trimMode)}><Scissors aria-hidden />{trimMode ? 'Done' : 'Trim'}</Button>` (lucide-react Scissors icon).
    - When `trimMode === true`:
      - Labels: `<div className='text-xs text-slate-600'>In: <span className='font-mono'>{formatDurationHHMMSS(inMs)}</span> · Out: <span className='font-mono'>{formatDurationHHMMSS(outMs)}</span></div>`.
      - `<Button size='sm' onClick={apply} disabled={applying || status === 'partial' || inMs >= outMs}>{applying ? <Loader2 className='animate-spin' aria-hidden /> : null}Apply</Button>`.
      - `<Button variant='outline' size='sm' onClick={restore} disabled={restoring || videoPathOriginal === null}>{restoring ? <Loader2 className='animate-spin' aria-hidden /> : null}Restore original</Button>`.
    - The panel is rendered with `data-testid='trim-controls'`.

    Update `src/renderer/src/pages/ProcedureReview.tsx`:
    - Import `useMediaUrl()` + `useTrim()`. Wire the trim hooks + the controls.
    - Right rail body (in order):
      - `<ProcedureNotesReview procedureId={procedureId} status={procedure.status} />`
      - `<TrimControls ... />` (NEW)
      - `<Card>` with procedure metadata (from Plan 02)
      - `<DeviceLostBanner>` (conditional on partial status)
    - `<video>` element's `src`: `useMediaUrl().url + '/media/' + procedure.patientId + '/' + procedureId + '/' + procedure.videoPath.replace(/^.*\//, '')` (the filename only). The `useEffect` on `procedure.videoPath` calls `videoRef.current.load()` to reload the mp4 after trim.
    - The video element's `onTimeUpdate` syncs `currentMs` for the scrubber.

  </action>
  <acceptance_criteria>
    - `buildTrimArgs` returns the expected argv shape; rejects `outMs <= inMs` with `EmptyFfmpegArgsError`.
    - `applyTrim` spawns ffmpeg with the expected args, awaits exit code 0, fsyncs the output, returns the trimmed path. Rejects `status !== 'completed'`.
    - `proceduresRepo.updateVideoPath(id, newRel, originalRel)` updates `video_path` + `video_path_original` (only on first trim via COALESCE).
    - `proceduresRepo.restoreFromOriginal(id)` re-points `video_path` to `video_path_original`; rejects if `video_path_original` is null OR the file is missing.
    - `procedures.trim` IPC handler validates input + status + invokes `applyTrim` + updates DB + audit; returns the updated Procedure.
    - `procedures.restore` IPC handler validates input + invokes `restoreFromOriginal` + audits; returns the updated Procedure.
    - `PreviewServer` exposes the `/media/<patientId>/<procedureId>/<file>` route with `Accept-Ranges: bytes` + path-escape protection (T-05-08) + regex-validated path components.
    - `recording.getMediaUrl()` returns `http://127.0.0.1:<mediaPort>`.
    - `Procedure.videoPathOriginal` is part of the entity type.
    - `useTrim.apply()` + `useTrim.restore()` wire the IPC round-trip + UI state transitions (applying toggle, toast on success/failure, trimMode reset on success).
    - `useMediaUrl()` returns the media server URL.
    - `Scrubber` renders `<TrimHandle>` (in + out) + `<TrimRegion>` when `trimMode === true`; handles use `setPointerCapture` + call `clampHandle`.
    - `TrimControls` toggles `trimMode` + wires Apply/Restore buttons + inline spinner + disabled states (partial, missing original).
    - `ProcedureReview` mounts `<TrimControls>` in the right rail + wires `<video>.src` to the PreviewServer `/media/` URL + reloads the video on `procedure.videoPath` change.
    - `trim-clamp.ts` exports the pure clamp function with the 1000ms minimum gap.
    - All Wave 0 tests pass (`ffmpeg-args`, `trim`, `preview-server`, `procedures-repo`, `ipc/procedures`, `trim-clamp`, `Scrubber`, `TrimControls`).
  </acceptance_criteria>
  <verify>
    <automated>npm run typecheck:node && npm run typecheck:web && npm run test:unit -- --run tests/main/recorder/ffmpeg-args.test.ts tests/main/recorder/trim.test.ts tests/main/recorder/preview-server.test.ts tests/main/db/procedures-repo.test.ts tests/main/ipc/procedures.test.ts tests/renderer/components/TrimControls.test.tsx tests/renderer/lib/trim-clamp.test.ts tests/renderer/components/Scrubber.test.tsx</automated>
  </verify>
  <done>
    Trim works end-to-end: drag handles on the scrubber, click Apply, ffmpeg spawns a one-shot subprocess writing a new trimmed mp4 sibling (never overwrites the original), the procedure row updates with `video_path` + `video_path_original` (first-trim-only), the audit row captures the trim. Restore re-points `video_path` to the original. The PreviewServer `/media/` route serves the mp4 with `Accept-Ranges: bytes` + path-escape protection. The partial-status gate is enforced at both the renderer (Apply disabled) and the IPC (rejects partial). All Wave 0 tests pass.
  </done>
</task>

<task type="auto">
  <name>Wave 0: integration smoke test for trim ffmpeg subprocess (real ffmpeg, lavfi source)</name>
  <files>
    tests/integration/trim-smoke.test.ts,
    tests/main/recorder/trim.test.ts
  </files>
  <read_first>
    - src/main/recorder/trim.ts (just created)
    - src/main/recorder/ffmpeg-args.ts (just extended)
    - src/main/recorder/ffmpeg-path.ts (Phase 4 — `defaultFfmpegPath()`)
    - tests/integration/concat-smoke.test.ts (Phase 4 — REF for the smoke-test pattern)
  </read_first>
  <action>
    Extend `tests/main/recorder/trim.test.ts` (Plan 03 tracer file — Plan 03 also creates the smoke file):
    - The existing 5 unit tests (spawn mock + exit-code-0 + repository update + audit + partial-status rejection) cover the in-process logic. This task adds the EXISTING-test-only smoke (the spawned ffmpeg binary is replaced with a stub in unit tests; the smoke test exercises the real binary).

    Create `tests/integration/trim-smoke.test.ts`:
    - Generate a 30-second lavfi source mp4 via `child_process.spawnSync(defaultFfmpegPath(), ['-f', 'lavfi', '-i', 'testsrc=duration=30:size=1280x720:rate=30', '-c:v', 'libx264', '-preset', 'veryfast', '-y', <tmpMp4>])`.
    - Insert a procedure row + a `procedure_segments` row (the test must keep the segment list trivially consistent with the canonical mp4 path).
    - Call `applyTrim({ procedureId, inMs: 5000, outMs: 25000 })`.
    - Assert the trimmed mp4 exists via `fs.statSync`, the trimmed file duration is approximately `20s` (verified via `ffprobe` or `spawnSync(defaultFfmpegPath(), ['-i', trimmedFile, '-f', 'null', '-'])` stderr).
    - The smoke test is OPT-IN via `process.env.RUN_SMOKE === '1'` — the CI default skips it. The test prints a log line at the start describing what it does + the runtime.
    - The smoke test uses `tempDir` from `node:fs.mkdtempSync` for the source + trimmed files; cleans up via `fs.rmSync(dir, { recursive: true, force: true })` on test teardown.
    - The smoke test verifies the moov atom is at the front (`-movflags +faststart` reading) by checking the first 32 bytes contain `ftyp` (mp4 brand) AT the offset 4 (the canonical mp4 layout with faststart).

  </action>
  <acceptance_criteria>
    - `tests/main/recorder/trim.test.ts` (the unit suite) covers spawn mock + exit-code-0 + repository update + audit + partial-status rejection — 5 tests pass.
    - `tests/integration/trim-smoke.test.ts` (the smoke test) is OPT-IN via `RUN_SMOKE=1`; runs the real ffmpeg binary against a lavfi source + verifies the trimmed mp4 duration + moov atom. When `RUN_SMOKE !== '1'`, the test prints a skip message and exits 0.
    - `npm run test:unit -- --run tests/main/recorder/trim.test.ts` passes.
    - `RUN_SMOKE=1 npm run test:integration -- --run tests/integration/trim-smoke.test.ts` passes on a developer machine with ffmpeg-static installed.
  </acceptance_criteria>
  <verify>
    <automated>npm run typecheck:node && npm run test:unit -- --run tests/main/recorder/trim.test.ts && RUN_SMOKE=1 npm run test:integration -- --run tests/integration/trim-smoke.test.ts</automated>
  </verify>
  <done>
    The trim feature has both unit coverage (spawn mock + repository + audit) and integration coverage (real ffmpeg against a 30-second lavfi source). The smoke test is opt-in so CI runs are fast; developers can verify the real ffmpeg path on their machines.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Renderer -> `procedures.trim` IPC | Untrusted renderer sends `{ id, inMs, outMs }`; main validates via `proceduresTrimInput` zod (D-13 partial-status gate + outMs > inMs). |
| Renderer -> `procedures.restore` IPC | Renderer requests restore; main rejects if `video_path_original` is null (D-09). |
| Renderer -> `<video>.src` URL | The renderer composes the URL as `mediaUrl + '/media/' + patientId + '/' + procedureId + '/' + filename`. The PreviewServer validates the path components with a strict regex + `path.relative()` escape check. |
| Main -> ffmpeg child | The trim subprocess uses `defaultFfmpegPath()` (asarUnpack-aware). The output path is a sibling of the input — never overwrites (PITFALLS §3). |
| Main -> `video_path_original` column | The column is populated on first trim only via `COALESCE` (D-07). The renderer never directly writes the column. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-05-21 | Tampering | `video_path` overwrite via trim | high | mitigate | The `applyTrim` function ALWAYS writes to `<sibling>-trimmed.mp4`. The original is never renamed or deleted. The `proceduresRepo.updateVideoPath` COALESCE pattern preserves `video_path_original` across re-trims. |
| T-05-22 | Tampering | Range request attack on PreviewServer `/media/` route | high | mitigate | The route validates path components via `/^[a-zA-Z0-9-]+$/` + `path.relative(userDataRoot, resolvedPath).startsWith('data/media/patients')`. Path-escape attempts return 403. Range request parsing is bounded by `stat.size` clamping. |
| T-05-23 | Denial of Service | Negative trim range | high | mitigate | `proceduresTrimInput` zod `.refine(d => d.outMs > d.inMs, ...)` rejects at the IPC boundary. The renderer clamp enforces the 1000ms minimum gap during drag. |
| T-05-24 | Information Disclosure | `procedure.trimmed` audit metadata path leak | low | mitigate | The audit metadata stores `originalVideoPath` + `trimmedVideoPath` as userData-relative paths (Fix 6). The audit row already carries the procedureId; no patient identifiers. |
| T-05-25 | Elevation | Restore from another procedure's original | medium | mitigate | `procedures.restore` IPC handler is keyed on `id` (the procedureId from the route). The repo's `restoreFromOriginal(id)` reads the row's `video_path_original` (per-procedure). Cross-procedure restore is impossible. |
| T-05-26 | Denial of Service | ffmpeg trim spawn flood | medium | mitigate | The trim handler is async + per-call; the renderer disables the Apply button during `applying`. The IPC handler does not pool ffmpeg (Anti-Pattern 5); each call spawns a fresh child. |
| T-05-27 | Denial of Service | Range request large file read | low | mitigate | The route uses `createReadStream(filePath).pipe(res)` — Node streams chunks at a time; the renderer can't request arbitrary byte ranges (the route returns the full file with `Accept-Ranges: bytes` header; Chromium re-requests on seek). v1.1: parse the `Range` header for partial content. |
| T-05-28 | Tampering | `/media/` regex bypass | low | mitigate | The regex `^/media/([a-zA-Z0-9-]+)/([a-zA-Z0-9-]+)/([\w.-]+)$` constrains each URL segment. The `path.relative(userDataRoot, resolvedPath)` check catches any escape attempt that survives the regex. |
| T-05-29 | Tampering | `video_path_original` column manipulation via restore | low | mitigate | The `restoreFromOriginal` method does NOT update `video_path_original` — the column is locked once populated. The renderer has no API to write the column directly. |
| T-05-30 | Denial of Service | Long-running trim blocks the IPC thread | low | mitigate | The trim subprocess is spawned via `spawn` (async); the IPC handler awaits the `'exit'` event. The renderer shows a spinner during the wait. The `applyTrim` function has a 5-minute timeout (the trim subprocess is killed after 5 min if it hasn't exited) — defensive against runaway ffmpeg. |
| T-05-31 | Tampering | `procedures.trim` on `status === 'recording'` | medium | mitigate | The IPC handler validates `status === 'completed'` (D-13 also applies to `recording` and `crashed`); the partial-status check is the documented case. The handler explicitly rejects all non-`completed` statuses. |
| T-05-32 | Tampering | `procedures.restore` without `video_path_original` | low | mitigate | The IPC handler rejects `IPC_VALIDATION` if `video_path_original` is null (per D-09). The repo's `restoreFromOriginal` performs the check + the file-existence check before the UPDATE. |
| T-05-SC | Tampering | npm/pip/cargo installs | high | mitigate | slopcheck + blocking human checkpoint for `[ASSUMED]`/`[SUS]` packages. Plan 03 adds NO new dependencies. |

</threat_model>

<verification>
- `npm run typecheck:node && npm run typecheck:web`
- `npm run test:unit -- --run tests/main/recorder/ffmpeg-args.test.ts tests/main/recorder/trim.test.ts tests/main/recorder/preview-server.test.ts tests/main/db/procedures-repo.test.ts tests/main/ipc/procedures.test.ts tests/renderer/components/TrimControls.test.tsx tests/renderer/lib/trim-clamp.test.ts tests/renderer/components/Scrubber.test.tsx`
- `RUN_SMOKE=1 npm run test:integration -- --run tests/integration/trim-smoke.test.ts` (developer-only).
- `package.json` adds NO new dependencies.
- Confirm the realtime test: in the dev app, record a 30-second procedure, navigate to Review, drag the trim handles to in=5s + out=25s, click Apply, observe the spinner + the success toast, then observe the `<video>` reloads to the trimmed clip. The `.partial.mp4` partial status disables the Trim button.
- Verify the canonical pattern: the trimmed file is `<basename>-trimmed.mp4` (a sibling of the original); the original is NEVER deleted. The `video_path_original` is populated on first trim only.
- Confirm the security audit: a request to `/media/../etc/passwd` returns 403; a request to `/media/foo/bar/baz.mp4` outside the userData root returns 403.
</verification>

<success_criteria>
Plan 03 ships the trim feature end-to-end: drag handles on the scrubber, ffmpeg trim subprocess with `-ss before -i -c copy -movflags +faststart`, `video_path_original` populated on first trim only, `procedures.trim` + `procedures.restore` IPC handlers, the PreviewServer `/media/` route with path-escape protection, and the `TrimControls` right-rail panel with partial-status gate. Plan 04 hardens the integration with the range-request security + Windows hardware smoke UAT.
</success_criteria>

<output>
Create `.planning/phases/05-screenshots-procedure-review-trim/05-03-SUMMARY.md` when done.
</output>
