---
phase: 05-screenshots-procedure-review-trim
plan: 06
type: execute
wave: 1
depends_on: []
files_modified:
  - src/main/recorder/recorder.ts
  - src/main/recorder/trim.ts
  - src/main/paths.ts
  - src/main/db/procedures-repo.ts
  - tests/main/recorder/trim.test.ts
autonomous: true
gap_closure: true
gap_ids:
  - G-05-5
requirements:
  - REV-04
must_haves:
  truths:
    - "Clicking Apply on a non-partial completed procedure runs ffmpeg against the canonical mp4 path, writes a trimmed sibling file, updates the procedure row, shows the Trim applied toast, and reloads the video to the trimmed clip."
    - "The procedures.video_path column shape is filename-only (e.g. video.mp4) — not the userData-relative full path that Phase 4 recorder currently stores. The resolver videoFilePath and the recorder relativeVideoPath now agree on the contract."
    - "The proceduresRepo.restoreFromOriginal method reads video_path_original through the same resolver — once the column shape is fixed, restore round-trips cleanly (currently masked because Trim never succeeds)."
    - "When the source video is missing on disk, the IPC_NOT_FOUND error surfaces the procedure status, the resolved absolute path that was stat'd, AND a list of files actually present in the procedure directory so future debugging is fast."
    - "All 484 pre-existing unit tests still pass; a new contract-guard test asserts that relativeVideoPath(patientId, procedureId) returns just the filename (locks the contract so this class of bug cannot regress)."
  artifacts:
    - "src/main/recorder/recorder.ts — `relativeVideoPath(patientId, procedureId)` (line 1255-1258) returns just `'video.mp4'` instead of the full userData-relative path. All call sites (`this.outputRelPath` assignments at lines 237, 248, 893, 1012, 1127, 1158) automatically pick up the new shape because they read the function's return value. A comment cites D-07 + the contract."
    - "src/main/recorder/trim.ts — when `existsSync(inputAbs)` fails (line 76-83), the IpcErrorException message is enriched with the resolved absolute path that was stat'd, the procedure status, AND a list of files in the procedure directory. The toast reflects the full picture for future debugging."
    - "src/main/paths.ts — the `videoFilePath` JSDoc (lines 40-46) is updated to explicitly state the contract: `videoRel` MUST be a filename within the procedure directory (NOT a userData-relative path). The existing implementation is unchanged — the fix is on the writer side."
    - "src/main/db/procedures-repo.ts — `restoreFromOriginal` (lines 283-305) is reviewed; once the column shape is fixed, the existing `videoFilePath(row.patient_id, id, row.video_path_original)` call returns the correct absolute path. No code change required — but a JSDoc note is added citing the contract."
    - "tests/main/recorder/trim.test.ts — adds a new test: `relativeVideoPath(patientId, procedureId) returns just the filename` (locks the contract). The existing bootstrap at lines 56-102 already uses `videoPath: 'video.mp4'` (filename shape) — this is now correct, no change needed there. The fixture at line 218 (`videoPath: 'video.mp4'`) is also already filename-shaped."
  key_links:
    - "Recorder insert + finalize -> `this.outputRelPath` -> proceduresRepo.insert/updateFinalized -> procedures.video_path column now stores `'video.mp4'` (filename-only)."
    - "Renderer Apply click -> `window.api.procedures.trim({ id, inMs, outMs })` -> main IPC -> applyTrim -> `videoFilePath(procedure.patientId, procedureId, procedure.videoPath)` joins `<userData>/data/media/patients/<id>/<id>` + `'video.mp4'` = correct absolute path -> existsSync passes -> ffmpeg spawn -> -trimmed sibling -> updateVideoPath -> video_path now `'video-trimmed.mp4'` -> video_path_original now `'video.mp4'` (first trim only via COALESCE)."
    - "Renderer Restore click -> `window.api.procedures.restore({ id })` -> main IPC -> proceduresRepo.restoreFromOriginal -> videoFilePath(..., row.video_path_original) returns the original absolute path -> existsSync passes -> re-points video_path to video_path_original."
    - "When source is missing -> IpcErrorException carries procedure status + stat path + actual files list -> toast surfaces the full context for fast debugging."
---

<objective>
Fix the path-shape contract drift between Phase 4 recorder and Phase 5 trim resolver. Change `relativeVideoPath` in `recorder.ts:1255-1258` to return just the filename (`video.mp4`) so it matches the resolver `videoFilePath` contract (which expects `videoRel` to be a filename within the procedure directory) and matches the existing `tests/main/recorder/trim.test.ts:75` fixture shape. Improve the trim error path to surface the resolved stat path + procedure status + actual files list when the source is missing. Document the contract on `videoFilePath` and confirm `restoreFromOriginal` works with the corrected shape. Add a contract-guard test so the bug class cannot regress.

Purpose: G-05-5 blocks UAT steps 5-7 (Trim, Verify trimmed, Restore). Once the contract aligns, the existing recorder + trim + restore plumbing works end-to-end with no other changes — the architectural pieces (trim subprocess, COALESCE on video_path_original, /media/ route) were already built correctly.

Output: trim works end-to-end on a non-partial completed procedure; the column shape is consistent end-to-end; the error toast gives a useful diagnostic when the source is missing; a new test guards the contract.
</objective>

<execution_context>
@C:/Users/Hussien Essam/.config/opencode/gsd-core/workflows/execute-plan.md
@C:/Users/Hussien Essam/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/05-screenshots-procedure-review-trim/05-UAT.md §Gaps G-05-5
@.planning/phases/05-screenshots-procedure-review-trim/05-CONTEXT.md (D-07, D-08, D-13, REV-04)
@.planning/debug/trim-source-missing.md (root cause analysis)
@.planning/ROADMAP.md §Phase 5
@.planning/REQUIREMENTS.md §REV-04
@src/main/recorder/recorder.ts (line 1255-1258 relativeVideoPath; line 248 procedures.insert videoPath; line 893 updateFinalized videoPath; line 1012 finalize via SupervisorChild; line 1127 stop-and-finalize)
@src/main/recorder/trim.ts (line 75-83 source-missing error; line 114-115 trimmed-rel return)
@src/main/paths.ts (lines 40-53 videoFilePath JSDoc + implementation)
@src/main/db/procedures-repo.ts (lines 283-305 restoreFromOriginal)
@tests/main/recorder/trim.test.ts (lines 56-102 bootstrap; line 75 fixture videoPath='video.mp4'; line 179 videoPathOriginal='video.mp4'; line 218 fixture)
@.opencode/skills/electron-ffmpeg/SKILL.md §1 (two-path architecture) + §9 (output contract)
</context>

<tasks>

<task type="auto">
  <name>Recorder path-shape contract fix + writer audit + trim error-path enrichment + contract docs</name>
  <files>
    src/main/recorder/recorder.ts,
    src/main/recorder/trim.ts,
    src/main/paths.ts,
    src/main/db/procedures-repo.ts
  </files>
  <read_first>
    - src/main/recorder/recorder.ts (line 1255-1258 relativeVideoPath; line 248 procedures.insert videoPath; line 893 updateFinalized; line 1012; line 1127)
    - src/main/recorder/trim.ts (line 75-83 source-missing error; line 114-115 trimmed-rel return)
    - src/main/paths.ts (lines 40-53 videoFilePath JSDoc + implementation)
    - src/main/db/procedures-repo.ts (lines 283-305 restoreFromOriginal)
  </read_first>
  <action>
    Update src/main/recorder/recorder.ts:
    - `relativeVideoPath(patientId, procedureId)` (line 1255-1258) returns just `'video.mp4'` instead of the full userData-relative path. The function signature stays the same — both args remain for API symmetry and future use — but the body is `return 'video.mp4';`. The comment cites D-07 + the contract: `video_path` is a filename within the procedure directory (the userData-relative form is NOT stored in the column; the resolver joins it at read time).
    - All call sites that read `this.outputRelPath` automatically pick up the new shape (lines 248, 893, 1012, 1127). Verify each one stores the value into `procedures.video_path` via the proceduresRepo. No other writer of `video_path` exists in the recorder.
    Update src/main/recorder/trim.ts:
    - When `existsSync(inputAbs)` fails (line 76-83), the IpcErrorException message is enriched. The new message includes: (a) the procedure id, (b) the procedure status, (c) the resolved absolute path that was stat'd (the doubled path becomes obvious in the toast — future debugging sees the symptom immediately), (d) the raw stored value (so the doctor sees what the column holds), AND (e) a list of files actually present in the procedure directory. Use `readdirSync` to enumerate; if that throws, fall back to an empty list. The message format: `Source video missing for procedure <id> (status=<status>): resolved <inputAbs> not found. Column holds <videoPath>. Files in procedure dir: [<file1>, <file2>, ...].`
    - The trimmed-rel return (line 114-115) stays as-is — `path.relative(path.dirname(inputAbs), outputAbs)` already returns just the filename (`video-trimmed.mp4`) because both paths share the same parent dir.
    Update src/main/paths.ts:
    - The `videoFilePath` JSDoc (lines 40-46) is updated to explicitly state the contract: `videoRel` MUST be a filename within the procedure directory (NOT a userData-relative path). The existing implementation is unchanged — the fix is on the writer side. The new JSDoc cites D-07 + G-05-5 + the recorder's relativeVideoPath function.
    Update src/main/db/procedures-repo.ts:
    - `restoreFromOriginal` (lines 283-305) is reviewed; once the column shape is fixed, the existing `videoFilePath(row.patient_id, id, row.video_path_original)` call returns the correct absolute path. No code change required to the resolver call. A short JSDoc note is added above the method: `Restore re-points video_path to video_path_original. Both columns are filename-only within the procedure directory per G-05-5; the resolver joins the userData root at read time.`
  </action>
  <acceptance_criteria>
    - src/main/recorder/recorder.ts `relativeVideoPath(patientId, procedureId)` returns just `'video.mp4'`.
    - All recorder INSERT/UPDATE sites (lines 248, 893, 1012, 1127) write the new filename-only shape via `this.outputRelPath`.
    - src/main/recorder/trim.ts source-missing error message includes the procedure id, status, resolved stat path, raw stored column value, AND a list of files in the procedure dir.
    - src/main/paths.ts `videoFilePath` JSDoc explicitly documents the filename-only contract.
    - src/main/db/procedures-repo.ts `restoreFromOriginal` JSDoc cites the contract.
    - No new dependencies added.
  </acceptance_criteria>
  <verify>
    <automated>npm run typecheck:node && npm run test:unit -- --run tests/main/recorder/trim.test.ts tests/main/db/procedures-repo.test.ts</automated>
  </verify>
  <done>
    The recorder contract is fixed: `procedures.video_path` now stores the filename-only shape. All call sites are auto-fixed because they read `this.outputRelPath`. The trim error path is enriched with the resolved stat path + procedure status + actual files list for future debugging. The contract is documented on `videoFilePath` and `restoreFromOriginal`. Once the new contract-guard test (Task 2) passes, the trim + restore round-trip works end-to-end without any other code change.
  </done>
</task>

<task type="auto">
  <name>Contract-guard test + end-to-end trim/restore round-trip</name>
  <files>
    tests/main/recorder/trim.test.ts
  </files>
  <read_first>
    - src/main/recorder/recorder.ts (line 1255-1258 relativeVideoPath — the function under test)
    - tests/main/recorder/trim.test.ts (lines 56-102 bootstrap; the existing trim/restore test cases at lines 121-316)
    - .planning/debug/trim-source-missing.md (the contract-drift diagnosis)
  </read_first>
  <action>
    Add a new describe block to tests/main/recorder/trim.test.ts:
    - `describe('relativeVideoPath contract (G-05-5 guard)')`:
      - 1. `relativeVideoPath(patientId, procedureId) returns just the filename 'video.mp4'` — the contract-guard test. Imports the function via the public surface (the function is module-private but exported indirectly via the recorder's `outputRelPath` setter; the simplest assertion is to bootstrap a procedure via the recorder's path-shaped write path AND verify the row's `video_path` is `'video.mp4'`, not the full userData-relative path). This locks the contract.
      - 2. `end-to-end: trim round-trip on a completed procedure with the production column shape` — calls `bootstrap({ status: 'completed' })`, then `applyTrim({ procedureId, inMs: 0, outMs: 5000 })`, then asserts (a) the trimmed file exists at `<procedureDir>/video-trimmed.mp4`, (b) `proceduresRepo.get(procedureId).videoPath === 'video-trimmed.mp4'`, (c) `videoPathOriginal === 'video.mp4'`, (d) `proceduresRepo.restoreFromOriginal(procedureId)` re-points `video_path` back to `'video.mp4'`. This exercises the full contract end-to-end.
    The existing bootstrap already uses `videoPath: 'video.mp4'` (filename shape — matches the new contract); no fixture change needed. The existing 5 trim tests continue to pass because their fixtures were already on the correct shape (the gap was that PRODUCTION was on the wrong shape, not the tests).
  </action>
  <acceptance_criteria>
    - 2 new tests added: (a) contract-guard on `relativeVideoPath` filename-only shape, (b) end-to-end trim + restore round-trip on the production column shape.
    - The new contract-guard test would have caught G-05-5 (a regression test that asserts the recorder writes `'video.mp4'`, not the full userData-relative path).
    - The end-to-end trim + restore test exercises the full applyTrim → proceduresRepo.updateVideoPath → proceduresRepo.restoreFromOriginal cycle.
    - All 484 pre-existing unit tests still pass.
    - No new dependencies added.
  </acceptance_criteria>
  <verify>
    <automated>npm run typecheck:node && npm run test:unit -- --run tests/main/recorder/trim.test.ts tests/main/db/procedures-repo.test.ts</automated>
  </verify>
  <done>
    G-05-5 has a contract-guard test that prevents the recorder from re-introducing the full-path shape, AND an end-to-end test that exercises trim + restore round-trip on the production column shape. Together with Task 1's recorder fix, the trim + restore IPC handlers work end-to-end against the canonical mp4 path. Clicking Apply in the trim panel writes a trimmed sibling file, updates the procedure row, and the video reloads. Clicking Restore re-points the row to the original. The error toast for the missing-source case gives the doctor the full diagnostic context.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Renderer -> `procedures.trim` IPC | Untrusted renderer sends `{ id, inMs, outMs }`; main validates via `proceduresTrimInput` zod (D-13 partial-status gate + outMs > inMs). |
| Renderer -> `procedures.restore` IPC | Renderer requests restore; main rejects if `video_path_original` is null (D-09). |
| Main -> ffmpeg child | The trim subprocess uses `defaultFfmpegPath()` (asarUnpack-aware). The output path is a sibling of the input — never overwrites (PITFALLS §3). |
| Main -> `video_path` column shape | Per D-07 + G-05-5: filename-only within the procedure directory. The resolver `videoFilePath` joins the userData root at read time. Both recorder writers and the resolver agree on this contract (locked by the new test). |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-48 | Tampering | Recorder writes the full userData-relative path into `video_path` again | high | mitigate | The new contract-guard test (Task 2) imports the recorder's path-shaped writer and asserts the stored value is just the filename. Any future regression to the full-path shape fails at the unit-test layer. |
| T-05-49 | Tampering | Resolver silently strips a leading `data/media/patients/...` prefix (Option A from the debug doc) | medium | accept | We picked Option B (change the writer) per the debug doc's recommendation — Option A would have introduced a defensive resolver that masks a contract drift. Option B keeps the contract surface explicit. |
| T-05-50 | Information Disclosure | Trim error toast exposes the resolved stat path | low | accept | The path is on a loopback workstation; the doctor is the only one with access. The enriched message dramatically improves future debuggability without leaking patient data. |
| T-05-51 | Elevation | A procedure with `status='recording'` is trimmed | medium | mitigate | Already enforced by `applyTrim` status gate (line 61-68 of trim.ts) — only `completed` is allowed. Plan 06 does not change this gate. |
| T-05-52 | Denial of Service | Trim subprocess timeout | low | mitigate | Already enforced by the 5-minute SIGTERM timeout (TRIM_TIMEOUT_MS in trim.ts:39) — Plan 04 hardening. Plan 06 does not change this. |
| T-05-53 | Tampering | `video_path_original` column manipulation via restore | low | mitigate | The `restoreFromOriginal` method does NOT update `video_path_original` — the column is locked once populated (D-07). The renderer has no API to write the column directly. Plan 06 does not change this. |
| T-05-SC | Tampering | npm/pip/cargo installs | high | mitigate | slopcheck + blocking human checkpoint for [ASSUMED]/[SUS] packages. Plan 06 adds NO new dependencies. |

</threat_model>

<verification>
- npm run typecheck:node && npm run typecheck:web
- npm run test:unit -- --run tests/main/recorder/trim.test.ts tests/main/db/procedures-repo.test.ts tests/main/ipc/procedures.test.ts
- The full unit suite passes: npm run test:unit reports 484 + new assertions, 0 regressions.
- Manual UAT steps 5-7 (Trim 5s-25s, Verify the trimmed mp4 plays, Restore and verify the original) now succeed — confirm by re-running the UAT after 05-06-SUMMARY.md exists; the gap status: resolved surfaces via the gap_ids frontmatter that /gsd-verify-work reconciles.
- Clicking Apply in the trim panel writes a `<basename>-trimmed.mp4` sibling; the `<video>` reloads to the trimmed clip.
- Clicking Restore re-points `<video>` to the original recording.
- The enriched error toast appears ONLY when the source is missing; normal trim + restore round-trips do not log the diagnostic message.
</verification>

<success_criteria>
G-05-5 is resolved: clicking Apply on a non-partial completed procedure runs ffmpeg against the canonical mp4 path, writes a trimmed sibling file, updates the procedure row, and the video reloads. The procedures.video_path column shape is filename-only end-to-end (recorder writers + resolver + restoreFromOriginal all agree). The error toast on missing source includes the resolved stat path + procedure status + actual files list for fast debugging. A new contract-guard test locks the shape so the bug class cannot regress. All 484 pre-existing tests + the new assertions pass. After this plan, UAT steps 5-7 (Trim, Verify trimmed, Restore) become re-runnable end-to-end.
</success_criteria>

<output>
Create .planning/phases/05-screenshots-procedure-review-trim/05-06-SUMMARY.md when done.
</output>