---
phase: 07-search-history-audit-ui-backup-restore-arabic-rtl
plan: 03
type: execute
wave: 3
depends_on:
  - 07-01
files_modified:
  - src/renderer/src/pages/Audit.tsx
  - src/renderer/src/hooks/useAudit.ts
  - src/renderer/src/pages/ProfileEditor.tsx
  - src/renderer/src/App.tsx
  - src/main/ipc/profile.ts
  - tests/renderer/pages/Audit.test.tsx
  - tests/renderer/hooks/useAudit.test.ts
  - tests/renderer/pages/profile-editor.test.tsx
autonomous: true
requirements:
  - AUDIT-01
  - AUDIT-02
  - I18N-01
user_setup: []

must_haves:
  truths:
    - statement: "Audit page renders a compact one-line-per-row summary (HH:MM:SS · user display name · action · entityType entityId) at 36px row height, paginated at 100/page (per D-05 + D-06 + UI-SPEC §Spacing)."
      verification: "tests/renderer/pages/Audit.test.tsx renders Audit with 3 mock rows → asserts each row renders at min-h-[36px] + contains the formatted timestamp + user + action + entity summary"
    - statement: "Audit page mounts and emits ONE `audit_view` audit row (debounced 1s, empty deps array) — not on every render (per D-08 + Pitfall 8)."
      verification: "tests/renderer/pages/Audit.test.tsx mounts Audit → waits 1100ms → asserts audit.log called exactly once + called with {action: 'audit_view', entityType: 'audit'}; subsequent state change does not re-fire"
    - statement: "Audit row click opens a Dialog with userId, action, entityType, entityId, metadata (JSON pretty-printed), outcome, createdAt (per D-06)."
      verification: "tests/renderer/pages/Audit.test.tsx clicks audit row → asserts dialog opens with data-testid='audit-detail-dialog' + pre block contains pretty-printed JSON metadata"
    - statement: "No write or delete IPC exists for audit_log — append-only invariant preserved (per AUDIT-02 + Phase 2 triggers)."
      verification: "tests/main/audit/append-only.test.ts (existing) still passes; no new IPC handler in src/main/ipc/audit.ts accepts UPDATE/DELETE intent"
    - statement: "ProfileEditor language picker (EN/AR radio) persists to doctor_profile.language via profile.update IPC; emits `language.changed` audit row with {from, to, scope: 'profile'} metadata (per D-17 + audit hooks section)."
      verification: "tests/renderer/pages/profile-editor.test.tsx selects AR radio → blur → asserts window.api.profile.update called with {language: 'ar', ...} → asserts audit.log called with {action: 'language.changed', metadata: {from: 'en', to: 'ar', scope: 'profile'}}"
    - statement: "useAudit({filters, page}) is an SWR-style hook returning {rows, total, loading, error, refresh} matching the useProcedures/useDoctorProfile pattern (per D-07)."
      verification: "tests/renderer/hooks/useAudit.test.tsx renders useAudit with filters → asserts window.api.audit.list called with merged params + refresh re-fetches"
  artifacts:
    - src/renderer/src/pages/Audit.tsx (new)
    - src/renderer/src/hooks/useAudit.ts (new)
    - src/renderer/src/pages/ProfileEditor.tsx (extended with language picker Card)
  prohibitions:
    - "DO NOT add any UI affordance to edit or delete audit rows — AUDIT-02 verbatim read-only"
    - "DO NOT add an export button to the audit page — Phase 7 ships copy-JSON only (deferred to v1.1)"
    - "DO NOT fire audit_view on every render — D-08 verbatim 1s debounce + empty deps array"
    - "DO NOT auto-translate the audit row copy — Phase 7 ships English copy first; i18n bundle is Plan 07-04's concern"
    - "DO NOT include the patient name in audit row entity column — UI-SPEC §PII verbatim 'Patient column shows entity_id (UUID) NOT the full name'"
    - "DO NOT change the existing doctor profile fields — only ADD the language picker Card below them"

artifacts_this_phase_produces:
  decorators: []
  classes: []
  functions:
    - useAudit({filters, page}): UseAuditResult
    - Audit page component
    - ProfileEditor language picker section
  ipc_channels: []
  cli_flags: []
  new_file_paths:
    - src/renderer/src/pages/Audit.tsx
    - src/renderer/src/hooks/useAudit.ts
    - tests/renderer/pages/Audit.test.tsx
    - tests/renderer/hooks/useAudit.test.ts
---

<objective>
Audit log UI page + ProfileEditor language picker per D-05..D-08 + D-17: read-only viewer over the existing `audit:list` IPC with date-range/user/action/entity-type filters + 100/page pagination + compact one-line rows + click-to-expand JSON detail Dialog + `audit_view` self-audit emit on mount (debounced 1s); ProfileEditor gains a "Language" Card below the existing Clinic+Doctor details with EN/AR radio that persists to `doctor_profile.language` via `profile.update` + emits a `language.changed` audit row.

Purpose: AUDIT-01/02 surface (read-only compliance viewer) + I18N-01 partial (per-doctor language preference UI shipped — Wizard step 4 + actual i18next bundle flip arrive in Plan 07-04). Audit page is the highest-value ship-gate artifact for the GCC clinic market — it proves the clinic can answer "who did what" without internet.
Output: Audit page + useAudit hook + ProfileEditor language picker Card + tests proving row layout + filter combo + audit_view emit + language persistence.
</objective>

<execution_context>
@C:/Users/Hussien Essam/.config/opencode/gsd-core/workflows/execute-plan.md
@C:/Users/Hussien Essam/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-search-history-audit-ui-backup-restore-arabic-rtl/07-CONTEXT.md (D-05, D-06, D-07, D-08, D-17 — locked)
@.planning/phases/07-search-history-audit-ui-backup-restore-arabic-rtl/07-UI-SPEC.md (E3 + E4 audit backstops, E8 language picker backstops)
@.planning/phases/07-search-history-audit-ui-backup-restore-arabic-rtl/07-RESEARCH.md (Pattern 2 audit-on-every-read self-audit)
@.planning/phases/07-search-history-audit-ui-backup-restore-arabic-rtl/07-VALIDATION.md (07-03-01..07-03-03 task map)

# Source-of-truth files
@src/renderer/src/hooks/useProcedures.ts (SWR pattern to copy for useAudit)
@src/renderer/src/hooks/useDoctorProfile.ts (SWR pattern)
@src/renderer/src/pages/ProfileEditor.tsx (existing — extend with language Card)
@src/renderer/src/components/SettingsSidebar.tsx (already has audit entry from Plan 07-02)
@src/shared/ipc-contract.ts (audit.list query type)
@src/main/ipc/audit.ts (audit.log + audit.list handlers — from Plan 07-01)
@src/main/db/audit.ts (audit() helper)
@src/renderer/src/lib/router.ts (Route union has 'audit' from Plan 07-02)
@src/renderer/src/App.tsx (route case to extend)
@.planning/research/PITFALLS.md (Pitfall 8 — audit page fires audit_view on every re-render → debounce + empty deps)
</context>

<tasks>

<task type="tracer">
  <name>Audit page + useAudit hook + audit_view self-audit emit (AUDIT-01/02 production-quality end-to-end)</name>
  <read_first>
    - src/renderer/src/hooks/useProcedures.ts (SWR-style pattern to mirror)
    - src/renderer/src/hooks/useDoctorProfile.ts (SWR-style pattern to mirror)
    - src/shared/ipc-contract.ts (audit.list query type + AuditEntry shape)
    - src/main/ipc/audit.ts (audit.log IPC handler from Plan 07-01 + existing audit.list)
    - src/main/db/audit.ts (audit() helper + AuditListRow + list query)
    - src/renderer/src/lib/router.ts (Route union — 'audit' case from Plan 07-02)
    - src/renderer/src/components/SettingsSidebar.tsx (audit button entry from Plan 07-02)
    - src/renderer/src/lib/format.ts (existing formatters — copy pattern for HH:MM:SS)
    - src/renderer/src/components/ConfirmDialog.tsx (Dialog pattern to mirror)
    - src/renderer/src/components/ui/dialog.tsx (existing shadcn Dialog)
    - .planning/phases/07-search-history-audit-ui-backup-restore-arabic-rtl/07-CONTEXT.md (D-05..D-08 verbatim)
    - .planning/phases/07-search-history-audit-ui-backup-restore-arabic-rtl/07-UI-SPEC.md (E3 + E4 backstops)
    - .planning/phases/07-search-history-audit-ui-backup-restore-arabic-rtl/07-UI-SPEC.md (Implementation Bindings Audit page layout)
    - .planning/research/PITFALLS.md (Pitfall 8 — audit_view spam)
  </read_first>
  <files>
    - src/renderer/src/pages/Audit.tsx
    - src/renderer/src/hooks/useAudit.ts
    - src/renderer/src/App.tsx
    - tests/renderer/pages/Audit.test.tsx
    - tests/renderer/hooks/useAudit.test.ts
  </files>
  <action>
    Create `src/renderer/src/hooks/useAudit.ts`: SWR-style hook mirroring `useProcedures` shape.
    ```ts
    export type AuditFilters = {
      from?: number; // ms
      to?: number;
      userId?: string;
      action?: string;
      entityType?: string;
    };
    export type UseAuditResult = {
      rows: AuditEntry[];
      total: number;
      loading: boolean;
      error: string | null;
      refresh: () => Promise<void>;
    };
    export function useAudit({ filters, page, pageSize }: { filters: AuditFilters; page: number; pageSize: number }): UseAuditResult { /* parallel fetch via Promise.all + setState + refreshInFlightRef guard, identical to useProcedures */ }
    ```
    Default `pageSize = 100` per D-05. Internally calls `window.api.audit.list({ ...filters, page, pageSize })` — the IPC contract from Plan 07-01.

    Create `src/renderer/src/pages/Audit.tsx`: full audit log viewer per 07-UI-SPEC.md §Implementation Bindings `### Audit page layout`.
    - Header: page kicker "Workspace" + `<h1 className="text-2xl font-semibold">Audit log</h1>` + "Back" `<Button variant="outline">` that navigates to `{name: 'settings-hub'}`.
    - Filter bar `<Card>`: horizontal flex row with `<input type="date">` × 2 (Date from + Date to) + Doctor `<Select>` populated from `window.api.auth.usersList()` (placeholder "Any user" matching UI-SPEC Copywriting) + Action `<Input>` (free text) + Entity Type `<Select>` (options: `user`, `patient`, `procedure`, `report`, `profile`, `screenshot`, `device`, `audit`, `backup`, `restore`, `language` per UI-SPEC) + Apply + Clear all buttons.
    - Results `<Card>`: `<ScrollArea>` with `<table>` rows. Each row: `min-h-[36px]` (UI-SPEC spacing exception), 4 columns (Time · User · Action · Entity), each cell is a `<span className="truncate">` with `data-testid="audit-row"`. Time is locale-aware via `new Intl.DateTimeFormat(lang === 'ar' ? 'ar-SA' : 'en-US', { hour: '2-digit', minute: '2-digit', second: '2-digit', hour12: false })` with `tabular-nums` class. Entity column shows `entity_type entity_id` (NOT patient name per UI-SPEC §PII).
    - Row click: opens the existing shadcn `<Dialog>` (max-w-2xl per UI-SPEC spacing) titled "Audit event details" with the full row data + a `<pre className="max-h-96 overflow-auto font-mono text-sm">` containing pretty-printed JSON metadata + a "Copy JSON" `<Button>` that uses `navigator.clipboard.writeText` + toast.success "Copied to clipboard".
    - Footer: "Page {page} of {totalPages}" + Previous/Next buttons.
    - Empty state: "No events match these filters." (matches UI-SPEC).
    - Loading state: existing Phase 2 spinner pattern (inline `<span className="size-4 animate-spin rounded-full border-2 border-slate-300 border-t-slate-600" />` next to "Loading…").
    - Error state: inline `<Alert variant="destructive">` above the table.

    D-08 verbatim: in a `useEffect(() => { ... }, [])` (empty deps array) — set a 1000ms `setTimeout` that calls `window.api.audit.log({ action: 'audit_view', entityType: 'audit' })`. Return cleanup `clearTimeout`. Pitfall 8 mitigation: empty deps array prevents re-fire on every state change. Add an inline comment: `// ponytail: empty deps — D-08 verbatim 1s debounce + one fire per page mount; re-fire on filter change would create audit spam per PITFALLS §Pitfall 8`.

    Extend `src/renderer/src/App.tsx`: add `case 'audit': return <Audit />;` to the switch statement (after the `settings-hub` case). Add `import Audit from './pages/Audit';` at the top.

    Tests:
    - `tests/renderer/hooks/useAudit.test.ts` (new): render a test component using `useAudit({filters: {action: 'login'}, page: 1, pageSize: 100})` with a mocked `window.api.audit.list` → assert hook returns `rows` matching the mock + `total` from mock + `loading: false` after promise settles. Assert refresh re-fetches. Assert error path: mock rejects → hook returns `error: <message>`.
    - `tests/renderer/pages/Audit.test.tsx` (new): mock `window.api.audit.list` + `window.api.audit.log` + `window.api.auth.usersList`. Three test cases:
      1. Row layout: render Audit with 3 mock rows → assert each row mounts with `data-testid="audit-row"` + min-h-[36px] className + contains the formatted timestamp + user + action + entity.
      2. audit_view emit: mount Audit → advance timer 1100ms → assert `window.api.audit.log` called exactly once with `{action: 'audit_view', entityType: 'audit'}`. Trigger a state change (e.g., change filter) → assert `audit.log` STILL called once (no re-fire).
      3. Detail Dialog: click an audit row → assert Dialog opens with `data-testid="audit-detail-dialog"` + pre block contains pretty-printed JSON metadata. Click "Copy JSON" → assert `navigator.clipboard.writeText` called with the JSON string + toast.success fires.
  </action>
  <acceptance_criteria>
    - `npm run typecheck` passes (web)
    - `npm run test:unit -- tests/renderer/pages/Audit.test.tsx` passes — row layout + audit_view emit + detail dialog green
    - `npm run test:unit -- tests/renderer/hooks/useAudit.test.ts` passes — SWR pattern green
    - `grep -c 'audit_view' src/renderer/src/pages/Audit.tsx` returns >= 1
    - `grep -c 'setTimeout' src/renderer/src/pages/Audit.tsx` returns >= 1 (the 1s debounce)
    - `grep -c 'min-h-\\[36px\\]' src/renderer/src/pages/Audit.tsx` returns >= 1 (row height per UI-SPEC)
    - `grep -c 'case .audit.' src/renderer/src/App.tsx` returns >= 1
    - All existing tests still pass (no regressions)
  </acceptance_criteria>
  <done>
    Audit page mounted at /audit with filter bar + 100/page table + detail Dialog + audit_view self-audit on mount (1s debounce + empty deps); useAudit SWR hook covers the data layer.
  </done>
</task>

<task type="auto">
  <name>ProfileEditor language picker Card + language.changed audit emit</name>
  <read_first>
    - src/renderer/src/pages/ProfileEditor.tsx (existing — extend with new Card)
    - src/shared/ipc-contract.ts (profile.update input type — extends with language from Plan 07-01)
    - src/main/ipc/profile.ts (profile.update handler — already accepts language from Plan 07-01)
    - src/main/ipc/audit.ts (audit.log IPC handler from Plan 07-01)
    - src/renderer/src/lib/format.ts (existing HH:MM:SS formatter)
    - src/renderer/src/components/ui/card.tsx (existing shadcn Card)
    - src/renderer/src/components/ui/label.tsx (existing shadcn Label)
    - .planning/phases/07-search-history-audit-ui-backup-restore-arabic-rtl/07-CONTEXT.md (D-17 + audit hooks section)
    - .planning/phases/07-search-history-audit-ui-backup-restore-arabic-rtl/07-UI-SPEC.md (E8 language picker backstops + Implementation Bindings `### Profile Editor language picker`)
  </read_first>
  <files>
    - src/renderer/src/pages/ProfileEditor.tsx
    - tests/renderer/pages/profile-editor.test.tsx
  </files>
  <action>
    Extend `src/renderer/src/pages/ProfileEditor.tsx`: add a new `<Card>` AFTER the existing Assets Card (so order: Clinic+Doctor details → Assets → Language):
    - `<CardHeader>` with `<CardTitle>Language</CardTitle>` + `<CardDescription>` "Choose the language for the clinic chrome. Report PDFs use the language chosen per-procedure."
    - `<CardContent>` with two `<label>` radio inputs (shadcn-style, NOT native — per UI-SPEC Implementation Bindings `### Profile Editor language picker`):
      - EN option: `<input type="radio" id="lang-en" name="lang" value="en">` + `<Label htmlFor="lang-en">English</Label>`
      - AR option: `<input type="radio" id="lang-ar" name="lang" value="ar" dir="rtl">` + `<Label htmlFor="lang-ar">العربية</Label>`
    - Local state `const [selectedLang, setSelectedLang] = useState<'en' | 'ar'>(profile?.language ?? 'en')` — derive initial value from `profile.language` (nullable, default 'en').

    Handler `handleLanguageChange(newLang: 'en' | 'ar')`: update local state + call `window.api.profile.update({ ...allCurrentFields, language: newLang })` via the existing `persist` callback shape + call `window.api.audit.log({ action: 'language.changed', entityType: 'language', metadata: { from: selectedLang, to: newLang, scope: 'profile' } })`. Both in `Promise.all` so they fire concurrently; show toast.success on completion.

    The persisted language field is read on next mount via the existing `useDoctorProfile.refresh()` flow — no new IPC needed. The Card mounts BELOW the existing Assets Card so the existing tests for those Cards remain green.

    Tests:
    - `tests/renderer/pages/profile-editor.test.tsx` (extend existing): assert the new Language Card mounts with `data-testid="profile-editor-language-card"` + both radio inputs + initial value matches `profile.language` or 'en' fallback. Select AR radio → assert `window.api.profile.update` called with `{ ..., language: 'ar' }` AND `window.api.audit.log` called with `{action: 'language.changed', metadata: {from: 'en', to: 'ar', scope: 'profile'}}`.
  </action>
  <acceptance_criteria>
    - `npm run typecheck` passes (web)
    - `npm run test:unit -- tests/renderer/pages/profile-editor.test.tsx` passes — language card mounts + AR selection persists + audit emit
    - `grep -c 'profile-editor-language-card' src/renderer/src/pages/ProfileEditor.tsx` returns >= 1
    - `grep -c 'language.changed' src/renderer/src/pages/ProfileEditor.tsx` returns >= 1
    - All existing tests still pass (no regressions)
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Renderer → main IPC | audit.log + audit.list + profile.update cross here; zod validates inputs |
| Main → DB | audit() helper inserts one row; profile.update writes via transaction + audit emit |
| Renderer → UI | audit row click opens Dialog; ProfileEditor language radio persists state |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07-11 | Repudiation | Audit page fires audit_view on every render (audit spam) | medium | mitigate | D-08 verbatim + Pitfall 8: empty deps array + 1000ms setTimeout + cleanup on unmount. Test asserts exactly-one-call after multiple state changes. |
| T-07-12 | Repudiation | Language change bypasses audit | medium | mitigate | ProfileEditor handler calls `audit.log({action: 'language.changed'})` in parallel with `profile.update`. Test asserts both calls fire. |
| T-07-13 | Tampering | Audit row click navigation (entity click-through) | low | accept | Per D-06 verbatim "No click-through to the entity — the entity-browse path lives on Patient List (D-01..D-03)". Audit page is for compliance/forensics, not navigation. The row's Dialog is the only outcome of a click. |
| T-07-14 | Information Disclosure | Audit row PII (patient name leaks via entity_type=patient rows) | low | mitigate | UI-SPEC §PII verbatim: "Patient column shows entity_id (UUID) NOT the full name — keeps audit export filenames + redaction polite per PITFALLS §Security Mistakes 'Logging sensitive content to a file'". The Audit page renders entity_id (UUID) in the Entity column; the patient name is NOT in the row. |
| T-07-15 | Information Disclosure | Audit detail dialog exposes metadata JSON (may contain procedure names, etc.) | low | accept | Metadata is the audit row's own metadata (already structured per Phase 2 Fix 6 — never contains field values, only field names). Acceptable per the Phase 2 design intent. |

**No high-severity risks introduced by this plan.** The plan is read-only UI + a single persist call per language change.
</threat_model>

<verification>
- `npm run typecheck` — web, no errors
- `npm run test:unit -- tests/renderer/pages/Audit.test.tsx tests/renderer/hooks/useAudit.test.ts tests/renderer/pages/profile-editor.test.tsx` — all green
- `npm run test:unit` — full suite (no regressions; target 600+ tests)
- Visual: open Settings → Audit → see 3 mock rows → click row → see detail Dialog with JSON → click Settings → Profile → see Language Card → click AR → see language persist
</verification>

<success_criteria>
1. Audit page renders rows at min-h-[36px] with formatted timestamps + user display name + action + entity_type entity_id
2. Filter bar applies Date from/to + User + Action + Entity Type filters (AND-combined)
3. Row click opens a detail Dialog with pretty-printed JSON metadata
4. Audit page mounts ONCE → fires `audit_view` once after 1s debounce → no re-fire on state change
5. ProfileEditor Language Card mounts below Assets Card with EN/AR radio
6. AR radio selection persists `language: 'ar'` to doctor_profile.language + emits `language.changed` audit row
</success_criteria>

<output>
Create `.planning/phases/07-search-history-audit-ui-backup-restore-arabic-rtl/07-03-SUMMARY.md` when done.
</output>
