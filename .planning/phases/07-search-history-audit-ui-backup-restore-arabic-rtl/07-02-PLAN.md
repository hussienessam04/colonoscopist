---
phase: 07-search-history-audit-ui-backup-restore-arabic-rtl
plan: 02
type: execute
wave: 2
depends_on:
  - 07-01
files_modified:
  - src/main/db/patients.ts
  - src/main/db/procedures-repo.ts
  - src/main/ipc/patients.ts
  - src/main/ipc/procedures.ts
  - src/shared/ipc-contract.ts
  - src/shared/validators.ts
  - src/preload/index.ts
  - src/renderer/src/pages/PatientsList.tsx
  - src/renderer/src/components/PatientRow.tsx
  - src/renderer/src/components/SettingsSidebar.tsx
  - src/renderer/src/lib/router.ts
  - src/renderer/src/components/ui/popover.tsx
  - src/renderer/src/lib/format.ts
  - tests/main/db/patients.test.ts
  - tests/main/db/procedures-repo.test.ts
  - tests/renderer/pages/patients-list.test.tsx
autonomous: true
requirements:
  - SRCH-01
  - SRCH-02
  - SRCH-03
user_setup: []

must_haves:
  truths:
    - statement: "Doctor can search patients by name substring AND/AND MRN exact AND/AND date range AND/AND doctor AND/AND procedure status; combinations AND across all dimensions (per D-02)."
      verification: "tests/main/db/patients.test.ts inserts 3 patients + 5 procedures (different doctors/status/dates) + asserts patientRepo.list({dateFrom, dateTo, doctorId, procedureStatus: ['completed']}) returns the expected subset"
    - statement: "Empty filter values mean 'no constraint' — the filter sidebar with all fields blank returns the full Phase 2 surface (active patients, 25/page)."
      verification: "tests/main/db/patients.test.ts asserts patientRepo.list({}) === patientRepo.list() — same 25-row page, full active set"
    - statement: "Procedure row click in the Patient List accordion navigates to '{ name: 'procedure-review', procedureId }' (per D-03 + SRCH-03)."
      verification: "tests/renderer/pages/patients-list.test.tsx renders PatientsList with 1 patient expanded → clicks procedure row → asserts useRoute().navigate was called with {name: 'procedure-review', procedureId: '...'}"
    - statement: "Report row click invokes window.api.reports.openPdf({id}) — the existing IPC from Phase 6 (per D-03)."
      verification: "tests/renderer/pages/patients-list.test.tsx clicks report row → asserts window.api.reports.openPdf called with {id: report.id, reveal: false}"
    - statement: "Date range applies to procedures.started_at (canonical procedure date per Phase 4 D-10 / D-02); doctor filter uses procedures.doctor_id; procedure status is a multi-select across 'completed'|'partial'|'recording' (per D-02)."
      verification: "tests/main/db/procedures-repo.test.ts asserts proceduresRepo.list({dateFrom, dateTo, doctorId}) filters on procedures.started_at + status IN (...) multi-select"
    - statement: "Pagination stays at 25/page (Phase 2 default); filter changes reset to page 1 (existing pattern)."
      verification: "tests/renderer/pages/patients-list.test.tsx changes doctor filter → asserts setPage called with 1 + refetch uses pageSize=25"
    - statement: "SettingsSidebar gains two new entries: 'Audit' and 'Backup & Restore' — entries render with FileSearch and HardDrive icons respectively (per D-05 + D-11)."
      verification: "tests/renderer/components/settings-sidebar.test.tsx asserts the new buttons mount with data-testid='settings-hub-audit' + 'settings-hub-backup-restore' + lucide icons FileSearch + HardDrive render"
  artifacts:
    - src/main/db/patients.ts (extended patientRepo.list + __resetPatientRepoCache preserved)
    - src/main/db/procedures-repo.ts (extended proceduresRepo.list + __resetProceduresRepoCache preserved)
    - src/renderer/src/pages/PatientsList.tsx (filter sidebar)
    - src/renderer/src/components/PatientRow.tsx (accordion expansion)
    - src/renderer/src/components/SettingsSidebar.tsx (Audit + Backup & Restore entries)
  prohibitions:
    - "DO NOT change Phase 2 patient search surface (name substring + MRN exact) — extend the existing repo method, do not fork"
    - "DO NOT change pagination default (25/page) — preserve Phase 2 D-04"
    - "DO NOT make filter combinations OR — D-02 verbatim AND across all dimensions"
    - "DO NOT auto-apply filters on keystroke — D-01 verbatim explicit Apply button"
    - "DO NOT navigate away from PatientsList on filter change — the table is the destination (per D-01)"
    - "DO NOT bypass patientRepo.list cache reset (__resetPatientRepoCache) when extending filter params"
    - "DO NOT add new shadcn components in this plan — popover is installed in Plan 07-01, used here as the filter dropdown wrapper only"

artifacts_this_phase_produces:
  decorators: []
  classes: []
  functions:
    - patientRepo.list(filter: PatientListInput): { rows: Patient[]; total: number }
    - proceduresRepo.list(filter: ProceduresListFilter): { rows: Procedure[]; total: number }
    - PatientList page with filter sidebar + accordion expansion
  ipc_channels: []
  cli_flags: []
  new_file_paths: []
---

<objective>
Cross-cutting search per D-01..D-04: extend `patientRepo.list` + `proceduresRepo.list` with `dateFrom`/`dateTo`/`doctorId`/`procedureStatus` filter params AND-combined; render a left-rail filter sidebar on PatientsList with name/MRN/date-range/doctor/status inputs + Apply + Clear; expand each PatientRow into a procedure+report accordion where procedure row click navigates to `'procedure-review'; procedureId` and report row click invokes the existing `reports.openPdf` IPC; add the SettingsSidebar entries for Audit + Backup & Restore (visual only — pages arrive in Plan 07-03 + 07-05).

Purpose: This is the SRCH-01..03 deliverable and the foundation for the doctor to find procedures by date/doctor/status when running audit checks, training new staff, or reviewing case history. Filter sidebar lives on PatientsList (D-01 verbatim) — not a new top-level route — so the doctor's mental model stays "I'm on Patient List, I refined the view".
Output: Patient List filter sidebar + PatientRow accordion + SettingsSidebar visual entries + tests proving all 5 filter dimensions AND-combine correctly.
</objective>

<execution_context>
@C:/Users/Hussien Essam/.config/opencode/gsd-core/workflows/execute-plan.md
@C:/Users/Hussien Essam/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-search-history-audit-ui-backup-restore-arabic-rtl/07-CONTEXT.md (D-01, D-02, D-03, D-04 — locked)
@.planning/phases/07-search-history-audit-ui-backup-restore-arabic-rtl/07-UI-SPEC.md (E1-E2 backstops for filter sidebar)
@.planning/phases/07-search-history-audit-ui-backup-restore-arabic-rtl/07-RESEARCH.md (Pattern 1 — Filter sidebar)

# Source-of-truth files
@src/main/db/patients.ts (patientRepo.list + cached stmts)
@src/main/db/procedures-repo.ts (proceduresRepo.list + cached stmts)
@src/main/ipc/patients.ts (listPatients handler)
@src/main/ipc/procedures.ts (list handler)
@src/shared/ipc-contract.ts (patients.list + procedures.list query types)
@src/shared/validators.ts (patientListQueryInput + proceduresListQueryInput)
@src/preload/index.ts (bridge)
@src/renderer/src/pages/PatientsList.tsx (existing top-bar layout — to be replaced by sidebar layout)
@src/renderer/src/components/PatientRow.tsx (existing PatientRow)
@src/renderer/src/components/SettingsSidebar.tsx (existing settings sidebar)
@src/renderer/src/lib/router.ts (Route union)
@.planning/research/PITFALLS.md (Translation Pitfalls table — login screen half-translated)
</context>

<tasks>

<task type="tracer">
  <name>Patient List filter sidebar + accordion expansion + per-row navigation (SRCH-01..03 production-quality end-to-end)</name>
  <read_first>
    - src/main/db/patients.ts (existing repo + cached stmts — extend with EXISTS subquery pattern per RESEARCH.md Pattern 1)
    - src/main/db/procedures-repo.ts (existing repo + cached stmts)
    - src/main/ipc/patients.ts (audit emit pattern for filter reads)
    - src/main/ipc/procedures.ts (audit emit pattern)
    - src/shared/validators.ts (extend patientListQueryInput + proceduresListQueryInput)
    - src/shared/ipc-contract.ts (extend patients.list + procedures.list query types)
    - src/preload/index.ts (existing bridge — signatures unchanged)
    - src/renderer/src/pages/PatientsList.tsx (current top-bar layout to be replaced)
    - src/renderer/src/components/PatientRow.tsx (current row — extend with accordion)
    - src/renderer/src/lib/router.ts (Route union — no change needed; 'procedure-review' exists)
    - src/renderer/src/hooks/useDoctorProfile.ts (existing SWR-style hook — copy pattern for any new useProceduresForPatient)
    - src/renderer/src/lib/format.ts (existing HH:MM:SS formatter)
    - .planning/phases/07-search-history-audit-ui-backup-restore-arabic-rtl/07-CONTEXT.md (D-01..D-04 verbatim)
    - .planning/phases/07-search-history-audit-ui-backup-restore-arabic-rtl/07-UI-SPEC.md (E1 + E3 backstops)
    - .planning/phases/07-search-history-audit-ui-backup-restore-arabic-rtl/07-RESEARCH.md (Pattern 1)
  </read_first>
  <files>
    - src/main/db/patients.ts
    - src/main/db/procedures-repo.ts
    - src/main/ipc/patients.ts
    - src/main/ipc/procedures.ts
    - src/shared/ipc-contract.ts
    - src/shared/validators.ts
    - src/renderer/src/pages/PatientsList.tsx
    - src/renderer/src/components/PatientRow.tsx
    - src/renderer/src/lib/format.ts
    - tests/main/db/patients.test.ts
    - tests/main/db/procedures-repo.test.ts
    - tests/renderer/pages/patients-list.test.tsx
  </files>
  <action>
    Extend `src/shared/validators.ts`: add to `patientListQueryInput` schema — `.extend({ dateFrom: z.string().regex(/^\d{4}-\d{2}-\d{2}$/, 'ISO yyyy-mm-dd').optional(), dateTo: z.string().regex(/^\d{4}-\d{2}-\d{2}$/, 'ISO yyyy-mm-dd').optional(), doctorId: z.string().uuid().optional(), procedureStatus: z.array(z.enum(['recording', 'completed', 'partial', 'crashed'])).optional() })`. Extend `proceduresListQueryInput` similarly with dateFrom/dateTo/doctorId (procedureStatus is per-patient filter on Patient List, not on procedures list directly).

    Extend `src/shared/ipc-contract.ts`: update the `patients.list` query type to include the four new fields. Update `procedures.list` query type similarly.

    Extend `src/main/db/patients.ts`: add an `EXISTS` subquery path to `patientRepo.list` per RESEARCH.md Pattern 1 — when ANY of dateFrom/dateTo/doctorId/procedureStatus is set, append `AND EXISTS (SELECT 1 FROM procedures WHERE p.id = procedures.patient_id AND procedures.started_at >= @dateFrom AND procedures.started_at <= @dateTo AND procedures.doctor_id = @doctorId AND procedures.status IN (@status0, @status1, ...))` to the WHERE clause. Build the WHERE dynamically; preserve `deleted_at IS NULL` as the first clause; keep `MAX_PAGE_SIZE = 200` cap. Preserve the cache pattern: `__resetPatientRepoCache()` invalidates the cached statements when the schema extension is read. The 4 existing prepared statements (`listActive`, `listByMrnActive`, `listBySearchActive`, plus 4 `All` variants) are unchanged — add ONE new statement: `listWithFiltersActive` with the dynamic WHERE shape parameterized via `@search` (optional), `@mrn` (optional), `@dateFromMs` (optional, derived from dateFrom via `Date.parse`), `@dateToMs` (optional, derived from dateTo via `Date.parse + 86_400_000` for inclusive end), `@doctorId` (optional), `@status` (optional, JSON-stringified array or comma-joined IN clause), `@limit`, `@offset`. The new statement uses `WHERE deleted_at IS NULL` + the dynamic EXISTS subquery + `ORDER BY full_name COLLATE NOCASE LIMIT @limit OFFSET @offset`. Use a single prepared statement to avoid combinatorial explosion (per RESEARCH.md Pattern 1 "no per-filter statement explosion").

    Extend `src/main/db/procedures-repo.ts`: add `listByFilters` prepared statement that takes `@dateFromMs`, `@dateToMs`, `@doctorId`, `@limit`, `@offset` and filters on `started_at >= @dateFromMs AND started_at <= @dateToMs AND doctor_id = @doctorId`. Default to existing listByPatient or listAll when no extra filters present. Preserve `__resetProceduresRepoCache()`. The list method's `filter: ProceduresListFilter` type gains `dateFrom?, dateTo?, doctorId?` (procedureStatus is per-patient filter on Patient List only).

    Extend `src/main/ipc/patients.ts`: `listPatients` parses the extended query schema, passes the filter to `patientRepo.list`, emits the audit row with the new filter fields in metadata.

    Extend `src/main/ipc/procedures.ts`: the `list` handler passes through dateFrom/dateTo/doctorId to `proceduresRepo.list`.

    Tests:
    - `tests/main/db/patients.test.ts`: add a test that seeds 3 patients + 5 procedures (varying startedAt + doctor_id + status), then asserts:
      - `patientRepo.list({})` returns all 3 active patients (preserves Phase 2 behavior)
      - `patientRepo.list({dateFrom: '2026-08-01', dateTo: '2026-08-31'})` returns only patients whose procedures fall in August
      - `patientRepo.list({doctorId: 'doc1'})` returns only patients with procedures by doctor1
      - `patientRepo.list({procedureStatus: ['completed']})` returns only patients with completed procedures
      - `patientRepo.list({dateFrom: '2026-08-01', dateTo: '2026-08-31', doctorId: 'doc1', procedureStatus: ['completed']})` returns the AND-combined intersection (one patient)
      - `patientRepo.list({includeDeleted: true})` returns soft-deleted patients (preserves existing Phase 2 behavior)
    - `tests/main/db/procedures-repo.test.ts`: add a test that seeds 5 procedures (varying startedAt + doctor_id + patient_id) and asserts `proceduresRepo.list({dateFrom, dateTo, doctorId})` returns the right subset.

    `src/renderer/src/pages/PatientsList.tsx`: RESTRUCTURE the layout to `grid gap-4 lg:grid-cols-[18rem_minmax(0,1fr)]` per 07-UI-SPEC.md (matches SettingsHub visual density per D-01 verbatim "filter sidebar matches SettingsHub's visual density"). The left column is a new `<Card>` "Search & filter" with CardHeader "Search & filter" + CardContent containing (in this vertical order):
    1. Search-by-name input (existing Phase 2 input moved here)
    2. MRN exact input (existing moved)
    3. Date range: "From" + "To" — two native `<input type="date">` (per RESEARCH.md Pattern 1 / agent discretion)
    4. Doctor: shadcn `<Select>` populated from `window.api.auth.usersList()` via `useEffect`; placeholder "Any doctor"
    5. Procedure status: 3 stacked `<Checkbox>` (Completed, Partial, Recording) per UI-SPEC
    6. Two buttons side-by-side at the bottom: `<Button variant="outline">Clear filters</Button>` + `<Button>Apply filters</Button>` (per UI-SPEC — Apply button at sidebar bottom, all inputs non-blocking no auto-apply on keystroke)
    The right column is the existing results Card (table + pagination) + an EXPANDED PatientRow rendering (see below).

    Local state shape: `const [filters, setFilters] = useState<Filters>({ search: '', mrn: '', dateFrom: '', dateTo: '', doctorId: null, status: [] })`. `Apply` triggers refetch with the filter object; `Clear` resets to defaults + refetch with empty filters. `useEffect` debounce-refetches ONLY the existing search/mrn inputs (preserves Phase 2 behavior); dateRange/doctorId/status trigger refetch on Apply button click (per D-01 verbatim "all inputs non-blocking").

    `src/renderer/src/components/PatientRow.tsx`: add an optional `expanded?: boolean` + `onToggleExpand?: () => void` + `procedures?: Procedure[]` + `reports?: Report[]` props. When `expanded` is true, render an accordion section below the existing row content showing the procedures list (each row has procedure date + status badge + "Open procedure" button that calls `navigate({name: 'procedure-review', procedureId})`) + a reports sub-row per procedure (each report row has status chip Draft/Finalized + "Open PDF" button that calls `window.api.reports.openPdf({id: report.id, reveal: false})`). Use the existing `<Accordion>` shadcn primitive or a simple `<details>/<summary>` — pick the simpler one (ponytail: stdlib first).

    `src/renderer/src/lib/format.ts`: add `formatProcedureDate(ms: number): string` returning `'YYYY-MM-DD HH:MM'` via `new Intl.DateTimeFormat('en-CA', { ... })` (sortable + locale-stable).

    Tests:
    - `tests/renderer/pages/patients-list.test.tsx` (extend existing): add tests that:
      - Render PatientsList with 1 patient expanded → click procedure row → assert useRoute().navigate was called with `{name: 'procedure-review', procedureId: 'proc-1'}`
      - Click report row → assert `window.api.reports.openPdf` was called with `{id: 'rpt-1', reveal: false}`
      - Type in doctor dropdown → click Apply → assert refetch was triggered with the new doctorId
      - Click Clear → assert filters reset + refetch triggered with empty filters
      - Change page while filtered → assert pagination preserves filter state
  </action>
  <acceptance_criteria>
    - `npm run typecheck` passes
    - `npm run test:unit -- tests/main/db/patients.test.ts` passes — filter combinations test green
    - `npm run test:unit -- tests/main/db/procedures-repo.test.ts` passes — dateFrom/dateTo/doctorId filters green
    - `npm run test:unit -- tests/renderer/pages/patients-list.test.tsx` passes — procedure row click + report row click + filter apply/clear green
    - `grep -c 'procedureStatus\\|dateFrom\\|doctorId' src/shared/validators.ts` returns >= 4
    - `grep -c 'EXISTS (SELECT 1 FROM procedures' src/main/db/patients.ts` returns >= 1
    - `grep -c 'lg:grid-cols-\\[18rem_minmax(0,1fr)\\]' src/renderer/src/pages/PatientsList.tsx` returns >= 1
    - All existing tests still pass (no regressions)
  </acceptance_criteria>
  <done>
    Patient List filter sidebar live with 5 dimensions AND-combined; procedure row click navigates to procedure-review; report row click invokes reports.openPdf; SettingsSidebar gains Audit + Backup & Restore visual entries; filter combinations proven via tests.
  </done>
</task>

<task type="auto">
  <name>SettingsSidebar visual entries + useDoctorProfile/SettingsHub surface mentions + audit emit for filter reads</name>
  <read_first>
    - src/renderer/src/components/SettingsSidebar.tsx (existing 3-button sidebar)
    - src/renderer/src/pages/SettingsHub.tsx (existing hub layout — will gain the new mentions in the description)
    - src/renderer/src/lib/router.ts (extend Route union with 'audit'; null + 'backup-restore'; null)
    - src/renderer/src/components/SettingsSidebar.tsx (extend SettingsTab type)
    - lucide-react (FileSearch + HardDrive icons — verify they exist; if not, use Search + Database)
    - tests/renderer/components/settings-sidebar.test.tsx (extend existing)
  </read_first>
  <files>
    - src/renderer/src/components/SettingsSidebar.tsx
    - src/renderer/src/lib/router.ts
    - src/renderer/src/pages/SettingsHub.tsx
    - tests/renderer/components/settings-sidebar.test.tsx
  </files>
  <action>
    Extend `src/renderer/src/lib/router.ts`: add to the Route union:
    - `{ name: 'audit' }`
    - `{ name: 'backup-restore' }`
    Keep the existing `'profile-edit'` (Plan 06-02) + `'settings-hub'` (Plan 03-05) entries untouched.

    Extend `src/renderer/src/components/SettingsSidebar.tsx`: extend the `SettingsTab` type union to `'capture' | 'users' | 'profile' | 'audit' | 'backup-restore'`. Add two new `<Button>` entries to the rendered sidebar AFTER the existing Profile button (so the visual order is Capture → Profile → Audit → Backup & Restore → Users):
    - Audit button: `<FileSearch>` icon + "Audit" label + `onClick={() => navigate({name: 'audit'})}` + `data-testid="settings-hub-audit"` + active state when `route.name === 'audit'`
    - Backup & Restore button: `<HardDrive>` icon + "Backup & restore" label + `onClick={() => navigate({name: 'backup-restore'})}` + `data-testid="settings-hub-backup-restore"` + active state when `route.name === 'backup-restore'`
    Verify the lucide-react icon names exist (`grep -r "FileSearch" node_modules/lucide-react/dist/lucide-react.d.ts | head -5`); if not present, fall back to `Search` + `Database` (per agent's discretion). Audit + Backup & Restore buttons are NOT admin-gated (per UI-SPEC §Implementation Bindings — every doctor sees them).

    Extend `src/renderer/src/pages/SettingsHub.tsx`: add two `<p>` description entries inside the existing CardContent describing Audit + Backup & Restore sections (mirrors the existing Capture/Profile/Users descriptions). Keep the existing `<SettingsSidebar />` mount unchanged (the sidebar's audit + backup-restore entries land there).

    Tests:
    - `tests/renderer/components/settings-sidebar.test.tsx` (extend existing): assert the new buttons mount + assert clicking Audit button calls navigate({name: 'audit'}) + assert the active state highlights correctly when route.name === 'audit'.
  </action>
  <acceptance_criteria>
    - `npm run typecheck` passes
    - `npm run test:unit -- tests/renderer/components/settings-sidebar.test.tsx` passes — new entries render + navigate correctly
    - `grep -c "'audit'\\|'backup-restore'" src/renderer/src/lib/router.ts` returns >= 2
    - `grep -c 'settings-hub-audit\\|settings-hub-backup-restore' src/renderer/src/components/SettingsSidebar.tsx` returns >= 2
    - All existing tests still pass (no regressions)
  </acceptance_criteria>
  <done>
    Audit + Backup & Restore visual entries wired into SettingsSidebar; Route union extended; SettingsHub descriptions mention the new sections; no renderer page mounts yet (those arrive in Plan 07-03 + 07-05).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Renderer → main IPC | search filter inputs cross here; zod schema validates date format + doctorId UUID |
| Main → DB | EXISTS subquery reads patients + procedures via prepared statements |
| Renderer → UI | filter sidebar inputs trigger Apply; row click navigates routes |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07-08 | Tampering | patientRepo.list EXISTS subquery (SQL injection vector) | high | mitigate | All filter inputs validated via zod (dateFrom/dateTo regex `^\d{4}-\d{2}-\d{2}$`, doctorId UUID). Prepared statement uses named parameters (@dateFromMs, @dateToMs, @doctorId) — no string concatenation. procedureStatus array passed as comma-separated named params (@status0, @status1, ...) inside IN clause. |
| T-07-09 | Information Disclosure | Filter metadata logged in audit (dateFrom/dateTo/doctorId) | low | accept | Per Fix 6 PII guard — metadata echoes filter shape, never patient names. Date ranges + doctor UUIDs are not PII per Phase 2 decision. |
| T-07-10 | Repudiation | filter read not audited | low | mitigate | listPatients already emits `patient_list` audit row (Phase 2). Plan 07-02 extends metadata with the new filter fields. |

**No high-severity risks introduced by this plan.** The plan extends existing surfaces (patientRepo.list + proceduresRepo.list + PatientRow + SettingsSidebar) that already have IPC validation + audit + RBAC gates.
</threat_model>

<verification>
- `npm run typecheck` — both node + web, no errors
- `npm run test:unit -- tests/main/db/patients.test.ts tests/main/db/procedures-repo.test.ts tests/renderer/pages/patients-list.test.tsx tests/renderer/components/settings-sidebar.test.tsx` — all green
- `npm run test:unit` — full suite (no regressions; target 600+ tests)
- Visual: open Patient List with a multi-filter applied + expand a patient → click "Open procedure" → assert navigation to procedure-review
</verification>

<success_criteria>
1. Doctor types name substring + picks date range + picks doctor + ticks Completed status → clicks Apply → result list filters by all 4 dimensions AND-combined
2. Empty filters (no constraints) returns Phase 2 default surface (25/page, all active patients)
3. Per-row procedure click navigates to `{name: 'procedure-review', procedureId}`
4. Per-row report click invokes `window.api.reports.openPdf({id, reveal: false})`
5. SettingsSidebar shows Audit + Backup & Restore buttons after Profile
6. Audit + Backup & Restore buttons navigate to `{name: 'audit'}` + `{name: 'backup-restore'}` respectively
</success_criteria>

<output>
Create `.planning/phases/07-search-history-audit-ui-backup-restore-arabic-rtl/07-02-SUMMARY.md` when done.
</output>
