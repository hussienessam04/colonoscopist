---
phase: 07-search-history-audit-ui-backup-restore-arabic-rtl
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/main/db/migrations/0007_doctor_profile_language_and_users_language.sql
  - src/main/db/migrations.ts
  - src/main/backup/snapshot.ts
  - src/main/backup/index.ts
  - src/main/backup/restore.ts
  - src/main/ipc/backup.ts
  - src/main/ipc/restore.ts
  - src/main/ipc/audit.ts
  - src/main/ipc/profile.ts
  - src/main/ipc/auth.ts
  - src/main/ipc/users.ts
  - src/main/paths.ts
  - src/main/index.ts
  - src/main/db/users.ts
  - src/main/db/doctor-profile-repo.ts
  - src/shared/ipc-contract.ts
  - src/shared/validators.ts
  - src/preload/index.ts
  - src/main/db/audit.ts
  - package.json
  - src/renderer/src/components/ui/popover.tsx
  - src/renderer/src/components/ui/tooltip.tsx
  - src/renderer/src/components/ui/slider.tsx
  - tests/main/backup/snapshot.test.ts
  - tests/main/backup/index.test.ts
  - tests/main/backup/restore.test.ts
  - tests/main/ipc/audit.test.ts
  - tests/main/ipc/profile.test.ts
  - tests/main/auth/wizard-bootstrap.test.ts
  - tests/main/db/migrations.test.ts
autonomous: true
requirements:
  - SET-05
  - SET-06
  - AUDIT-01
  - I18N-01
user_setup: []

must_haves:
  truths:
    - statement: "Doctor can run a backup; the resulting zip contains app.db (single coherent file post-PRAGMA wal_checkpoint(TRUNCATE) + better-sqlite3 db.backup) + media/ + profiles/ + reports/ subtrees, and the active data/ folder is unchanged."
      verification: "tests/main/backup/index.test.ts creates backup → unzip-asserts subtree contents → diff active data/ before vs after is empty"
    - statement: "Restoring a backup zip unpacks into <userData>/data-restore-<timestamp>/ (sibling of active data/); the active data/ folder is byte-identical before and after."
      verification: "tests/main/backup/restore.test.ts asserts staging dir exists + active data/ byte-difference via checksum"
    - statement: "Restored DB passes PRAGMA integrity_check end-to-end; any non-ok result is returned to the renderer (string)."
      verification: "tests/main/backup/restore.test.ts asserts integrity_check returns 'ok' for known-good zip + non-ok string for corrupted fixture"
    - statement: "yauzl entry path filter rejects absolute paths + .. segments + outside-staging-dir paths BEFORE writing to disk."
      verification: "tests/main/backup/restore.test.ts asserts ../escape.txt and /etc/passwd entries are skipped without touching filesystem"
    - statement: "Migration 0007 adds users.language TEXT NOT NULL DEFAULT 'en' + doctor_profile.language TEXT NULL in one file; idempotent (re-running migrations.ts does not double-add columns)."
      verification: "tests/main/db/migrations.test.ts runs runMigrations twice and asserts PRAGMA table_info(users) + PRAGMA table_info(doctor_profile) match expected shape"
    - statement: "Profile.update IPC accepts language?: 'en' | 'ar' | null; persists to doctor_profile.language."
      verification: "tests/main/ipc/profile.test.ts asserts update({language: 'ar'}) writes the row + get() returns language: 'ar'"
    - statement: "Wizard bootstrap + users.create accept language?: 'en' | 'ar'; persist to users.language."
      verification: "tests/main/auth/wizard-bootstrap.test.ts asserts wizard({language: 'ar'}) sets users.language = 'ar' on the new admin row"
    - statement: "audit.log IPC channel writes a row via the existing audit() helper; no bypass path."
      verification: "tests/main/ipc/audit.test.ts asserts log({action, entityType}) appends one audit_log row + subsequent UPDATE/DELETE attempts throw via audit_log_no_* triggers"
    - statement: "shadcn primitives popover, tooltip, slider are installed (Radix under shadcn official registry) and compile against the existing components.json (D-21: D-21 shadcn additions ship via npx shadcn add; Calendar NOT installed per D-21 ladder)."
      verification: "ls src/renderer/src/components/ui/{popover,tooltip,slider}.tsx succeeds + tsc -p tsconfig.web.json passes"
    - statement: "Backup/Restore IPC handlers register after auth in main/index.ts so requireSession() resolves."
      verification: "src/main/index.ts:registerBackupIpc + registerRestoreIpc lines exist after registerAuthIpc() + npm run typecheck:node passes"
  artifacts:
    - src/main/db/migrations/0007_doctor_profile_language_and_users_language.sql
    - src/main/backup/snapshot.ts (walCheckpoint + dbBackup)
    - src/main/backup/index.ts (createBackup + revealBackup)
    - src/main/backup/restore.ts (previewRestore + unpackRestore + integrityCheck)
    - src/main/ipc/backup.ts
    - src/main/ipc/restore.ts
    - src/renderer/src/components/ui/popover.tsx
    - src/renderer/src/components/ui/tooltip.tsx
    - src/renderer/src/components/ui/slider.tsx
    - tests/main/backup/snapshot.test.ts
    - tests/main/backup/index.test.ts
    - tests/main/backup/restore.test.ts
  prohibitions:
    - "DO NOT use in-memory zip libraries (adm-zip, zip-a-folder) — clinic-scale data OOMs main process"
    - "DO NOT overwrite active data/ on restore — staging dir is sibling of data/ via restoreStagingDir(timestamp)"
    - "DO NOT capture WAL/SHM separately in the zip — use db.backup() to temp file THEN zip the temp file"
    - "DO NOT use tailwindcss-rtl plugin — Tailwind 3.4 native rtl: variants cover the surface"
    - "DO NOT add calendar shadcn component — out of scope per REQUIREMENTS"
    - "DO NOT bypass the audit() helper for any write path"
    - "DO NOT skip the yauzl entry-path filter — defense-in-depth against zip-slip"

artifacts_this_phase_produces:
  decorators: []
  classes: []
  functions:
    - createBackup(opts: {destZipPath: string}): Promise<{path: string; sizeBytes: number; procedureCount: number}>
    - revealBackup(path: string): void
    - previewRestore({zipPath, stagingDir}): Promise<{filename, totalSize, dbIntegrityCheck, procedureCount}>
    - unpackRestore({zipPath, stagingDir}): Promise<{fileCount, stagingDir}>
    - integrityCheck(stagingDir): string
    - walCheckpoint(db): void
    - dbBackup(db, outPath): Promise<void>
    - safeEntryPath(entry, stagingDir): string | null
  ipc_channels:
    - BACKUP_CREATE: backup:create
    - BACKUP_REVEAL: backup:reveal
    - RESTORE_PREVIEW: restore:preview
    - RESTORE_UNPACK: restore:unpack
    - AUDIT_LOG: audit:log
  cli_flags: []
  new_file_paths:
    - src/main/db/migrations/0007_doctor_profile_language_and_users_language.sql
    - src/main/backup/snapshot.ts
    - src/main/backup/index.ts
    - src/main/backup/restore.ts
    - src/main/ipc/backup.ts
    - src/main/ipc/restore.ts
    - src/renderer/src/components/ui/popover.tsx
    - src/renderer/src/components/ui/tooltip.tsx
    - src/renderer/src/components/ui/slider.tsx
    - tests/main/backup/snapshot.test.ts
    - tests/main/backup/index.test.ts
    - tests/main/backup/restore.test.ts
    - tests/main/ipc/audit.test.ts
---

<objective>
Phase 7 Foundation: Migration 0007 (language columns on users + doctor_profile), the entire backup/restore main-side module (snapshot + archiver zip + yauzl unpack + integrity_check + IPC), the audit.log IPC channel for the audit-on-every-read pattern (D-08), extended profile.update + wizard.bootstrap + users.create to accept `language?: 'en'|'ar'|null`, shadcn primitive installs (popover, tooltip, slider), and the corresponding contract/preload/test wiring. No renderer UI in this plan; the four parallel waves (07-02 search, 07-03 audit+profile, 07-04 i18n+AR PDF, 07-05 backup/restore UI) build their UI on top of this trunk.

Purpose: This is the shared trunk that every Phase 7 surface depends on. Shipping the main-side backup module end-to-end (real archiver round-trip, real yauzl unpack, real PRAGMA integrity_check) as the tracer is the highest-risk component — if path-traversal or partial-DB restore slip past here they corrupt clinic data.
Output: Migration 0007 + backup module + IPC contract extensions + 3 shadcn primitives + tests proving backup→staging→integrity_check round-trip is correct end-to-end.
</objective>

<execution_context>
@C:/Users/Hussien Essam/.config/opencode/gsd-core/workflows/execute-plan.md
@C:/Users/Hussien Essam/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-search-history-audit-ui-backup-restore-arabic-rtl/07-CONTEXT.md (D-09, D-10, D-14, D-15, D-16, D-17, D-18 — locked inputs)
@.planning/phases/07-search-history-audit-ui-backup-restore-arabic-rtl/07-RESEARCH.md (Validation Architecture §Backup/Restore)
@.planning/phases/07-search-history-audit-ui-backup-restore-arabic-rtl/07-VALIDATION.md (07-01-01..07-01-07 task map)
@.planning/research/PITFALLS.md (Pitfall 9 — backup captures partial DB)
@.planning/research/ARCHITECTURE.md (Backup/restore main side pattern)

# Source-of-truth files to read before each task
@src/main/db/migrations.ts
@src/main/db/patients.ts
@src/main/db/audit.ts
@src/main/db/users.ts
@src/main/db/doctor-profile-repo.ts
@src/main/paths.ts
@src/main/ipc/audit.ts
@src/main/ipc/profile.ts
@src/main/ipc/auth.ts
@src/main/ipc/users.ts
@src/shared/ipc-contract.ts
@src/shared/validators.ts
@src/preload/index.ts
@src/main/index.ts
@tests/main/db/migrations.test.ts
</context>

<tasks>

<task type="tracer">
  <name>Migration 0007 + backup module end-to-end round-trip (snapshot + archiver zip + yauzl unpack + integrity check)</name>
  <read_first>
    - src/main/db/migrations.ts (register 0007 + ?raw import shape)
    - src/main/db/migrations/0004_doctor_profile_and_reports.sql (style + idempotent backfill pattern)
    - src/main/db/audit.ts (audit() helper signature)
    - src/main/paths.ts (dataDir + mediaDir + profilesDir + reportsDir)
    - src/main/db/users.ts (users row shape for language column add)
    - src/main/db/doctor-profile-repo.ts (doctor_profile row shape)
    - src/main/db/audit.ts (DB connection via getDb())
    - .planning/phases/07-search-history-audit-ui-backup-restore-arabic-rtl/07-CONTEXT.md (D-09, D-10, D-14, D-15, D-16, D-17, D-18)
    - .planning/research/PITFALLS.md (Pitfall 9)
  </read_first>
  <files>
    - src/main/db/migrations/0007_doctor_profile_language_and_users_language.sql
    - src/main/db/migrations.ts
    - src/main/backup/snapshot.ts
    - src/main/backup/index.ts
    - src/main/backup/restore.ts
    - src/main/paths.ts
    - package.json
    - tests/main/db/migrations.test.ts
    - tests/main/backup/snapshot.test.ts
    - tests/main/backup/index.test.ts
    - tests/main/backup/restore.test.ts
  </files>
  <action>
    Per D-17 + D-18, create `src/main/db/migrations/0007_doctor_profile_language_and_users_language.sql` with TWO `ALTER TABLE` statements: `ALTER TABLE users ADD COLUMN language TEXT NOT NULL DEFAULT 'en'` + `ALTER TABLE doctor_profile ADD COLUMN language TEXT NULL`. One migration file covers both columns per CONTEXT.md. Idempotent — the migrations runner's `_migrations` table guards against re-execution.

    Register 0007 in `src/main/db/migrations.ts` by adding `import doctorProfileLanguageSql from './migrations/0007_doctor_profile_language_and_users_language.sql?raw';` and pushing `{ id: 7, name: 'doctor_profile_language_and_users_language', up: doctorProfileLanguageSql }` to the MIGRATIONS array.

    Extend `src/main/paths.ts` with `restoreStagingDir(timestamp: number): string` returning `path.join(app.getPath('userData'), \`data-restore-\${timestamp}\`)`. Mirror the dataDir() mkdir-on-demand pattern.

    Create `src/main/backup/snapshot.ts` with two exports:
    - `walCheckpoint(db: Database.Database): void` — runs `db.pragma('wal_checkpoint(TRUNCATE)')` synchronously (better-sqlite3 pragmas are sync). Per D-10 / PITFALLS §Pitfall 9, this MUST happen before db.backup() to guarantee the main DB file is fully coherent.
    - `dbBackup(db: Database.Database, outPath: string): Promise<void>` — calls `await db.backup(outPath)` (better-sqlite3 returns a promise in v11). Cleans up via `unlink(outPath)` in the `finally` block.

    Create `src/main/backup/index.ts` with:
    - `createBackup(opts: { destZipPath: string }): Promise<{ path: string; sizeBytes: number; procedureCount: number }>` per D-09/D-10: (1) `walCheckpoint(db)`, (2) `dbBackup(db, tempPath)`, (3) archiver streams `tempPath` as `app.db` + `mediaDir()` as `media/` + `profilesDir()` as `profiles/` + `reportsDir()` as `reports/`, (4) on `output.on('close')` resolve + run `unlink(tempPath)`. Use `archiver('zip', { zlib: { level: 6 } })` — NOT in-memory zip. On any throw, run `unlink(tempPath)` in the catch. The `procedureCount` is `(getDb().prepare('SELECT COUNT(*) AS c FROM procedures').get() as {c:number}).c`.
    - `revealBackup(path: string): void` — wraps `shell.showItemInFolder(path)` (lazy import `electron.shell`); for unit tests, guard with try/catch since `shell.showItemInFolder` throws outside Electron.
    - Internal helper `safeEntryPath(entry, stagingDir)` is NOT in this file (it lives in restore.ts).

    Create `src/main/backup/restore.ts` with:
    - `safeEntryPath(entry: yauzl.Entry, stagingDir: string): string | null` per D-15: (1) `entry.fileName.replace(/\\/g, '/')`, (2) reject if `path.isAbsolute(normalized)` OR `normalized.includes('..')` OR `path.resolve(stagingDir, normalized)` does not start with `path.resolve(stagingDir)`. Returns the resolved path on accept, null on reject. Mirrors Phase 5 P04 ALLOWED_SUBDIRS defense-in-depth.
    - `previewRestore({ zipPath, stagingDir }): Promise<{ filename: string; totalSize: number; dbIntegrityCheck: string; procedureCount: number }>` — opens the zip via `yauzl.open(zipPath, { lazyEntries: true }, ...)`, iterates entries to sum uncompressed size (skip directories), then calls `integrityCheck(stagingDir)` after unpacking to a temporary preview dir. Returns counts parsed from the unpacked DB via `(stmts of getDb()).prepare('SELECT COUNT(*) AS c FROM procedures').get() as {c:number}).c`. `dbIntegrityCheck` is the literal return string from `PRAGMA integrity_check`.
    - `unpackRestore({ zipPath, stagingDir }): Promise<{ fileCount: number; stagingDir: string }>` — streams yauzl entries one-by-one via `zip.on('entry')`; for each entry: `safeEntryPath`; if null skip; if `\/$\/` test, mkdir recursive and continue; else mkdir dirname recursive + createWriteStream + pipe entry.openReadStream to writer + on close count++ and call `zip.readEntry()`. On `zip.on('end')` resolve with fileCount.
    - `integrityCheck(stagingDir: string): string` — opens `path.join(stagingDir, 'app.db')` via better-sqlite3 NEW connection (not the main getDb()), runs `db.prepare('PRAGMA integrity_check').get() as { integrity_check: string }`, returns the string. Close the DB on both success and error paths.

    `package.json`: add `archiver` ^8.0.0, `yauzl` ^3.4.0 to dependencies; `@types/archiver` ^8.0.0, `@types/yauzl` ^3.4.0 to devDependencies. Run `npm install` (electron-rebuild is in postinstall — Phase 1 already covers native rebuilds; archiver + yauzl are pure JS so no rebuild needed). Add `i18next` ^26.3.6, `react-i18next` ^17.0.11, `i18next-browser-languagedetector` ^8.2.1 to dependencies for Plan 07-04 (forward-installed to keep Wave 1 the single dep-add plan).

    Install shadcn primitives: run `npx shadcn@latest add popover tooltip slider` from the repo root. Verify each generated file uses `@radix-ui/react-popover` / `@radix-ui/react-tooltip` / `@radix-ui/react-slider` — Radix primitives, no third-party registry. Generated files appear at `src/renderer/src/components/ui/{popover,tooltip,slider}.tsx`.

    Tests:
    - `tests/main/db/migrations.test.ts`: extend the existing migrations test to run `runMigrations(db)` on an in-memory better-sqlite3 DB (already covered by the existing migration test), then assert `PRAGMA table_info(users)` includes `language TEXT NOT NULL DEFAULT 'en'` and `PRAGMA table_info(doctor_profile)` includes `language` (nullable). Run runMigrations a second time on the same DB — assert no error (idempotency).
    - `tests/main/backup/snapshot.test.ts` (new): create an in-memory DB, insert one row, call `walCheckpoint(db)`, assert no throw + `db.pragma('journal_mode')` still returns 'wal'. Call `dbBackup(db, tempPath)` to a real temp file, assert `existsSync(tempPath)` + size > 0 + `db.prepare('SELECT count(*) AS c FROM sqlite_master').get()` matches the source DB.
    - `tests/main/backup/index.test.ts` (new): mock `archiver` (vi.spyOn or override the import per the project's vitest config — verify by reading `tests/setup.ts` and the existing test pattern in `tests/main/recorder/concat.test.ts`). Run `createBackup({ destZipPath: tmpZip })`, assert exit ok + `existsSync(tmpZip)` + size > 0 + the temp db file is unlinked after the call.
    - `tests/main/backup/restore.test.ts` (new): build a fixture zip via `archiver` containing `app.db` (real in-memory DB dumped to temp + moved into zip) + `media/patients/p1/proc1/screenshots/test.jpg` (a 1-byte file). Run `unpackRestore({zipPath, stagingDir})`, assert fileCount === 3 + `existsSync(stagingDir + '/app.db')` + integrity check returns 'ok'. Then build a SECOND fixture zip with entry `../../etc/passwd` + run unpackRestore — assert fileCount does not include the unsafe entry + no file at `path.resolve(stagingDir, '../../etc/passwd')`. Then test `integrityCheck` on a deliberately corrupted sqlite file (e.g. truncate bytes) — assert it returns a non-'ok' string.
  </action>
  <acceptance_criteria>
    - `npm run typecheck` passes (both node + web)
    - `npm run test:unit -- tests/main/db/migrations.test.ts` passes (0007 case) — explicit grep for `0007` or `language` in the test file
    - `npm run test:unit -- tests/main/backup/snapshot.test.ts` passes — temp db file created + cleaned up
    - `npm run test:unit -- tests/main/backup/index.test.ts` passes — `createBackup` produces a zip > 0 bytes + unlinks temp db
    - `npm run test:unit -- tests/main/backup/restore.test.ts` passes — safe entry filter rejects `..` + absolute paths + integrity check returns 'ok' for known-good fixture + non-'ok' for corrupted fixture
    - `ls src/renderer/src/components/ui/{popover,tooltip,slider}.tsx` returns all three files
    - `cat src/main/db/migrations/0007_doctor_profile_language_and_users_language.sql` shows BOTH ALTER TABLE statements (users + doctor_profile) in ONE file
    - `grep -c 'language' src/main/paths.ts` returns 0 (restoreStagingDir is in paths.ts but does not reference language — sanity)
  </acceptance_criteria>
  <done>
    Migration 0007 applied idempotently; backup module end-to-end proven via real archiver round-trip + yauzl unpack + PRAGMA integrity_check; yauzl entry-path filter rejects zip-slip attempts; 3 shadcn primitives installed; archiver + yauzl + i18next deps in package.json.
  </done>
</task>

<task type="auto">
  <name>IPC contract extension + preload bridge + audit.log + profile.update(extends language) + wizard bootstrap(extends language) + users.create(extends language) + main registration</name>
  <read_first>
    - src/shared/ipc-contract.ts (IPC constants + IpcContract type — exact insertion points)
    - src/shared/validators.ts (existing zod schemas to extend)
    - src/preload/index.ts (bridge shape)
    - src/main/ipc/audit.ts (existing AUDIT_LIST handler)
    - src/main/ipc/profile.ts (existing profile.update handler — schema + repo call shape)
    - src/main/ipc/auth.ts (existing wizard handler)
    - src/main/ipc/users.ts (existing users.create handler)
    - src/main/db/audit.ts (audit() helper — exact signature)
    - src/main/db/doctor-profile-repo.ts (upsert input type — extend with language)
    - src/main/db/users.ts (UserRow + create input — extend with language)
    - src/main/auth/index.ts (wizardBootstrap signature — extend with language)
    - src/main/index.ts (IPC registration order)
    - .planning/phases/07-search-history-audit-ui-backup-restore-arabic-rtl/07-CONTEXT.md (Audit hooks section + D-08 + D-17 + D-18)
  </read_first>
  <files>
    - src/shared/ipc-contract.ts
    - src/shared/validators.ts
    - src/preload/index.ts
    - src/main/ipc/audit.ts
    - src/main/ipc/backup.ts
    - src/main/ipc/restore.ts
    - src/main/ipc/profile.ts
    - src/main/ipc/auth.ts
    - src/main/ipc/users.ts
    - src/main/db/audit.ts
    - src/main/db/users.ts
    - src/main/db/doctor-profile-repo.ts
    - src/main/auth/index.ts
    - src/main/index.ts
    - tests/main/ipc/audit.test.ts
    - tests/main/ipc/profile.test.ts
    - tests/main/auth/wizard-bootstrap.test.ts
  </files>
  <action>
    `src/shared/ipc-contract.ts`: extend the IPC constants object with `BACKUP_CREATE: 'backup:create'`, `BACKUP_REVEAL: 'backup:reveal'`, `RESTORE_PREVIEW: 'restore:preview'`, `RESTORE_UNPACK: 'restore:unpack'`, `AUDIT_LOG: 'audit:log'`. Extend the IpcContract interface with the new namespaces:
    - `backup: { create: (input: { destPath: string }) => Promise<{ path: string; sizeBytes: number; procedureCount: number }>; reveal: (input: { path: string }) => Promise<{ ok: true }>; }`
    - `restore: { preview: (input: { zipPath: string; stagingDir: string }) => Promise<RestorePreview>; unpack: (input: { zipPath: string; stagingDir: string }) => Promise<{ fileCount: number; stagingDir: string }>; }` where `RestorePreview = { filename: string; totalSize: number; dbIntegrityCheck: string; procedureCount: number }` — declare this type at the bottom of ipc-contract.ts.
    - `audit: { log: (input: { action: string; entityType?: string; entityId?: string; metadata?: Record<string, unknown> }) => Promise<{ ok: true }>; }`
    Extend `profile.update` input with `language?: 'en' | 'ar' | null`. Extend `auth.wizard` input (WizardSubmitInput) with `language?: 'en' | 'ar'` (default 'en'). Extend `users.create` input with `language?: 'en' | 'ar'` (default 'en').

    `src/shared/validators.ts`: extend `wizardInput` with `language: z.enum(['en', 'ar']).optional()` (default 'en'). Add `userInput` extension with `language: z.enum(['en', 'ar']).optional()`. Extend `doctorProfileUpdateSchema` with `language: z.enum(['en', 'ar']).nullable().optional()`. Add new `auditLogInput = z.object({ action: z.string().min(1).max(120), entityType: z.string().min(1).max(60).optional(), entityId: z.string().max(120).optional(), metadata: z.record(z.unknown()).optional() }).strict()`. Add `backupCreateInput = z.object({ destPath: z.string().min(1).max(2000) }).strict()`. Add `restorePreviewInput = z.object({ zipPath: z.string().min(1).max(2000), stagingDir: z.string().min(1).max(2000) }).strict()`. Add `restoreUnpackInput = z.object({ zipPath: z.string().min(1).max(2000), stagingDir: z.string().min(1).max(2000) }).strict()`.

    `src/preload/index.ts`: extend the `api` object with `backup.create`, `backup.reveal`, `restore.preview`, `restore.unpack`, `audit.log` as `ipcRenderer.invoke(IPC.X, input)` wrappers. Extend `profile.update`, `auth.wizard`, `users.create` payloads to forward the new fields (signature is unchanged).

    `src/main/ipc/audit.ts`: register `IPC.AUDIT_LOG` handler — calls `auditLogInput.parse(raw)` then `audit({ action, entityType, entityId, userId: requireSession(), metadata })`. Returns `{ ok: true }`. Add a `requireSession()` helper at module top (copy from patients.ts).

    `src/main/ipc/profile.ts`: extend the existing handler to forward `language` from the parsed input to `doctorProfileRepo.upsert({ ...input, language: input.language ?? null })`. Update `doctorProfileRepo.upsert` input type accordingly.

    `src/main/ipc/auth.ts`: extend `AUTH_WIZARD` handler to forward `language` (default 'en' if undefined) to `wizardBootstrap(parsed)`. Update `wizardBootstrap` in `src/main/auth/index.ts` to INSERT the users row with `language` column populated.

    `src/main/ipc/users.ts`: extend the existing `users.create` handler to accept `language?: 'en'|'ar'` and persist via `userRepo.create({ ..., language })`. Extend `userRepo.create` + `UserRow` to carry `language` (default 'en').

    `src/main/ipc/backup.ts` (new): register `IPC.BACKUP_CREATE` + `IPC.BACKUP_REVEAL`. BACKUP_CREATE parses `backupCreateInput` then calls `createBackup({ destZipPath })` and emits `audit({ action: 'backup.created', entityType: 'backup', entityId: path.basename(destZipPath), metadata: { path: destZipPath, sizeBytes, procedureCount } })`. BACKUP_REVEAL parses `{ path: z.string().min(1) }` then calls `revealBackup(path)`. On any thrown error from createBackup, emit `audit({ action: 'backup.failed', outcome: 'failed', metadata: { error } })` and re-throw.

    `src/main/ipc/restore.ts` (new): register `IPC.RESTORE_PREVIEW` + `IPC.RESTORE_UNPACK`. PREVIEW parses `restorePreviewInput` then calls `previewRestore(...)` and emits `audit({ action: 'restore.previewed', metadata: { zipPath, stagingDir, contents: { procedureCount } } })`. UNPACK parses `restoreUnpackInput` then calls `unpackRestore(...)` and emits `audit({ action: 'restore.completed', metadata: { stagingDir, integrityCheck, fileCount } })`. On throw, emit `restore.failed` audit row with error message and re-throw.

    `src/main/db/audit.ts`: no surface change to the `audit()` helper (it already accepts `metadata?: Record<string, unknown> | null`). The `auditLogInput` validator enforces Record<string, unknown> at the IPC boundary; the helper JSON.stringifies.

    `src/main/index.ts`: add `import { registerBackupIpc } from './ipc/backup'` + `import { registerRestoreIpc } from './ipc/restore'`. Call `registerBackupIpc()` + `registerRestoreIpc()` AFTER `registerAuthIpc()` (and after `registerProfileIpc` / `registerReportsIpc` per the existing ordering — see line 77-78) so `requireSession()` resolves.

    Tests:
    - `tests/main/ipc/audit.test.ts` (new): assert `audit.log({ action: 'audit_view', entityType: 'audit' })` appends one audit_log row + count increments by 1. Assert `audit.log({ action: 'backup.created' })` with metadata object round-trips through (metadata is stored as JSON string).
    - `tests/main/ipc/profile.test.ts` (extend existing): add a test that calls `profile.update({ language: 'ar', fullNameEn: 'Dr. X', clinicNameEn: 'Clinic' })` then `profile.get()` returns `language: 'ar'`.
    - `tests/main/auth/wizard-bootstrap.test.ts` (extend existing): add a test that calls `wizard({ fullName, clinicName, pin, confirmPin, language: 'ar' })` then asserts the users row has `language = 'ar'`.
  </action>
  <acceptance_criteria>
    - `npm run typecheck` passes
    - `npm run test:unit -- tests/main/ipc/audit.test.ts` passes — audit.log writes one row
    - `npm run test:unit -- tests/main/ipc/profile.test.ts` passes — language field persists
    - `npm run test:unit -- tests/main/auth/wizard-bootstrap.test.ts` passes — wizard with language='ar' creates user with language='ar'
    - `grep -c 'BACKUP_CREATE\\|BACKUP_REVEAL\\|RESTORE_PREVIEW\\|RESTORE_UNPACK\\|AUDIT_LOG' src/shared/ipc-contract.ts` returns >= 5
    - `grep -c 'registerBackupIpc\\|registerRestoreIpc' src/main/index.ts` returns >= 2
    - `grep -c 'language' src/shared/validators.ts` returns >= 3 (wizardInput, userInput, doctorProfileUpdateSchema)
    - All existing tests still pass (no regressions)
  </acceptance_criteria>
  <done>
    Backup/restore + audit.log + language-field IPC contracts wired end-to-end through preload → main → audit() helper; profile.update + wizard + users.create persist language; backup/restore IPC handlers register in main/index.ts after auth; all contract-guard tests green; no regressions.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Renderer → main IPC | All backup/restore/audit/log/profile/wizard inputs cross here; zod schema validates every payload |
| Main → filesystem | Backup writes arbitrary userData files; restore writes arbitrary files within stagingDir |
| Main → better-sqlite3 → filesystem | db.backup() reads the live DB; integrity_check reads the staged DB |
| Main → Electron shell | revealBackup invokes shell.showItemInFolder |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07-01 | Tampering | src/main/backup/restore.ts (yauzl entry path filter) | high | mitigate | D-15 verbatim: safeEntryPath rejects absolute paths + .. segments + paths resolved outside stagingDir BEFORE createWriteStream. 3 contract-guard tests in restore.test.ts: fixture zip with `../../etc/passwd` entry is skipped + fixture zip with absolute path entry is skipped + valid entry passes. |
| T-07-02 | Tampering | src/main/backup/index.ts (archiver zip contents) | high | mitigate | D-10 verbatim: walCheckpoint(TRUNCATE) THEN db.backup(tempPath) THEN zip the temp file. Temp db file is unlinked in finally after the close event. Test asserts the temp file no longer exists after createBackup resolves. |
| T-07-03 | Information Disclosure | src/main/backup/index.ts (zip includes patient PII) | medium | accept | D-09 verbatim — backup includes `media/patients/<id>/<id>/screenshots/` + `profiles/<userId>/signature.{png,jpg}` + `reports/<reportId>.pdf`. Encryption deferred to v1.1 per REQUIREMENTS.md (out-of-scope explicitly). Acceptable risk for v1 with OS userData ACLs. |
| T-07-04 | Repudiation | src/main/ipc/audit.ts (audit.log channel) | medium | mitigate | Per AUDIT-01 + Phase 2 Fix 6: every audit event routes through the `audit()` helper; no other write paths. Phase 2's audit_log_no_update + audit_log_no_delete triggers reject any UPDATE/DELETE attempts. Test asserts attempting UPDATE on audit_log throws. |
| T-07-05 | Information Disclosure | restore.unpack writes user-controlled filenames to disk | low | accept | safeEntryPath guards the path traversal class; yauzl decodes UTF-8 filenames per zip spec (PITFALLS §Known Threat Patterns "Backup zip with crafted UTF-8 filenames"). |
| T-07-06 | Denial of Service | createBackup zip path is renderer-supplied (any path) | low | mitigate | zod validation: `destPath` is `.string().min(1).max(2000)` — bounded length prevents DoS via path string. Renderer is sandboxed (Phase 1 security baseline) so it cannot pass arbitrary native paths beyond what the user picks via Electron's `dialog.showSaveDialog` in Plan 07-05. |
| T-07-SC | Tampering | npm install for archiver + yauzl + i18next (new deps) | high | mitigate | RESEARCH.md §Package Legitimacy Audit confirms all 6 packages: archiver 12yrs/12M-wk, yauzl 11yrs/25M-wk, i18next 13yrs/9M-wk, react-i18next 9yrs/5M-wk, i18next-browser-languagedetector 8yrs/3M-wk, @types/archiver/yauzl via DefinitelyTyped. All `[VERIFIED]` via npm view 2026-08-10. No [ASSUMED]/[SUS] — no blocking-human checkpoint required. |

**Defense-in-depth summary:**
1. **Path traversal** (T-07-01): yauzl entry filename normalized to forward slashes, rejected if absolute / contains `..` / resolved path is outside stagingDir. Mirrors Phase 5 P04 ALLOWED_SUBDIRS pattern.
2. **Partial DB** (T-07-02): WAL checkpoint ensures main DB file is coherent before db.backup().
3. **Append-only audit** (T-07-04): SQLite triggers reject UPDATE/DELETE on audit_log; all writes route through `audit()` helper.
4. **Input validation** (T-07-06): zod schemas bound every renderer-supplied string length; zod parse error → IPC_VALIDATION → renderer surface.
</threat_model>

<verification>
- `npm run typecheck` — both node + web, no errors
- `npm run test:unit -- tests/main/db/migrations.test.ts tests/main/backup/snapshot.test.ts tests/main/backup/index.test.ts tests/main/backup/restore.test.ts tests/main/ipc/audit.test.ts tests/main/ipc/profile.test.ts tests/main/auth/wizard-bootstrap.test.ts` — all green
- `npm run test:unit` — full suite (target: 600+ tests across ~80 files, baseline 559/72 from Phase 6)
- Visual: `cat src/main/db/migrations/0007_doctor_profile_language_and_users_language.sql | head -20` shows both ALTER TABLEs
</verification>

<success_criteria>
1. Migration 0007 applied on next launch; existing DBs pick up new `users.language` + `doctor_profile.language` columns without data loss
2. `backup.create({ destPath })` streams a coherent zip containing `app.db` + `media/` + `profiles/` + `reports/`
3. `restore.unpack({ zipPath, stagingDir })` streams the zip into staging dir ONLY; active `data/` is byte-identical before/after
4. `restore.preview({ zipPath, stagingDir })` returns `dbIntegrityCheck` string from `PRAGMA integrity_check`
5. `audit.log` writes one row via `audit()` helper
6. `profile.update({ language: 'ar' })` persists to `doctor_profile.language`
7. Wizard + users.create accept `language` and persist to `users.language`
8. shadcn popover/tooltip/slider installed; typecheck passes
</success_criteria>

<output>
Create `.planning/phases/07-search-history-audit-ui-backup-restore-arabic-rtl/07-01-SUMMARY.md` when done.
</output>
