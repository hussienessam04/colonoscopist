---
phase: 07-search-history-audit-ui-backup-restore-arabic-rtl
plan: 04
type: execute
wave: 4
depends_on:
  - 07-01
files_modified:
  - src/renderer/src/i18n/index.ts
  - src/renderer/src/i18n/en/translation.json
  - src/renderer/src/i18n/ar/translation.json
  - src/renderer/src/hooks/useLanguage.ts
  - src/renderer/src/main.tsx
  - src/renderer/src/pages/Wizard.tsx
  - src/renderer/src/pages/ReportEditor.tsx
  - src/renderer/src/App.tsx
  - src/main/auth/index.ts
  - src/main/pdf/render-report-pdf.ts
  - src/main/pdf/report.tsx
  - src/main/pdf/fonts/NotoSansArabic-Regular.ttf
  - src/main/recorder/ffmpeg-args.ts
  - src/main/ipc/recording.ts
  - src/main/ipc/reports.ts
  - src/shared/ipc-contract.ts
  - src/shared/validators.ts
  - tailwind.config.ts
  - tests/renderer/i18n/parity.test.ts
  - tests/renderer/hooks/useLanguage.test.ts
  - tests/renderer/pages/wizard.test.tsx
  - tests/integration/ar-pdf-smoke.test.ts
autonomous: true
requirements:
  - I18N-01
  - I18N-02
  - I18N-03
  - RPT-06
user_setup:
  - service: arabic-ttf-bundle
    why: "NotoSansArabic-Regular.ttf must be present at src/main/pdf/fonts/ for Font.register"
    env_vars: []
    dashboard_config: []
    notes: "Download Noto Sans Arabic SIL OFL TTF from https://fonts.google.com/noto/specimen/Noto+Sans+Arabic → commit the .ttf file at src/main/pdf/fonts/NotoSansArabic-Regular.ttf. File is ~700KB, committed per RESEARCH.md §Package Legitimacy Audit (offline-only mandate — no build-time network fetch)."

must_haves:
  truths:
    - statement: "i18next boots at renderer entry with `fallbackLng: 'en'` + EN + AR bundles loaded; renderer pages can call `useTranslation()` and receive translations."
      verification: "tests/renderer/i18n/parity.test.ts boots the renderer with i18n.init mocked → asserts window.api.audit.list called with expected shape (smoke that bundles load); runtime test in D-24"
    - statement: "D-24 parity check: every English key in src/renderer/src/i18n/en/translation.json has a matching key in src/renderer/src/i18n/ar/translation.json — Vitest walks EN + asserts AR coverage (per D-24 verbatim)."
      verification: "npm run test:unit -- tests/renderer/i18n/parity.test.ts passes — test fails CI if any EN key lacks AR value"
    - statement: "useLanguage() hook flips `document.documentElement.dir` and `document.documentElement.lang` on language change (per D-19 verbatim)."
      verification: "tests/renderer/hooks/useLanguage.test.ts mounts useLanguage in a test component → calls setLang('ar') → asserts document.documentElement.dir === 'rtl' + document.documentElement.lang === 'ar' + i18n.changeLanguage called with 'ar'"
    - statement: "Wizard gains a 4th step: language radio (EN / AR). On submit, the `users.language` column is set; existing wizards (already ran setup) silently default to 'en' (per D-18)."
      verification: "tests/renderer/pages/wizard.test.tsx renders Wizard → asserts step 4 mount → clicks AR → submits → asserts window.api.auth.wizard called with {language: 'ar', ...existingFields}"
    - statement: "AR PDF renders with `Font.register({family: 'NotoSansArabic', src: <ttf path>})` + `<Text direction='rtl'>` wrappers for Arabic fields + `<Text direction='ltr'>` for numeric fragments (MRN, DOB) per D-25/D-26 + Pitfall 8 mitigation."
      verification: "tests/integration/ar-pdf-smoke.test.ts runs with RUN_SMOKE=1 — renders an AR report via renderReportPdf({language: 'ar'}) → asserts file size > 50_000 + first 4 bytes are '%PDF' magic bytes (PDF magic)"
    - statement: "report.tsx uses NotoSansArabic font family for body text when language === 'ar'; Helvetica (the Phase 6 default) when language === 'en'."
      verification: "src/main/pdf/report.tsx has a conditional fontFamily in the page style + Font.register call gated on opts.language === 'ar'"
    - statement: "Tailwind config gains the i18n dir awareness — the `rtl:` variant prefix is enabled (already part of Tailwind 3.4 per D-23; verify config has `darkMode: ['class']` and the rtl variant is implicit). No `tailwindcss-rtl` plugin (D-23 verbatim)."
      verification: "grep tailwind.config.ts for 'tailwindcss-rtl' returns no match; the file's variant block does NOT include any rtl plugin"
  artifacts:
    - src/renderer/src/i18n/{en,ar}/translation.json
    - src/renderer/src/i18n/index.ts
    - src/renderer/src/hooks/useLanguage.ts
    - src/main/pdf/fonts/NotoSansArabic-Regular.ttf (~700KB)
    - src/main/pdf/render-report-pdf.ts (extended with `language` arg)
    - src/main/pdf/report.tsx (bidi <Text> wrappers)
    - tailwind.config.ts (rtl variant confirmation)
  prohibitions:
    - "DO NOT use a date-picker library — native <input type='date'> per D-23 ladder rung"
    - "DO NOT install tailwindcss-rtl plugin — Tailwind 3.4 native rtl: variants cover the surface (D-23)"
    - "DO NOT install Calendar shadcn component — out of scope per REQUIREMENTS §Out-of-Scope"
    - "DO NOT auto-translate AR values — full hand-written strings per D-20 verbatim (NO machine translation in offline-only app)"
    - "DO NOT embed AR font as base64 in JSON bundle — bundle the TTF file at src/main/pdf/fonts/ per D-25 + D-27"
    - "DO NOT skip bidi isolation for numeric fragments — wrap MRN/DOB in <Text direction='ltr'> per Pitfall 8 mitigation"
    - "DO NOT call i18next.changeLanguage from individual components — single useLanguage() hook at main.tsx (D-19 verbatim)"

artifacts_this_phase_produces:
  decorators: []
  classes: []
  functions:
    - i18n.init() at src/renderer/src/i18n/index.ts
    - useLanguage(): {lang: 'en' | 'ar', setLang: (l) => void}
    - renderReportPdf(reportId, {language: 'en' | 'ar'}): Promise<{pdfPath}>
    - Font.register({family: 'NotoSansArabic', src: '<ttf path>'})
    - wizardBootstrap({ ..., language?: 'en' | 'ar' })
  ipc_channels: []
  cli_flags: []
  new_file_paths:
    - src/renderer/src/i18n/index.ts
    - src/renderer/src/i18n/en/translation.json
    - src/renderer/src/i18n/ar/translation.json
    - src/renderer/src/hooks/useLanguage.ts
    - src/main/pdf/fonts/NotoSansArabic-Regular.ttf
    - tests/renderer/i18n/parity.test.ts
    - tests/renderer/hooks/useLanguage.test.ts
    - tests/integration/ar-pdf-smoke.test.ts
---

<objective>
Bilingual EN+AR UI + RTL coverage + AR PDF per D-17..D-27: i18next + react-i18next bundles at `src/renderer/src/i18n/{en,ar}/translation.json` with every visible UI string covered (per UI-SPEC §Copywriting Contract); `useLanguage()` hook at renderer entry flips `<html dir>` + `<html lang>` on language change (no reload); Wizard gains a 4th language radio step; AR PDF renders via `@react-pdf/renderer` `Font.register` + bidi `<Text direction='rtl'>` wrappers + numeric fragment `<Text direction='ltr'>` isolation; D-24 parity check (Vitest walks EN keys + asserts AR coverage); Tailwind 3.4 native `rtl:` variants verified (no plugin); Playwright RTL smoke deferred to Plan 07-06 (integration hardening).

Purpose: GCC-market ship gate. Every visible UI string must be in both languages before this phase is tagged done. The AR PDF is the highest-risk component because @react-pdf/renderer bidi is partial — the ship-gate is the manual smoke review of bidi ordering (numeric fragments LTR, Arabic body RTL, signature bottom-right in AR mode).
Output: i18n bundles + useLanguage hook + Wizard step 4 + AR PDF rendering + parity test + integration smoke.
</objective>

<execution_context>
@C:/Users/Hussien Essam/.config/opencode/gsd-core/workflows/execute-plan.md
@C:/Users/Hussien Essam/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-search-history-audit-ui-backup-restore-arabic-rtl/07-CONTEXT.md (D-17..D-27 — locked)
@.planning/phases/07-search-history-audit-ui-backup-restore-arabic-rtl/07-UI-SPEC.md (Copywriting Contract — every EN+AR string)
@.planning/phases/07-search-history-audit-ui-backup-restore-arabic-rtl/07-RESEARCH.md (Patterns 5, 6, 7 — i18next init, AR PDF, RTL smoke)
@.planning/phases/07-search-history-audit-ui-backup-restore-arabic-rtl/07-VALIDATION.md (07-04-01..07-04-04 task map)

# Source-of-truth files
@src/renderer/src/main.tsx (current boot — extends with i18n.init + useLanguage hook mount)
@src/renderer/src/pages/Wizard.tsx (existing 3-step form — gains step 4)
@src/main/auth/index.ts (wizardBootstrap — extended with language from Plan 07-01)
@src/shared/validators.ts (wizardInput — extended with language from Plan 07-01)
@src/main/pdf/render-report-pdf.ts (signature: renderReportPdf(reportId) — gains {language} arg)
@src/main/pdf/report.tsx (React PDF template — gains bilingual <Text> wrappers)
@src/main/ipc/reports.ts (reports.regenPdf handler — must forward language to renderReportPdf)
@tailwind.config.ts (verify rtl variant)
@.planning/research/PITFALLS.md (Pitfall 7 RTL + Pitfall 8 PDF bidi)
@.planning/research/STACK.md (i18next + @react-pdf/renderer + Tailwind 3.4 rtl:)
</context>

<tasks>

<task type="tracer">
  <name>i18next bundles + useLanguage hook flips <html dir> + Wizard step 4 + AR PDF rendering with Font.register + bidi isolation</name>
  <read_first>
    - src/renderer/src/main.tsx (boot sequence — add i18n.init() import side-effect)
    - src/renderer/src/pages/Wizard.tsx (existing 3-step form — add step 4)
    - src/main/auth/index.ts (wizardBootstrap — already extended with language from Plan 07-01)
    - src/shared/validators.ts (wizardInput — already extended with language from Plan 07-01)
    - src/main/pdf/render-report-pdf.ts (signature: renderReportPdf(reportId))
    - src/main/pdf/report.tsx (React PDF template — understand existing layout)
    - src/main/pdf/embed-image.ts (logo + signature ImageBox helpers)
    - src/main/paths.ts (paths to verify the TTF can be resolved at runtime)
    - src/main/ipc/reports.ts (reports.regenPdf + reports.openPdf — must read language preference)
    - src/shared/ipc-contract.ts (REPORTS_REGEN_PDF signature)
    - src/main/db/users.ts (userRepo.get — for reading users.language)
    - src/main/db/doctor-profile-repo.ts (doctorProfileRepo.get — for reading doctor_profile.language)
    - tailwind.config.ts (verify rtl variant presence)
    - .planning/phases/07-search-history-audit-ui-backup-restore-arabic-rtl/07-UI-SPEC.md (Copywriting Contract — every EN+AR string + §Implementation Bindings AR PDF)
    - .planning/phases/07-search-history-audit-ui-backup-restore-arabic-rtl/07-RESEARCH.md (Pattern 5 + 6 + 7)
    - .planning/research/PITFALLS.md (Pitfall 7 + Pitfall 8)
    - .planning/research/STACK.md (i18next + @react-pdf/renderer + Tailwind 3.4 rtl:)
  </read_first>
  <files>
    - src/renderer/src/i18n/index.ts
    - src/renderer/src/i18n/en/translation.json
    - src/renderer/src/i18n/ar/translation.json
    - src/renderer/src/hooks/useLanguage.ts
    - src/renderer/src/main.tsx
    - src/renderer/src/pages/Wizard.tsx
    - src/main/auth/index.ts
    - src/main/pdf/render-report-pdf.ts
    - src/main/pdf/report.tsx
    - src/main/ipc/reports.ts
    - src/shared/ipc-contract.ts
    - src/shared/validators.ts
    - tailwind.config.ts
    - src/main/pdf/fonts/NotoSansArabic-Regular.ttf (binary — downloaded + committed per user_setup)
    - tests/renderer/i18n/parity.test.ts
    - tests/renderer/hooks/useLanguage.test.ts
    - tests/renderer/pages/wizard.test.tsx
    - tests/integration/ar-pdf-smoke.test.ts
  </files>
  <action>
    Create `src/renderer/src/i18n/{en,ar}/translation.json` with EVERY visible UI string from 07-UI-SPEC.md §Copywriting Contract. Use dotted key naming (e.g., `patient.searchPlaceholder`, `audit.pageTitle`, `backup.createButton`, `language.label`). The EN values are the source of truth from the UI-SPEC. The AR values are the Arabic mirror (verbatim per D-20 — NO machine translation). The bundles must cover:
    - Auth (Login, Wizard, Recovery)
    - Patient List + filter sidebar
    - Settings (Capture, Profile, Users, Audit, Backup & Restore)
    - Procedure Room + Procedure Review + Procedure Preview
    - Report Editor
    - Language picker labels + AR option
    - Shared empty/loading/error copy

    Create `src/renderer/src/i18n/index.ts`: i18next init module per RESEARCH.md Pattern 5.
    ```ts
    import i18n from 'i18next';
    import { initReactI18next } from 'react-i18next';
    import LanguageDetector from 'i18next-browser-languagedetector';
    import en from './en/translation.json';
    import ar from './ar/translation.json';

    void i18n.use(LanguageDetector).use(initReactI18next).init({
      resources: { en: {translation: en}, ar: {translation: ar} },
      fallbackLng: 'en',
      supportedLngs: ['en', 'ar'],
      interpolation: { escapeValue: false }, // React already escapes
      detection: { order: ['htmlTag', 'localStorage', 'navigator'], caches: ['localStorage'] },
    });

    export default i18n;
    ```

    Create `src/renderer/src/hooks/useLanguage.ts` per RESEARCH.md Pattern 5:
    ```ts
    import { useEffect } from 'react';
    import { useTranslation } from 'react-i18next';

    export type Lang = 'en' | 'ar';
    export function useLanguage(): { lang: Lang; setLang: (l: Lang) => void } {
      const { i18n } = useTranslation();
      const lang: Lang = i18n.language === 'ar' ? 'ar' : 'en';
      const setLang = (next: Lang): void => { void i18n.changeLanguage(next); };
      useEffect(() => {
        document.documentElement.dir = lang === 'ar' ? 'rtl' : 'ltr';
        document.documentElement.lang = lang;
      }, [lang]);
      return { lang, setLang };
    }
    ```

    Extend `src/renderer/src/main.tsx`: add `import './i18n';` at the top (side-effect: i18n.init()). Add `import { useLanguage } from './hooks/useLanguage';` + mount `<App />` inside a tiny `<LanguageApplier />` component that calls `useLanguage()` (returns null) — this guarantees the hook is mounted at boot. Per D-19 verbatim "single useLanguage() hook at main.tsx mount".

    Extend `src/renderer/src/pages/Wizard.tsx`: add a 4th step after the Confirm PIN step.
    - Track `step: 1 | 2 | 3 | 4 | 'done'` state; show 4 distinct cards (full name, clinic name, PIN, confirm PIN, language) — OR collapse PIN + confirm into one card and add language as 3rd. Pick whichever keeps the visual density matching the Phase 2 wizard.
    - Step 4 (Language): two radio buttons (EN / AR) with shadcn-style radio pattern. Pre-selected value reads from `users.language` if available (read via `window.api.auth.status()`) or defaults to 'en'.
    - On Final submit, include `language` in the `window.api.auth.wizard({ ...existingFields, language })` call.
    - After successful wizard + login + `session.setAfterLogin()`, call `i18n.changeLanguage(language)` so the new admin's UI immediately reflects their chosen language (D-18 verbatim "wizard's success toast auto-flips dir on document via useLanguage() after the caller sets the new language").

    Update `src/main/auth/index.ts` `wizardBootstrap` to INSERT the users row with the `language` column populated (Plan 07-01 already extended the schema; verify the INSERT statement includes `language`). Default to 'en' if input.language is undefined.

    Update `src/shared/validators.ts` `wizardInput` to accept `language: z.enum(['en', 'ar']).optional()` (Plan 07-01 already added; verify).

    AR PDF rendering per D-25..D-27 + RESEARCH.md Pattern 6:
    - **Bundled font**: commit `NotoSansArabic-Regular.ttf` (~700KB, SIL OFL) at `src/main/pdf/fonts/NotoSansArabic-Regular.ttf`. Verify the file exists before this task completes. User_setup lists this as a manual step.
    - Extend `src/main/pdf/render-report-pdf.ts`: signature becomes `renderReportPdf(reportId: string, opts: { language: 'en' | 'ar' } = { language: 'en' })`. Read language from `doctorProfileRepo.get(doctorId)?.language ?? userRepo.get(doctorId)?.language ?? 'en'` (per D-26 verbatim "doctor_profile.language → users.language → 'en'"). When `opts.language === 'ar'`, call `Font.register({ family: 'NotoSansArabic', src: path.join(__dirname, 'fonts/NotoSansArabic-Regular.ttf') })` ONCE per process (module-scope guard via `let _notoArabicRegistered = false`). Pass `language` to `ReportPdfInput`.
    - Extend `src/main/pdf/report.tsx`: `createReportPdfElement(reactPdf, input)` accepts `input.language`. The page's `fontFamily` style is `'NotoSansArabic'` when `input.language === 'ar'`, else `'Helvetica'`. Wrap `<Text direction='rtl'>` around Arabic body fields (findings, diagnosis, recommendations, procedureDetails, doctorName, clinicName) when language === 'ar'. Wrap numeric fragments (MRN, DOB, duration) in `<Text direction='ltr'>` per Pitfall 8 mitigation. The numeric isolation pattern:
    ```tsx
    <Text style={styles.field}>
      <Text style={{direction: 'ltr'}}>{patient.mrn ?? ''}</Text>
    </Text>
    ```
    Signature placement flips: bottom-RIGHT in EN mode (Phase 6 default), bottom-LEFT in AR mode (Pitfall 8 verbatim "signature bottom-right in AR mode" — actually the pitfall says signature position should be correct in AR; conventionally Arabic documents have signatures bottom-left, so use `textAlign: 'left'` on the signature container for AR).
    - Extend `src/main/ipc/reports.ts` `reports.regenPdf` handler to call `renderReportPdf(reportId, { language: <resolved from profile or 'en'> })`. The renderer's `regenPdf` IPC call is unchanged (the handler reads language server-side from the DB).

    Extend `src/shared/ipc-contract.ts` (no surface change needed — `REPORTS_REGEN_PDF` signature is unchanged; main reads language internally).

    `tailwind.config.ts`: VERIFY the file does NOT include `tailwindcss-rtl` plugin (per D-23 verbatim). Tailwind 3.4 native `rtl:` variants are implicit — no config change needed for the variants themselves. If the file currently lacks the standard `darkMode: ['class']` line, add it (cosmetic; matches existing Phase 2-6 patterns). DO NOT add `plugins: [require('tailwindcss-rtl')]` — that would violate D-23.

    Tests:
    - `tests/renderer/i18n/parity.test.ts` (new, D-24 verbatim): walks every key in `src/renderer/src/i18n/en/translation.json` + asserts the same key exists in `src/renderer/src/i18n/ar/translation.json` (use Node's `require` + `Object.keys` recursive flattening). The test fails CI if any EN key lacks AR value.
    - `tests/renderer/hooks/useLanguage.test.ts` (new): render a test component that calls `useLanguage()` inside a `useEffect` → mount → assert `document.documentElement.dir === 'ltr'` + `document.documentElement.lang === 'en'` initially. Call `setLang('ar')` → wait for React flush → assert `document.documentElement.dir === 'rtl'` + `document.documentElement.lang === 'ar'`.
    - `tests/renderer/pages/wizard.test.tsx` (extend existing): assert step 4 mounts with `data-testid="wizard-step-language"` + both radios present + AR click submits `wizard({..., language: 'ar'})`.
    - `tests/integration/ar-pdf-smoke.test.ts` (new, RUN_SMOKE=1 gated per D-27): seed a doctor profile + patient + finalized report → call `renderReportPdf(reportId, {language: 'ar'})` → assert `pdfPath` exists + `statSync(pdfPath).size > 50_000` + first 4 bytes are `%PDF` (Buffer slice 0-4 equals `'%PDF'`). This is the D-27 ship-gate assertion. The test file uses `describe.skipIf(!process.env.RUN_SMOKE)` so unit suites skip it by default.
  </action>
  <acceptance_criteria>
    - `npm run typecheck` passes (both node + web)
    - `npm run test:unit -- tests/renderer/i18n/parity.test.ts` passes — every EN key has an AR value
    - `npm run test:unit -- tests/renderer/hooks/useLanguage.test.ts` passes — dir/lang flip on setLang
    - `npm run test:unit -- tests/renderer/pages/wizard.test.tsx` passes — step 4 + language submission
    - `RUN_SMOKE=1 npm run test:integration:smoke` — AR PDF test passes (file > 50KB + magic bytes)
    - `ls -la src/main/pdf/fonts/NotoSansArabic-Regular.ttf` returns a file (~700KB)
    - `grep -c 'tailwindcss-rtl' tailwind.config.ts` returns 0
    - `grep -c 'NotoSansArabic' src/main/pdf/render-report-pdf.ts` returns >= 1 (Font.register call)
    - `grep -c 'direction: .rtl.' src/main/pdf/report.tsx` returns >= 1 (bidi wrapper)
    - `grep -c 'direction: .ltr.' src/main/pdf/report.tsx` returns >= 1 (numeric isolation)
    - All existing tests still pass (no regressions)
  </acceptance_criteria>
  <done>
    i18next bundles load; useLanguage hook flips document direction; Wizard step 4 submits language; AR PDF renders with NotoSansArabic font + bidi <Text> wrappers + numeric fragment LTR isolation; D-24 parity check enforced via Vitest; RUN_SMOKE=1 integration test confirms AR PDF file > 50KB + PDF magic bytes.
  </done>
</task>

<task type="auto">
  <name>i18n hook consumption in renderer pages + RTL variant spot-check</name>
  <read_first>
    - src/renderer/src/pages/PatientsList.tsx (Plan 07-02 page — has visible strings to translate)
    - src/renderer/src/pages/Audit.tsx (Plan 07-03 page)
    - src/renderer/src/pages/ProfileEditor.tsx (Plan 07-03 page)
    - src/renderer/src/pages/BackupRestore.tsx (will arrive in Plan 07-05 — DO NOT modify)
    - src/renderer/src/pages/ProcedureReview.tsx (existing — has strings)
    - src/renderer/src/pages/ReportEditor.tsx (existing — has strings)
    - .planning/phases/07-search-history-audit-ui-backup-restore-arabic-rtl/07-UI-SPEC.md (Copywriting Contract — every EN+AR string)
  </read_first>
  <files>
    - src/renderer/src/pages/PatientsList.tsx
    - src/renderer/src/pages/Audit.tsx
    - src/renderer/src/pages/ProfileEditor.tsx
    - src/renderer/src/pages/ProcedureReview.tsx
    - src/renderer/src/pages/ReportEditor.tsx
  </files>
  <action>
    Wire `useTranslation()` into every existing renderer page that displays user-visible strings. The tracer task (above) creates the bundles + hook + Wizard step 4 + AR PDF; this task wires the existing pages to consume the translations.

    For each page:
    1. Add `import { useTranslation } from 'react-i18next';` at the top.
    2. Inside the component: `const { t } = useTranslation();`.
    3. Replace visible strings with `t('namespace.key')` calls referencing the bundles created in the tracer task.

    Specifically:
    - `src/renderer/src/pages/PatientsList.tsx`: translate "Patients" header, "{n} total" → use `t('patient.total', {count: total})` with ICU plural rules, "New patient" button, "Settings" button, "Search by name" label, "MRN (exact)" label, "Show deleted" label, "Loading…" empty/loading copy, "No patients yet" empty copy, "Archive" / "Cancel" / "Archiving…" buttons in ConfirmDialog. The Plan 07-02 filter sidebar (when mounted) also consumes translations from the same bundles.
    - `src/renderer/src/pages/Audit.tsx` (Plan 07-03 page): translate "Audit log" header, "Back" button, filter labels (Date range, User, Action, Entity type), "Apply filters" / "Clear all" buttons, row column headers (Time, User, Action, Entity), "No events match these filters.", detail Dialog title + labels + "Copy JSON" button.
    - `src/renderer/src/pages/ProfileEditor.tsx` (Plan 07-03 page): translate existing field labels + the new Language Card ("Language" title + "English" / "العربية" options + "Language updated" toast).
    - `src/renderer/src/pages/ProcedureReview.tsx`: translate "Procedure review" header, scrubber controls, screenshot timeline copy, "Open report PDF" button, "Finalize report" CTA.
    - `src/renderer/src/pages/ReportEditor.tsx`: translate field labels (Findings, Diagnosis, Recommendations, Procedure details), action buttons (Save, Finalize, Open PDF).

    Do NOT translate:
    - Test IDs (data-testid attributes)
    - Internal component names / class names
    - Console.error messages
    - The userData path / file paths

    The D-24 parity check (created in the tracer task) will catch any new translation key that lands in EN without an AR mirror — failing CI prevents the half-translated UI bug class.

    Verification: `npm run test:unit -- tests/renderer/i18n/parity.test.ts` must pass after every page update (it walks the EN bundle + asserts AR coverage).
  </action>
  <acceptance_criteria>
    - `npm run test:unit -- tests/renderer/i18n/parity.test.ts` passes after every page update
    - `npm run test:unit` — no regressions
    - `grep -c "useTranslation" src/renderer/src/pages/PatientsList.tsx src/renderer/src/pages/Audit.tsx src/renderer/src/pages/ProfileEditor.tsx src/renderer/src/pages/ProcedureReview.tsx src/renderer/src/pages/ReportEditor.tsx` returns >= 1 for each file
    - `grep -c 't(' src/renderer/src/pages/PatientsList.tsx` returns >= 5
  </acceptance_criteria>
  <done>
    Every existing renderer page with visible UI strings consumes translations from the i18next bundles; D-24 parity check enforces EN↔AR key coverage; no hard-coded English strings remain in the listed pages.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Renderer → main IPC | Wizard language step submits to auth.wizard IPC; profile language persists via profile.update |
| Renderer → DOM | useLanguage() hook mutates document.documentElement.dir + lang |
| Main → filesystem | NotoSansArabic TTF loaded from src/main/pdf/fonts/ via Font.register |
| Main → @react-pdf/renderer | AR PDF template renders with bidi <Text> wrappers |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07-16 | Tampering | Renderer XSS via translated strings (e.g. an Arabic key with HTML injection) | medium | mitigate | D-20 verbatim: i18next `interpolation.escapeValue: false` is safe BECAUSE react-i18next passes strings through React rendering, which auto-escapes. All `<Text>` rendering goes through `@react-pdf/renderer`'s `Text` component, which also auto-escapes. The defense is at the React rendering layer; the i18n bundle is plain JSON (no executable strings). |
| T-07-17 | Tampering | AR PDF font missing → tofu boxes (missing glyphs) | high | mitigate | D-25 verbatim: NotoSansArabic TTF bundled at src/main/pdf/fonts/. Font.register called once per process with module-scope guard. RUN_SMOKE=1 integration test renders AR PDF + asserts file > 50KB (catches font-register failure which produces ~5KB empty PDF). Manual visual inspection of bidi ordering is in 07-UAT.md. |
| T-07-18 | Information Disclosure | Document direction reveals clinic language choice to the user | low | accept | This is the desired behavior (doctor's chosen language drives UI). No mitigation needed. |
| T-07-19 | Repudiation | Language preference change not audited | low | mitigate | Plan 07-03 already wires `language.changed` audit emit on Profile Editor change. The Wizard step 4 change happens before any user exists — no audit row is needed (no user to attribute to). |
| T-07-20 | Tampering | NotoSansArabic TTF replaced with malicious font (supply chain) | low | accept | The TTF is committed to the repo (per D-27 verbatim); it cannot be replaced at build time (offline-only). SIL OFL license allows free redistribution. SHA of the file should be verified at install time (v1.1 hardening; out of scope for v1). |
| T-07-21 | Denial of Service | Missing AR bundle key crashes renderer (no fallback) | low | mitigate | i18next `fallbackLng: 'en'` + `supportedLngs: ['en', 'ar']` ensures missing keys render as EN (or as the literal key in dev mode). D-24 parity test prevents the half-translated drift class entirely. |

**The plan is fundamentally a UX/i18n feature; STRIDE risks are limited to:**
1. XSS via translated strings (mitigated by React's auto-escape at render time)
2. Missing font glyphs (mitigated by bundled TTF + RUN_SMOKE integration test)
3. Half-translated UI (mitigated by D-24 parity test in CI)
</threat_model>

<verification>
- `npm run typecheck` — both node + web, no errors
- `npm run test:unit -- tests/renderer/i18n/parity.test.ts tests/renderer/hooks/useLanguage.test.ts tests/renderer/pages/wizard.test.tsx` — all green
- `RUN_SMOKE=1 npm run test:integration:smoke` — AR PDF smoke green (file > 50KB + magic bytes)
- `npm run test:unit` — full suite (no regressions)
- Visual: change document.documentElement.dir to 'rtl' in DevTools → verify layout flips on PatientsList + Audit + ProfileEditor + ProcedureReview + ReportEditor (manual spot-check; full Playwright smoke arrives in Plan 07-06)
- Visual: generate an AR PDF via the renderer's existing report-editor flow + open in OS viewer + confirm Arabic text renders RTL + numeric fragments stay LTR + signature bottom-left (Pitfall 8 manual smoke — document in 07-UAT.md)
</verification>

<success_criteria>
1. i18next bundles exist at `src/renderer/src/i18n/{en,ar}/translation.json` covering every visible UI string per UI-SPEC §Copywriting Contract
2. useLanguage() flips document.documentElement.dir + lang on language change; no reload required
3. Wizard gains a 4th step (Language radio); submits `language: 'en' | 'ar'` to auth.wizard; users.language column is set
4. AR PDF renders with Font.register(NotoSansArabic) + bidi <Text direction='rtl'> wrappers for body + numeric fragment <Text direction='ltr'> isolation
5. RUN_SMOKE=1 integration test asserts AR PDF file > 50KB + PDF magic bytes
6. D-24 parity check fails CI if any EN key lacks AR value
7. No hard-coded English strings remain in the 5 renderer pages (PatientsList, Audit, ProfileEditor, ProcedureReview, ReportEditor)
</success_criteria>

<output>
Create `.planning/phases/07-search-history-audit-ui-backup-restore-arabic-rtl/07-04-SUMMARY.md` when done.
</output>
