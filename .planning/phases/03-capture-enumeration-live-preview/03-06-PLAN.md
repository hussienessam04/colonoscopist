---
phase: 03-capture-enumeration-live-preview
plan: 06
type: execute
wave: 5
depends_on:
  - 03-05
files_modified:
  - src/renderer/src/hooks/useVideoPreview.ts
  - src/renderer/src/components/SettingsSidebar.tsx
  - src/renderer/src/pages/SettingsHub.tsx
  - src/renderer/src/pages/SettingsCapture.tsx
  - src/renderer/src/pages/SettingsUsers.tsx
  - tests/renderer/hooks/use-video-preview.test.ts
  - tests/renderer/components/settings-sidebar.test.tsx
  - tests/renderer/pages/settings-hub.test.tsx
  - tests/renderer/pages/settings-capture.test.tsx
  - tests/renderer/pages/settings-users.test.tsx
  - tests/integration/renderer-main-capture-contract.test.ts
autonomous: true
requirements:
  - CAPT-03
  - SET-01
  - SET-04
gap_closure: true
gap_ids: [G-03-5, G-03-6]
must_haves:
  truths:
    - "ProcedureRoom renders without crashing on open — presetHints() returns { framerate } (no throw) when a stored custom preset has an empty or missing resolution."
    - "A unit test for useVideoPreview asserts the hook mounts and updates without throwing when the supplied custom preset has resolution === '' and when resolution is undefined at runtime."
    - "SettingsHub, SettingsCapture, and SettingsUsers each render the SAME shared sidebar nav with two entries — Capture and Users; the sidebar is visible on every Settings page, not only on settings-hub."
    - "The Capture sidebar button carries data-active=\"true\" when the current route is settings-capture, the Users button carries data-active=\"true\" when the current route is settings-users, and neither is active on the Hub. The active tab uses a visually distinct variant (default vs outline) so the highlight is visible to the user."
    - "Non-admin sessions still see the Users sidebar entry disabled, regardless of which Settings page is mounted; clicking the disabled button never changes the route."
    - "Existing tests for SettingsHub, SettingsCapture, SettingsUsers, and ProcedureRoom keep passing; the new SettingsSidebar component test plus the integration-contract static-analysis assertions pin the contract so a future refactor cannot silently remove the sidebar from the sub-pages."
  artifacts:
    - "src/renderer/src/hooks/useVideoPreview.ts: presetHints() defensive guard — `const raw = preset.resolution?.trim(); if (!raw) return { framerate: preset.framerate };` before the `.split(/[x×]/)` call."
    - "src/renderer/src/components/SettingsSidebar.tsx: new shared component; `<SettingsSidebar activeTab?: 'capture' | 'users' />` renders the Capture + Users nav buttons with `data-active=\"true\" | \"false\"` on each, disables Users when `!currentUser?.isFirstAdmin`, and uses the default variant for the active entry and outline for the inactive entry."
    - "src/renderer/src/pages/SettingsHub.tsx: replaces the inline `<aside>` with `<SettingsSidebar />` (no activeTab); layout flipped to a left-sidebar two-column grid so the sub-pages share the same orientation."
    - "src/renderer/src/pages/SettingsCapture.tsx: renders `<SettingsSidebar activeTab=\"capture\" />` in a three-column grid on large screens (sidebar | preview | controls); the existing preview box and controls aside are unchanged."
    - "src/renderer/src/pages/SettingsUsers.tsx: renders `<SettingsSidebar activeTab=\"users\" />` in a left-sidebar two-column grid; the existing users table (max-w-3xl) is moved into the right column and the existing header Back button is preserved unchanged."
    - "tests/renderer/hooks/use-video-preview.test.ts: two new tests in a new `describe` block — one mounts the hook with `{ preset: 'custom', resolution: '', framerate: 30 }` and asserts no throw; the other mounts it with `{ preset: 'custom', resolution: undefined as unknown as string, framerate: 30 }` and asserts no throw."
    - "tests/renderer/components/settings-sidebar.test.tsx (new): admin/non-admin active-tab coverage, navigation to settings-capture and settings-users, disabled Users click does NOT route, and the data-active attribute is set exactly on the active row."
    - "tests/renderer/pages/settings-hub.test.tsx: existing 8 cases kept; one new case asserts neither sidebar button carries data-active=\"true\" on the Hub (the Hub is the router, not a destination)."
    - "tests/renderer/pages/settings-capture.test.tsx: existing 7 cases kept; one new case asserts the Capture sidebar button carries data-active=\"true\" and the Users button carries data-active=\"false\"."
    - "tests/renderer/pages/settings-users.test.tsx: existing cases kept; one new case asserts the Users sidebar button carries data-active=\"true\" (admin) and the Capture button carries data-active=\"false\"."
    - "tests/integration/renderer-main-capture-contract.test.ts: two new describe blocks — (a) G-03-5: useVideoPreview source contains a defensive guard for empty custom resolution (regex assertion that the `.split(/[x×]/)` call is wrapped by a non-empty trim check); (b) G-03-6: SettingsSidebar source exists, exports SettingsSidebar, and the three settings pages (SettingsHub, SettingsCapture, SettingsUsers) each import and render <SettingsSidebar />."
  key_links:
    - "useVideoPreview.ts presetHints() defensive guard → unblocks ProcedureRoom open (G-03-5) → all 5 blocked UAT items (empty state, Start/Stop/Finish, override picker, Finish back-route, permission flow) become reachable for human UAT."
    - "SettingsSidebar component (capture highlight wired to activeTab prop) → mounted on SettingsHub (no activeTab), SettingsCapture (activeTab='capture'), SettingsUsers (activeTab='users') → sidebar always visible on every Settings page (G-03-6)."
    - "SettingsSidebar active-tab highlight uses data-active attribute → assertion pattern locked by the new SettingsSidebar component test + the SettingsCapture/SettingsUsers page tests → future refactor cannot silently drop the highlight."
    - "Integration contract test (USE_VIDEO_PREVIEW_SRC + new SETTINGS_SIDEBAR_SRC fixture) → static-analysis pin that survives a refactor that swaps the inline `<aside>` for the component but forces the new component to be wired on all three pages."
---

<objective>
Close the two diagnosed UAT gaps — G-03-5 (ProcedureRoom crashes on open because `useVideoPreview.presetHints()` dereferences `preset.resolution.split(...)` without guarding against an empty/missing resolution) and G-03-6 (the Settings sub-pages lose the sidebar nav once the doctor navigates to `settings-capture` or `settings-users` because the sidebar lives only on the inline `settings-hub` page). Both fixes are renderer-only; the existing IPC, settings, and audit code paths are untouched.

Purpose: Get the live preview back (G-03-5) so the doctor can actually open the Procedure Room and the blocked hardware-dependent UAT items become reachable, and keep the sidebar nav visible on every Settings page so the doctor can always see which section is active and switch back to the other (G-03-6). The two gaps are independent and run in the same wave in parallel.

Output: A defensive `presetHints()` guard in `useVideoPreview.ts` with a regression test, a new shared `SettingsSidebar` component, the three settings pages refactored to use it, a new `SettingsSidebar` component test, an active-tab assertion in each of the three page tests, and a small static-analysis extension to the cross-process integration contract that pins both the defensive guard and the per-page sidebar mounting.
</objective>

<execution_context>
@C:/Users/Hussien Essam/.config/opencode/gsd-core/workflows/execute-plan.md
@C:/Users/Hussien Essam/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-capture-enumeration-live-preview/03-UAT.md
@.planning/phases/03-capture-enumeration-live-preview/03-05-PLAN.md
@.planning/phases/03-capture-enumeration-live-preview/03-02-SUMMARY.md
@.planning/phases/03-capture-enumeration-live-preview/03-05-SUMMARY.md
@src/renderer/src/hooks/useVideoPreview.ts
@src/renderer/src/pages/SettingsHub.tsx
@src/renderer/src/pages/SettingsCapture.tsx
@src/renderer/src/pages/SettingsUsers.tsx
@src/renderer/src/App.tsx
@src/renderer/src/lib/router.ts
@src/renderer/src/store/session.ts
@src/renderer/src/components/ui/button.tsx
@src/renderer/src/components/ui/card.tsx
@tests/renderer/hooks/use-video-preview.test.ts
@tests/renderer/pages/settings-hub.test.tsx
@tests/renderer/pages/settings-capture.test.tsx
@tests/renderer/pages/settings-users.test.tsx
@tests/renderer/pages/procedure-room.test.tsx
@tests/renderer/components/patient-row.test.tsx
@tests/integration/renderer-main-capture-contract.test.ts
@tests/renderer/setup.ts
@.opencode/skills/electron-vite/SKILL.md
</context>

<interfaces>
<!-- Key types/contracts the executor needs; extracted once from existing source. -->

From `src/renderer/src/hooks/useVideoPreview.ts` (the bug site):
```ts
function presetHints(preset?: QualityPreset): {
  resolution?: [number, number];
  framerate?: number;
} {
  if (!preset) return {};
  if (preset.preset === 'sd') return { resolution: [720, 480], framerate: 30 };
  if (preset.preset === 'hd') return { resolution: [1920, 1080], framerate: 30 };
  // Line 35 — the unguarded `.split(/[x×]/)` is the crash site when
  // `preset.resolution` is undefined or empty.
  const [width, height] = preset.resolution.split(/[x×]/).map(Number);
  return width && height
    ? { resolution: [width, height], framerate: preset.framerate }
    : { framerate: preset.framerate };
}
```

From `src/shared/ipc-contract.ts` (QualityPreset type — read-only; no plan-time change):
```ts
export type QualityPreset =
  | { preset: 'sd' }
  | { preset: 'hd' }
  | { preset: 'custom'; resolution: string; framerate: number };
// The TS contract says `resolution` is a required string for `{ preset: 'custom' }`,
// but the runtime payload from `presetRepo.getPreset()` may carry undefined/empty
// when a stored row is corrupt or the validator drift is exploited.
```

From `src/renderer/src/pages/SettingsHub.tsx` (the sidebar to extract):
The inline `<aside>` at the bottom of the file becomes `<SettingsSidebar />` (no activeTab) inside the same grid cell. The two existing `data-testid` values (`settings-hub-capture`, `settings-hub-users`) MUST be preserved by the new component so the existing test suite keeps passing.

From `src/renderer/src/pages/SettingsCapture.tsx` (existing layout — PRE-REFACTOR):
Header: "Settings" breadcrumb + "Capture" title + Back Button → patients. Below header: `<div className="grid gap-5 lg:grid-cols-[minmax(0,1fr)_20rem]">` with preview box on the left and controls aside on the right. After refactor: 3-column grid on lg — sidebar | preview | controls.

From `src/renderer/src/pages/SettingsUsers.tsx` (existing layout — PRE-REFACTOR):
Header: "Settings · Users" + Back to patients Button + Add user Dialog. Body: single-column users table. After refactor: 2-column grid on lg — sidebar | users table card.

From `tests/renderer/setup.ts` (per-test mock infrastructure):
Each test installs a fresh `window.api` mock via `mockApi()` in `beforeEach`, resets the route to `initialRoute`, and calls `session.reset()`. The cleanup hook restores all mocks.

From `tests/renderer/pages/settings-hub.test.tsx` (existing assertions to preserve):
8 cases covering admin/non-admin, sidebar entries, Back header, click-to-route, disabled-Users stays put. The new sub-page sidebar must NOT break these — the existing `data-testid="settings-hub-capture"` and `data-testid="settings-hub-users"` selectors must continue to find the buttons.

From `src/renderer/src/components/ui/button.tsx` (variant used by SettingsSidebar):
The existing `variant: { default: 'bg-primary text-primary-foreground …', outline: 'border border-input bg-background …' }` is the visual signal. `activeTab === 'capture' | 'users'` → `variant="default"`; else `variant="outline"`. This is the colored highlight the user asked for.

From `src/renderer/src/store/session.ts` (gate referenced by SettingsSidebar):
`const { currentUser } = useSession(); const isAdmin = currentUser?.isFirstAdmin ?? false;` — the existing gate is reused; the new component does not introduce a new role check.

From `src/renderer/src/lib/router.ts` (route variants — read-only):
The `Route` union already declares `settings-hub`, `settings-users`, `settings-capture`. No route variant changes in this plan; the new SettingsSidebar only consumes the existing variants.
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Guard presetHints() against empty/missing custom resolution and pin the contract with a regression test (G-03-5)</name>
  <files>src/renderer/src/hooks/useVideoPreview.ts, tests/renderer/hooks/use-video-preview.test.ts, tests/integration/renderer-main-capture-contract.test.ts</files>
  <read_first>
    - src/renderer/src/hooks/useVideoPreview.ts
    - src/renderer/src/pages/ProcedureRoom.tsx
    - src/shared/ipc-contract.ts
    - src/shared/validators.ts
    - src/main/capture/presetRepo.ts
    - tests/renderer/hooks/use-video-preview.test.ts
    - tests/integration/renderer-main-capture-contract.test.ts
    - .planning/phases/03-capture-enumeration-live-preview/03-UAT.md
    - .planning/phases/03-capture-enumeration-live-preview/03-02-SUMMARY.md
  </read_first>
  <behavior>
    - useVideoPreview mount with `{ preset: 'custom', resolution: '', framerate: 30 }` does not throw on the initial render or any re-render (regression: today line 35 throws because `''.split` throws TypeError).
    - useVideoPreview mount with `{ preset: 'custom', resolution: undefined as unknown as string, framerate: 30 }` does not throw (corrupt-row payload path).
    - After the fix, the constraints object passed to `getUserMedia` either omits width/height entirely or includes them from a valid preset; it does NOT include NaN values from a failed split.
    - ProcedureRoom-mounted tests in `tests/renderer/pages/procedure-room.test.tsx` keep passing on the existing `{ preset: 'hd' }` happy path.
    - The integration contract test asserts the source between `preset.resolution` and `.split(/[x×]/)` contains a `?.trim()` followed by a falsy return.
  </behavior>
  <action>
    Write the regression test FIRST so it fails on the bug and passes on the fix. In `tests/renderer/hooks/use-video-preview.test.ts`, add a new `describe('useVideoPreview - malformed custom preset (G-03-5)')` block with two tests: (1) `mounts without throwing for an empty custom resolution` - `renderHook(() => useVideoPreview('browser-1', { preset: 'custom', resolution: '', framerate: 30 }))` - assert no throw on mount, `result.current.error` is `null`, `start` is a function. Do NOT call `start()`. (2) `mounts without throwing when custom resolution is undefined at runtime` - same shape with `resolution: undefined as unknown as string`. Same assertions. Run the new tests once to confirm they FAIL on the current source. Do NOT yet modify `useVideoPreview.ts`.
    Then fix `src/renderer/src/hooks/useVideoPreview.ts`. Replace the `custom` branch of `presetHints()` so it reads `const raw = preset.resolution?.trim(); if (!raw) return { framerate: preset.framerate };` BEFORE the `.split(/[x×]/)` call. The existing `const [width, height] = raw.split(/[x×]/).map(Number);` follows immediately after the new guard. Add a one-line inline comment: `// ponytail: defensive guard - corrupt row may carry empty/missing resolution; skip the WxH hint and let the device constrain by framerate alone.`
    Re-run the new tests to confirm they pass; run the full hook test file to confirm the 5 existing tests still pass; run `tests/renderer/pages/procedure-room.test.tsx` to confirm no regression on the `{ preset: 'hd' }` happy path.
    Finally, extend `tests/integration/renderer-main-capture-contract.test.ts`. Add a new `describe('G-03-5 - useVideoPreview guards malformed custom preset')` block right after the Plan 03-05 PatientRow block. The block reads `USE_VIDEO_PREVIEW_SRC` (already imported at top of file) and asserts with the regex `/preset\.resolution[\s\S]{0,80}\?\.trim\([\s\S]{0,200}return\s*\{\s*framerate\s*:\s*preset\.framerate\s*\}/.test(USE_VIDEO_PREVIEW_SRC)` that the defensive guard is present. Also add a positive assertion that the `.split(/[x×]/)` literal still exists.
    Do NOT modify `src/shared/validators.ts` - the existing `qualityPresetSchema` already enforces `z.string().regex(/^\d{2,5}[x×]\d{2,5}$/, ...)` for `{ preset: 'custom' }`. The runtime guard is defense-in-depth for corrupt-row reads from presetRepo. Note this reasoning in the SUMMARY.
  </action>
  <acceptance_criteria>
    - `src/renderer/src/hooks/useVideoPreview.ts` `presetHints()` contains the new `const raw = preset.resolution?.trim(); if (!raw) return { framerate: preset.framerate };` guard before the `.split(/[x×]/)` call.
    - `tests/renderer/hooks/use-video-preview.test.ts` has a new `describe` block with at least 2 tests mounting the hook with empty and undefined resolution; the existing 5 tests keep passing.
    - `tests/renderer/pages/procedure-room.test.tsx` keeps passing (no seed change).
    - `tests/integration/renderer-main-capture-contract.test.ts` has a new `describe('G-03-5 - useVideoPreview guards malformed custom preset')` block with two assertions: the defensive guard regex matches, and the `.split(/[x×]/)` literal still exists.
    - `npm run typecheck:web` exits 0.
    - `npm run test:unit -- tests/renderer/hooks/use-video-preview.test.ts tests/renderer/pages/procedure-room.test.tsx tests/integration/renderer-main-capture-contract.test.ts` exits 0 with the new tests passing.
  </acceptance_criteria>
  <verify>
    <automated>npm run typecheck:web && npm run test:unit -- tests/renderer/hooks/use-video-preview.test.ts tests/renderer/pages/procedure-room.test.tsx tests/integration/renderer-main-capture-contract.test.ts</automated>
  </verify>
  <done>useVideoPreview.ts presetHints() no longer throws on a stored custom preset with an empty or missing resolution; two new tests assert the hook mounts without throwing on both shapes; the integration contract statically asserts the guard is present; the existing 5 hook tests + the ProcedureRoom tests continue to pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extract SettingsSidebar, mount on all three Settings pages, and pin the active-tab highlight with tests + integration contract (G-03-6)</name>
  <files>src/renderer/src/components/SettingsSidebar.tsx, src/renderer/src/pages/SettingsHub.tsx, src/renderer/src/pages/SettingsCapture.tsx, src/renderer/src/pages/SettingsUsers.tsx, tests/renderer/components/settings-sidebar.test.tsx, tests/renderer/pages/settings-hub.test.tsx, tests/renderer/pages/settings-capture.test.tsx, tests/renderer/pages/settings-users.test.tsx, tests/integration/renderer-main-capture-contract.test.ts</files>
  <read_first>
    - src/renderer/src/pages/SettingsHub.tsx
    - src/renderer/src/pages/SettingsCapture.tsx
    - src/renderer/src/pages/SettingsUsers.tsx
    - src/renderer/src/components/ui/button.tsx
    - src/renderer/src/components/ui/card.tsx
    - src/renderer/src/lib/router.ts
    - src/renderer/src/store/session.ts
    - tests/renderer/pages/settings-hub.test.tsx
    - tests/renderer/pages/settings-capture.test.tsx
    - tests/renderer/pages/settings-users.test.tsx
    - tests/renderer/setup.ts
    - tests/integration/renderer-main-capture-contract.test.ts
    - .planning/phases/03-capture-enumeration-live-preview/03-UAT.md
    - .planning/phases/03-capture-enumeration-live-preview/03-05-PLAN.md
  </read_first>
  <behavior>
    - A new `SettingsSidebar` component (`src/renderer/src/components/SettingsSidebar.tsx`) renders the same two nav buttons (Capture + Users) that the old inline `<aside>` on SettingsHub rendered, with the same `data-testid` values (`settings-hub-capture`, `settings-hub-users`) so existing assertions still pass.
    - `<SettingsSidebar activeTab="capture" />` sets `data-active="true"` on the Capture button and `data-active="false"` on the Users button; the Capture button uses `variant="default"` (filled, primary background) and the Users button uses `variant="outline"`.
    - `<SettingsSidebar activeTab="users" />` flips the highlight: `data-active="false"` on Capture, `data-active="true"` on Users; Users uses `variant="default"`, Capture uses `variant="outline"`.
    - `<SettingsSidebar />` (no prop) leaves both buttons at `data-active="false"` and keeps both at `variant="outline"` - the Hub is a router, not a destination.
    - Non-admin sessions always see the Users button disabled on every page that mounts the sidebar (Hub, Capture, Users); clicking the disabled button never changes the route.
    - SettingsHub, SettingsCapture, and SettingsUsers each render exactly one `<SettingsSidebar />` instance; the new sidebar is the ONLY sidebar on all three pages; the existing `data-testid="settings-hub-capture"` and `data-testid="settings-hub-users"` selectors continue to find the buttons from the existing settings-hub test suite.
    - ProcedureRoom and the Patient List are untouched.
  </behavior>
  <action>
    Write the new SettingsSidebar component test FIRST so it fails on the missing component and passes once the component is wired. Create `tests/renderer/components/settings-sidebar.test.tsx` (the directory `tests/renderer/components/` already exists; the patient-row test is there). The new file imports `SettingsSidebar` from `@/components/SettingsSidebar`, `getApi` from `../setup`, `session` from `@/store/session`, `setRoute` + `getRoute` from `@/lib/router`, and `@testing-library/user-event`. The new file has one `describe('SettingsSidebar')` block with at least 6 cases:
    1. `admin: Capture and Users both render enabled with data-active="false"` - set admin session, render `<SettingsSidebar />` (no activeTab), assert both buttons exist and are not disabled, both have `data-active="false"`.
    2. `non-admin: Users is disabled` - set non-admin session, render `<SettingsSidebar activeTab="capture" />`, assert Users button is disabled.
    3. `activeTab="capture" highlights Capture` - render `<SettingsSidebar activeTab="capture" />`, assert Capture button has `data-active="true"` and Users has `data-active="false"`.
    4. `activeTab="users" highlights Users` - render `<SettingsSidebar activeTab="users" />`, assert the inverse.
    5. `admin: clicking Capture calls navigate({ name: 'settings-capture' })` - set admin session, setRoute(patients), render `<SettingsSidebar />`, click Capture button, assert `getRoute().name === 'settings-capture'`.
    6. `non-admin: clicking the disabled Users button does NOT route` - set non-admin session, setRoute(patients), render `<SettingsSidebar activeTab="capture" />`, click Users (disabled), assert `getRoute().name === 'patients'`.
    Run the new test file to confirm it FAILS (SettingsSidebar does not exist yet).
    Then create `src/renderer/src/components/SettingsSidebar.tsx`. The component imports `Shield` + `Video` from `lucide-react`, `Button` from `@/components/ui/button`, `useSession` from `@/store/session`, and `useRoute` from `@/lib/router`. Export `type SettingsTab = 'capture' | 'users'` and a default `SettingsSidebar` function component receiving `{ activeTab }: { activeTab?: SettingsTab }`. The component renders an `<aside className="flex flex-col gap-3 rounded-lg border bg-card p-4 shadow-sm">` containing: an `<p>` "Sections" header (the existing one from SettingsHub), then a Capture `<Button>` with `variant={activeTab === 'capture' ? 'default' : 'outline'}`, `data-testid="settings-hub-capture"`, `data-active={activeTab === 'capture' ? 'true' : 'false'}`, an `<Video>` icon, calling `navigate({ name: 'settings-capture' })`; then a Users `<Button>` with the same `variant`/`data-active` pattern keyed on `activeTab === 'users'`, `data-testid="settings-hub-users"`, `disabled={!isAdmin}`, `title={isAdmin ? 'Manage users' : 'Admin only'}`, a `<Shield>` icon, calling `navigate({ name: 'settings-users' })`. Keep the styles and icons identical to the existing inline `<aside>` on SettingsHub - the only visual change is the active variant.
    Then refactor `src/renderer/src/pages/SettingsHub.tsx` to import `SettingsSidebar` from `@/components/SettingsSidebar` (the new relative path), remove the inline `<aside>` block (the "Sections" header + both buttons), and replace it with `<SettingsSidebar />` (no activeTab - the Hub is the router). Flip the two-column grid layout from `lg:grid-cols-[minmax(0,1fr)_20rem]` to `lg:grid-cols-[20rem_minmax(0,1fr)]` so the sidebar sits on the left, matching the orientation on the sub-pages. Keep the existing header (Back button + "Settings" title) and the welcome Card content unchanged.
    Then refactor `src/renderer/src/pages/SettingsCapture.tsx` to import `SettingsSidebar` from `@/components/SettingsSidebar`. Restructure the existing grid from `lg:grid-cols-[minmax(0,1fr)_20rem]` (preview + controls) to a 3-column grid `lg:grid-cols-[16rem_minmax(0,1fr)_20rem]` so the layout is sidebar | preview | controls on large screens. Render `<SettingsSidebar activeTab="capture" />` as the first grid cell, the existing preview box as the second grid cell, and the existing controls aside as the third grid cell. Keep the existing header (Settings breadcrumb + Capture title + Back to patients Button) unchanged. On smaller screens (below lg), the grid collapses to a single column - the sidebar renders above the preview.
    Then refactor `src/renderer/src/pages/SettingsUsers.tsx` to import `SettingsSidebar` from `@/components/SettingsSidebar`. Restructure the single-column layout (max-w-3xl) into a 2-column grid `lg:grid-cols-[20rem_minmax(0,1fr)]` with `<SettingsSidebar activeTab="users" />` on the left and the existing users table (wrapped in a Card) on the right. Keep the existing header (Settings · Users title + Back to patients Button + Add user Dialog) unchanged. The "Admin only" fallback UI (the `if (!isAdmin)` early return branch) is unchanged - the sidebar is not mounted in that branch.
    Then extend the existing page tests:
    - `tests/renderer/pages/settings-hub.test.tsx`: add one new case `neither sidebar button is active on the Hub` - render `<SettingsHub />`, find both sidebar buttons, assert both have `data-active="false"`. The existing 8 cases are unchanged.
    - `tests/renderer/pages/settings-capture.test.tsx`: add one new case `sidebar mount: Capture is active, Users is not` - render `<SettingsCapture />`, find both sidebar buttons, assert Capture has `data-active="true"` and Users has `data-active="false"`. The existing 7 cases are unchanged.
    - `tests/renderer/pages/settings-users.test.tsx`: add one new case `sidebar mount: Users is active, Capture is not` (admin session) - render `<SettingsUsers />`, find both sidebar buttons, assert Users has `data-active="true"` and Capture has `data-active="false"`. The existing cases are unchanged.
    Run the new SettingsSidebar test file and the three updated page tests to confirm they pass. Run the FULL renderer suite (`tests/renderer`) to confirm no regression on the existing tests.
    Finally, extend the integration contract in `tests/integration/renderer-main-capture-contract.test.ts`. Add a new `describe('G-03-6 - SettingsSidebar is mounted on every Settings page')` block at the end of the file. The block reads `SETTINGS_SIDEBAR_SRC` (a new src fixture: `const SETTINGS_SIDEBAR_SRC = read('src/renderer/src/components/SettingsSidebar.tsx');` - add the new fixture to the top-level src constants block with the others). The block asserts:
    - `SETTINGS_SIDEBAR_SRC` is non-empty and contains `export function SettingsSidebar` (the named export).
    - `SETTINGS_HUB_SRC` (the existing fixture) contains `SettingsSidebar` import and a `<SettingsSidebar` JSX render site.
    - `SETTINGS_CAPTURE_SRC` (the existing fixture) contains `SettingsSidebar` import and a `<SettingsSidebar` JSX render site.
    - `SETTINGS_USERS_SRC` - a new src fixture: `const SETTINGS_USERS_SRC = read('src/renderer/src/pages/SettingsUsers.tsx');` - contains `SettingsSidebar` import and a `<SettingsSidebar` JSX render site.
    - `SETTINGS_SIDEBAR_SRC` contains `data-active` (the active-tab marker is the contract).
    Do NOT remove the existing Plan 03-05 describe blocks (don't break prior contracts).
    Do NOT modify `src/renderer/src/lib/router.ts` (the route variants already exist).
    Do NOT modify `src/renderer/src/store/session.ts` (the admin gate is reused).
  </action>
  <acceptance_criteria>
    - `src/renderer/src/components/SettingsSidebar.tsx` exists, exports `SettingsSidebar` (and a `SettingsTab` type), and `<SettingsSidebar activeTab="capture | users" />` sets `data-active="true"` on the matching button and `data-active="false"` on the other; the active button uses `variant="default"` and the inactive uses `variant="outline"`.
    - `src/renderer/src/pages/SettingsHub.tsx` imports the new `SettingsSidebar` and renders `<SettingsSidebar />` (no activeTab); the inline `<aside>` is removed; the grid layout is flipped to `lg:grid-cols-[20rem_minmax(0,1fr)]`.
    - `src/renderer/src/pages/SettingsCapture.tsx` imports the new `SettingsSidebar` and renders `<SettingsSidebar activeTab="capture" />` inside a 3-column grid `lg:grid-cols-[16rem_minmax(0,1fr)_20rem]`; the preview box and controls aside are unchanged.
    - `src/renderer/src/pages/SettingsUsers.tsx` imports the new `SettingsSidebar` and renders `<SettingsSidebar activeTab="users" />` inside a 2-column grid `lg:grid-cols-[20rem_minmax(0,1fr)]`; the existing admin-only fallback branch is unchanged.
    - `tests/renderer/components/settings-sidebar.test.tsx` (new) has at least 6 cases covering both roles, the activeTab highlight on both directions, navigation, and the disabled-click guard.
    - The three page tests have one new case each asserting the active tab attribute on the mounted sidebar.
    - `tests/integration/renderer-main-capture-contract.test.ts` has a new `describe('G-03-6 - SettingsSidebar is mounted on every Settings page')` block with the four fixture assertions above.
    - `npm run typecheck:web` exits 0.
    - `npm run test:unit -- tests/renderer/components/settings-sidebar.test.tsx tests/renderer/pages/settings-hub.test.tsx tests/renderer/pages/settings-capture.test.tsx tests/renderer/pages/settings-users.test.tsx tests/integration/renderer-main-capture-contract.test.ts` exits 0 with all new tests passing.
  </acceptance_criteria>
  <verify>
    <automated>npm run typecheck:web && npm run test:unit -- tests/renderer/components/settings-sidebar.test.tsx tests/renderer/pages/settings-hub.test.tsx tests/renderer/pages/settings-capture.test.tsx tests/renderer/pages/settings-users.test.tsx tests/integration/renderer-main-capture-contract.test.ts</automated>
  </verify>
  <done>SettingsSidebar is mounted on SettingsHub, SettingsCapture, and SettingsUsers; the active tab carries data-active="true" and uses the default (filled) variant; both sub-pages keep the sidebar visible after navigation; the new component test + the three page tests + the integration contract all pin the contract so a future refactor cannot silently remove the sidebar from any of the three pages.</done>
</task>

## Multi-Source Coverage Audit

| Source | ID | Feature/Requirement | Plan | Status | Notes |
|---|---|---|---|---|---|
| GOAL | — | Doctor can open Procedure Room and see a live preview; Settings nav is visible on every Settings page | 03-06 | COVERED | Both gaps are renderer-only; the existing IPC + settings + audit code paths are untouched. |
| UAT | G-03-5 | ProcedureRoom renders without crashing on open; presetHints() handles malformed custom presets | 03-06 | COVERED | Task 1 - defensive guard + regression test + integration contract. |
| UAT | G-03-6 | Settings sub-pages (SettingsCapture + SettingsUsers) keep the sidebar nav visible with the active tab highlighted | 03-06 | COVERED | Task 2 - shared SettingsSidebar component + per-page mount + 4 active-tab tests. |
| REQ | CAPT-03 | Live preview decoupled from recorder | 03-06 | COVERED | Task 1 unblocks ProcedureRoom render so the previously-blocked CAPT-03 UAT items become reachable. |
| REQ | SET-01 | Capture device setting is accessible | 03-06 | COVERED | Task 2 keeps the Settings entry visible on every Settings page. |
| REQ | SET-04 | Users admin-gated | 03-06 | COVERED | Task 2 SettingsSidebar disables Users on non-admin sessions regardless of which page is mounted. |
| CONTEXT | D-01..D-11 | Previously implemented capture behavior | Existing 03-01..03-05 | EXCLUDED | No diagnosed gap; this closure does not alter those locked behaviors. |
| RESEARCH | — | Renderer-only fix, no IPC, no schema change | 03-06 | COVERED | All edits are inside `src/renderer/src/` and tests; the existing `qualityPresetSchema` is already strict. |

## Threat Model

### Trust Boundaries

| Boundary | Description |
|---|---|
| Renderer → presetRepo runtime payload | A stored custom preset can carry an empty or missing `resolution` string at runtime (corrupt row, validator drift). The `useVideoPreview` hook reads this payload on every render. |
| Authenticated renderer → Settings hub | A logged-in doctor clicks the PatientsList header Settings button to open the hub. |
| Session role → Settings → Users | `currentUser.isFirstAdmin` controls whether the shared sidebar Users button is enabled across Hub, Capture, and Users. |
| Authenticated renderer → Settings sub-pages | Navigating to `settings-capture` or `settings-users` must keep the sidebar visible (and the active tab highlighted) so the doctor can switch sections. |
| Route lifecycle → OS media handle | ProcedureRoom `Start` / `Stop` / `Finish` / unmount paths must release every `MediaStreamTrack` (already covered by 03-02). |

### STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-3-21 | Denial of Service | `useVideoPreview.presetHints()` | high | mitigate | Defensive guard in `presetHints()`: a stored custom preset with empty/missing `resolution` returns `{ framerate }` instead of throwing TypeError. Verified by the new G-03-5 hook tests + integration contract assertion. |
| T-3-22 | Elevation of Privilege | SettingsSidebar Users button | medium | mitigate | `disabled={!isAdmin}` is enforced inside the shared `SettingsSidebar` component so every page that mounts it (Hub, Capture, Users) inherits the gate. Verified by the admin/non-admin routes in the new component test. |
| T-3-23 | Tampering | SettingsSidebar destination | medium | mitigate | The two nav buttons call `navigate({ name: 'settings-capture' })` and `navigate({ name: 'settings-users' })` with the typed literals; the integration contract pins the destinations on all three pages. |
| T-3-24 | Information Disclosure | SettingsCapture / SettingsUsers no-sidebar state | low | mitigate | Shared `SettingsSidebar` is mounted on all three pages; integration contract asserts the import + render site is present on every page. |
| T-3-SC | Tampering | npm installs | high | mitigate | No package installation occurs; reuse the installed shadcn primitives (Button, Card). |

<verification>
- `npm run typecheck:web` exits 0.
- `npm run test:unit -- tests/renderer/hooks/use-video-preview.test.ts tests/renderer/pages/procedure-room.test.tsx tests/renderer/components/settings-sidebar.test.tsx tests/renderer/pages/settings-hub.test.tsx tests/renderer/pages/settings-capture.test.tsx tests/renderer/pages/settings-users.test.tsx tests/integration/renderer-main-capture-contract.test.ts` exits 0 with all new tests passing.
- `npm run typecheck` exits 0 (both renderer and node projects).
- `npm run test:unit` exits 0 with full suite (197 baseline + ~6 net new: 2 G-03-5 hook tests + 6 SettingsSidebar component tests + 3 page-tests active-tab cases - some overlap with existing assertions).
- `useVideoPreview.ts` contains the `preset.resolution?.trim()` guard before the `.split(/[x×]/)` call.
- `SettingsSidebar` is imported and rendered on SettingsHub, SettingsCapture, and SettingsUsers.
- The active tab carries `data-active="true"` and uses the default variant; the inactive tab carries `data-active="false"` and uses the outline variant.
- Users button is disabled on non-admin sessions regardless of which Settings page is mounted.
</verification>

<success_criteria>
- ProcedureRoom renders without crashing on open when the stored custom preset has an empty or missing resolution; the live preview + Start/Stop/Finish + override picker + Finish back-route + permission flow UAT items become reachable for human UAT.
- SettingsHub, SettingsCapture, and SettingsUsers each render the shared sidebar nav with two entries (Capture + Users); the sidebar is visible on every Settings page, not only on settings-hub.
- The active Settings tab carries `data-active="true"` and uses the default (filled) variant; the inactive tab carries `data-active="false"` and uses the outline variant.
- Non-admin sessions see the Users button disabled on every Settings page; clicking the disabled button never changes the route.
- The new SettingsSidebar component test covers both roles, both highlight directions, navigation, and the disabled-click guard.
- The integration contract statically asserts the runtime guard in `useVideoPreview.ts` and the per-page sidebar mounting on all three pages.
- The full automated test suite + both typecheck projects remain green.
</success_criteria>

<output>
Create `.planning/phases/03-capture-enumeration-live-preview/03-06-SUMMARY.md` when done.
</output>
