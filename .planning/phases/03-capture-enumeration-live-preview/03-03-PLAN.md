---
phase: 03-capture-enumeration-live-preview
plan: 03
type: execute
wave: 2
depends_on:
  - 03-01
  - 03-02
files_modified:
  - .planning/phases/03-capture-enumeration-live-preview/03-VALIDATION.md
  - tests/main/capture/devices.test.ts
  - tests/main/capture/auto-detect-preset.test.ts
  - tests/main/capture/preset-repo.test.ts
  - tests/main/capture/audit-trace.test.ts
  - tests/shell/security-baseline.test.ts
  - tests/integration/renderer-main-capture-contract.test.ts
autonomous: false
requirements:
  - CAPT-01
  - CAPT-02
  - CAPT-03
  - CAPT-10
  - SET-01
  - SET-02
must_haves:
  truths:
    - "The full automated unit suite, both typecheck projects (renderer + node), and `npm run build` all exit 0 after Wave 1 completes."
    - "Security baseline test asserts all five webPreferences flags including `permissions: ['media']` alongside `contextIsolation: true`, `nodeIntegration: false`, `sandbox: true`, `webSecurity: true` (BLOCKER 3)."
    - "Audit-trace test proves session-scoped doctorId (BLOCKER 4), the `capture.no_device` event (BLOCKER 5), and first-save `matched` regex metadata (Q-A)."
    - "Devices test proves the dshow parser excludes the audio section and that CAPT-10 canonicalization round-trips non-ASCII + whitespace edge cases."
    - "Auto-detect and preset-repo tests prove SD-priority, HD fallback, first-read auto-save, manual override persistence, doctor isolation, and device isolation."
    - "Integration test proves the `IpcContract` shapes match the preload bridge exposure and the renderer uses, including the absence of `doctorId` from `setPreset`/`setDefaultDevice` payloads."
    - "Scope guards prove no `MediaRecorder`, no `capture:start`/`capture:stop` IPC channels, no child-process recording handler, and no mp4 output path exist in Phase 3 source."
    - "Windows bench smoke confirms EasyCap and/or HDMI/DVI hardware enumerates and renders a silent live preview from both Procedure Room and Settings → Capture surfaces."
  artifacts:
    - "tests/main/capture/devices.test.ts: dshow parser (video-only) + canonicalization round-trip edge cases"
    - "tests/main/capture/auto-detect-preset.test.ts: SD-priority / HD fallback / matched-regex metadata"
    - "tests/main/capture/preset-repo.test.ts: first-read auto-save / manual override / doctor isolation / device isolation"
    - "tests/main/capture/audit-trace.test.ts: session-scoped doctorId / `capture.no_device` event / first-save `matched` field (BLOCKER 4 + 5 + Q-A)"
    - "tests/shell/security-baseline.test.ts: five-flag webPreferences assertion including `permissions: ['media']` (BLOCKER 3)"
    - "tests/integration/renderer-main-capture-contract.test.ts: `IpcContract` <-> preload bridge <-> renderer usage alignment; payload shape lacks `doctorId`"
    - ".planning/phases/03-capture-enumeration-live-preview/03-VALIDATION.md: integrated automated + UAT status"
  key_links:
    - "Full suite green proves Plan 03-01 main foundation + Plan 03-02 renderer wiring are contract-aligned (BLOCKER 1, 6)."
    - "Windows smoke proves renderer-enumerated browser deviceId resolves a live stream and that the canonical dshow name surfaces in the dropdown."
    - "Security baseline + scope guards preserve the Electron baseline and prevent Phase 4 leakage into Phase 3."
---

<objective>
Run the integrated Phase 3 validation gate: full automated suite, both typecheck projects, production build, the cross-process integration test, and a Windows hardware smoke test. Record validation status without introducing recording behavior.

Purpose: Catch cross-process wiring failures and hardware-only failures that Plan 03-01 / 03-02 unit tests cannot prove — especially the dshow-name ↔ browser-deviceId bridge (BLOCKER 1) and the security-baseline regression check on `permissions: ['media']` (BLOCKER 3).

Output: Green full suite, both typechecks, build, the new integration test, validated UAT smoke evidence, and an updated `03-VALIDATION.md`.
</objective>

<execution_context>
@C:/Users/Hussien Essam/.config/opencode/gsd-core/workflows/execute-plan.md
@C:/Users/Hussien Essam/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/03-capture-enumeration-live-preview/03-CONTEXT.md
@.planning/phases/03-capture-enumeration-live-preview/03-RESEARCH.md
@.planning/phases/03-capture-enumeration-live-preview/03-VALIDATION.md
@.planning/phases/03-capture-enumeration-live-preview/03-01-PLAN.md
@.planning/phases/03-capture-enumeration-live-preview/03-02-PLAN.md
@src/main/window.ts
@src/main/ipc/capture.ts
@src/renderer/src/pages/ProcedureRoom.tsx
@src/renderer/src/pages/SettingsCapture.tsx
@src/shared/ipc-contract.ts
@package.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the moved-from-03-01 tests + integration contract test; assert five-flag security baseline + Phase 3 scope guards</name>
  <files>tests/main/capture/devices.test.ts, tests/main/capture/auto-detect-preset.test.ts, tests/main/capture/preset-repo.test.ts, tests/main/capture/audit-trace.test.ts, tests/shell/security-baseline.test.ts, tests/integration/renderer-main-capture-contract.test.ts</files>
  <read_first>
    - .planning/phases/03-capture-enumeration-live-preview/03-VALIDATION.md
    - src/main/window.ts
    - src/main/ipc/capture.ts
    - src/main/capture/auto-detect-preset.ts
    - src/main/capture/preset-repo.ts
    - src/main/capture/devices.ts
    - src/main/capture/canonicalize.ts
    - src/shared/ipc-contract.ts
    - src/preload/index.ts
    - src/renderer/src/hooks/useVideoPreview.ts
    - src/renderer/src/hooks/useCaptureDeviceMap.ts
    - src/renderer/src/pages/SettingsCapture.tsx
    - src/renderer/src/pages/ProcedureRoom.tsx
    - package.json
  </read_first>
  <action>
    Create the test files moved from Plan 03-01 plus the new integration test:
    1. `tests/main/capture/devices.test.ts` — mock `child_process.spawn` for ffmpeg `-list_devices true -f dshow -i dummy`; assert the parser returns only VIDEO-section entries (no audio between `DirectShow audio devices` and the next blank line); assert names with non-ASCII characters, spaces, zero-width chars, and trailing whitespace round-trip unchanged after canonicalization.
    2. `tests/main/capture/auto-detect-preset.test.ts` — SD patterns `EasyCap`, `USB Video`, `USB2.0 TV`, `CVBS`, `Composite`, `S-Video`; HD patterns `HDMI`, `1080p`, word-boundary `HD`, `Digital`, `DVI`, `UVC`; unknown names return HD; assert the returned `MatchedPattern` is the FIRST matching regex group name.
    3. `tests/main/capture/preset-repo.test.ts` — first-read auto-save materializes `{preset:'sd'|'hd'}` and persists; manual override persists; doctor isolation (two sessions, two doctorIds, same device — independent rows); device isolation (one doctor, two deviceIds — independent rows); custom preset round-trips; invalid values are rejected by zod; canonicalization happens before persistence.
    4. `tests/main/capture/audit-trace.test.ts` — every persistent mutation writes the expected action with canonical `deviceName`; the first-save `matched` field is present per Q-A; the `capture.no_device` handler writes a row with `doctorId: session.userId` and `source: 'renderer-empty-state'` (BLOCKER 5); `setPreset` and `setDefaultDevice` IGNORE any `doctorId` in the payload (BLOCKER 4) and derive from `requireSession()`; an attempted cross-doctor payload is rejected.
    5. `tests/shell/security-baseline.test.ts` — assert all five webPreferences: `contextIsolation: true`, `nodeIntegration: false`, `sandbox: true`, `webSecurity: true`, and `permissions: ['media']` (BLOCKER 3); fail if any is missing or extra security-relevant flags are removed.
    6. `tests/integration/renderer-main-capture-contract.test.ts` — load `src/shared/ipc-contract.ts` types via `tsc --noEmit` against both renderer and node projects; assert `IpcContract.capture` methods match `window.api.capture` exposed in `src/preload/index.ts`; assert `setPreset` and `setDefaultDevice` payload shapes do NOT include a `doctorId` field (BLOCKER 4); assert the renderer does NOT call `setDefaultDevice` or `setPreset` from `ProcedureRoom.tsx` (Settings is the only caller of `setDefaultDevice`); assert `useCaptureDeviceMap` is the only consumer of `mediaDevices.enumerateDevices` and is consumed in both surfaces (BLOCKER 1).
    7. Phase 3 scope guards — assert no `MediaRecorder`, `capture:start`, `capture:stop`, recording child-process handler, or `mp4` output path exists in Phase 3 source (grep-based or AST-based; baseline these as known-bad tokens). Implement per D-08, D-10, D-11; BLOCKER 1, 3, 4, 5; Q-A.
  </action>
  <acceptance_criteria>
    - `tests/main/capture/devices.test.ts` proves video-only parser output and CAPT-10 normalization round-trip edge cases.
    - `tests/main/capture/auto-detect-preset.test.ts` proves SD-priority, HD fallback, and first-regex-group `matched` name.
    - `tests/main/capture/preset-repo.test.ts` proves first-read auto-save, manual override persistence, doctor isolation, device isolation, and zod rejection of invalid input.
    - `tests/main/capture/audit-trace.test.ts` proves session-scoped doctorId (BLOCKER 4), `capture.no_device` event (BLOCKER 5), and first-save `matched` metadata (Q-A).
    - `tests/shell/security-baseline.test.ts` asserts all five webPreferences flags (BLOCKER 3).
    - `tests/integration/renderer-main-capture-contract.test.ts` proves the contract alignment and the absence of `doctorId` in `setPreset` / `setDefaultDevice` payloads.
    - Phase 3 scope guards (in any one of the above or a dedicated `tests/shell/phase3-scope.test.ts` sub-section) prove no recording primitives.
  </acceptance_criteria>
  <verify>
    <automated>npm run test:unit -- tests/main/capture tests/shell/security-baseline.test.ts tests/integration/renderer-main-capture-contract.test.ts</automated>
  </verify>
  <done>All six test files plus the scope guards compile and pass; security baseline and contract integration tests are green; moved tests cover the full CAPT-01 / CAPT-02 / CAPT-10 / SET-01 / SET-02 surface.</done>
</task>

<task type="auto">
  <name>Task 2: Run full automated gates (unit suite + both typechecks + build) and update validation status</name>
  <files>.planning/phases/03-capture-enumeration-live-preview/03-VALIDATION.md</files>
  <read_first>
    - .planning/phases/03-capture-enumeration-live-preview/03-VALIDATION.md
    - package.json
    - src/main/window.ts
    - src/main/ipc/capture.ts
  </read_first>
  <action>
    Run the full unit suite (`npm run test:unit`), both TypeScript projects (`npm run typecheck` which runs renderer + node), and the production build (`npm run build`). If any failure surfaces, fix only within this plan's files (no scope creep into Phase 4). Update `.planning/phases/03-capture-enumeration-live-preview/03-VALIDATION.md` with the automated status, requirement coverage table (CAPT-01, CAPT-02, CAPT-03, CAPT-10, SET-01, SET-02), and `nyquist_compliant: true` + `wave_0_complete: true` once all automated gates pass. Record any sub-process failures verbatim for the UAT step.
  </action>
  <acceptance_criteria>
    - `npm run test:unit` exits 0.
    - `npm run typecheck` exits 0 (both renderer and node projects).
    - `npm run build` exits 0.
    - `03-VALIDATION.md` is updated with automated status and requirement coverage.
  </acceptance_criteria>
  <verify>
    <automated>npm run test:unit && npm run typecheck && npm run build</automated>
  </verify>
  <done>The integrated Phase 3 codebase is type-safe, buildable, and fully tested with the hardened Electron configuration intact; the validation doc reflects the pass.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Windows capture enumeration and live-preview UAT smoke (BLOCKER 1 hardware proof)</name>
  <read_first>
    - .planning/phases/03-capture-enumeration-live-preview/03-VALIDATION.md
    - .planning/ROADMAP.md
    - .planning/REQUIREMENTS.md
    - .planning/phases/03-capture-enumeration-live-preview/03-CONTEXT.md
  </read_first>
  <what-built>Integrated Settings → Capture and Procedure Room preview flow, including ffmpeg DirectShow enumeration (canonical dshow names), browser `mediaDevices.enumerateDevices()` bridging for `getUserMedia` (BLOCKER 1), per-doctor default + per-doctor-per-device preset persistence (session-scoped, BLOCKER 4), reactive Settings preview pane (BLOCKER 6), session-only Procedure Room override, no-device audit hook (BLOCKER 5), previousRoute-aware Finish (Q-B), component-local preview state in Settings (Q-C), and explicit preview lifecycle controls.</what-built>
  <how-to-verify>
    1. On the Windows bench, connect an EasyCap SD capture card and/or HDMI/DVI capture card, launch the app, and authenticate.
    2. Enter Settings → Capture. Confirm the device dropdown shows the canonical dshow display name AND that the live preview opens with the correct browser deviceId (BLOCKER 1). Names with spaces, non-ASCII characters, and trailing whitespace appear cleanly and consistently.
    3. Select the EasyCap device and confirm first-use preset is SD analog 720×480; select HDMI/DVI and confirm first-use preset is HD digital 1920×1080. Select Custom and verify only W×H resolution plus framerate 25/30/50/60 are available.
    4. Change the preset radio while preview is running and confirm the live preview re-opens with the new constraints (BLOCKER 6). Click Save and confirm only Save persists — changing the device/preset and leaving without Save does not mutate settings.
    5. Save a default device/preset, enter Procedure Room, confirm the `Last used: <X>` label, confirm the saved device is selected. Choose another device in the room, start and stop preview, then Finish; confirm the next entry still starts from the Settings-saved default.
    6. With no saved device or with the saved device unplugged, confirm the black inline empty state with Open Settings CTA appears while remaining on Procedure Room; confirm in the audit log that a `capture.no_device` row was written (BLOCKER 5). Confirm Record is visible but disabled and no recording begins.
    7. Navigate away while preview is running and confirm the capture device indicator/light is released.
  </how-to-verify>
  <acceptance_criteria>
    - EasyCap and HDMI/DVI hardware enumerate and render live preview on Windows; the dropdown label matches the dshow name while `getUserMedia` uses the matching browser deviceId (BLOCKER 1).
    - Preview is silent and Start/Stop/Finish lifecycle releases hardware handles.
    - Settings persistence and Procedure Room session-only override match D-01 through D-10.
    - Settings preview re-opens reactively on preset change (BLOCKER 6).
    - Disabled Record remains disabled and no video file is created.
    - `capture.no_device` audit row exists after the empty state renders (BLOCKER 5).
  </acceptance_criteria>
  <verify>
    <automated>npm run test:unit && npm run typecheck</automated>
    <human-check>Complete the Windows smoke steps above; record device classes tested and any failure text in 03-VALIDATION.md. If the dropdown label and the rendered preview come from the same dshow canonical name but different deviceIds, the BLOCKER 1 bridge is correct.</human-check>
  </verify>
  <resume-signal>Type "approved" with device classes tested, or describe hardware/permission failures verbatim for remediation.</resume-signal>
  <done>Human UAT approves real device enumeration, the dshow ↔ browser deviceId bridge (BLOCKER 1), live preview, preset behavior, persistence boundaries, empty state with audit row, disabled Record, and cleanup on both required surfaces, or reports concrete hardware/permission failures for remediation before phase completion.</done>
</task>

</tasks>

<artifacts>
- tests/main/capture/devices.test.ts (moved from 03-01)
- tests/main/capture/auto-detect-preset.test.ts (moved from 03-01)
- tests/main/capture/preset-repo.test.ts (moved from 03-01)
- tests/main/capture/audit-trace.test.ts (moved from 03-01)
- tests/shell/security-baseline.test.ts (extended with five-flag assertion including `permissions: ['media']`)
- tests/integration/renderer-main-capture-contract.test.ts (NEW — contract alignment + BLOCKER 4 payload check)
- .planning/phases/03-capture-enumeration-live-preview/03-VALIDATION.md (updated automated + UAT status)
</artifacts>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Packaged app → Windows capture hardware | OS / device drivers and permissions determine availability. |
| Integrated renderer ↔ main IPC | Cross-process contracts must remain aligned after both Wave 1 plans. |
| Human UAT → release decision | Hardware evidence is required before calling CAPT-01 / CAPT-03 complete (BLOCKER 1 hardware proof). |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-3-13 | Denial of Service | Windows media permission / device lifecycle | high | mitigate | Verify `permissions: ['media']` flag and track cleanup on real hardware; document `NotAllowedError`, `NotFoundError`, `NotReadableError`, or driver failures in `03-VALIDATION.md`. |
| T-3-14 | Tampering | Phase 3 scope boundary | medium | mitigate | Automated negative assertions reject `MediaRecorder`, `capture:start`, `capture:stop`, recording child-process handlers, and mp4 output paths before Phase 4. |
| T-3-15 | Elevation of Privilege | contract drift between renderer and main | high | mitigate | Integration test asserts `IpcContract` ↔ preload bridge ↔ renderer usage alignment and the absence of `doctorId` from `setPreset`/`setDefaultDevice` payloads. |
| T-3-SC | Tampering | npm / published package supply chain | high | mitigate | Re-run package legitimacy / slopcheck against the approved RESEARCH audit; block on any changed `[ASSUMED]` / `[SUS]` package. |
</threat_model>

<verification>
- `npm run test:unit`
- `npm run typecheck`
- `npm run build`
- Human `/gsd-verify-work 3` smoke on EasyCap and HDMI/DVI where available (BLOCKER 1 hardware proof)
- Update `03-VALIDATION.md` frontmatter to `nyquist_compliant: true` and `wave_0_complete: true` only after automated and human gates pass.
</verification>

<success_criteria>
All automated gates pass and a Windows operator confirms device enumeration, the dshow ↔ browser deviceId bridge (BLOCKER 1), silent live preview, reactive Settings preview (BLOCKER 6), persistence boundaries, empty-state audit row (BLOCKER 5), disabled Record, and cleanup on both required surfaces.
</success_criteria>

<output>
Create `.planning/phases/03-capture-enumeration-live-preview/03-03-SUMMARY.md` when done.
</output>
