---
phase: 03-capture-enumeration-live-preview
plan: 04
type: execute
wave: 3
depends_on:
  - 03-02
  - 03-03
files_modified:
  - src/renderer/src/pages/PatientsList.tsx
  - tests/renderer/pages/patients-list.test.tsx
  - tests/integration/renderer-main-capture-contract.test.ts
autonomous: true
requirements:
  - CAPT-01
  - CAPT-03
  - SET-01
gap_closure: true
gap_ids: [G-03-1, G-03-2]
must_haves:
  truths:
    - "Every logged-in doctor can open Settings on the Patient List, choose Capture, and reach the existing Settings → Capture device dropdown and live preview (D-09; G-03-1; G-03-2)."
    - "The Settings menu shows Capture and Users; Capture is always enabled, while Users is enabled only for the first admin according to `useSession().currentUser.isFirstAdmin`."
    - "Renderer component and static-analysis integration tests prevent the Settings → Capture entry point from becoming unreachable again."
  artifacts:
    - "src/renderer/src/pages/PatientsList.tsx: `PatientsList` header Settings `DropdownMenu` with Capture and Users items"
    - "tests/renderer/pages/patients-list.test.tsx: role-aware Settings menu and Capture navigation coverage"
    - "tests/integration/renderer-main-capture-contract.test.ts: static assertion linking PatientsList to `settings-capture`"
  key_links:
    - "Patient List Settings trigger -> `DropdownMenuItem` Capture -> `navigate({ name: 'settings-capture' })` -> existing `SettingsCapture` route"
    - "`useSession().currentUser.isFirstAdmin` -> Users menu item `disabled` state -> `settings-users` navigation"
---

<objective>
Close G-03-1 and G-03-2 by making the existing Settings → Capture page discoverable from the Patient List header for every authenticated doctor, while retaining role-gated access to Settings → Users.

Purpose: Restore the missing user entry point so the doctor can reach the already-built device dropdown and D-09 live preview, then resume the blocked Windows hardware UAT.
Output: One renderer navigation fix plus focused component and static-analysis regression coverage.
</objective>

<execution_context>
@C:/Users/Hussien Essam/.config/opencode/gsd-core/workflows/execute-plan.md
@C:/Users/Hussien Essam/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-capture-enumeration-live-preview/03-CONTEXT.md
@.planning/phases/03-capture-enumeration-live-preview/03-UAT.md
@.planning/phases/03-capture-enumeration-live-preview/03-02-SUMMARY.md
@.planning/phases/03-capture-enumeration-live-preview/03-03-SUMMARY.md
@.opencode/skills/electron-vite/SKILL.md
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Expose Settings → Capture from the Patient List with role-aware menu tests</name>
  <files>src/renderer/src/pages/PatientsList.tsx, tests/renderer/pages/patients-list.test.tsx</files>
  <read_first>
    - src/renderer/src/pages/PatientsList.tsx
    - src/renderer/src/components/ui/dropdown-menu.tsx
    - src/renderer/src/store/session.ts
    - src/renderer/src/lib/router.ts
    - src/renderer/src/pages/SettingsCapture.tsx
    - tests/renderer/pages/patients-list.test.tsx
    - tests/renderer/pages/settings-users.test.tsx
    - .planning/phases/03-capture-enumeration-live-preview/03-CONTEXT.md
    - .planning/phases/03-capture-enumeration-live-preview/03-UAT.md
  </read_first>
  <behavior>
    - Admin session: opening Settings displays menu items named Capture and Users, both enabled.
    - Non-admin session: opening Settings displays Capture enabled and Users disabled.
    - Any authenticated session: selecting Capture calls the existing router with `{ name: 'settings-capture' }`, making the D-09 page reachable and closing G-03-1/G-03-2.
    - Admin session: selecting Users preserves navigation to `{ name: 'settings-users' }`.
  </behavior>
  <action>
    First extend `tests/renderer/pages/patients-list.test.tsx` with deterministic admin and non-admin session helpers that await `session.refresh()` before rendering. Assert the Settings trigger opens a Radix menu containing `menuitem` roles for Capture and Users, assert Users carries the disabled state for a non-admin, and assert selecting Capture changes the state-based route to `settings-capture`; retain coverage for admin Users navigation. Then replace the single Settings button in `PatientsList` with the existing shadcn `DropdownMenu`, `DropdownMenuTrigger`, `DropdownMenuContent`, and `DropdownMenuItem` primitives. Render the existing outline Button through `asChild` as the Settings trigger. Wire Capture to `navigate({ name: 'settings-capture' })` for every logged-in doctor per D-09 and the diagnosed G-03-1/G-03-2 entry-point gap. Wire Users to `navigate({ name: 'settings-users' })` and set its `disabled` prop from the existing `currentUser?.isFirstAdmin` check. Keep this renderer-only: do not change routes, IPC, SettingsCapture, session state, or add dependencies.
  </action>
  <acceptance_criteria>
    - `PatientsList.tsx` imports `DropdownMenu`, `DropdownMenuTrigger`, `DropdownMenuContent`, and `DropdownMenuItem` from `@/components/ui/dropdown-menu` and uses them for the header Settings entry.
    - Opening Settings as an admin renders exactly one Capture menu item and one Users menu item; neither is disabled.
    - Opening Settings as a non-admin renders both items; Capture is enabled and Users has the disabled menu-item state.
    - Clicking Capture routes to `{ name: 'settings-capture' }` for admin and non-admin sessions, making the existing D-09 device picker/live-preview page reachable and resolving G-03-1/G-03-2.
    - Clicking Users as an admin routes to `{ name: 'settings-users' }`; clicking disabled Users as a non-admin does not navigate.
    - `npm run test:unit -- tests/renderer/pages/patients-list.test.tsx` passes.
  </acceptance_criteria>
  <verify>
    <automated>npm run typecheck:web && npm run test:unit -- tests/renderer/pages/patients-list.test.tsx</automated>
  </verify>
  <done>The Patient List Settings control exposes Capture to every authenticated doctor and keeps Users role-gated, with component tests proving labels, enabled states, and destinations.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Pin the Patient List → Settings Capture route in the integration contract and run the full regression gate</name>
  <files>tests/integration/renderer-main-capture-contract.test.ts</files>
  <read_first>
    - tests/integration/renderer-main-capture-contract.test.ts
    - src/renderer/src/pages/PatientsList.tsx
    - src/renderer/src/pages/SettingsCapture.tsx
    - src/renderer/src/App.tsx
    - src/renderer/src/lib/router.ts
    - tests/renderer/pages/patients-list.test.tsx
    - .planning/phases/03-capture-enumeration-live-preview/03-UAT.md
  </read_first>
  <behavior>
    - Static-analysis contract: PatientsList source contains a navigation call whose destination is `settings-capture`.
    - Static-analysis contract: PatientsList continues to reference the existing `settings-users` destination, preserving admin settings access.
    - Full regression: all pre-existing 189 tests plus the new component and integration assertions pass.
  </behavior>
  <action>
    Extend `tests/integration/renderer-main-capture-contract.test.ts` using its existing source-reading/static-analysis style: read `src/renderer/src/pages/PatientsList.tsx` once into a constant and add a focused navigation describe/test that asserts `PatientsList` contains `navigate({ name: 'settings-capture' })`. Also assert the existing `settings-users` destination remains present so the fix does not remove admin access. Keep the test scoped to navigation reachability; do not duplicate the component test's Radix interaction or role assertions. Run the focused tests, then the complete unit suite to prove no regression from the 189-test baseline.
  </action>
  <acceptance_criteria>
    - `renderer-main-capture-contract.test.ts` reads `PatientsList.tsx` and has a named assertion proving Patient List navigation reaches `settings-capture` for G-03-1/G-03-2.
    - The integration test also asserts the `settings-users` route remains reachable from PatientsList source.
    - `npm run test:unit -- tests/integration/renderer-main-capture-contract.test.ts tests/renderer/pages/patients-list.test.tsx` passes.
    - `npm run typecheck` passes.
    - `npm run test:unit` passes with no failures and a total greater than 189 tests.
  </acceptance_criteria>
  <verify>
    <automated>npm run typecheck && npm run test:unit</automated>
  </verify>
  <done>The static contract prevents Settings → Capture from losing its Patient List entry point, and the complete suite passes beyond the existing 189-test baseline.</done>
</task>

</tasks>

## Artifacts this phase produces

- `src/renderer/src/pages/PatientsList.tsx`: existing `PatientsList` symbol updated with a role-aware Settings `DropdownMenu`.
- `tests/renderer/pages/patients-list.test.tsx`: admin/non-admin menu-state and Capture/Users navigation test cases.
- `tests/integration/renderer-main-capture-contract.test.ts`: `PATIENTS_LIST_SRC` source fixture and Patient List → Settings Capture static navigation assertion.

## Multi-Source Coverage Audit

| Source | ID | Feature/Requirement | Plan | Status | Notes |
|---|---|---|---|---|---|
| GOAL | — | Doctor can pick a capture device and see live preview | 03-04 | COVERED | Restores the missing route into the existing implementation. |
| REQ | CAPT-01 | Available capture devices appear in Settings → Capture | 03-04 | COVERED | Patient List entry makes the existing enumerated dropdown reachable. |
| REQ | CAPT-03 | Doctor can reach and use live preview | 03-04 | COVERED | Capture menu routes to the existing D-09 preview surface. |
| REQ | SET-01 | Capture device setting is accessible | 03-04 | COVERED | Direct header entry is visible to all logged-in doctors. |
| CONTEXT | D-09 | Settings → Capture has live preview | 03-04 | COVERED | Entry point targets the existing page; menu vs button was not locked. |
| UAT | G-03-1 | Reach populated dropdown and matching live preview | 03-04 | COVERED | Root cause is navigation discoverability, not page implementation. |
| UAT | G-03-2 | Expose Phase 3 capture surfaces in Settings | 03-04 | COVERED | Capture is a first-class Settings menu item. |
| RESEARCH | — | Reuse typed renderer routing and existing shadcn primitives | 03-04 | COVERED | No IPC changes or packages are introduced. |
| CONTEXT | D-01..D-08, D-10..D-11 | Previously implemented capture behavior | Existing 03-01..03-03 | EXCLUDED | No diagnosed gap; this closure does not alter those locked behaviors. |

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Authenticated renderer → settings routes | A logged-in doctor chooses Capture or Users from the Patient List header. |
| Session role → Users navigation | `currentUser.isFirstAdmin` controls whether the Users menu item can be selected. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-3-13 | Elevation of Privilege | PatientsList Users menu item | medium | mitigate | Bind `DropdownMenuItem.disabled` to `!currentUser?.isFirstAdmin` and test both roles; existing destination remains unchanged. |
| T-3-14 | Denial of Service | Settings → Capture entry point | medium | mitigate | Keep Capture enabled for every authenticated doctor and pin its route with component plus static-analysis tests. |
| T-3-15 | Tampering | state-based route destination | low | mitigate | Use the existing typed literal route `{ name: 'settings-capture' }`; do not accept user-provided route values. |
| T-3-SC | Tampering | npm installs | high | mitigate | No package installation occurs; reuse the installed shadcn DropdownMenu primitives. |
</threat_model>

<verification>
- `npm run typecheck:web`
- `npm run test:unit -- tests/renderer/pages/patients-list.test.tsx`
- `npm run test:unit -- tests/integration/renderer-main-capture-contract.test.ts`
- `npm run typecheck && npm run test:unit`
- Confirm the full test count is greater than the previous 189-test baseline with zero failures.
</verification>

<success_criteria>
From the Patient List, every authenticated doctor can open Settings and choose Capture to reach the existing Settings → Capture dropdown and D-09 live preview; Users remains visible but disabled for non-admins and enabled for the first admin; focused and full regression gates pass, resolving G-03-1 and G-03-2.
</success_criteria>

<output>
Create `.planning/phases/03-capture-enumeration-live-preview/03-04-SUMMARY.md` when done.
</output>
