---
phase: 03-capture-enumeration-live-preview
plan: 07
type: execute
wave: 6
depends_on:
  - 03-01
files_modified:
  - src/main/ipc/capture.ts
  - tests/main/capture/audit-trace.test.ts
  - tests/integration/renderer-main-capture-contract.test.ts
  - tests/renderer/pages/settings-capture.test.tsx
autonomous: true
requirements:
  - CAPT-02
  - SET-02
gap_closure: true
gap_ids: [G-03-7]
must_haves:
  truths:
    - "Main capture IPC `getPreset()` returns a bare `QualityPreset` (or `null`) — exactly the shape the existing `IpcContract.capture.getPreset` declaration promises (`Promise<QualityPreset | null>` at `src/shared/ipc-contract.ts:180`). The wrapper `{ preset, matched }` is gone from the IPC response."
    - "Renderer hydration of a saved custom preset works in production: when `window.api.capture.getPreset({ deviceId })` resolves with `{ preset: 'custom', resolution: '1280x720', framerate: 30 }`, `SettingsCapture`'s form is `{ kind: 'custom', resolution: '1280x720', framerate: 30 }`. The Custom radio is checked, the resolution input carries the saved value, and the framerate select shows the saved fps."
    - "Audit metadata for `capture.preset_changed` continues to write the `matched` regex on first-save (Q-A): the existing `if (result.isFirstSave) { audit({ metadata: { matched: result.matched } }) }` branches still run, only the IPC return value drops the wrapper. The `audit-trace` test still asserts that the first-save row carries `matched` and the subsequent-read row does not."
    - "The static-analysis integration contract pins (a) `IpcContract.capture.getPreset` declares `Promise<QualityPreset | null>`; (b) `src/main/ipc/capture.ts` source returns bare `QualityPreset` (no `matched` in the return expression); (c) `src/preload/index.ts` `getPreset` forwarder is unchanged."
    - "All 220 shipped tests still pass; the new regression tests (1 main-process audit assertion update + 1 renderer hydration test + 1 integration-contract G-03-7 block with 3 assertions) pin the contract so a future refactor that re-introduces the wrapper fails the contract test before it ships."
  artifacts:
    - "src/main/ipc/capture.ts: `getPreset()` return type changes from `{ preset: QualityPreset; matched: MatchedPattern | null }` to `QualityPreset | null`; the trailing `return { preset: result.preset, matched: result.matched ?? null };` (line 124) is replaced with `return result.preset;`. The `MatchedPattern` import stays (still used by `getOrAutoDetectPreset`'s local typed return). The if/else audit branches are unchanged — the Q-A audit metadata still uses the local `result.matched`."
    - "tests/main/capture/audit-trace.test.ts: line 88-92 assertion is updated from the wrapper shape `{ preset: { preset: 'custom', resolution: '720x480', framerate: 30 }, matched: null }` to the bare shape `{ preset: 'custom', resolution: '720x480', framerate: 30 }`. The surrounding audit assertions (action sequence, user_id, deviceName, stage, no-`matched`-on-subsequent-read) are unchanged."
    - "tests/integration/renderer-main-capture-contract.test.ts: new `describe('G-03-7 — getPreset IPC response matches contract')` block with three assertions — (i) `IPC_CONTRACT_SRC` contains the literal `getPreset:` … `Promise<QualityPreset | null>` declaration; (ii) `CAPTURE_IPC_SRC` source regex `/export function getPreset[\s\S]*?QualityPreset \| null/m` matches (return type declares bare shape — the sync function returns `QualityPreset | null`, the IPC handler at `ipcMain.handle` wraps it in `Promise` per Electron convention) AND the literal wrapper expression `{ preset: result.preset, matched: result.matched` does NOT appear in the source (return value is bare); (iii) `PRELOAD_SRC` `getPreset` forwarder still calls `ipcRenderer.invoke(IPC.CAPTURE_GET_PRESET, input)` unchanged."
    - "tests/renderer/pages/settings-capture.test.tsx: new test `hydrates a saved custom preset from getPreset (G-03-7)` — setRoute('settings-capture'); makeMediaMock(); mock `api.capture.getPreset.mockResolvedValue({ preset: 'custom', resolution: '1280x720', framerate: 30 })`; render <SettingsCapture />; await screen.findByTestId('preset-custom') and assert `custom` radio is `checked`; find `custom-resolution` input and assert its `value` is `'1280x720'`; find framerate trigger and assert it shows `'30 fps'`. The existing 7 tests keep passing unchanged."
  key_links:
    - "IpcContract.capture.getPreset (`Promise<QualityPreset | null>`, line 180) → main `src/main/ipc/capture.ts` `getPreset()` returns bare `QualityPreset` → preload `src/preload/index.ts` `getPreset` forwards unchanged → renderer `SettingsCapture.tsx:37-46` `fromPreset()` correctly hydrates the form for `{ preset: 'sd' | 'hd' | 'custom', … }`. The renderer code is unchanged; only the main-side wrapper is dropped."
    - "Audit metadata: the local `getOrAutoDetectPreset` typed return `{ preset, matched?, isFirstSave }` is unchanged; the existing audit `if (result.isFirstSave)` / `else` branches still read `result.matched` from the local — only the IPC return expression drops the wrapper. The Q-A audit contract is preserved with zero drift."
    - "Integration contract: source-of-truth regex on `src/main/ipc/capture.ts` pins the bare return shape AND asserts the wrapper literal `{ preset: result.preset, matched` is absent. A future refactor that re-introduces the wrapper fails both halves of the contract test before it can ship."
---

<objective>
Close G-03-7 by aligning the main `getPreset()` IPC response with the existing `IpcContract.capture.getPreset` declaration (`Promise<QualityPreset | null>` at `src/shared/ipc-contract.ts:180`). The wrapper `{ preset, matched }` was introduced by Plan 03-01 commit `f15ab5f` for Q-A audit-metadata capture but never propagated to the IPC contract type; renderer code in `SettingsCapture.tsx:37-46` and `ProcedureRoom.tsx:78-82` already consumes the bare `QualityPreset` shape, so dropping the wrapper at the main boundary restores the declared contract and unblocks saved custom-preset hydration in production.

Purpose: SET-02 + roadmap success criterion 3 (SD/HD/Custom remembered per doctor) become observable. The audit metadata for `capture.preset_changed` continues to write `matched` on first-save (Q-A preserved by keeping the existing if/else audit branches and the local `result.matched` variable — only the IPC return expression changes).

Output: Main `getPreset()` returns bare `QualityPreset | null`; the audit-trace test asserts the bare shape; a new integration-contract `describe('G-03-7')` block pins the source shape so a future refactor cannot silently re-introduce the wrapper; a new renderer regression test asserts saved-custom-preset hydration end-to-end through the renderer.

This plan is renderer + main + tests; no migration, no settings table change, no schema change.
</objective>

<execution_context>
@C:/Users/Hussien Essam/.config/opencode/gsd-core/workflows/execute-plan.md
@C:/Users/Hussien Essam/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-capture-enumeration-live-preview/03-UAT.md
@.planning/phases/03-capture-enumeration-live-preview/03-VERIFICATION.md
@.planning/phases/03-capture-enumeration-live-preview/03-CONTEXT.md
@.planning/phases/03-capture-enumeration-live-preview/03-01-SUMMARY.md
@.planning/phases/03-capture-enumeration-live-preview/03-02-SUMMARY.md
@.planning/phases/03-capture-enumeration-live-preview/03-03-SUMMARY.md
@.planning/phases/03-capture-enumeration-live-preview/03-06-SUMMARY.md
@src/shared/ipc-contract.ts
@src/main/ipc/capture.ts
@src/main/capture/preset-repo.ts
@src/main/capture/auto-detect-preset.ts
@src/preload/index.ts
@src/renderer/src/pages/SettingsCapture.tsx
@src/renderer/src/pages/ProcedureRoom.tsx
@tests/main/capture/audit-trace.test.ts
@tests/renderer/setup.ts
@tests/renderer/pages/settings-capture.test.tsx
@tests/renderer/pages/procedure-room.test.tsx
@tests/integration/renderer-main-capture-contract.test.ts
@.opencode/skills/electron-vite/SKILL.md
</context>

<interfaces>
<!-- Key types/contracts the executor needs; extracted once from existing source. -->

From `src/shared/ipc-contract.ts` (line 180 — already correct, no plan-time change):
```ts
getPreset: (input: { deviceId: string }) => Promise<QualityPreset | null>;
```
This is the declared contract. The renderer code, the preload forwarder, and the test mocks already assume this shape; only main has drifted.

From `src/main/ipc/capture.ts` (the bug site — line 99 and line 124):
```ts
// Current (buggy) signature:
export function getPreset(input: unknown): { preset: QualityPreset; matched: MatchedPattern | null } { … }
// Current (buggy) return:
return { preset: result.preset, matched: result.matched ?? null };
```
The wrapper was introduced to surface `matched` to "consumers" (Q-A audit context) but no IPC consumer reads `matched` — it lives in audit metadata only. The fix is to drop the wrapper.

From `src/main/ipc/capture.ts:55-65` (`getOrAutoDetectPreset` — UNCHANGED):
```ts
function getOrAutoDetectPreset(
  doctorId: string,
  deviceId: string,
): { preset: QualityPreset; matched?: MatchedPattern; isFirstSave: boolean } {
  const existing = presetRepo.getPreset(doctorId, deviceId);
  if (existing) return { preset: existing, isFirstSave: false };
  const inferred = autoDetectPreset(deviceId);
  const preset: QualityPreset = { preset: inferred.preset };
  presetRepo.setPreset(doctorId, deviceId, preset);
  return { preset, matched: inferred.matched, isFirstSave: true };
}
```
This function still returns `{ preset, matched?, isFirstSave }` — the local typed return is fine. The audit metadata branches read `result.matched` from this local. Only the IPC return expression changes.

From `src/renderer/src/pages/SettingsCapture.tsx:37-46` (`fromPreset` — already correct for bare shape):
```ts
function fromPreset(preset: QualityPreset | null | undefined): PresetForm {
  if (!preset) return defaultForm();
  if (preset.preset === 'sd') return { kind: 'sd', resolution: '720x480', framerate: 30 };
  if (preset.preset === 'hd') return { kind: 'hd', resolution: '1920x1080', framerate: 30 };
  return {
    kind: 'custom',
    resolution: preset.resolution,
    framerate: preset.framerate,
  };
}
```
The renderer consumes `QualityPreset` directly. Once main returns the bare shape, `fromPreset` correctly handles all 3 preset kinds. With the current wrapper, `preset.preset` is the inner object `{ preset: 'sd' }` (not the string `'sd'`), so the `=== 'sd' | 'hd' | 'custom'` checks all fail and `fromPreset` falls through to `{ kind: 'custom', resolution: undefined, framerate: undefined }` for every preset kind — confirming the verifier's bug report (the verifier's specific claim that sd/hd "accidentally match" is not what the code actually does; ALL preset kinds currently fail to hydrate in production).

From `src/renderer/src/pages/ProcedureRoom.tsx:78-82` (consumer — already correct for bare shape):
```ts
.then((nextPreset) => {
  if (!cancelled) setPreset(nextPreset ?? undefined);
})
```
`nextPreset` is `QualityPreset | null` per the contract; `setPreset`'s state is `useState<QualityPreset>()`. After the fix this works for all 3 preset kinds. With the current wrapper, `nextPreset` is the wrapper object and `useVideoPreview` receives it as a "preset" — the hook's `presetHints` then sees `preset.preset === 'sd'` as false (preset.preset is an object), so it falls through to `.split(...)` on `preset.resolution` which is undefined and crashes for any wrapper that isn't `{ preset: { preset: 'custom', … } }`. (The wrapper shape is structurally broken on the Procedure Room side too; G-03-7 closes both surfaces.)

From `src/preload/index.ts:41` (forwarder — UNCHANGED):
```ts
getPreset: (input) => ipcRenderer.invoke(IPC.CAPTURE_GET_PRESET, input),
```
The forwarder doesn't transform the response; whatever main returns is what the renderer sees.

From `tests/renderer/setup.ts:42,80` (mock factory — already correct for bare shape):
```ts
// type:
getPreset: ReturnType<typeof vi.fn>;
// factory:
getPreset: vi.fn(),
```
Each test installs its own mock via `mockResolvedValue({ preset: 'hd' })` (see settings-capture.test.tsx:48 and procedure-room.test.tsx:59). These mocks return the bare shape — which means the renderer tests pass while production fails. The fix makes production match the tests.

From `tests/main/capture/audit-trace.test.ts:88-92` (the assertion that needs updating):
```ts
const presetRead = getPreset({ deviceId: 'EasyCap USB Video' });
expect(presetRead).toEqual({
  preset: { preset: 'custom', resolution: '720x480', framerate: 30 },
  matched: null,
});
```
After the fix, `presetRead` is the bare `QualityPreset`, so this becomes:
```ts
expect(presetRead).toEqual({
  preset: 'custom', resolution: '720x480', framerate: 30,
});
```
The surrounding audit-row assertions on lines 95-134 are unchanged — they read the `audit_log` table, not the IPC return.
</interfaces>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Drop the getPreset wrapper at the main boundary and pin the bare-shape contract (G-03-7)</name>
  <files>src/main/ipc/capture.ts, tests/main/capture/audit-trace.test.ts, tests/integration/renderer-main-capture-contract.test.ts</files>
  <read_first>
    - src/shared/ipc-contract.ts
    - src/main/ipc/capture.ts
    - src/main/capture/preset-repo.ts
    - src/main/capture/auto-detect-preset.ts
    - src/shared/validators.ts
    - tests/main/capture/audit-trace.test.ts
    - tests/integration/renderer-main-capture-contract.test.ts
    - src/preload/index.ts
    - src/renderer/src/pages/SettingsCapture.tsx
    - src/renderer/src/pages/ProcedureRoom.tsx
    - .planning/phases/03-capture-enumeration-live-preview/03-VERIFICATION.md
    - .planning/phases/03-capture-enumeration-live-preview/03-CONTEXT.md
  </read_first>
  <behavior>
    - The IPC contract source (`src/shared/ipc-contract.ts:180`) declares `getPreset: (input: { deviceId: string }) => Promise<QualityPreset | null>;` — this is unchanged.
    - Main `src/main/ipc/capture.ts` `getPreset()` function declares a return type `Promise<QualityPreset | null>` (or simply `QualityPreset | null` since the function itself is sync — the IPC handler wraps it in an async context). The function body ends with `return result.preset;` — NOT `return { preset: result.preset, matched: … };`.
    - The audit metadata branches (`if (result.isFirstSave) { audit({ metadata: { matched: result.matched } }) }` and the subsequent-read branch) are UNCHANGED — Q-A audit metadata still writes `matched` on first save.
    - `tests/main/capture/audit-trace.test.ts:88-92` asserts `presetRead` equals the bare shape `{ preset: 'custom', resolution: '720x480', framerate: 30 }`. The surrounding audit-row assertions (action sequence, user_id, deviceName, stage, no-matched-on-subsequent-read) keep passing unchanged.
    - `tests/integration/renderer-main-capture-contract.test.ts` has a new `describe('G-03-7 — getPreset IPC response matches contract')` block with three assertions: (i) `IPC_CONTRACT_SRC` contains the literal `getPreset:` followed by `Promise<QualityPreset | null>`; (ii) `CAPTURE_IPC_SRC` source regex `/export function getPreset[\s\S]*?QualityPreset \| null/m` matches (return type declares bare shape) AND the literal `{ preset: result.preset, matched` is absent from the source; (iii) `PRELOAD_SRC` `getPreset` forwarder still calls `ipcRenderer.invoke(IPC.CAPTURE_GET_PRESET, input)`.
  </behavior>
  <action>
    Write the integration-contract assertions FIRST so they FAIL on the current wrapper-bearing source (RED). Open `tests/integration/renderer-main-capture-contract.test.ts`. Add a new `describe('G-03-7 — getPreset IPC response matches contract')` block AFTER the existing `G-03-6 — SettingsSidebar is mounted on every Settings page` block. The new block contains three `it()` cases:
    1. `IpcContract.capture.getPreset declares Promise<QualityPreset | null>` — `expect(IPC_CONTRACT_SRC).toMatch(/getPreset:\s*\([^)]*\)\s*=>\s*Promise<QualityPreset \| null>/);` (a literal-source regex that finds the declared method signature).
    2. `src/main/ipc/capture.ts getPreset returns bare QualityPreset (or null) — no matched wrapper` — read the captured `CAPTURE_IPC_SRC` (already declared at the top of the file at line 43), assert `expect(CAPTURE_IPC_SRC).toMatch(/export function getPreset[\s\S]{0,500}QualityPreset \| null/m)` (return type declares bare shape — the sync function returns `QualityPreset | null`, the IPC handler at `ipcMain.handle` wraps it in `Promise` per Electron convention) AND `expect(CAPTURE_IPC_SRC).not.toMatch(/\{\s*preset:\s*result\.preset,\s*matched:\s*result\.matched/)` (the wrapper literal is absent from the source). Both assertions are on the captured source — no Electron runtime needed.
    3. `preload getPreset forwarder unchanged — still invokes IPC.CAPTURE_GET_PRESET` — assert `expect(PRELOAD_SRC).toMatch(/getPreset:\s*\([^)]*\)\s*=>\s*ipcRenderer\.invoke\(IPC\.CAPTURE_GET_PRESET,\s*input\)/);`. The forwarder doesn't transform the response; whatever main returns is what the renderer sees.
    Run the focused integration contract test (`npm run test:unit -- tests/integration/renderer-main-capture-contract.test.ts`) and confirm the THREE new assertions FAIL on the current wrapper-bearing source (RED). Capture the failure output for the SUMMARY.

    Now fix `src/main/ipc/capture.ts`. Locate the `getPreset` function (lines 99-125). Change the return type annotation from `{ preset: QualityPreset; matched: MatchedPattern | null }` to `QualityPreset | null`. Replace the trailing `return { preset: result.preset, matched: result.matched ?? null };` (line 124) with `return result.preset;`. The if/else audit branches above are UNCHANGED — they read `result.matched` from the local `getOrAutoDetectPreset` return. Add a one-line inline comment immediately above the new `return result.preset;`: `// ponytail: drop the matched wrapper — Q-A audit metadata already lives in the audit_log via the if/else branches above; only the IPC response shape needed to match the declared QualityPreset | null contract.` Note: `MatchedPattern` is still used as a type annotation in `getOrAutoDetectPreset`'s return type — keep the import at line 21.

    Then update `tests/main/capture/audit-trace.test.ts`. Locate the assertion block at lines 88-92. The current assertion is `expect(presetRead).toEqual({ preset: { preset: 'custom', resolution: '720x480', framerate: 30 }, matched: null });`. Change it to the bare shape: `expect(presetRead).toEqual({ preset: 'custom', resolution: '720x480', framerate: 30 });`. The rest of the test (lines 73-135 — action sequence `['capture.device_changed', 'capture.preset_changed', 'capture.preset_changed', 'capture.no_device']`, user_id matches doctorId, deviceName is the canonical form, manual preset row has no matched field, subsequent-read row has no matched field) is unchanged. Lines 137-161 (`first-save auto-detect emits matched regex metadata; subsequent reads do not`) are also unchanged — they read the audit_log, not the IPC return.

    Re-run the focused integration contract test and confirm the THREE new assertions PASS (GREEN). Re-run the audit-trace test and confirm it passes. Run the FULL main capture suite to confirm no regression on the other main capture tests (canonicalize, devices, auto-detect-preset, preset-repo).

    Do NOT modify `src/shared/ipc-contract.ts` — the contract declaration is already correct (`Promise<QualityPreset | null>`); only main drifted. Do NOT modify `src/preload/index.ts` — the forwarder is already correct. Do NOT modify `src/renderer/src/pages/SettingsCapture.tsx` or `src/renderer/src/pages/ProcedureRoom.tsx` — the renderer code already correctly consumes the bare shape (after the main fix). Do NOT modify `tests/renderer/setup.ts` — the mock factory already returns the bare shape via each test's `mockResolvedValue({ preset: 'hd' })`.
  </action>
  <acceptance_criteria>
    - `src/main/ipc/capture.ts:99` `getPreset` return type annotation is `QualityPreset | null`.
    - `src/main/ipc/capture.ts:124` (or wherever the return statement ends up after the rewrite) returns `result.preset` directly — no `{ preset: result.preset, matched: ... }` wrapper.
    - `src/main/ipc/capture.ts` source contains the literal `return result.preset;` (or a structurally identical expression) and does NOT contain the literal `{ preset: result.preset, matched: result.matched` anywhere in the file.
    - `src/main/ipc/capture.ts` if/else audit branches are unchanged: the first-save branch still writes `metadata: { deviceName, preset: result.preset.preset, matched: result.matched }` and the subsequent-read branch still writes `metadata: { deviceName, preset: result.preset.preset }`.
    - `tests/main/capture/audit-trace.test.ts:88-92` asserts `presetRead` equals the bare shape `{ preset: 'custom', resolution: '720x480', framerate: 30 }`.
    - `tests/integration/renderer-main-capture-contract.test.ts` has a new `describe('G-03-7 — getPreset IPC response matches contract')` block at the end of the file with three assertions (contract declaration regex, main source return-type regex + wrapper-literal absence, preload forwarder regex).
    - `npm run typecheck` exits 0 (both `typecheck:node` and `typecheck:web`).
    - `npm run test:unit -- tests/integration/renderer-main-capture-contract.test.ts tests/main/capture/audit-trace.test.ts` exits 0 with all assertions passing.
    - The full main capture suite (`tests/main/capture/*.test.ts`) passes with no failures.
  </acceptance_criteria>
  <verify>
    <automated>npm run typecheck && npm run test:unit -- tests/integration/renderer-main-capture-contract.test.ts tests/main/capture/audit-trace.test.ts tests/main/capture/*.test.ts</automated>
  </verify>
  <done>Main getPreset() returns bare QualityPreset | null (no wrapper); the audit metadata Q-A branches still write matched on first-save; the audit-trace test asserts the bare shape; the integration-contract G-03-7 describe block pins (contract declaration regex, main source return-type regex + wrapper-literal absence, preload forwarder regex); the full main capture suite passes with no regressions; npm run typecheck exits 0.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add a saved custom-preset hydration regression test in the Settings Capture suite (G-03-7)</name>
  <files>tests/renderer/pages/settings-capture.test.tsx</files>
  <read_first>
    - tests/renderer/pages/settings-capture.test.tsx
    - src/renderer/src/pages/SettingsCapture.tsx
    - src/renderer/src/components/ui/radio-group.tsx (or wherever the Custom radio lives)
    - 03-07-PLAN.md (Task 1 — the main-side wrapper drop)
  </read_first>
  <behavior>
    - SettingsCapture renders with the Custom radio checked when the saved preset is `{ preset: 'custom', resolution: '1280x720', framerate: 30 }`.
    - The Custom resolution input carries value `'1280x720'`.
    - The framerate select shows `'30 fps'`.
    - The test fails on the old wrapper-bearing source (asserts what production currently fails to do) and passes on the new bare-shape source.
    - Existing 7 settings-capture tests keep passing.
  </behavior>
  <action>
    Write the test FIRST. Run it once against the CURRENT source (before Task 1's main fix is applied) — it should FAIL because production returns `{ preset: 'custom', resolution: '1280x720', framerate: 30, matched: null }` and the bare-shape mock makes the test assert on the bare shape (so it would also fail against the wrapper shape, but the mock factory uses `mockResolvedValue({ preset: 'hd' })` etc. so the test mock for THIS new case needs the bare shape to make the test deterministic). Then run it AFTER Task 1's main fix — it should PASS.

    Add ONE new test to the existing `describe('Settings → Capture', ...)` block in `tests/renderer/pages/settings-capture.test.tsx`:

    ```ts
    it('hydrates a saved custom preset from getPreset (G-03-7)', async () => {
      setRoute({ name: 'settings-capture' });
      makeMediaMock();
      const api = getApi();
      api.capture.getPreset.mockResolvedValue({
        preset: 'custom',
        resolution: '1280x720',
        framerate: 30,
      });
      render(<SettingsCapture />);

      const customRadio = await screen.findByTestId('preset-custom');
      expect(customRadio).toBeChecked();

      const resolutionInput = (await screen.findByTestId('custom-resolution')) as HTMLInputElement;
      expect(resolutionInput.value).toBe('1280x720');

      const framerateTrigger = await screen.findByLabelText(/^framerate$/i);
      expect(framerateTrigger.textContent).toContain('30 fps');
    });
    ```

    Run `npm run test:unit -- tests/renderer/pages/settings-capture.test.tsx` — confirm the new test PASSES on the post-Task-1 source (with the main wrapper dropped). The existing 7 tests keep passing.
  </action>
  <acceptance_criteria>
    - `tests/renderer/pages/settings-capture.test.tsx` has a new `it('hydrates a saved custom preset from getPreset (G-03-7)', ...)` case.
    - The new test passes after Task 1's main fix is applied.
    - The existing 7 settings-capture tests continue to pass.
    - `npm run test:unit -- tests/renderer/pages/settings-capture.test.tsx tests/integration/renderer-main-capture-contract.test.ts tests/main/capture/audit-trace.test.ts` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>npm run test:unit -- tests/renderer/pages/settings-capture.test.tsx tests/integration/renderer-main-capture-contract.test.ts tests/main/capture/audit-trace.test.ts</automated>
  </verify>
  <done>SettingsCapture renderer hydrates saved custom presets correctly through the IPC; the renderer regression test pins the behavior; the existing settings-capture test suite stays green.</done>
</task>

## Multi-Source Coverage Audit

| Source | ID | Feature/Requirement | Plan | Status | Notes |
|---|---|---|---|---|---|
| VERIFICATION | G-03-7 | `IpcContract.capture.getPreset` response shape matches the declared `Promise<QualityPreset \| null>` contract; saved custom presets hydrate in production renderer | 03-07 | COVERED | Task 1 drops the wrapper at the main boundary; Task 2 adds a renderer regression test. The integration-contract G-03-7 describe block pins the bare shape against future drift. |
| REQ | SET-02 | Quality presets selectable and remembered per doctor | 03-07 | COVERED | Saved `{ preset: 'custom', resolution, framerate }` rows now hydrate correctly via the bare-shape IPC response. SET-02 was previously marked UNMET in VERIFICATION due to the wrapper. |
| REQ | CAPT-02 | Per-doctor default + per-doctor-per-device preset persistence | 03-07 | COVERED | The audit metadata Q-A branches still write `matched` on first-save (unchanged); only the IPC return shape changes. The `presetRepo` write/read path is untouched. |
| CONTEXT | Q-A | First save writes `{ deviceName, preset, matched }`; subsequent reads write `{ deviceName, preset }` only | 03-07 | PRESERVED | The audit branches read `result.matched` from the local `getOrAutoDetectPreset` return — Q-A contract is preserved with zero drift. The wrapper drop is IPC-only. |
| RESEARCH | — | No new research items introduced | n/a | N/A | Gap-closure plan does not introduce new dependencies or integrations. |

## Threat Model

### Trust Boundaries

| Boundary | Description |
|---|---|
| Renderer → main IPC | Renderer calls `window.api.capture.getPreset({ deviceId })` over the typed contextBridge. Main returns the IPC response; the renderer must consume it according to the declared type. |
| Main → audit log | The first-save branch writes `matched` regex to `audit_log` (Q-A). This is internal to main and not exposed over IPC. |
| Test mocks → production | Renderer tests mock `getPreset` via `mockResolvedValue(...)`. The mock shape must match the production shape, otherwise tests pass while production fails (the latent bug being closed). |

### STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-3-25 | Tampering | `src/main/ipc/capture.ts` `getPreset()` return shape drift | medium | mitigate | Integration contract G-03-7 describe block pins (a) the contract declaration regex, (b) the main source return-type regex AND the absence of the wrapper literal, (c) the preload forwarder unchanged. A future refactor that re-introduces the wrapper fails the contract test. |
| T-3-26 | Repudiation | Audit metadata dropped during wrapper drop | medium | mitigate | The if/else audit branches read `result.matched` from the local `getOrAutoDetectPreset` return — UNCHANGED. The `audit-trace` test still asserts that the first-save row carries `matched` and the subsequent-read row does not. |
| T-3-27 | Information Disclosure | Renderer never sees `matched` after the wrapper drop | low | accept | `matched` was a leakage of internal auto-detect heuristic detail to the renderer; dropping it from the IPC response is the correct disposition. Future UI that wants to show "why this preset was auto-detected" should read the audit log, not the IPC. |

<verification>
- `npm run typecheck` exits 0.
- `npm run test:unit -- tests/integration/renderer-main-capture-contract.test.ts tests/main/capture/audit-trace.test.ts tests/renderer/pages/settings-capture.test.tsx tests/main/capture/*.test.ts` exits 0.
- `npm run test:unit` exits 0 across the full renderer + integration + main capture suites.
- `src/main/ipc/capture.ts` source contains the literal `return result.preset;` and does NOT contain `{ preset: result.preset, matched: result.matched` anywhere.
- The integration contract G-03-7 describe block has 3 passing assertions.

<success_criteria>
- Saved custom presets hydrate correctly in production: when `getPreset` resolves with `{ preset: 'custom', resolution: '1280x720', framerate: 30 }`, the Custom radio is checked, the resolution input shows the saved value, the framerate shows the saved fps.
- The audit metadata Q-A branches still write `matched` on first-save and skip it on subsequent reads.
- The integration contract G-03-7 describe block pins the bare shape against future drift.
- The renderer regression test (settings-capture) pins the hydration behavior.
- The full automated test suite + both typecheck projects remain green.

<output>
Create `03-07-SUMMARY.md` when done.
</output>