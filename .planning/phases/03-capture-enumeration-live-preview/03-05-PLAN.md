---
phase: 03-capture-enumeration-live-preview
plan: 05
type: execute
wave: 4
depends_on:
  - 03-04
files_modified:
  - src/renderer/src/pages/SettingsHub.tsx
  - src/renderer/src/lib/router.ts
  - src/renderer/src/App.tsx
  - src/renderer/src/pages/PatientsList.tsx
  - src/renderer/src/components/PatientRow.tsx
  - tests/renderer/pages/settings-hub.test.tsx
  - tests/renderer/components/patient-row.test.tsx
  - tests/renderer/pages/patients-list.test.tsx
  - tests/renderer/pages/procedure-room.test.tsx
  - tests/integration/renderer-main-capture-contract.test.ts
autonomous: true
requirements:
  - CAPT-01
  - CAPT-03
  - SET-01
  - SET-04
gap_closure: true
gap_ids: [G-03-3, G-03-4]
must_haves:
  truths:
    - "Every authenticated doctor can open Settings from the Patient List and reach a dedicated hub page whose right-side sidebar lists Capture and Users; the header affordance is a single Button (not a transient menu), and the existing SettingsCapture + SettingsUsers pages are reached through the hub's nav entries."
    - "The Users sidebar entry is visible to every authenticated doctor but is reachable only by the first admin; the Capture entry is reachable by every authenticated doctor."
    - "Every non-deleted PatientRow exposes an Open Procedure Room action; clicking it navigates to `{ name: 'procedure-room', patientId }` so the existing D-07 Procedure Room is reachable from the Patient List and Finish returns to the Patient List via the preserved previousRoute (Q-B)."
    - "The App.tsx `patient-detail` placeholder route is removed; Procedure Room is the Phase 3 patient surface and no UI code points at the placeholder."
    - "Renderer component tests, a new PatientRow component test, a new SettingsHub page test, a Procedure Room reachability assertion, and the static-analysis integration contract all prevent the diagnosed entry-point gaps from reappearing."
  artifacts:
    - "src/renderer/src/pages/SettingsHub.tsx: SettingsHub page (two-column layout: main content + right-side sidebar nav with Back header)"
    - "src/renderer/src/lib/router.ts: `Route` variant `settings-hub`"
    - "src/renderer/src/App.tsx: `settings-hub` case rendered; `patient-detail` placeholder case removed"
    - "src/renderer/src/pages/PatientsList.tsx: header Settings affordance is a single Button that calls `navigate({ name: 'settings-hub' })`"
    - "src/renderer/src/components/PatientRow.tsx: `Open Procedure Room` DropdownMenuItem for non-deleted patients (calls `navigate({ name: 'procedure-room', patientId })`)"
    - "tests/renderer/pages/settings-hub.test.tsx: sidebar nav coverage, admin-only Users entry, Capture entry for all doctors, Back header"
    - "tests/renderer/components/patient-row.test.tsx: Open Procedure Room entry visible for non-deleted patients; deleted-patient row does not render the entry; entry navigates with the patientId"
    - "tests/renderer/pages/patients-list.test.tsx: header Settings Button routes to settings-hub (replaces the DropdownMenu interaction coverage with a Button-click assertion)"
    - "tests/renderer/pages/procedure-room.test.tsx: reachability test asserts the PatientRow `Open Procedure Room` action lands on Procedure Room with the patientId preserved"
    - "tests/integration/renderer-main-capture-contract.test.ts: extended static-analysis walker; PatientsList Settings trigger no longer wired via a transient menu, new SettingsHub source contains nav to `settings-users` (admin-gated) and `settings-capture`, PatientRow source contains the `procedure-room` navigation with the patientId, and the `patient-detail` placeholder is gone from App.tsx"
  key_links:
    - "PatientsList header Button -> `navigate({ name: 'settings-hub' })` -> App.tsx -> SettingsHub page (Back -> patients)"
    - "SettingsHub sidebar Users entry -> admin gate via `currentUser.isFirstAdmin` -> `navigate({ name: 'settings-users' })`; Capture entry -> `navigate({ name: 'settings-capture' })`"
    - "PatientRow `Open Procedure Room` DropdownMenuItem -> `navigate({ name: 'procedure-room', patientId })` -> useRoute.previous snapshot (Q-B) -> Finish returns to Patient List"
    - "App.tsx renders `settings-hub` case; `patient-detail` case removed so no UI code points at the Phase 1 placeholder"
---

<objective>
Close the two diagnosed navigation gaps: deliver a dedicated Settings hub page (replacing the transient header menu) so the existing SettingsCapture and SettingsUsers pages are reachable through a real sidebar nav, and wire every non-deleted PatientRow to the existing Procedure Room so the doctor can actually open a procedure. This restores the two missing entry points the user reported and unblocks the previously-blocked Windows hardware UAT.

Purpose: Replace the transient header menu with a real Settings surface and give the patient list a real Procedure Room entry — both surfaces already exist (D-09 SettingsCapture, D-07 Procedure Room); the missing piece is navigation. No IPC, settings table, or audit changes ship in this plan; it is renderer-only.

Output: New `SettingsHub` page, `settings-hub` route, `Open Procedure Room` action on PatientRow, removal of the `patient-detail` placeholder route, and the renderer + integration tests that pin all four entry points so a future refactor cannot silently lose them.
</objective>

<execution_context>
@C:/Users/Hussien Essam/.config/opencode/gsd-core/workflows/execute-plan.md
@C:/Users/Hussien Essam/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-capture-enumeration-live-preview/03-CONTEXT.md
@.planning/phases/03-capture-enumeration-live-preview/03-UAT.md
@.planning/phases/03-capture-enumeration-live-preview/03-02-SUMMARY.md
@.planning/phases/03-capture-enumeration-live-preview/03-04-SUMMARY.md
@src/renderer/src/App.tsx
@src/renderer/src/lib/router.ts
@src/renderer/src/store/route.ts
@src/renderer/src/store/session.ts
@src/renderer/src/pages/PatientsList.tsx
@src/renderer/src/components/PatientRow.tsx
@src/renderer/src/pages/SettingsCapture.tsx
@src/renderer/src/pages/SettingsUsers.tsx
@src/renderer/src/pages/ProcedureRoom.tsx
@src/renderer/src/components/ui/button.tsx
@src/renderer/src/components/ui/card.tsx
@.opencode/skills/electron-vite/SKILL.md
</context>

<interfaces>
<!-- Key types/contracts the executor needs; extracted once from existing source. -->

From src/renderer/src/lib/router.ts:
```ts
export type Route =
  | { name: 'wizard' }
  | { name: 'login' }
  | { name: 'patients' }
  | { name: 'patient-new' }
  | { name: 'patient-edit'; id: string }
  | { name: 'patient-detail'; id: string }
  | { name: 'settings-users' }
  | { name: 'settings-capture' }
  | { name: 'procedure-room'; patientId?: string };
export { getRoute, setRoute, navigate, useRoute } from '@/store/route';
```

From src/renderer/src/store/route.ts:
```ts
export function useRoute(): {
  route: Route;
  current: Route;
  previous: Route | null;
  navigate: (next: Route) => void;
};
```

From src/renderer/src/store/session.ts:
```ts
export function useSession(): {
  status: AuthStatus | null;
  loading: boolean;
  currentUser: UserPublic | null;
  refresh: () => Promise<void>;
  signOut: () => Promise<void>;
};
// currentUser.isFirstAdmin gates the Users hub entry.
```

From src/renderer/src/components/PatientRow.tsx (existing props):
```ts
type Props = {
  patient: Patient;
  canRestore: boolean;
  onEdit: (patient: Patient) => void;
  onDelete: (patient: Patient) => void;
  onRestore: (patient: Patient) => void;
};
// patient.deletedAt !== null => deleted row; deleted rows MUST NOT render the
// new Open Procedure Room entry (deleted patients are not procedures).
```

From src/renderer/src/pages/PatientsList.tsx:
```ts
// canRestore = currentUser?.isFirstAdmin ?? false; reused for the hub admin gate.
```

From src/renderer/src/pages/ProcedureRoom.tsx (Q-B / D-07):
```ts
function finish(): void {
  preview.stop();
  navigate(previous ?? { name: 'patients' });
}
// useRoute().previous snapshot is preserved across the row action; clicking
// Open Procedure Room in PatientRow automatically snapshots 'patients' as previous.
```
</interfaces>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Ship a Settings hub page, the `settings-hub` route, the PatientsList header Button, and the integration test that pins the new entry shape</name>
  <files>src/renderer/src/pages/SettingsHub.tsx, src/renderer/src/lib/router.ts, src/renderer/src/App.tsx, src/renderer/src/pages/PatientsList.tsx, tests/renderer/pages/settings-hub.test.tsx, tests/renderer/pages/patients-list.test.tsx, tests/integration/renderer-main-capture-contract.test.ts</files>
  <read_first>
    - src/renderer/src/pages/PatientsList.tsx
    - src/renderer/src/lib/router.ts
    - src/renderer/src/App.tsx
    - src/renderer/src/store/route.ts
    - src/renderer/src/store/session.ts
    - src/renderer/src/pages/SettingsCapture.tsx
    - src/renderer/src/pages/SettingsUsers.tsx
    - src/renderer/src/components/ui/button.tsx
    - src/renderer/src/components/ui/card.tsx
    - tests/renderer/pages/patients-list.test.tsx
    - tests/integration/renderer-main-capture-contract.test.ts
    - .planning/phases/03-capture-enumeration-live-preview/03-04-SUMMARY.md
    - .planning/phases/03-capture-enumeration-live-preview/03-UAT.md
  </read_first>
  <behavior>
    - PatientsList header Settings affordance is a single shadcn Button (not a transient menu); clicking it routes to `{ name: 'settings-hub' }` for every authenticated session.
    - SettingsHub renders a right-side sidebar nav with two entries — Users and Capture; the hub's main area shows a welcome card pointing to both sections; the header has a Back button that navigates to `{ name: 'patients' }`.
    - SettingsHub Users sidebar entry is reachable only by the first admin; non-admin sessions see the entry but it is disabled and clicking it does not navigate; Capture is reachable for every authenticated session.
    - Existing SettingsCapture and SettingsUsers pages are unchanged and still work end-to-end; the hub is the new wrapper.
    - Static-analysis contract: PatientsList source no longer contains a transient menu wrapper for the Settings affordance; SettingsHub source contains a navigation call to `settings-users` and a navigation call to `settings-capture`; the App.tsx `settings-hub` route is rendered.
  </behavior>
  <action>
    First extend the integration contract in `tests/integration/renderer-main-capture-contract.test.ts` so the regression cannot ship: add a `SETTINGS_HUB_SRC` source fixture that reads the new `SettingsHub.tsx` once; add a describe block that asserts `SettingsHub` source contains both `navigate({ name: 'settings-users' })` (admin-gated) and `navigate({ name: 'settings-capture' })` (every doctor); add an assertion that `PatientsList` source no longer wraps the Settings affordance in a transient menu (positive assertion: contains `navigate({ name: 'settings-hub' })`); and add an assertion that the `settings-hub` case is rendered in `App.tsx`. Also rewrite the existing `patients-list.test.tsx` Settings coverage to match the new affordance: replace each test that opens a transient menu and clicks Capture/Users with a single test that asserts the Settings header Button routes to `settings-hub` for an authenticated session (the test re-seeds mocks and awaits `session.refresh()` exactly the way the 03-04 settings-users test does). Then create `src/renderer/src/pages/SettingsHub.tsx` as a function component using the existing shadcn primitives only: render a header with title "Settings" + a Back Button that calls `navigate({ name: 'patients' })`; render a two-column layout (main content + right-side sidebar nav) where the main content is a single `Card` with welcome copy that lists the two available sections, and the sidebar lists two Button-style nav entries — Capture (always enabled, calls `navigate({ name: 'settings-capture' })`) and Users (calls `navigate({ name: 'settings-users' })`, `disabled` when `!currentUser?.isFirstAdmin`). Add the `settings-hub` route variant to `src/renderer/src/lib/router.ts`'s `Route` union (additive; do not remove existing variants). Add a `settings-hub` case to `App.tsx`'s switch that returns the new `SettingsHub` page. Finally, in `src/renderer/src/pages/PatientsList.tsx`, replace the existing Settings affordance (the shadcn DropdownMenu wrapper plus the trigger asChild + the two items) with a single outline Button that calls `navigate({ name: 'settings-hub' })` on click; the Button keeps the existing Settings icon and label and the existing `data-testid="settings-trigger"` so the existing 03-04 Playwright/manual-test affordance still works; remove the DropdownMenu and the three DropdownMenu imports added in 03-04 since the hub now owns role-gating internally. Create `tests/renderer/pages/settings-hub.test.tsx` with the deterministic `setAdminSession` / `setNonAdminSession` helpers from `tests/renderer/pages/patients-list.test.tsx` and cover: admin sees both sidebar entries enabled and clicking either navigates; non-admin sees Capture enabled and Users disabled, clicking disabled Users does not navigate, and clicking Capture still navigates; the Back button returns to the patients route. Match the existing 02-03 and 03-04 test pattern (seed mocks THEN `await session.refresh()` THEN render). No new dependencies, no IPC changes, no settings table changes; SettingsCapture and SettingsUsers are touched only if a test imports them, and otherwise left alone.
  </action>
  <acceptance_criteria>
    - `src/renderer/src/pages/SettingsHub.tsx` exists and exports a default function component `SettingsHub` that returns a main element with a header (Back Button + "Settings" title) and a two-column layout (main content + right-side sidebar nav).
    - `SettingsHub` sidebar contains exactly two nav entries — Users and Capture — rendered as buttons; Users is `disabled` when `currentUser?.isFirstAdmin` is false; Capture is always enabled.
    - `SettingsHub` imports `useSession` from `@/store/session` and `useRoute` from `@/lib/router`; it calls `navigate({ name: 'settings-users' })` from the Users button, `navigate({ name: 'settings-capture' })` from the Capture button, and `navigate({ name: 'patients' })` from the Back button.
    - `src/renderer/src/lib/router.ts` `Route` union has a new `{ name: 'settings-hub' }` variant; existing variants are preserved.
    - `src/renderer/src/App.tsx` switch renders `<SettingsHub />` for `case 'settings-hub'`.
    - `src/renderer/src/pages/PatientsList.tsx` header Settings affordance is a single `Button` (no `DropdownMenu` wrapper, no `DropdownMenu*` imports from `@/components/ui/dropdown-menu`); the button calls `navigate({ name: 'settings-hub' })` on click and retains `data-testid="settings-trigger"`.
    - `tests/renderer/pages/settings-hub.test.tsx` exists with at least 6 passing assertions covering: admin sees Users + Capture enabled, non-admin sees Users disabled, Back navigates to patients, admin/non-admin clicking Capture routes, admin clicking Users routes, non-admin clicking disabled Users does NOT route.
    - `tests/renderer/pages/patients-list.test.tsx` Settings-menu describe block is replaced with a single assertion that the Settings header Button routes to `settings-hub` for an authenticated session; the prior DropdownMenu interaction tests are removed.
    - `tests/integration/renderer-main-capture-contract.test.ts` `SETTINGS_HUB_SRC` fixture is added and the new describe block has assertions for: `settings-users` navigation, `settings-capture` navigation, `settings-hub` route render in App.tsx, and the PatientsList Settings affordance pointing at `settings-hub` (no transient menu wrapper).
    - `npm run typecheck:web` and `npm run test:unit -- tests/renderer/pages/settings-hub.test.tsx tests/renderer/pages/patients-list.test.tsx tests/integration/renderer-main-capture-contract.test.ts` both pass.
  </acceptance_criteria>
  <verify>
    <automated>npm run typecheck:web && npm run test:unit -- tests/renderer/pages/settings-hub.test.tsx tests/renderer/pages/patients-list.test.tsx tests/integration/renderer-main-capture-contract.test.ts</automated>
  </verify>
  <done>The new SettingsHub page is reachable through a single header Button on the Patient List; the hub's right-side sidebar exposes Capture to every authenticated doctor and Users to the first admin only; the existing SettingsCapture and SettingsUsers pages are reached through the hub; the static-analysis contract prevents the new entry shape from being silently lost.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire every non-deleted PatientRow to the Procedure Room, remove the patient-detail placeholder, and pin the new entry shape with a component test + reachability test + integration contract</name>
  <files>src/renderer/src/components/PatientRow.tsx, src/renderer/src/App.tsx, tests/renderer/components/patient-row.test.tsx, tests/renderer/pages/procedure-room.test.tsx, tests/integration/renderer-main-capture-contract.test.ts</files>
  <read_first>
    - src/renderer/src/components/PatientRow.tsx
    - src/renderer/src/pages/PatientsList.tsx
    - src/renderer/src/App.tsx
    - src/renderer/src/lib/router.ts
    - src/renderer/src/store/route.ts
    - src/renderer/src/pages/ProcedureRoom.tsx
    - src/shared/ipc-contract.ts
    - tests/renderer/pages/procedure-room.test.tsx
    - tests/renderer/pages/patients-list.test.tsx
    - tests/integration/renderer-main-capture-contract.test.ts
    - .planning/phases/03-capture-enumeration-live-preview/03-02-SUMMARY.md
    - .planning/phases/03-capture-enumeration-live-preview/03-CONTEXT.md
    - .planning/phases/03-capture-enumeration-live-preview/03-UAT.md
  </read_first>
  <behavior>
    - A non-deleted PatientRow exposes a menu item labeled "Open Procedure Room"; clicking it routes to `{ name: 'procedure-room', patientId: <row.patient.id> }` so `useRoute().previous` is automatically set to the `patients` route snapshot (Q-B) and Finish returns to the Patient List.
    - A deleted PatientRow (where `patient.deletedAt !== null`) does NOT render the Open Procedure Room entry; deleted patients cannot start a procedure.
    - The existing Edit / Delete / Restore actions on PatientRow continue to work exactly as before; the new action is the first item in the existing shadcn DropdownMenu.
    - The `patient-detail` route case is removed from App.tsx; no UI code anywhere in the renderer points at the Phase 1 placeholder. The `Route` union still declares `patient-detail` (the case is dead but kept for backward compat with the existing `patients:softDelete` audit row payload shape and any persisted deep-links — additive removal only when no caller remains).
    - The Procedure Room page is reachable from the Patient List row action and Finish routes back to the Patient List; this is asserted by a reachability test that renders PatientRow inside the existing patient list scaffolding and confirms the route advances to `procedure-room` with the patientId.
    - Static-analysis contract: `PatientRow` source contains the `procedure-room` navigation with the `patientId` payload; `App.tsx` source no longer renders a `patient-detail` placeholder case.
  </behavior>
  <action>
    First, extend the integration contract in `tests/integration/renderer-main-capture-contract.test.ts` so the regression cannot ship: add a `PATIENT_ROW_SRC` source fixture that reads `PatientRow.tsx` once; add a describe block that asserts `PatientRow` source contains `navigate({ name: 'procedure-room', patientId: ... })` (the literal `procedure-room` route name plus the `patientId` field); add a new assertion that the `App.tsx` source no longer contains the Phase 1 `patient-detail` placeholder (`Patient detail (Phase 4)` text or equivalent sentinel — if a different marker is used, the test must check the absence of the entire placeholder case). Then create `tests/renderer/components/patient-row.test.tsx` (new) using the existing `tests/renderer/setup.ts` mockApi helper: render a non-deleted PatientRow with a deterministic patient fixture and assert the actions menu contains an `Open Procedure Room` menuitem; click the menuitem and assert the route advances to `{ name: 'procedure-room', patientId: <id> }` via `getRoute()`; render a deleted PatientRow (where `patient.deletedAt` is a non-null epoch) and assert the `Open Procedure Room` menuitem is NOT in the menu. Then modify `src/renderer/src/components/PatientRow.tsx` to add the new entry: in the existing shadcn DropdownMenu Content, add a new `DropdownMenuItem` as the first child — it is rendered only when `!isDeleted`; its `onSelect` calls `useRoute().navigate({ name: 'procedure-room', patientId: patient.id })`. Reuse the existing `useRoute` import already pulled in by other files in the codebase; keep the existing Edit / Delete / Restore items unchanged below the new entry. Then remove the `patient-detail` case from `src/renderer/src/App.tsx`'s switch (delete the `case 'patient-detail':` block and its placeholder JSX). The `Route` union's `patient-detail` variant is preserved in `router.ts` so any persisted deep-link or audit payload that still mentions it does not break the build; the variant is simply unreached. Finally, extend `tests/renderer/pages/procedure-room.test.tsx` with a single reachability test that mirrors the existing 03-02 procedure-room test setup: render `PatientsList` (which renders `PatientRow`) after seeding an authenticated session, open Alice Carter's actions menu, click the new entry, and assert `getRoute().name === 'procedure-room'` and the route's `patientId` equals Alice's id. No new dependencies, no IPC contract changes, no audit changes; only the four renderer files plus the four test files listed in `files_modified` are touched.
  </action>
  <acceptance_criteria>
    - `src/renderer/src/components/PatientRow.tsx` renders a new `DropdownMenuItem` labeled "Open Procedure Room" as the first item in the existing actions menu; the item is rendered only when `!isDeleted`; its `onSelect` calls `navigate({ name: 'procedure-room', patientId: patient.id })`.
    - The existing Edit / Delete / Restore menu items continue to render and function exactly as before; their test selectors (e.g. `actions for Alice Carter` button) are unchanged.
    - `src/renderer/src/App.tsx` no longer contains a `case 'patient-detail':` block; the `Route` union's `patient-detail` variant is still declared in `router.ts` (additive removal) but is unreachable from any UI code.
    - `tests/renderer/components/patient-row.test.tsx` (new) covers: (1) non-deleted row has the new menuitem and clicking it navigates with the patientId; (2) deleted row does NOT have the menuitem.
    - `tests/renderer/pages/procedure-room.test.tsx` reachability test renders the patient list, opens a non-deleted row's actions menu, clicks the new entry, and asserts the route becomes `procedure-room` with the correct `patientId`.
    - `tests/integration/renderer-main-capture-contract.test.ts` extended with: a `PATIENT_ROW_SRC` source fixture; a new describe block asserting `PatientRow` source contains the `procedure-room` navigation with `patientId`; an assertion that `App.tsx` no longer renders the `patient-detail` placeholder case.
    - `npm run typecheck:web` and `npm run test:unit -- tests/renderer/components/patient-row.test.tsx tests/renderer/pages/procedure-room.test.tsx tests/integration/renderer-main-capture-contract.test.ts` both pass.
  </acceptance_criteria>
  <verify>
    <automated>npm run typecheck:web && npm run test:unit -- tests/renderer/components/patient-row.test.tsx tests/renderer/pages/procedure-room.test.tsx tests/integration/renderer-main-capture-contract.test.ts</automated>
  </verify>
  <done>Every non-deleted PatientRow exposes an `Open Procedure Room` action that lands the doctor on the existing Procedure Room with the patientId preserved; Finish from Procedure Room returns to the Patient List via the existing previousRoute snapshot (Q-B); the App.tsx `patient-detail` placeholder is removed so no UI code points at the dead surface; the component test + reachability test + integration contract all pin the new entry shape.</done>
</task>

</tasks>

## Artifacts this phase produces

- `src/renderer/src/pages/SettingsHub.tsx`: new `SettingsHub` default-exported function component (header Back + two-column layout + right-side sidebar nav with Users + Capture).
- `src/renderer/src/lib/router.ts`: `Route` union gains `{ name: 'settings-hub' }` variant; existing variants preserved.
- `src/renderer/src/App.tsx`: switch renders `<SettingsHub />` for `case 'settings-hub'`; `case 'patient-detail':` placeholder removed.
- `src/renderer/src/pages/PatientsList.tsx`: header Settings affordance is a single `Button` that calls `navigate({ name: 'settings-hub' })`; the prior `DropdownMenu` wrapper and its three imports are removed.
- `src/renderer/src/components/PatientRow.tsx`: new first `DropdownMenuItem` "Open Procedure Room" (non-deleted rows only) that calls `navigate({ name: 'procedure-room', patientId: patient.id })`.
- `tests/renderer/pages/settings-hub.test.tsx`: new page test for the hub (admin/non-admin, sidebar entries, Back).
- `tests/renderer/components/patient-row.test.tsx`: new component test for the new entry (non-deleted + deleted coverage + click navigation).
- `tests/renderer/pages/patients-list.test.tsx`: Settings-menu describe block replaced with a single header Button → `settings-hub` assertion.
- `tests/renderer/pages/procedure-room.test.tsx`: new reachability test from the row action into Procedure Room.
- `tests/integration/renderer-main-capture-contract.test.ts`: `SETTINGS_HUB_SRC` + `PATIENT_ROW_SRC` source fixtures; new describe blocks pin the SettingsHub nav literals, the App.tsx `settings-hub` case, the PatientsList `settings-hub` header Button, the PatientRow `procedure-room` row action, and the absence of the `patient-detail` placeholder in App.tsx.

## Multi-Source Coverage Audit

| Source | ID | Feature/Requirement | Plan | Status | Notes |
|---|---|---|---|---|---|
| GOAL | — | Doctor can reach the device picker, live preview, and Procedure Room from a real entry surface | 03-05 | COVERED | Both entry points (Settings hub + row action) are real, persistent surfaces. |
| REQ | CAPT-01 | Available capture devices appear in Settings → Capture | 03-05 | COVERED | Hub → settings-capture link is the new entry. |
| REQ | CAPT-03 | Doctor can reach and use live preview | 03-05 | COVERED | Hub → settings-capture is reachable for every authenticated doctor. |
| REQ | SET-01 | Capture device setting is accessible | 03-05 | COVERED | Hub Capture entry. |
| REQ | SET-04 | Users admin-gated | 03-05 | COVERED | Hub Users entry stays admin-gated via `isFirstAdmin`. |
| UAT | gap settings-hub | Settings entry navigates to a hub page with right-side sidebar nav | 03-05 | COVERED | Task 1 — SettingsHub + integration contract. |
| UAT | gap procedure-room-entry | Procedure Room is reachable from a patient row in the Patient List | 03-05 | COVERED | Task 2 — PatientRow menuitem + App.tsx cleanup. |
| CONTEXT | D-09 | Settings → Capture has a live preview pane | 03-05 | EXCLUDED | Page already exists; route wire only. |
| CONTEXT | Q-B | previousRoute Finish returns to source | 03-05 | COVERED | Reachability test asserts the route payload + previous snapshot semantics. |
| CONTEXT | Q-C | Settings preview state is component-local | 03-05 | EXCLUDED | Already implemented in 03-02; no change. |
| CONTEXT | D-01..D-08, D-10, D-11 | Previously implemented capture behavior | Existing 03-01..03-04 | EXCLUDED | No diagnosed gap; this closure does not alter those locked behaviors. |
| RESEARCH | — | State-based routing, shadcn primitives, no IPC changes | 03-05 | COVERED | All work is renderer-only; no IPC, no settings table, no new dependencies. |

## Threat Model

## Trust Boundaries

| Boundary | Description |
|---|---|
| Authenticated renderer → Settings hub | A logged-in doctor clicks the PatientsList header Settings Button to open the hub. |
| Session role → Settings → Users | `currentUser.isFirstAdmin` controls whether the hub Users sidebar entry is enabled. |
| Authenticated renderer → Procedure Room | A non-deleted PatientRow's `Open Procedure Room` menuitem carries the patientId and lands on the existing Procedure Room. |
| Route lifecycle → OS media handle | Procedure Room `Finish` and unmount paths must release every `MediaStreamTrack` (already covered by 03-02). |
| Persisted deep-link → dead `patient-detail` | The `patient-detail` route variant remains in the `Route` union but is unreachable from UI; the App.tsx case is removed so no component renders the Phase 1 placeholder. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-3-16 | Elevation of Privilege | SettingsHub Users sidebar entry | medium | mitigate | Bind `Button.disabled` to `!currentUser?.isFirstAdmin` and test both roles; the existing destination (`settings-users`) is unchanged. |
| T-3-17 | Denial of Service | PatientsList header Settings affordance | medium | mitigate | Render a single outline Button (not a transient menu) for every authenticated session; pin the route with component + static-analysis tests. |
| T-3-18 | Tampering | PatientRow `Open Procedure Room` payload | medium | mitigate | Navigate with the typed literal `{ name: 'procedure-room', patientId: patient.id }`; the integration contract pins the route name + the `patientId` field. |
| T-3-19 | Information Disclosure | App.tsx `patient-detail` placeholder | low | mitigate | Remove the case so the Phase 1 placeholder text is no longer rendered; integration contract asserts the placeholder is gone. |
| T-3-20 | Repudiation | deleted-patient procedure start | medium | mitigate | The new `Open Procedure Room` item is rendered only when `!isDeleted`; component test asserts deleted rows do NOT render the entry. |
| T-3-SC | Tampering | npm installs | high | mitigate | No package installation occurs; reuse the installed shadcn primitives (Button, Card). |

<verification>
- `npm run typecheck:web`
- `npm run test:unit -- tests/renderer/pages/settings-hub.test.tsx tests/renderer/components/patient-row.test.tsx tests/renderer/pages/patients-list.test.tsx tests/renderer/pages/procedure-room.test.tsx tests/integration/renderer-main-capture-contract.test.ts`
- `npm run typecheck`
- `npm run test:unit` (full suite; confirm test count > 197 and zero failures)
- Confirm the App.tsx `case 'patient-detail':` block is removed; confirm the `Route` union still declares `patient-detail` (additive removal only).
- Confirm the PatientsList header Settings affordance is a Button, not a DropdownMenu wrapper.
- Confirm PatientRow renders the new `Open Procedure Room` menuitem only for non-deleted patients.
- Confirm no renderer file points at the `Patient detail (Phase 4)` placeholder text.
</verification>

<success_criteria>
Every authenticated doctor can open Settings from the Patient List and reach a dedicated hub page whose right-side sidebar lists Capture (always enabled) and Users (admin only); every non-deleted PatientRow exposes an `Open Procedure Room` action that lands on the existing Procedure Room with the patientId preserved; Finish returns to the Patient List via the existing previousRoute snapshot (Q-B); the App.tsx `patient-detail` placeholder is removed; focused and full regression gates pass beyond the existing 197-test baseline.
</success_criteria>

<output>
Create `.planning/phases/03-capture-enumeration-live-preview/03-05-SUMMARY.md` when done.
</output>
